window.__XF_SKILL_PACKS__=window.__XF_SKILL_PACKS__||{};window.__XF_SKILL_PACKS__["github-pack"]={"id": "github-pack", "skills": [{"id": "github-ci-failure-summary", "aiInstallPrompt": "Install the skill 'github-ci-failure-summary' by writing the file below.\n\nUniversal path: ./skills/github-ci-failure-summary/SKILL.md\nGrok path: ~/.grok/skills/github-ci-failure-summary/SKILL.md\nCursor path: ~/.cursor/skills/github-ci-failure-summary/SKILL.md\nClaude path: ~/.claude/skills/github-ci-failure-summary/SKILL.md\nVS Code agent path: .github/skills/github-ci-failure-summary/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: github-ci-failure-summary\ndescription: >\n  Summarize CI failure from GitHub. Requires integrations: @github. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"github ci failure summary\", /github-ci-failure-summary.\nmetadata:\n  category: github\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-ci-failure-summary\n\nSummarize CI failure from GitHub.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this when a red ✗ is blocking a merge and nobody wants to scroll 4,000 lines of workflow logs: a PR check just failed, `main` went red after a merge, or a nightly build broke overnight. It digs out the first real error, distinguishes \"your change broke it\" from \"the build is flaky/infra is down\", and hands back a diagnosis with a proposed fix.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which failure: a PR number, a workflow run URL/ID, or \"latest failed run on main\" - if the user gives only a repo, find the most recent failed run\n- Whether this check passed before on the same branch (helps split regression vs flake)\n- What the user wants back: quick diagnosis only, or diagnosis + proposed fix + draft comment for the PR thread\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Engineer-to-engineer. Lead with the verdict, quote the exact failing line, link the run and the suspect commit. No log dumps.\n- **Audience:** The developer whose PR is blocked - they want to know \"is it me, and what do I change?\"\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation - this includes posting PR comments, re-running workflows, and pushing any fix commit.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Locate the failed run and identify which jobs and steps failed (often one root failure cascades - find the *first* failing step, not the loudest).\n2. Pull the failing step's logs and isolate the first genuine error: the assertion message, compiler error, or non-zero exit - skip past warnings, deprecation notices, and the wall of retry noise. Quote the minimal decisive excerpt (a few lines, not hundreds).\n3. **Classify the failure**:\n   - *Regression* - a test that exercises code touched by the diff now fails\n   - *Flake* - timeout, port already in use, race, \"works on re-run\"; check whether the same test failed on unrelated recent runs\n   - *Infra/environment* - runner out of disk, network fetch failed, action version yanked, secret/credential expired\n   - *Config drift* - lockfile mismatch, node/python version bump, cache poisoning\n4. Correlate with the diff: map the failing test/file to the PR's changed files and the run's head commit. If the same failure exists on `main`'s latest run, say clearly: **not caused by this PR**.\n5. Check history for repeat offenders: has this exact job failed in several recent runs across different PRs? If yes, call it out as a known-flaky candidate worth quarantining.\n6. Propose next steps ranked by likelihood: exact code/config change for a regression, \"re-run - evidence says flake (failed on runs X, Y with no related diff)\" for a flake, or \"fix the workflow file / rotate the secret\" for infra.\n7. If the user wants it, draft a PR comment summarizing the diagnosis, and offer to post it or trigger a re-run - as approval items in the review table, never automatically.\n\n## Output format\n\n**Verdict** - one line: `❌ <job> failed at <step> - <regression | flake | infra | config> (confidence: high/med/low)`\n\n**Evidence** - the decisive log excerpt (≤10 lines, fenced) with a link to the run and the failing step.\n\n**Why** - 2-4 sentences connecting the error to the diff (or exonerating it), naming suspect files/commits with links.\n\n**Recommended fix** - concrete change or action, plus fallback if confidence is low.\n\n**Proposed actions** (only if any) - numbered: e.g. `1. Post diagnosis comment on PR #123` · `2. Re-run failed jobs`. Wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Find the FIRST failing step - downstream failures (upload artifact, report step) are usually symptoms\n- Check whether `main` is also red before blaming the PR\n- Quote exact error text; paraphrased errors send people down wrong paths\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent log contents, test names, or commit SHAs without a successful `use_tool` call\n- Don't paste hundreds of log lines - extract the decisive excerpt and link the rest\n- Don't declare \"flake, just re-run\" without evidence (prior failures of the same test on unrelated changes); mislabeling a real regression as flaky ships bugs\n- Don't auto-post comments, re-run workflows, or push fixes - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /github-ci-failure-summary or describe the task from the skill description.", "skillContent": "---\nname: github-ci-failure-summary\ndescription: >\n  Summarize CI failure from GitHub. Requires integrations: @github. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"github ci failure summary\", /github-ci-failure-summary.\nmetadata:\n  category: github\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-ci-failure-summary\n\nSummarize CI failure from GitHub.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this when a red ✗ is blocking a merge and nobody wants to scroll 4,000 lines of workflow logs: a PR check just failed, `main` went red after a merge, or a nightly build broke overnight. It digs out the first real error, distinguishes \"your change broke it\" from \"the build is flaky/infra is down\", and hands back a diagnosis with a proposed fix.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which failure: a PR number, a workflow run URL/ID, or \"latest failed run on main\" - if the user gives only a repo, find the most recent failed run\n- Whether this check passed before on the same branch (helps split regression vs flake)\n- What the user wants back: quick diagnosis only, or diagnosis + proposed fix + draft comment for the PR thread\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Engineer-to-engineer. Lead with the verdict, quote the exact failing line, link the run and the suspect commit. No log dumps.\n- **Audience:** The developer whose PR is blocked - they want to know \"is it me, and what do I change?\"\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation - this includes posting PR comments, re-running workflows, and pushing any fix commit.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Locate the failed run and identify which jobs and steps failed (often one root failure cascades - find the *first* failing step, not the loudest).\n2. Pull the failing step's logs and isolate the first genuine error: the assertion message, compiler error, or non-zero exit - skip past warnings, deprecation notices, and the wall of retry noise. Quote the minimal decisive excerpt (a few lines, not hundreds).\n3. **Classify the failure**:\n   - *Regression* - a test that exercises code touched by the diff now fails\n   - *Flake* - timeout, port already in use, race, \"works on re-run\"; check whether the same test failed on unrelated recent runs\n   - *Infra/environment* - runner out of disk, network fetch failed, action version yanked, secret/credential expired\n   - *Config drift* - lockfile mismatch, node/python version bump, cache poisoning\n4. Correlate with the diff: map the failing test/file to the PR's changed files and the run's head commit. If the same failure exists on `main`'s latest run, say clearly: **not caused by this PR**.\n5. Check history for repeat offenders: has this exact job failed in several recent runs across different PRs? If yes, call it out as a known-flaky candidate worth quarantining.\n6. Propose next steps ranked by likelihood: exact code/config change for a regression, \"re-run - evidence says flake (failed on runs X, Y with no related diff)\" for a flake, or \"fix the workflow file / rotate the secret\" for infra.\n7. If the user wants it, draft a PR comment summarizing the diagnosis, and offer to post it or trigger a re-run - as approval items in the review table, never automatically.\n\n## Output format\n\n**Verdict** - one line: `❌ <job> failed at <step> - <regression | flake | infra | config> (confidence: high/med/low)`\n\n**Evidence** - the decisive log excerpt (≤10 lines, fenced) with a link to the run and the failing step.\n\n**Why** - 2-4 sentences connecting the error to the diff (or exonerating it), naming suspect files/commits with links.\n\n**Recommended fix** - concrete change or action, plus fallback if confidence is low.\n\n**Proposed actions** (only if any) - numbered: e.g. `1. Post diagnosis comment on PR #123` · `2. Re-run failed jobs`. Wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Find the FIRST failing step - downstream failures (upload artifact, report step) are usually symptoms\n- Check whether `main` is also red before blaming the PR\n- Quote exact error text; paraphrased errors send people down wrong paths\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent log contents, test names, or commit SHAs without a successful `use_tool` call\n- Don't paste hundreds of log lines - extract the decisive excerpt and link the rest\n- Don't declare \"flake, just re-run\" without evidence (prior failures of the same test on unrelated changes); mislabeling a real regression as flaky ships bugs\n- Don't auto-post comments, re-run workflows, or push fixes - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "github-dependabot-review", "aiInstallPrompt": "Install the skill 'github-dependabot-review' by writing the file below.\n\nUniversal path: ./skills/github-dependabot-review/SKILL.md\nGrok path: ~/.grok/skills/github-dependabot-review/SKILL.md\nCursor path: ~/.cursor/skills/github-dependabot-review/SKILL.md\nClaude path: ~/.claude/skills/github-dependabot-review/SKILL.md\nVS Code agent path: .github/skills/github-dependabot-review/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: github-dependabot-review\ndescription: >\n  Summarize Dependabot upgrade PRs. Requires integrations: @github. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"github dependabot review\", /github-dependabot-review.\nmetadata:\n  category: github\n  type: connector\n  cadence: weekly\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-dependabot-review\n\nSummarize Dependabot upgrade PRs.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this weekly (or when the bot has flooded the PR list) to turn a stack of Dependabot/Renovate PRs into a merge plan: which updates are security-critical, which are safe patch bumps to batch-merge, which majors carry breaking changes that need human work, and in what order to land them without breaking each other's rebases.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which repo(s), and whether to include Renovate or other bot authors besides `dependabot[bot]`\n- Risk appetite: auto-mergeable classes (e.g. \"devDependency patches are fine\") vs \"everything needs eyes\"\n- Whether CI must be green before a merge is even proposed (default: yes)\n- Any pinned/frozen dependencies the team never bumps (framework majors, known-bad versions)\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Risk-ranked and terse. Link each PR and each upstream changelog/advisory. Say \"breaking\" only when the changelog or semver says so.\n- **Audience:** The engineer on dependency duty who wants to clear the queue in one pass.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never merge, close, or comment on a PR without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. List open PRs authored by `dependabot[bot]` (and other configured bots). Group by package ecosystem (npm, pip, cargo, actions, docker) and dedupe superseded PRs - Dependabot often leaves an old PR open after opening a newer bump of the same package.\n2. For each PR, extract from the PR body: package, old → new version, semver jump (patch/minor/major), and whether Dependabot flags it as a **security update** (CVE/GHSA reference). Security updates go to the top regardless of semver size.\n3. **Classify risk**:\n   - *Low* - patch bumps, devDependencies, lockfile-only changes, GitHub Actions pin updates\n   - *Medium* - minor bumps of runtime dependencies; check the release notes excerpt in the PR body for behavior changes and deprecations\n   - *High* - major bumps, anything touching auth/crypto/serialization/build tooling, packages with peer-dependency ripple effects\n4. Check each PR's CI status and mergeability (conflicts). A red or conflicted bot PR is a \"needs attention\" item, not a merge candidate.\n5. Detect related bumps that must land together (e.g. a package and its types/plugin siblings) and note ordering: merge low-risk first, then rebase the rest - recommend using Dependabot's own `@dependabot rebase` comment rather than manual rebases.\n6. Propose the plan: batch-merge list (low risk, green CI), review list (medium), human-work list (majors, with the specific breaking changes quoted from changelogs), and close list (superseded/frozen deps, each with a reason).\n7. On `apply`, execute only approved merges/comments/closes one at a time, re-checking CI state right before each merge, then report what landed.\n\n## Output format\n\n**Queue snapshot** - `N bot PRs · S security · M majors · X conflicted/red CI · D superseded`\n\n**Security updates** (if any, always first):\n\n| PR | Package | Old → New | Advisory | CI | Recommendation |\n|---|---|---|---|---|---|\n\n**Merge plan** - three ordered groups:\n1. **Batch-merge now** (low risk, green) - PR list with one-line justification each\n2. **Review then merge** (medium) - PR + the specific changelog line that needs a human glance\n3. **Needs work** (major/breaking/red CI) - PR + quoted breaking change + suggested owner\n\n**Close as superseded/frozen** - PR list with reasons.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or name a group (e.g. `apply batch`).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the release-notes/changelog excerpt Dependabot embeds in the PR body - it usually states breaking changes outright\n- Prioritize security bumps above everything, even when they're majors\n- Re-check CI status immediately before any approved merge - the queue shifts as earlier merges trigger rebases\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent CVE IDs, version numbers, or changelog contents without a successful `use_tool` call\n- Don't merge, close, or comment without `apply` - even \"obviously safe\" patch bumps\n- Don't treat a green CI as proof a major bump is safe; many breaking changes aren't covered by tests - semver majors always need human review\n- Don't batch-merge PRs that touch the same lockfile simultaneously; land sequentially and let the bot rebase\n- Don't recommend merging a bump the team has deliberately pinned - check for `ignore` config and prior closed PRs of the same bump\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /github-dependabot-review`\n```\n\nAfter writing the file, confirm it exists. The user can then run /github-dependabot-review or describe the task from the skill description.", "skillContent": "---\nname: github-dependabot-review\ndescription: >\n  Summarize Dependabot upgrade PRs. Requires integrations: @github. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"github dependabot review\", /github-dependabot-review.\nmetadata:\n  category: github\n  type: connector\n  cadence: weekly\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-dependabot-review\n\nSummarize Dependabot upgrade PRs.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this weekly (or when the bot has flooded the PR list) to turn a stack of Dependabot/Renovate PRs into a merge plan: which updates are security-critical, which are safe patch bumps to batch-merge, which majors carry breaking changes that need human work, and in what order to land them without breaking each other's rebases.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which repo(s), and whether to include Renovate or other bot authors besides `dependabot[bot]`\n- Risk appetite: auto-mergeable classes (e.g. \"devDependency patches are fine\") vs \"everything needs eyes\"\n- Whether CI must be green before a merge is even proposed (default: yes)\n- Any pinned/frozen dependencies the team never bumps (framework majors, known-bad versions)\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Risk-ranked and terse. Link each PR and each upstream changelog/advisory. Say \"breaking\" only when the changelog or semver says so.\n- **Audience:** The engineer on dependency duty who wants to clear the queue in one pass.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never merge, close, or comment on a PR without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. List open PRs authored by `dependabot[bot]` (and other configured bots). Group by package ecosystem (npm, pip, cargo, actions, docker) and dedupe superseded PRs - Dependabot often leaves an old PR open after opening a newer bump of the same package.\n2. For each PR, extract from the PR body: package, old → new version, semver jump (patch/minor/major), and whether Dependabot flags it as a **security update** (CVE/GHSA reference). Security updates go to the top regardless of semver size.\n3. **Classify risk**:\n   - *Low* - patch bumps, devDependencies, lockfile-only changes, GitHub Actions pin updates\n   - *Medium* - minor bumps of runtime dependencies; check the release notes excerpt in the PR body for behavior changes and deprecations\n   - *High* - major bumps, anything touching auth/crypto/serialization/build tooling, packages with peer-dependency ripple effects\n4. Check each PR's CI status and mergeability (conflicts). A red or conflicted bot PR is a \"needs attention\" item, not a merge candidate.\n5. Detect related bumps that must land together (e.g. a package and its types/plugin siblings) and note ordering: merge low-risk first, then rebase the rest - recommend using Dependabot's own `@dependabot rebase` comment rather than manual rebases.\n6. Propose the plan: batch-merge list (low risk, green CI), review list (medium), human-work list (majors, with the specific breaking changes quoted from changelogs), and close list (superseded/frozen deps, each with a reason).\n7. On `apply`, execute only approved merges/comments/closes one at a time, re-checking CI state right before each merge, then report what landed.\n\n## Output format\n\n**Queue snapshot** - `N bot PRs · S security · M majors · X conflicted/red CI · D superseded`\n\n**Security updates** (if any, always first):\n\n| PR | Package | Old → New | Advisory | CI | Recommendation |\n|---|---|---|---|---|---|\n\n**Merge plan** - three ordered groups:\n1. **Batch-merge now** (low risk, green) - PR list with one-line justification each\n2. **Review then merge** (medium) - PR + the specific changelog line that needs a human glance\n3. **Needs work** (major/breaking/red CI) - PR + quoted breaking change + suggested owner\n\n**Close as superseded/frozen** - PR list with reasons.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or name a group (e.g. `apply batch`).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the release-notes/changelog excerpt Dependabot embeds in the PR body - it usually states breaking changes outright\n- Prioritize security bumps above everything, even when they're majors\n- Re-check CI status immediately before any approved merge - the queue shifts as earlier merges trigger rebases\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent CVE IDs, version numbers, or changelog contents without a successful `use_tool` call\n- Don't merge, close, or comment without `apply` - even \"obviously safe\" patch bumps\n- Don't treat a green CI as proof a major bump is safe; many breaking changes aren't covered by tests - semver majors always need human review\n- Don't batch-merge PRs that touch the same lockfile simultaneously; land sequentially and let the bot rebase\n- Don't recommend merging a bump the team has deliberately pinned - check for `ignore` config and prior closed PRs of the same bump\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /github-dependabot-review`\n"}, {"id": "github-issue-triage", "aiInstallPrompt": "Install the skill 'github-issue-triage' by writing the file below.\n\nUniversal path: ./skills/github-issue-triage/SKILL.md\nGrok path: ~/.grok/skills/github-issue-triage/SKILL.md\nCursor path: ~/.cursor/skills/github-issue-triage/SKILL.md\nClaude path: ~/.claude/skills/github-issue-triage/SKILL.md\nVS Code agent path: .github/skills/github-issue-triage/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: github-issue-triage\ndescription: >\n  Triage and label new GitHub issues. Requires integrations: @github. Uses search_tool + use_tool. Cadence: daily. Triggers: \"github issue triage\", /github-issue-triage.\nmetadata:\n  category: github\n  type: connector\n  cadence: daily\n  risk: medium\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-issue-triage\n\nTriage and label new GitHub issues.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this when untriaged issues are piling up - typically each morning on an active repo, after a release ships (bug reports spike in the first 48 hours), or when a maintainer returns from time off to 30+ unlabeled issues. It turns a raw issue queue into a labeled, prioritized, de-duplicated backlog with close/label/assign proposals ready for one-word approval.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which repo(s) to triage, and the cutoff (e.g. \"issues opened or updated since last run\", \"everything with no labels\")\n- The repo's label taxonomy - fetch the live label list first; never invent labels. If the repo lacks a usable taxonomy, propose one (type: `bug`/`feature`/`docs`/`question`; priority: `P0`-`P3`; status: `needs-repro`, `needs-info`, `duplicate`, `wontfix`) as a separate approval item\n- Staleness policy: how long an issue can sit in `needs-info` before a close is proposed (default 14 days)\n- Whether the user wants label-only mode, or also assignee suggestions, close proposals, and reply drafts\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Link every issue by number. Neutral, maintainer-voice reply drafts - thank the reporter, ask precisely for what's missing.\n- **Audience:** Repo maintainers reviewing your proposals before anything is written.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Fetch the repo's existing labels, then list target issues (untriaged/new since cutoff). Note total count; if over ~50, triage the newest and highest-reaction issues first and say what was deferred.\n2. **Classify each issue**: type (bug / feature request / question / docs / support). For bugs, check whether the report contains reproduction steps, expected-vs-actual behavior, and version/environment info - if any are missing, mark `needs-repro` or `needs-info` and draft a specific reply asking for exactly the missing pieces.\n3. **Detect duplicates**: search open and recently-closed issues for matching error messages, stack-trace fingerprints, and title keywords. Propose closing the newer issue with a \"duplicate of #N\" comment - never close the one with more discussion or reactions.\n4. **Score priority** from real signals, not vibes: 👍 reaction count and distinct commenters (breadth), crash/data-loss/security keywords (severity), whether a linked PR already exists (someone cares enough to fix it), whether it affects a default configuration vs an edge case, and reporter follow-through (responded to previous questions). Map to P0 (drop everything) / P1 (this sprint) / P2 (backlog) / P3 (nice-to-have).\n5. **Flag stale threads**: issues sitting in `needs-info` past the staleness window get a proposed final-warning comment or close; issues with maintainer activity are never flagged stale regardless of age.\n6. **Suggest assignees** only from evidence: git blame on the affected path, CODEOWNERS, or who fixed similar past issues. If no signal, leave unassigned rather than guessing.\n7. Present the review table (see Output format). Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n8. After applying, report exactly which labels/comments/closures were written, with links.\n\n## Output format\n\n**Queue snapshot** - one line: `N untriaged · X bugs / Y features / Z questions · D suspected duplicates · S stale`\n\n**Triage table** - one row per issue:\n\n| # | Title (linked) | Type | Proposed labels | Priority + why (1 short clause) | Dup of | Suggested assignee | Proposed action |\n|---|---|---|---|---|---|---|---|\n\n**Reply drafts** - numbered, one per issue needing a comment (needs-info requests, duplicate-close notes, stale warnings), quoted in full so the user can review the exact wording.\n\n**Apply instructions** - remind the user: `apply all`, `apply 1,3`, or `skip`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Fetch the repo's real label list before proposing any label - repos differ (`kind/bug` vs `bug` vs `type: bug`)\n- Search closed issues too when hunting duplicates; the canonical report is often already fixed or explained there\n- Read the issue body and last few comments before classifying - titles lie (\"App broken\" is often a config question)\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent labels, milestones, or assignees that don't exist in the repo - verify with a live `use_tool` call\n- Don't auto-apply labels, post comments, or close issues - propose and wait for `apply`\n- Don't close an issue as duplicate when the \"duplicate\" is actually a distinct bug with a similar symptom; when unsure, propose a cross-link comment instead\n- Don't propose closing issues with recent reporter activity just because they're old - staleness means *waiting on info*, not *unfixed*\n- Don't rank priority by recency alone; a 3-month-old crash with 40 👍 outranks yesterday's typo report\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **daily**\n\nExample: `/loop 7d Run /github-issue-triage`\n```\n\nAfter writing the file, confirm it exists. The user can then run /github-issue-triage or describe the task from the skill description.", "skillContent": "---\nname: github-issue-triage\ndescription: >\n  Triage and label new GitHub issues. Requires integrations: @github. Uses search_tool + use_tool. Cadence: daily. Triggers: \"github issue triage\", /github-issue-triage.\nmetadata:\n  category: github\n  type: connector\n  cadence: daily\n  risk: medium\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-issue-triage\n\nTriage and label new GitHub issues.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this when untriaged issues are piling up - typically each morning on an active repo, after a release ships (bug reports spike in the first 48 hours), or when a maintainer returns from time off to 30+ unlabeled issues. It turns a raw issue queue into a labeled, prioritized, de-duplicated backlog with close/label/assign proposals ready for one-word approval.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which repo(s) to triage, and the cutoff (e.g. \"issues opened or updated since last run\", \"everything with no labels\")\n- The repo's label taxonomy - fetch the live label list first; never invent labels. If the repo lacks a usable taxonomy, propose one (type: `bug`/`feature`/`docs`/`question`; priority: `P0`-`P3`; status: `needs-repro`, `needs-info`, `duplicate`, `wontfix`) as a separate approval item\n- Staleness policy: how long an issue can sit in `needs-info` before a close is proposed (default 14 days)\n- Whether the user wants label-only mode, or also assignee suggestions, close proposals, and reply drafts\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Link every issue by number. Neutral, maintainer-voice reply drafts - thank the reporter, ask precisely for what's missing.\n- **Audience:** Repo maintainers reviewing your proposals before anything is written.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Fetch the repo's existing labels, then list target issues (untriaged/new since cutoff). Note total count; if over ~50, triage the newest and highest-reaction issues first and say what was deferred.\n2. **Classify each issue**: type (bug / feature request / question / docs / support). For bugs, check whether the report contains reproduction steps, expected-vs-actual behavior, and version/environment info - if any are missing, mark `needs-repro` or `needs-info` and draft a specific reply asking for exactly the missing pieces.\n3. **Detect duplicates**: search open and recently-closed issues for matching error messages, stack-trace fingerprints, and title keywords. Propose closing the newer issue with a \"duplicate of #N\" comment - never close the one with more discussion or reactions.\n4. **Score priority** from real signals, not vibes: 👍 reaction count and distinct commenters (breadth), crash/data-loss/security keywords (severity), whether a linked PR already exists (someone cares enough to fix it), whether it affects a default configuration vs an edge case, and reporter follow-through (responded to previous questions). Map to P0 (drop everything) / P1 (this sprint) / P2 (backlog) / P3 (nice-to-have).\n5. **Flag stale threads**: issues sitting in `needs-info` past the staleness window get a proposed final-warning comment or close; issues with maintainer activity are never flagged stale regardless of age.\n6. **Suggest assignees** only from evidence: git blame on the affected path, CODEOWNERS, or who fixed similar past issues. If no signal, leave unassigned rather than guessing.\n7. Present the review table (see Output format). Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n8. After applying, report exactly which labels/comments/closures were written, with links.\n\n## Output format\n\n**Queue snapshot** - one line: `N untriaged · X bugs / Y features / Z questions · D suspected duplicates · S stale`\n\n**Triage table** - one row per issue:\n\n| # | Title (linked) | Type | Proposed labels | Priority + why (1 short clause) | Dup of | Suggested assignee | Proposed action |\n|---|---|---|---|---|---|---|---|\n\n**Reply drafts** - numbered, one per issue needing a comment (needs-info requests, duplicate-close notes, stale warnings), quoted in full so the user can review the exact wording.\n\n**Apply instructions** - remind the user: `apply all`, `apply 1,3`, or `skip`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Fetch the repo's real label list before proposing any label - repos differ (`kind/bug` vs `bug` vs `type: bug`)\n- Search closed issues too when hunting duplicates; the canonical report is often already fixed or explained there\n- Read the issue body and last few comments before classifying - titles lie (\"App broken\" is often a config question)\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent labels, milestones, or assignees that don't exist in the repo - verify with a live `use_tool` call\n- Don't auto-apply labels, post comments, or close issues - propose and wait for `apply`\n- Don't close an issue as duplicate when the \"duplicate\" is actually a distinct bug with a similar symptom; when unsure, propose a cross-link comment instead\n- Don't propose closing issues with recent reporter activity just because they're old - staleness means *waiting on info*, not *unfixed*\n- Don't rank priority by recency alone; a 3-month-old crash with 40 👍 outranks yesterday's typo report\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **daily**\n\nExample: `/loop 7d Run /github-issue-triage`\n"}, {"id": "github-pr-digest", "aiInstallPrompt": "Install the skill 'github-pr-digest' by writing the file below.\n\nUniversal path: ./skills/github-pr-digest/SKILL.md\nGrok path: ~/.grok/skills/github-pr-digest/SKILL.md\nCursor path: ~/.cursor/skills/github-pr-digest/SKILL.md\nClaude path: ~/.claude/skills/github-pr-digest/SKILL.md\nVS Code agent path: .github/skills/github-pr-digest/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: github-pr-digest\ndescription: >\n  Digest open GitHub PRs needing review. Requires integrations: @github. Uses search_tool + use_tool. Cadence: daily. Triggers: \"github pr digest\", /github-pr-digest.\nmetadata:\n  category: github\n  type: connector\n  cadence: daily\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-pr-digest\n\nDigest open GitHub PRs needing review.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this at the start of the day (or before a team standup) to answer one question: **which PRs should get review attention today, in what order?** It's for tech leads and reviewers juggling multiple repos who need waiting-on-who clarity, not another flat list of open PRs.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which repos (or org + team filter), and whose perspective - \"PRs waiting on *me*\", \"PRs my team owns\", or \"everything open\"\n- Whether drafts should be included (default: exclude, but list drafts older than a week as possibly-forgotten)\n- What counts as blocked for this team (e.g. required approvals count, must CI be green)\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Scannable in 60 seconds. Every PR linked. State whose move it is in two or three words (\"waiting on author\").\n- **Audience:** Reviewers deciding where to spend their next review hour.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- This skill is read-only by default. If the user asks you to nudge authors or post the digest anywhere, show the draft first and wait for `apply`.\n- Never comment, approve, merge, or close without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. List open PRs in scope. For each, gather: author, reviewers requested vs reviews given, review states (approved / changes requested / commented), CI status, mergeability, draft flag, age, and time since last activity.\n2. **Determine whose move it is** - this is the core value:\n   - *Waiting on reviewer* - review requested, no review yet, CI green\n   - *Waiting on author* - changes requested, or CI red, or merge conflicts\n   - *Ready to merge* - approvals met, CI green, no conflicts (call these out loudly; they're free wins)\n   - *Stalled* - no activity from anyone in 5+ days (note who moved last)\n3. **Order the review queue** by: unblocks-others first (release blockers, PRs other PRs are stacked on, bugfixes referenced by open incidents), then small-and-old before large-and-new - a 30-line PR waiting 4 days beats a 2,000-line PR from this morning. Estimate review effort from diff size and files touched (S <100 lines / M <500 / L 500+).\n4. Summarize each PR in one line from its title, description, and changed paths - what it does and blast radius (e.g. \"migrates auth middleware; touches every route\"), not a restated title.\n5. Flag hygiene problems separately: PRs with no description, no linked issue, or 1,000+ lines with no reviewer - these need author action before review time is spent.\n6. If asked to nudge, draft per-author messages listing their waiting PRs (polite, specific: \"needs rebase + one approval\") and hold for `apply`.\n\n## Output format\n\n**Headline** - `N open · R waiting on review · A waiting on author · M ready to merge · S stalled`\n\n**🟢 Ready to merge** - linked list, one line each (who can press the button).\n\n**👀 Review queue (do in this order)** -\n\n| # | PR | One-line summary | Size | Age / idle | Why this order | Suggested reviewer |\n|---|---|---|---|---|---|---|\n\n**⏳ Waiting on author** - PR · what's needed (rebase / fix CI / address review) · idle time.\n\n**🧊 Stalled / needs a decision** - PR · who moved last · suggested unblock.\n\n**Nudge drafts** (only if requested) - per-author messages, held for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Distinguish \"no review yet\" from \"changes requested\" - they need opposite people to act\n- Surface ready-to-merge PRs at the very top; approved-but-unmerged PRs rot into conflicts\n- Weight small old PRs above large new ones in the queue\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent PR states, reviewers, or CI results without a successful `use_tool` call\n- Don't dump a flat list sorted by number - the ordering and whose-move labels ARE the deliverable\n- Don't count a `commented` review as an approval when computing \"ready to merge\"\n- Don't include dependabot/bot PRs in the human review queue (point to /github-dependabot-review instead)\n- Don't post nudges or the digest to any channel without `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **daily**\n\nExample: `/loop 7d Run /github-pr-digest`\n```\n\nAfter writing the file, confirm it exists. The user can then run /github-pr-digest or describe the task from the skill description.", "skillContent": "---\nname: github-pr-digest\ndescription: >\n  Digest open GitHub PRs needing review. Requires integrations: @github. Uses search_tool + use_tool. Cadence: daily. Triggers: \"github pr digest\", /github-pr-digest.\nmetadata:\n  category: github\n  type: connector\n  cadence: daily\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-pr-digest\n\nDigest open GitHub PRs needing review.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this at the start of the day (or before a team standup) to answer one question: **which PRs should get review attention today, in what order?** It's for tech leads and reviewers juggling multiple repos who need waiting-on-who clarity, not another flat list of open PRs.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which repos (or org + team filter), and whose perspective - \"PRs waiting on *me*\", \"PRs my team owns\", or \"everything open\"\n- Whether drafts should be included (default: exclude, but list drafts older than a week as possibly-forgotten)\n- What counts as blocked for this team (e.g. required approvals count, must CI be green)\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Scannable in 60 seconds. Every PR linked. State whose move it is in two or three words (\"waiting on author\").\n- **Audience:** Reviewers deciding where to spend their next review hour.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- This skill is read-only by default. If the user asks you to nudge authors or post the digest anywhere, show the draft first and wait for `apply`.\n- Never comment, approve, merge, or close without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. List open PRs in scope. For each, gather: author, reviewers requested vs reviews given, review states (approved / changes requested / commented), CI status, mergeability, draft flag, age, and time since last activity.\n2. **Determine whose move it is** - this is the core value:\n   - *Waiting on reviewer* - review requested, no review yet, CI green\n   - *Waiting on author* - changes requested, or CI red, or merge conflicts\n   - *Ready to merge* - approvals met, CI green, no conflicts (call these out loudly; they're free wins)\n   - *Stalled* - no activity from anyone in 5+ days (note who moved last)\n3. **Order the review queue** by: unblocks-others first (release blockers, PRs other PRs are stacked on, bugfixes referenced by open incidents), then small-and-old before large-and-new - a 30-line PR waiting 4 days beats a 2,000-line PR from this morning. Estimate review effort from diff size and files touched (S <100 lines / M <500 / L 500+).\n4. Summarize each PR in one line from its title, description, and changed paths - what it does and blast radius (e.g. \"migrates auth middleware; touches every route\"), not a restated title.\n5. Flag hygiene problems separately: PRs with no description, no linked issue, or 1,000+ lines with no reviewer - these need author action before review time is spent.\n6. If asked to nudge, draft per-author messages listing their waiting PRs (polite, specific: \"needs rebase + one approval\") and hold for `apply`.\n\n## Output format\n\n**Headline** - `N open · R waiting on review · A waiting on author · M ready to merge · S stalled`\n\n**🟢 Ready to merge** - linked list, one line each (who can press the button).\n\n**👀 Review queue (do in this order)** -\n\n| # | PR | One-line summary | Size | Age / idle | Why this order | Suggested reviewer |\n|---|---|---|---|---|---|---|\n\n**⏳ Waiting on author** - PR · what's needed (rebase / fix CI / address review) · idle time.\n\n**🧊 Stalled / needs a decision** - PR · who moved last · suggested unblock.\n\n**Nudge drafts** (only if requested) - per-author messages, held for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Distinguish \"no review yet\" from \"changes requested\" - they need opposite people to act\n- Surface ready-to-merge PRs at the very top; approved-but-unmerged PRs rot into conflicts\n- Weight small old PRs above large new ones in the queue\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent PR states, reviewers, or CI results without a successful `use_tool` call\n- Don't dump a flat list sorted by number - the ordering and whose-move labels ARE the deliverable\n- Don't count a `commented` review as an approval when computing \"ready to merge\"\n- Don't include dependabot/bot PRs in the human review queue (point to /github-dependabot-review instead)\n- Don't post nudges or the digest to any channel without `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **daily**\n\nExample: `/loop 7d Run /github-pr-digest`\n"}, {"id": "github-release-notes", "aiInstallPrompt": "Install the skill 'github-release-notes' by writing the file below.\n\nUniversal path: ./skills/github-release-notes/SKILL.md\nGrok path: ~/.grok/skills/github-release-notes/SKILL.md\nCursor path: ~/.cursor/skills/github-release-notes/SKILL.md\nClaude path: ~/.claude/skills/github-release-notes/SKILL.md\nVS Code agent path: .github/skills/github-release-notes/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: github-release-notes\ndescription: >\n  Generate release notes from commits/PRs. Requires integrations: @github. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"github release notes\", /github-release-notes.\nmetadata:\n  category: github\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-release-notes\n\nGenerate release notes from commits/PRs.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this when cutting a release - a version tag is about to ship and someone must turn a pile of merged PRs into notes humans can read. Works for public changelogs (users deciding whether to upgrade), internal releases (what's in this deploy?), and catch-up notes when several unreleased versions have accumulated.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Repo, the range (`last tag → HEAD` by default - confirm the last tag with a live call), and the new version number\n- Audience: end users (plain language, no internal refactors) vs developers (API changes, migration notes) vs internal team (everything including chores)\n- Existing conventions: look at previous releases in the repo and match their section names, emoji use, and tone before inventing a format\n- Versioning sanity check: if there are breaking changes and the proposed bump is a patch, flag it\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** User-benefit first (\"Fixed crash when…\" not \"Refactored handler…\"). Credit contributors. Link every PR/issue.\n- **Audience:** As chosen above - default to end users if unstated.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show the full draft; wait for user to say `apply` before creating or publishing any GitHub Release or tag.\n- Never publish a release, push a tag, or edit CHANGELOG files without explicit confirmation. Prefer creating the release as a **draft** even after approval, so the user pulls the final trigger in the UI.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Resolve the range: find the latest tag (or the one the user names) and collect merged PRs and direct commits between it and the target ref. Report the raw counts so the user can sanity-check scope.\n2. **Categorize** each change - use PR labels first, then conventional-commit prefixes (`feat:`, `fix:`, `perf:`, `docs:`, `chore:`), then read the PR description as fallback: 🚨 Breaking changes · ✨ Features · 🐛 Fixes · ⚡ Performance · 🔒 Security · 📚 Docs · 🧰 Internal/chores.\n3. **Hunt breaking changes deliberately**: `BREAKING CHANGE` footers, `breaking` labels, major dependency bumps, removed/renamed public APIs visible in PR titles, config or schema migrations. Each one gets a \"what breaks → what to do instead\" migration note. This section can never be silently empty if evidence exists.\n4. Rewrite each entry as user-facing benefit, not implementation: merge multiple PRs that ship one feature into a single bullet; drop pure chores for end-user notes (keep for internal). Format: `Description of the change (#123, thanks @contributor)`.\n5. Add the frame: a 1-2 sentence highlights paragraph up top (the \"why upgrade\" hook), first-time contributor shout-outs, and the compare link (`v1.2.0...v1.3.0`). Note anything shipped behind a feature flag as such.\n6. Flag gaps honestly: PRs with empty descriptions that you couldn't classify go in an \"unclassified - please review\" list for the user rather than being guessed at.\n7. Present the draft. After `apply`, create the GitHub Release **as a draft** with the notes and target tag, and link it.\n\n## Output format\n\n**Range check** - `v1.2.0 → v1.3.0 · 47 PRs, 3 direct commits · 12 authors (2 first-time)`\n\n**Draft release notes** - the complete, copy-ready markdown:\n\n```\n## v1.3.0 - <date>\n\n<1-2 sentence highlights>\n\n### 🚨 Breaking changes\n- <what breaks> → <migration step> (#PR)\n\n### ✨ New features\n### 🐛 Bug fixes\n### ⚡ Performance\n### 🔒 Security\n### 🧰 Internal   ← internal audience only\n\n**New contributors:** @a, @b\n**Full changelog:** v1.2.0...v1.3.0\n```\n\n**Unclassified** - PRs needing a human call, with links.\n\n**Proposed action** - `1. Create draft release v1.3.0 on GitHub` - wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Match the repo's existing release-notes style before imposing your own\n- Lead with breaking changes and give each a migration path\n- Collapse many PRs into one bullet when they're one user-visible change\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent PR numbers, contributor handles, or changes not in the actual range - every bullet must trace to a real `use_tool` result\n- Don't paste raw commit messages as notes (\"fix typo\", \"wip\", \"address review\") - rewrite for the audience\n- Don't bury or omit breaking changes because they're awkward; that's the one section users read\n- Don't publish the release or push tags without `apply` - and even then create it as a draft\n- Don't include revert-pairs (a PR and its revert) as if both shipped\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /github-release-notes or describe the task from the skill description.", "skillContent": "---\nname: github-release-notes\ndescription: >\n  Generate release notes from commits/PRs. Requires integrations: @github. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"github release notes\", /github-release-notes.\nmetadata:\n  category: github\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-release-notes\n\nGenerate release notes from commits/PRs.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this when cutting a release - a version tag is about to ship and someone must turn a pile of merged PRs into notes humans can read. Works for public changelogs (users deciding whether to upgrade), internal releases (what's in this deploy?), and catch-up notes when several unreleased versions have accumulated.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Repo, the range (`last tag → HEAD` by default - confirm the last tag with a live call), and the new version number\n- Audience: end users (plain language, no internal refactors) vs developers (API changes, migration notes) vs internal team (everything including chores)\n- Existing conventions: look at previous releases in the repo and match their section names, emoji use, and tone before inventing a format\n- Versioning sanity check: if there are breaking changes and the proposed bump is a patch, flag it\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** User-benefit first (\"Fixed crash when…\" not \"Refactored handler…\"). Credit contributors. Link every PR/issue.\n- **Audience:** As chosen above - default to end users if unstated.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show the full draft; wait for user to say `apply` before creating or publishing any GitHub Release or tag.\n- Never publish a release, push a tag, or edit CHANGELOG files without explicit confirmation. Prefer creating the release as a **draft** even after approval, so the user pulls the final trigger in the UI.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Resolve the range: find the latest tag (or the one the user names) and collect merged PRs and direct commits between it and the target ref. Report the raw counts so the user can sanity-check scope.\n2. **Categorize** each change - use PR labels first, then conventional-commit prefixes (`feat:`, `fix:`, `perf:`, `docs:`, `chore:`), then read the PR description as fallback: 🚨 Breaking changes · ✨ Features · 🐛 Fixes · ⚡ Performance · 🔒 Security · 📚 Docs · 🧰 Internal/chores.\n3. **Hunt breaking changes deliberately**: `BREAKING CHANGE` footers, `breaking` labels, major dependency bumps, removed/renamed public APIs visible in PR titles, config or schema migrations. Each one gets a \"what breaks → what to do instead\" migration note. This section can never be silently empty if evidence exists.\n4. Rewrite each entry as user-facing benefit, not implementation: merge multiple PRs that ship one feature into a single bullet; drop pure chores for end-user notes (keep for internal). Format: `Description of the change (#123, thanks @contributor)`.\n5. Add the frame: a 1-2 sentence highlights paragraph up top (the \"why upgrade\" hook), first-time contributor shout-outs, and the compare link (`v1.2.0...v1.3.0`). Note anything shipped behind a feature flag as such.\n6. Flag gaps honestly: PRs with empty descriptions that you couldn't classify go in an \"unclassified - please review\" list for the user rather than being guessed at.\n7. Present the draft. After `apply`, create the GitHub Release **as a draft** with the notes and target tag, and link it.\n\n## Output format\n\n**Range check** - `v1.2.0 → v1.3.0 · 47 PRs, 3 direct commits · 12 authors (2 first-time)`\n\n**Draft release notes** - the complete, copy-ready markdown:\n\n```\n## v1.3.0 - <date>\n\n<1-2 sentence highlights>\n\n### 🚨 Breaking changes\n- <what breaks> → <migration step> (#PR)\n\n### ✨ New features\n### 🐛 Bug fixes\n### ⚡ Performance\n### 🔒 Security\n### 🧰 Internal   ← internal audience only\n\n**New contributors:** @a, @b\n**Full changelog:** v1.2.0...v1.3.0\n```\n\n**Unclassified** - PRs needing a human call, with links.\n\n**Proposed action** - `1. Create draft release v1.3.0 on GitHub` - wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Match the repo's existing release-notes style before imposing your own\n- Lead with breaking changes and give each a migration path\n- Collapse many PRs into one bullet when they're one user-visible change\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent PR numbers, contributor handles, or changes not in the actual range - every bullet must trace to a real `use_tool` result\n- Don't paste raw commit messages as notes (\"fix typo\", \"wip\", \"address review\") - rewrite for the audience\n- Don't bury or omit breaking changes because they're awkward; that's the one section users read\n- Don't publish the release or push tags without `apply` - and even then create it as a draft\n- Don't include revert-pairs (a PR and its revert) as if both shipped\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "github-stale-pr-nag", "aiInstallPrompt": "Install the skill 'github-stale-pr-nag' by writing the file below.\n\nUniversal path: ./skills/github-stale-pr-nag/SKILL.md\nGrok path: ~/.grok/skills/github-stale-pr-nag/SKILL.md\nCursor path: ~/.cursor/skills/github-stale-pr-nag/SKILL.md\nClaude path: ~/.claude/skills/github-stale-pr-nag/SKILL.md\nVS Code agent path: .github/skills/github-stale-pr-nag/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: github-stale-pr-nag\ndescription: >\n  List PRs idle more than 7 days. Requires integrations: @github. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"github stale pr nag\", /github-stale-pr-nag.\nmetadata:\n  category: github\n  type: connector\n  cadence: weekly\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-stale-pr-nag\n\nList PRs idle more than 7 days.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this weekly to stop PR rot: branches drifting into conflict, contributors quietly giving up, and approved work never merging. It finds every PR idle past the threshold, works out *why* it stalled and *who* can unstick it, and drafts a tailored (not boilerplate) nudge for each - held for your approval.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Repos in scope and the idle threshold (default 7 days since last human activity - commits, comments, or reviews; ignore bot noise when computing idleness)\n- Nudge channel: comment on the PR itself (default) vs a compiled list for the user to relay elsewhere\n- Team norms: is it acceptable to propose closing abandoned PRs, and after how long (default: propose close at 30+ days idle with an unresponsive author)\n- Anyone currently OOO who shouldn't be pinged\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Nudges are friendly and blame-free - name the *state*, not the person's failure (\"this needs a rebase\" not \"you've ignored this\"). Every nudge names one concrete next step.\n- **Audience:** PR authors and reviewers reading a public comment thread.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show every draft nudge comment in a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never post comments or close PRs without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. List open PRs and compute true idle time from the last **human** event (commit, review, comment) - a bot rebase or CI re-run doesn't make a PR \"active\". Filter to those past the threshold; exclude drafts unless very old (14+ days, likely forgotten).\n2. **Diagnose why each PR stalled** - the nudge depends on it:\n   - *Never reviewed* - review requested, nobody responded → nudge the reviewer(s)\n   - *Changes requested, author silent* - → nudge the author with the count of unresolved threads\n   - *Approved but unmerged* - often just forgotten → nudge whoever has merge rights (easiest win)\n   - *CI red / merge conflict* - → nudge the author with the specific blocker\n   - *Discussion deadlock* - humans disagree in threads → escalate to the user for a decision rather than nagging either side\n3. Sort by cost of continued delay: approved-unmerged first, then conflict-risk (big diffs against fast-moving files), then oldest.\n4. Draft one nudge per PR, tailored to the diagnosis and addressed to the person whose move it is. One concrete ask, one sentence of context, no guilt. Vary the wording - identical boilerplate across ten PRs reads as spam and gets ignored.\n5. For PRs idle 30+ days with an already-nudged, unresponsive author: propose a close-with-comment (\"closing to keep the queue honest - reopen anytime, branch is preserved\") as a separate, clearly-marked item.\n6. Present the review table; on `apply`, post approved comments and report each with a link.\n\n## Output format\n\n**Stale queue** - `N PRs past threshold · A approved-unmerged · R unreviewed · C author-blocked · X proposed closes`\n\n**Review table** -\n\n| # | PR | Idle | Diagnosis | Whose move | Proposed action | Draft nudge (full text) |\n|---|---|---|---|---|---|---|\n\n**Proposed closes** (separate section, if any) - PR, total age, nudge history, full close comment text.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip`. Closes are never included in `apply all` - they must be approved by number.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Compute idleness from human activity only - bots keep zombie PRs looking alive\n- Direct each nudge at the person who can actually act (reviewer vs author vs merger)\n- Highlight approved-but-unmerged PRs as the cheapest wins\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent PR activity, reviewer names, or timestamps without a successful `use_tool` call\n- Don't post any comment without `apply` - nudges are public and reputational\n- Don't send identical copy-paste nudges to every PR; tailored beats boilerplate\n- Don't nag the author when the reviewer is the bottleneck (or vice versa)\n- Don't propose closing a PR that has recent author effort just because reviews lagged - that punishes the wrong person\n- Don't bundle close actions into `apply all`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /github-stale-pr-nag`\n```\n\nAfter writing the file, confirm it exists. The user can then run /github-stale-pr-nag or describe the task from the skill description.", "skillContent": "---\nname: github-stale-pr-nag\ndescription: >\n  List PRs idle more than 7 days. Requires integrations: @github. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"github stale pr nag\", /github-stale-pr-nag.\nmetadata:\n  category: github\n  type: connector\n  cadence: weekly\n  risk: low\n  connectors: github\n  integrations: @github\nallowed-tools: search_tool, use_tool\n---\n# /github-stale-pr-nag\n\nList PRs idle more than 7 days.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this weekly to stop PR rot: branches drifting into conflict, contributors quietly giving up, and approved work never merging. It finds every PR idle past the threshold, works out *why* it stalled and *who* can unstick it, and drafts a tailored (not boilerplate) nudge for each - held for your approval.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Repos in scope and the idle threshold (default 7 days since last human activity - commits, comments, or reviews; ignore bot noise when computing idleness)\n- Nudge channel: comment on the PR itself (default) vs a compiled list for the user to relay elsewhere\n- Team norms: is it acceptable to propose closing abandoned PRs, and after how long (default: propose close at 30+ days idle with an unresponsive author)\n- Anyone currently OOO who shouldn't be pinged\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Nudges are friendly and blame-free - name the *state*, not the person's failure (\"this needs a rebase\" not \"you've ignored this\"). Every nudge names one concrete next step.\n- **Audience:** PR authors and reviewers reading a public comment thread.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show every draft nudge comment in a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never post comments or close PRs without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. List open PRs and compute true idle time from the last **human** event (commit, review, comment) - a bot rebase or CI re-run doesn't make a PR \"active\". Filter to those past the threshold; exclude drafts unless very old (14+ days, likely forgotten).\n2. **Diagnose why each PR stalled** - the nudge depends on it:\n   - *Never reviewed* - review requested, nobody responded → nudge the reviewer(s)\n   - *Changes requested, author silent* - → nudge the author with the count of unresolved threads\n   - *Approved but unmerged* - often just forgotten → nudge whoever has merge rights (easiest win)\n   - *CI red / merge conflict* - → nudge the author with the specific blocker\n   - *Discussion deadlock* - humans disagree in threads → escalate to the user for a decision rather than nagging either side\n3. Sort by cost of continued delay: approved-unmerged first, then conflict-risk (big diffs against fast-moving files), then oldest.\n4. Draft one nudge per PR, tailored to the diagnosis and addressed to the person whose move it is. One concrete ask, one sentence of context, no guilt. Vary the wording - identical boilerplate across ten PRs reads as spam and gets ignored.\n5. For PRs idle 30+ days with an already-nudged, unresponsive author: propose a close-with-comment (\"closing to keep the queue honest - reopen anytime, branch is preserved\") as a separate, clearly-marked item.\n6. Present the review table; on `apply`, post approved comments and report each with a link.\n\n## Output format\n\n**Stale queue** - `N PRs past threshold · A approved-unmerged · R unreviewed · C author-blocked · X proposed closes`\n\n**Review table** -\n\n| # | PR | Idle | Diagnosis | Whose move | Proposed action | Draft nudge (full text) |\n|---|---|---|---|---|---|---|\n\n**Proposed closes** (separate section, if any) - PR, total age, nudge history, full close comment text.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip`. Closes are never included in `apply all` - they must be approved by number.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Compute idleness from human activity only - bots keep zombie PRs looking alive\n- Direct each nudge at the person who can actually act (reviewer vs author vs merger)\n- Highlight approved-but-unmerged PRs as the cheapest wins\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent PR activity, reviewer names, or timestamps without a successful `use_tool` call\n- Don't post any comment without `apply` - nudges are public and reputational\n- Don't send identical copy-paste nudges to every PR; tailored beats boilerplate\n- Don't nag the author when the reviewer is the bottleneck (or vice versa)\n- Don't propose closing a PR that has recent author effort just because reviews lagged - that punishes the wrong person\n- Don't bundle close actions into `apply all`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@github) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /github-stale-pr-nag`\n"}]};

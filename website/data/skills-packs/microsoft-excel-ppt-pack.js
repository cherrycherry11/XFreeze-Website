window.__XF_SKILL_PACKS__=window.__XF_SKILL_PACKS__||{};window.__XF_SKILL_PACKS__["microsoft-excel-ppt-pack"]={"id": "microsoft-excel-ppt-pack", "skills": [{"id": "m365-excel-budget-upload", "aiInstallPrompt": "Install the skill 'm365-excel-budget-upload' by writing the file below.\n\nUniversal path: ./skills/m365-excel-budget-upload/SKILL.md\nGrok path: ~/.grok/skills/m365-excel-budget-upload/SKILL.md\nCursor path: ~/.cursor/skills/m365-excel-budget-upload/SKILL.md\nClaude path: ~/.claude/skills/m365-excel-budget-upload/SKILL.md\nVS Code agent path: .github/skills/m365-excel-budget-upload/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: m365-excel-budget-upload\ndescription: >\n  Upload local budget xlsx to SharePoint. Requires integrations: @sharepoint, @filesystem, @excel. Uses search_tool + use_tool. Cadence: monthly. Triggers: \"m365 excel budget upload\", /m365-excel-budget-upload.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: monthly\n  risk: medium\n  connectors: microsoft-sharepoint,filesystem\n  integrations: @sharepoint,@filesystem,@excel\nallowed-tools: search_tool, use_tool\n---\n# /m365-excel-budget-upload\n\nUpload local budget xlsx to SharePoint.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nMonth-end: the budget workbook is finalized locally and has to land in the SharePoint finance library - where the team, and possibly a Power BI refresh, expect an exact name in an exact place. This validates the workbook before it ships, uploads it correctly, and proves the upload happened.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Local file path of the finished workbook\n- Target site › library › folder, and the naming convention (e.g. `FY26-{MM}-budget.xlsx`)\n- Replace behavior: same filename (SharePoint keeps version history - usually right) vs a new dated sibling file\n- Validation expectations: required sheets/tabs, key totals cells, columns that must exist\n- Who to notify once it's up (draft only)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@excel** - Microsoft Excel (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/xlsx`\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local Excel editing\n\nAfter downloading from @onedrive:\n- Run **`/xlsx`** for spreadsheet analysis and edits\n- User may attach `@excel` in the prompt\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Pre-flight the local file: it exists, isn't a `~$` Office lock/temp file, isn't zero bytes, and opens cleanly.\n2. Validate contents: required sheets present; scan for `#REF!`, `#VALUE!`, `#DIV/0!` and other error cells; spot-check that totals rows actually compute; confirm the month/period inside the workbook matches the filename about to be used.\n3. Check the target folder: does a file for this period already exist? Report whether the upload will create version 2+ of the same name (recoverable, keeps links/Power BI connections intact) or a new file.\n4. Present the pre-flight results and upload plan; upload on apply.\n5. Verify: re-list the target folder, confirm filename, size, and timestamp; capture the version number.\n6. Return an org-scoped sharing link (never an anyone-link for budget data) and the notification draft.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Pre-flight checks** - checklist with pass/fail per item: file OK · required sheets · error-cell scan (locations if found) · totals sanity · period matches filename.\n\n**Upload plan** - | Source path | → SharePoint path | New file or version? | Link scope |\n\nAfter apply - **Uploaded**: link · version # · size/timestamp verification line · **Notification draft** naming the file, period, and what changed vs last month (only if the user supplied that context).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint / @filesystem / @excel in the prompt when the session needs that app\n- Block the upload (report, don't proceed) when error cells or missing sheets are found - a broken budget in the library is worse than a late one\n- Prefer same-name replacement so version history and downstream connections (Power BI, links) survive\n- Verify after upload by re-listing, not by assuming success\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't upload a workbook containing formula errors without flagging them first\n- Don't create `final_v2_FINAL` siblings when same-name versioning does the job\n- Don't generate anyone-can-view links for financial data\n- Don't delete or move the local source file\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint, @filesystem, @excel) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **monthly**\n\nExample: `/loop 7d Run /m365-excel-budget-upload`\n```\n\nAfter writing the file, confirm it exists. The user can then run /m365-excel-budget-upload or describe the task from the skill description.", "skillContent": "---\nname: m365-excel-budget-upload\ndescription: >\n  Upload local budget xlsx to SharePoint. Requires integrations: @sharepoint, @filesystem, @excel. Uses search_tool + use_tool. Cadence: monthly. Triggers: \"m365 excel budget upload\", /m365-excel-budget-upload.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: monthly\n  risk: medium\n  connectors: microsoft-sharepoint,filesystem\n  integrations: @sharepoint,@filesystem,@excel\nallowed-tools: search_tool, use_tool\n---\n# /m365-excel-budget-upload\n\nUpload local budget xlsx to SharePoint.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nMonth-end: the budget workbook is finalized locally and has to land in the SharePoint finance library - where the team, and possibly a Power BI refresh, expect an exact name in an exact place. This validates the workbook before it ships, uploads it correctly, and proves the upload happened.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Local file path of the finished workbook\n- Target site › library › folder, and the naming convention (e.g. `FY26-{MM}-budget.xlsx`)\n- Replace behavior: same filename (SharePoint keeps version history - usually right) vs a new dated sibling file\n- Validation expectations: required sheets/tabs, key totals cells, columns that must exist\n- Who to notify once it's up (draft only)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@excel** - Microsoft Excel (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/xlsx`\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local Excel editing\n\nAfter downloading from @onedrive:\n- Run **`/xlsx`** for spreadsheet analysis and edits\n- User may attach `@excel` in the prompt\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Pre-flight the local file: it exists, isn't a `~$` Office lock/temp file, isn't zero bytes, and opens cleanly.\n2. Validate contents: required sheets present; scan for `#REF!`, `#VALUE!`, `#DIV/0!` and other error cells; spot-check that totals rows actually compute; confirm the month/period inside the workbook matches the filename about to be used.\n3. Check the target folder: does a file for this period already exist? Report whether the upload will create version 2+ of the same name (recoverable, keeps links/Power BI connections intact) or a new file.\n4. Present the pre-flight results and upload plan; upload on apply.\n5. Verify: re-list the target folder, confirm filename, size, and timestamp; capture the version number.\n6. Return an org-scoped sharing link (never an anyone-link for budget data) and the notification draft.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Pre-flight checks** - checklist with pass/fail per item: file OK · required sheets · error-cell scan (locations if found) · totals sanity · period matches filename.\n\n**Upload plan** - | Source path | → SharePoint path | New file or version? | Link scope |\n\nAfter apply - **Uploaded**: link · version # · size/timestamp verification line · **Notification draft** naming the file, period, and what changed vs last month (only if the user supplied that context).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint / @filesystem / @excel in the prompt when the session needs that app\n- Block the upload (report, don't proceed) when error cells or missing sheets are found - a broken budget in the library is worse than a late one\n- Prefer same-name replacement so version history and downstream connections (Power BI, links) survive\n- Verify after upload by re-listing, not by assuming success\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't upload a workbook containing formula errors without flagging them first\n- Don't create `final_v2_FINAL` siblings when same-name versioning does the job\n- Don't generate anyone-can-view links for financial data\n- Don't delete or move the local source file\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint, @filesystem, @excel) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **monthly**\n\nExample: `/loop 7d Run /m365-excel-budget-upload`\n"}, {"id": "m365-excel-download-analyze", "aiInstallPrompt": "Install the skill 'm365-excel-download-analyze' by writing the file below.\n\nUniversal path: ./skills/m365-excel-download-analyze/SKILL.md\nGrok path: ~/.grok/skills/m365-excel-download-analyze/SKILL.md\nCursor path: ~/.cursor/skills/m365-excel-download-analyze/SKILL.md\nClaude path: ~/.claude/skills/m365-excel-download-analyze/SKILL.md\nVS Code agent path: .github/skills/m365-excel-download-analyze/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: m365-excel-download-analyze\ndescription: >\n  Download Excel from OneDrive, analyze with xlsx skill. Requires integrations: @onedrive, @filesystem, @excel. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"m365 excel download analyze\", /m365-excel-download-analyze.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@excel\nallowed-tools: search_tool, use_tool\n---\n# /m365-excel-download-analyze\n\nDownload Excel from OneDrive, analyze with xlsx skill.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nThere's a workbook in OneDrive and a real question about it - what's driving the variance, which rows are outliers, is this data even clean? This downloads the file, maps what's actually in it (formulas, hidden sheets, pivots), runs the analysis locally via the xlsx tooling, and reports findings with every number traceable to a sheet and range.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which file: path, or search terms to find it in OneDrive\n- The actual question (top drivers? month-over-month? duplicates? data quality?) - 'analyze it' gets one clarifying question\n- Which sheets/ranges matter, if the user knows\n- Output: narrative findings, a table, or a new summary sheet/file (write-back is proposed separately)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@excel** - Microsoft Excel (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/xlsx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local Excel editing\n\nAfter downloading from @onedrive:\n- Run **`/xlsx`** for spreadsheet analysis and edits\n- User may attach `@excel` in the prompt\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Locate and download the workbook to the local scratchpad; note the source path and modified timestamp for the report header.\n2. Map the workbook before analyzing: every sheet (including **hidden** ones), used ranges, header rows, which cells are formulas vs static values, pivot tables, filtered ranges. The map prevents analyzing a presentation sheet as if it were data.\n3. Identify the real data table(s); note quality issues quietly logged for the caveats section - merged header cells, mixed types in a column, totals rows embedded mid-data, trailing blank-ish rows.\n4. Run the analysis that answers the question using the local xlsx tooling: aggregations, deltas, outliers, duplicate detection - computed from the actual cells, never estimated.\n5. Tie every reported number to its provenance: `'Sales'!B2:B145`, filter applied, rows excluded and why.\n6. If the user wants results written back: propose a **new** sheet or new file - never overwriting source cells - as a separate apply step.\n7. This run is read-only: report findings and drafts in chat; do not modify, move, send, or mark anything without a separate confirmed `apply`.\n\n## Output format\n\n**Workbook map** - | Sheet | Rows × cols used | Formulas? | Hidden? | Role (data / report / scratch) | + data-quality notes.\n\n**Findings** - each one: the statement, the numbers behind it, and the source range. Example shape: \"Region West drives 62% of the variance - `Data!F2:F890`, Mar vs Feb, 3 refund rows excluded (listed below).\"\n\n**Caveats** - exclusions, assumptions, quality issues that could move the answer.\n\n**Optional write-back plan** - new sheet/file proposal, held for apply.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @excel in the prompt when the session needs that app\n- Check hidden sheets and note filtered-out rows - data hides there\n- Distinguish formula cells from static values before treating anything as ground truth\n- Give provenance (sheet!range) for every reported figure\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't modify the source workbook during analysis - write-back is a separate proposed step\n- Don't report a number you didn't compute from the file\n- Don't average a column that contains an embedded totals row without excluding it\n- Don't ignore hidden sheets/columns silently - at minimum, list them\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @excel) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /m365-excel-download-analyze or describe the task from the skill description.", "skillContent": "---\nname: m365-excel-download-analyze\ndescription: >\n  Download Excel from OneDrive, analyze with xlsx skill. Requires integrations: @onedrive, @filesystem, @excel. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"m365 excel download analyze\", /m365-excel-download-analyze.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@excel\nallowed-tools: search_tool, use_tool\n---\n# /m365-excel-download-analyze\n\nDownload Excel from OneDrive, analyze with xlsx skill.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nThere's a workbook in OneDrive and a real question about it - what's driving the variance, which rows are outliers, is this data even clean? This downloads the file, maps what's actually in it (formulas, hidden sheets, pivots), runs the analysis locally via the xlsx tooling, and reports findings with every number traceable to a sheet and range.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which file: path, or search terms to find it in OneDrive\n- The actual question (top drivers? month-over-month? duplicates? data quality?) - 'analyze it' gets one clarifying question\n- Which sheets/ranges matter, if the user knows\n- Output: narrative findings, a table, or a new summary sheet/file (write-back is proposed separately)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@excel** - Microsoft Excel (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/xlsx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local Excel editing\n\nAfter downloading from @onedrive:\n- Run **`/xlsx`** for spreadsheet analysis and edits\n- User may attach `@excel` in the prompt\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Locate and download the workbook to the local scratchpad; note the source path and modified timestamp for the report header.\n2. Map the workbook before analyzing: every sheet (including **hidden** ones), used ranges, header rows, which cells are formulas vs static values, pivot tables, filtered ranges. The map prevents analyzing a presentation sheet as if it were data.\n3. Identify the real data table(s); note quality issues quietly logged for the caveats section - merged header cells, mixed types in a column, totals rows embedded mid-data, trailing blank-ish rows.\n4. Run the analysis that answers the question using the local xlsx tooling: aggregations, deltas, outliers, duplicate detection - computed from the actual cells, never estimated.\n5. Tie every reported number to its provenance: `'Sales'!B2:B145`, filter applied, rows excluded and why.\n6. If the user wants results written back: propose a **new** sheet or new file - never overwriting source cells - as a separate apply step.\n7. This run is read-only: report findings and drafts in chat; do not modify, move, send, or mark anything without a separate confirmed `apply`.\n\n## Output format\n\n**Workbook map** - | Sheet | Rows × cols used | Formulas? | Hidden? | Role (data / report / scratch) | + data-quality notes.\n\n**Findings** - each one: the statement, the numbers behind it, and the source range. Example shape: \"Region West drives 62% of the variance - `Data!F2:F890`, Mar vs Feb, 3 refund rows excluded (listed below).\"\n\n**Caveats** - exclusions, assumptions, quality issues that could move the answer.\n\n**Optional write-back plan** - new sheet/file proposal, held for apply.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @excel in the prompt when the session needs that app\n- Check hidden sheets and note filtered-out rows - data hides there\n- Distinguish formula cells from static values before treating anything as ground truth\n- Give provenance (sheet!range) for every reported figure\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't modify the source workbook during analysis - write-back is a separate proposed step\n- Don't report a number you didn't compute from the file\n- Don't average a column that contains an embedded totals row without excluding it\n- Don't ignore hidden sheets/columns silently - at minimum, list them\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @excel) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "m365-excel-pivot-report", "aiInstallPrompt": "Install the skill 'm365-excel-pivot-report' by writing the file below.\n\nUniversal path: ./skills/m365-excel-pivot-report/SKILL.md\nGrok path: ~/.grok/skills/m365-excel-pivot-report/SKILL.md\nCursor path: ~/.cursor/skills/m365-excel-pivot-report/SKILL.md\nClaude path: ~/.claude/skills/m365-excel-pivot-report/SKILL.md\nVS Code agent path: .github/skills/m365-excel-pivot-report/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: m365-excel-pivot-report\ndescription: >\n  Refresh pivot report workbook. Requires integrations: @onedrive, @filesystem, @excel. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"m365 excel pivot report\", /m365-excel-pivot-report.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: weekly\n  risk: medium\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@excel\nallowed-tools: search_tool, use_tool\n---\n# /m365-excel-pivot-report\n\nRefresh pivot report workbook.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nThe weekly reporting workbook lives in OneDrive: a raw-data sheet feeds pivot tables that feed the report page. Every week someone appends the new extract, refreshes, sanity-checks, and re-uploads - and every few months someone breaks it by pasting columns in the wrong order. This does the round-trip carefully.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Workbook location in OneDrive, and which sheet is raw data vs which are pivots/report\n- The new data: an export file, another workbook, or pasted rows - and its period\n- Append below existing data vs replace the data sheet contents\n- Upload back to the same path/name (version-preserving - default) or elsewhere\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@excel** - Microsoft Excel (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/xlsx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local Excel editing\n\nAfter downloading from @onedrive:\n- Run **`/xlsx`** for spreadsheet analysis and edits\n- User may attach `@excel` in the prompt\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Download the workbook; map data sheet(s), each pivot (and its source range), and any formula cells referencing pivot output.\n2. Schema-check the incoming data against the data sheet: same columns, same order, same headers, compatible types, date formats matching. A silent column swap is the classic pivot-killer - any mismatch stops the run with a report.\n3. Append (or replace) in the data sheet only; never hand-edit pivot or report sheets.\n4. Extend the pivot source: if pivots read a Table or named range, confirm it grew to include the new rows; if they read a static range, extend it explicitly.\n5. Refresh pivots via the local xlsx tooling, then sanity-check: total row count = old + new; pivot grand totals match the data sheet's own sums; the report's date range now covers the new period. Be honest about limits - slicers or external data connections that the tooling can't refresh get flagged for a one-time open-in-Excel, not silently skipped.\n6. Show the refresh report; on apply, upload to the same path/name so SharePoint/OneDrive versioning keeps last week recoverable, and confirm the new version.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Refresh report**\n- **Data in**: rows appended · period · source\n- **Schema check**: per-column pass/fail table (blocks on fail)\n- **Pivot status**: | Pivot | Source range before → after | Refreshed? | Grand total (old → new) |\n- **Sanity checks**: row-count math · grand-total vs source-sum match · period coverage\n- **Honest flags**: anything needing a manual open-in-Excel (slicers, external connections)\n\n**Upload plan** → after apply: link + version number, with 'last week is version N−1' rollback note.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @excel in the prompt when the session needs that app\n- Stop and report on any schema mismatch instead of forcing the append\n- Confirm the pivot source range actually grew before calling it refreshed\n- Upload over the same filename so version history is the rollback\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't overwrite formula cells or pivot sheets with static values\n- Don't append columns in a different order than the existing schema\n- Don't upload when a sanity check fails - a wrong report shipped weekly is trusted and wrong\n- Don't claim a refresh happened for elements the tooling couldn't actually refresh\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @excel) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /m365-excel-pivot-report`\n```\n\nAfter writing the file, confirm it exists. The user can then run /m365-excel-pivot-report or describe the task from the skill description.", "skillContent": "---\nname: m365-excel-pivot-report\ndescription: >\n  Refresh pivot report workbook. Requires integrations: @onedrive, @filesystem, @excel. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"m365 excel pivot report\", /m365-excel-pivot-report.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: weekly\n  risk: medium\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@excel\nallowed-tools: search_tool, use_tool\n---\n# /m365-excel-pivot-report\n\nRefresh pivot report workbook.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nThe weekly reporting workbook lives in OneDrive: a raw-data sheet feeds pivot tables that feed the report page. Every week someone appends the new extract, refreshes, sanity-checks, and re-uploads - and every few months someone breaks it by pasting columns in the wrong order. This does the round-trip carefully.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Workbook location in OneDrive, and which sheet is raw data vs which are pivots/report\n- The new data: an export file, another workbook, or pasted rows - and its period\n- Append below existing data vs replace the data sheet contents\n- Upload back to the same path/name (version-preserving - default) or elsewhere\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@excel** - Microsoft Excel (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/xlsx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local Excel editing\n\nAfter downloading from @onedrive:\n- Run **`/xlsx`** for spreadsheet analysis and edits\n- User may attach `@excel` in the prompt\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Download the workbook; map data sheet(s), each pivot (and its source range), and any formula cells referencing pivot output.\n2. Schema-check the incoming data against the data sheet: same columns, same order, same headers, compatible types, date formats matching. A silent column swap is the classic pivot-killer - any mismatch stops the run with a report.\n3. Append (or replace) in the data sheet only; never hand-edit pivot or report sheets.\n4. Extend the pivot source: if pivots read a Table or named range, confirm it grew to include the new rows; if they read a static range, extend it explicitly.\n5. Refresh pivots via the local xlsx tooling, then sanity-check: total row count = old + new; pivot grand totals match the data sheet's own sums; the report's date range now covers the new period. Be honest about limits - slicers or external data connections that the tooling can't refresh get flagged for a one-time open-in-Excel, not silently skipped.\n6. Show the refresh report; on apply, upload to the same path/name so SharePoint/OneDrive versioning keeps last week recoverable, and confirm the new version.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Refresh report**\n- **Data in**: rows appended · period · source\n- **Schema check**: per-column pass/fail table (blocks on fail)\n- **Pivot status**: | Pivot | Source range before → after | Refreshed? | Grand total (old → new) |\n- **Sanity checks**: row-count math · grand-total vs source-sum match · period coverage\n- **Honest flags**: anything needing a manual open-in-Excel (slicers, external connections)\n\n**Upload plan** → after apply: link + version number, with 'last week is version N−1' rollback note.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @excel in the prompt when the session needs that app\n- Stop and report on any schema mismatch instead of forcing the append\n- Confirm the pivot source range actually grew before calling it refreshed\n- Upload over the same filename so version history is the rollback\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't overwrite formula cells or pivot sheets with static values\n- Don't append columns in a different order than the existing schema\n- Don't upload when a sanity check fails - a wrong report shipped weekly is trusted and wrong\n- Don't claim a refresh happened for elements the tooling couldn't actually refresh\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @excel) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /m365-excel-pivot-report`\n"}, {"id": "m365-pptx-download-edit", "aiInstallPrompt": "Install the skill 'm365-pptx-download-edit' by writing the file below.\n\nUniversal path: ./skills/m365-pptx-download-edit/SKILL.md\nGrok path: ~/.grok/skills/m365-pptx-download-edit/SKILL.md\nCursor path: ~/.cursor/skills/m365-pptx-download-edit/SKILL.md\nClaude path: ~/.claude/skills/m365-pptx-download-edit/SKILL.md\nVS Code agent path: .github/skills/m365-pptx-download-edit/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: m365-pptx-download-edit\ndescription: >\n  Download PPT from OneDrive, edit with pptx skill, re-upload. Requires integrations: @onedrive, @filesystem, @powerpoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"m365 pptx download edit\", /m365-pptx-download-edit.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@powerpoint\nallowed-tools: search_tool, use_tool\n---\n# /m365-pptx-download-edit\n\nDownload PPT from OneDrive, edit with pptx skill, re-upload.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nA deck in OneDrive needs surgical edits - retitle slides, swap figures, update a section, add a slide - without wrecking the template. This downloads it, edits via the local pptx tooling inside the existing layouts and theme, shows a slide-by-slide diff, and re-uploads with version history as the undo.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which deck (path or search) and the edits, ideally slide-by-slide\n- Brand/template constraints: which master/layouts are canonical; anything off-limits (logo slide, legal footer)\n- Whether speaker notes change too\n- Re-upload: same name (new version - default) vs a copy\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@powerpoint** - Microsoft Powerpoint (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/pptx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local PowerPoint editing\n\nAfter downloading from @onedrive:\n- Run **`/pptx`** (bundled skill) for all `.pptx` create/edit work\n- User may also attach `@powerpoint` in the prompt to scope the session to Office\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Download; inventory the deck first: slide number, title, layout - and reconcile against the user's slide references, since hidden slides make 'slide 7' ambiguous. Confirm targets before touching anything.\n2. Make edits inside existing placeholders and layouts: replace text in the title/body placeholders rather than dropping new floating text boxes; keep theme fonts and colors rather than hard-coding.\n3. After text changes, check for overflow - text spilling out of placeholders is the visible signature of an automated edit.\n4. Touch speaker notes only if asked; leave slide numbering/footers consistent with the deck's convention.\n5. Produce the slide-by-slide diff of every change; deleted slides get their content listed in the diff so nothing vanishes unaccounted.\n6. On apply: save and upload (same name → new version), verify the upload, and return link + version so the previous deck is one restore away.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Deck map & targets** - | Slide # | Title | Layout | Planned change | - confirmed before editing.\n\n**Edit diff** - per changed slide: before → after text, notes changed (y/n), overflow check result. Deleted slides: full content listed.\n\n**Upload plan** → after apply: link · new version # · rollback note ('previous deck = version N−1').\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @powerpoint in the prompt when the session needs that app\n- Edit inside existing placeholders and keep theme fonts/colors\n- Reconcile slide numbers against actual titles before editing - hidden slides shift counts\n- Run an overflow check on every edited text frame\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't rebuild a slide from scratch when a text edit suffices\n- Don't detach from the slide master or hard-code brand colors\n- Don't delete slides without listing their content in the review\n- Don't re-upload before the diff is approved\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @powerpoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /m365-pptx-download-edit or describe the task from the skill description.", "skillContent": "---\nname: m365-pptx-download-edit\ndescription: >\n  Download PPT from OneDrive, edit with pptx skill, re-upload. Requires integrations: @onedrive, @filesystem, @powerpoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"m365 pptx download edit\", /m365-pptx-download-edit.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@powerpoint\nallowed-tools: search_tool, use_tool\n---\n# /m365-pptx-download-edit\n\nDownload PPT from OneDrive, edit with pptx skill, re-upload.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nA deck in OneDrive needs surgical edits - retitle slides, swap figures, update a section, add a slide - without wrecking the template. This downloads it, edits via the local pptx tooling inside the existing layouts and theme, shows a slide-by-slide diff, and re-uploads with version history as the undo.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which deck (path or search) and the edits, ideally slide-by-slide\n- Brand/template constraints: which master/layouts are canonical; anything off-limits (logo slide, legal footer)\n- Whether speaker notes change too\n- Re-upload: same name (new version - default) vs a copy\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@powerpoint** - Microsoft Powerpoint (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/pptx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local PowerPoint editing\n\nAfter downloading from @onedrive:\n- Run **`/pptx`** (bundled skill) for all `.pptx` create/edit work\n- User may also attach `@powerpoint` in the prompt to scope the session to Office\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Download; inventory the deck first: slide number, title, layout - and reconcile against the user's slide references, since hidden slides make 'slide 7' ambiguous. Confirm targets before touching anything.\n2. Make edits inside existing placeholders and layouts: replace text in the title/body placeholders rather than dropping new floating text boxes; keep theme fonts and colors rather than hard-coding.\n3. After text changes, check for overflow - text spilling out of placeholders is the visible signature of an automated edit.\n4. Touch speaker notes only if asked; leave slide numbering/footers consistent with the deck's convention.\n5. Produce the slide-by-slide diff of every change; deleted slides get their content listed in the diff so nothing vanishes unaccounted.\n6. On apply: save and upload (same name → new version), verify the upload, and return link + version so the previous deck is one restore away.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Deck map & targets** - | Slide # | Title | Layout | Planned change | - confirmed before editing.\n\n**Edit diff** - per changed slide: before → after text, notes changed (y/n), overflow check result. Deleted slides: full content listed.\n\n**Upload plan** → after apply: link · new version # · rollback note ('previous deck = version N−1').\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @powerpoint in the prompt when the session needs that app\n- Edit inside existing placeholders and keep theme fonts/colors\n- Reconcile slide numbers against actual titles before editing - hidden slides shift counts\n- Run an overflow check on every edited text frame\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't rebuild a slide from scratch when a text edit suffices\n- Don't detach from the slide master or hard-code brand colors\n- Don't delete slides without listing their content in the review\n- Don't re-upload before the diff is approved\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @powerpoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "m365-pptx-quarterly-deck", "aiInstallPrompt": "Install the skill 'm365-pptx-quarterly-deck' by writing the file below.\n\nUniversal path: ./skills/m365-pptx-quarterly-deck/SKILL.md\nGrok path: ~/.grok/skills/m365-pptx-quarterly-deck/SKILL.md\nCursor path: ~/.cursor/skills/m365-pptx-quarterly-deck/SKILL.md\nClaude path: ~/.claude/skills/m365-pptx-quarterly-deck/SKILL.md\nVS Code agent path: .github/skills/m365-pptx-quarterly-deck/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: m365-pptx-quarterly-deck\ndescription: >\n  Fill quarterly review deck from data. Requires integrations: @onedrive, @filesystem, @powerpoint. Uses search_tool + use_tool. Cadence: quarterly. Triggers: \"m365 pptx quarterly deck\", /m365-pptx-quarterly-deck.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: quarterly\n  risk: medium\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@powerpoint\nallowed-tools: search_tool, use_tool\n---\n# /m365-pptx-quarterly-deck\n\nFill quarterly review deck from data.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nQBR season: last quarter's deck is the skeleton, this quarter's numbers live in Excel, and someone has to marry them without leaving a single 'Q1' or stale figure behind. This copies the deck forward, sweeps every label, fills data slides from the source workbook, and marks narrative slides for the human.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Last quarter's deck (the template) and where the new one should live\n- Quarter labeling convention (Q3 FY26? 2026-Q2?) and the as-of date\n- Metric sources: which workbook(s)/sheets are canonical for each KPI\n- Which slides are data vs narrative vs unchanged boilerplate\n- Audience (board vs all-hands changes what gets flagged for narrative)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@powerpoint** - Microsoft Powerpoint (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/pptx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local PowerPoint editing\n\nAfter downloading from @onedrive:\n- Run **`/pptx`** (bundled skill) for all `.pptx` create/edit work\n- User may also attach `@powerpoint` in the prompt to scope the session to Office\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Copy last quarter's deck to the new filename first - the prior deck is a record and never edited in place.\n2. Global label sweep: every quarter reference in titles, body text, chart titles, footers, and the title slide gets updated; list each replacement so the sweep is auditable.\n3. Pull this quarter's metrics from the source workbook(s), computing QoQ deltas from the actual cells; record the source cell for every number placed.\n4. Update data slides: tables and text figures directly; charts honestly - a chart with editable underlying data gets updated, a chart that's a pasted **image** cannot be edited and is flagged for manual redo, never faked with an overlay.\n5. Narrative slides ('key wins', 'outlook') get a `[NEEDS NARRATIVE]` marker plus a suggested bullet drawn strictly from the data movement - commentary on *why* metrics moved is the user's, not invented.\n6. Consistency pass (same KPI identical on every slide it appears), then review of the slide-by-slide plan; on apply, save and upload, returning link + open-items list.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Deck build plan** - | Slide | Type (data / narrative / label-only / boilerplate) | Number source (workbook!cell) | Status (updated / needs your narrative / manual chart redo) |\n\n**Metric summary** - | KPI | Last Q | This Q | Δ | Source cell | - every value computed, none estimated.\n\n**Label sweep log** - each old → new replacement and where.\n\n**Open items for you** - narrative slides awaiting words, image-charts needing manual redo. After apply: deck link.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @powerpoint in the prompt when the session needs that app\n- Copy the deck before touching anything - last quarter's file is immutable\n- Log every label replacement; the stale-'Q1'-on-slide-19 failure is the one this exists to kill\n- Distinguish editable charts from pasted images and say which is which\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent commentary for why a metric moved - mark it for the user\n- Don't leave any prior-quarter label or figure anywhere, including footers and chart titles\n- Don't edit last quarter's deck in place\n- Don't paper over an un-editable chart image with a text box\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @powerpoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **quarterly**\n\nExample: `/loop 7d Run /m365-pptx-quarterly-deck`\n```\n\nAfter writing the file, confirm it exists. The user can then run /m365-pptx-quarterly-deck or describe the task from the skill description.", "skillContent": "---\nname: m365-pptx-quarterly-deck\ndescription: >\n  Fill quarterly review deck from data. Requires integrations: @onedrive, @filesystem, @powerpoint. Uses search_tool + use_tool. Cadence: quarterly. Triggers: \"m365 pptx quarterly deck\", /m365-pptx-quarterly-deck.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: quarterly\n  risk: medium\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@powerpoint\nallowed-tools: search_tool, use_tool\n---\n# /m365-pptx-quarterly-deck\n\nFill quarterly review deck from data.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nQBR season: last quarter's deck is the skeleton, this quarter's numbers live in Excel, and someone has to marry them without leaving a single 'Q1' or stale figure behind. This copies the deck forward, sweeps every label, fills data slides from the source workbook, and marks narrative slides for the human.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Last quarter's deck (the template) and where the new one should live\n- Quarter labeling convention (Q3 FY26? 2026-Q2?) and the as-of date\n- Metric sources: which workbook(s)/sheets are canonical for each KPI\n- Which slides are data vs narrative vs unchanged boilerplate\n- Audience (board vs all-hands changes what gets flagged for narrative)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@powerpoint** - Microsoft Powerpoint (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/pptx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local PowerPoint editing\n\nAfter downloading from @onedrive:\n- Run **`/pptx`** (bundled skill) for all `.pptx` create/edit work\n- User may also attach `@powerpoint` in the prompt to scope the session to Office\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Copy last quarter's deck to the new filename first - the prior deck is a record and never edited in place.\n2. Global label sweep: every quarter reference in titles, body text, chart titles, footers, and the title slide gets updated; list each replacement so the sweep is auditable.\n3. Pull this quarter's metrics from the source workbook(s), computing QoQ deltas from the actual cells; record the source cell for every number placed.\n4. Update data slides: tables and text figures directly; charts honestly - a chart with editable underlying data gets updated, a chart that's a pasted **image** cannot be edited and is flagged for manual redo, never faked with an overlay.\n5. Narrative slides ('key wins', 'outlook') get a `[NEEDS NARRATIVE]` marker plus a suggested bullet drawn strictly from the data movement - commentary on *why* metrics moved is the user's, not invented.\n6. Consistency pass (same KPI identical on every slide it appears), then review of the slide-by-slide plan; on apply, save and upload, returning link + open-items list.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Deck build plan** - | Slide | Type (data / narrative / label-only / boilerplate) | Number source (workbook!cell) | Status (updated / needs your narrative / manual chart redo) |\n\n**Metric summary** - | KPI | Last Q | This Q | Δ | Source cell | - every value computed, none estimated.\n\n**Label sweep log** - each old → new replacement and where.\n\n**Open items for you** - narrative slides awaiting words, image-charts needing manual redo. After apply: deck link.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @powerpoint in the prompt when the session needs that app\n- Copy the deck before touching anything - last quarter's file is immutable\n- Log every label replacement; the stale-'Q1'-on-slide-19 failure is the one this exists to kill\n- Distinguish editable charts from pasted images and say which is which\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent commentary for why a metric moved - mark it for the user\n- Don't leave any prior-quarter label or figure anywhere, including footers and chart titles\n- Don't edit last quarter's deck in place\n- Don't paper over an un-editable chart image with a text box\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @powerpoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **quarterly**\n\nExample: `/loop 7d Run /m365-pptx-quarterly-deck`\n"}, {"id": "m365-pptx-sales-deck-refresh", "aiInstallPrompt": "Install the skill 'm365-pptx-sales-deck-refresh' by writing the file below.\n\nUniversal path: ./skills/m365-pptx-sales-deck-refresh/SKILL.md\nGrok path: ~/.grok/skills/m365-pptx-sales-deck-refresh/SKILL.md\nCursor path: ~/.cursor/skills/m365-pptx-sales-deck-refresh/SKILL.md\nClaude path: ~/.claude/skills/m365-pptx-sales-deck-refresh/SKILL.md\nVS Code agent path: .github/skills/m365-pptx-sales-deck-refresh/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: m365-pptx-sales-deck-refresh\ndescription: >\n  Update sales deck metrics from Excel. Requires integrations: @onedrive, @filesystem, @powerpoint. Uses search_tool + use_tool. Cadence: monthly. Triggers: \"m365 pptx sales deck refresh\", /m365-pptx-sales-deck-refresh.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: monthly\n  risk: medium\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@powerpoint\nallowed-tools: search_tool, use_tool\n---\n# /m365-pptx-sales-deck-refresh\n\nUpdate sales deck metrics from Excel.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nThe standard sales deck quotes numbers - customer count, growth, case-study stats - that go stale monthly and are customer-facing, so wrong is worse than stale. This audits every claim in the deck against the canonical metrics workbook, updates what's verifiable, and quarantines what isn't.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The deck's location and the canonical metrics workbook (which cells/ranges are the source of truth per claim)\n- The deck's rounding/format conventions ('500+', '~3x', '$12M ARR')\n- Who approves customer-facing claim changes, and whether logos/testimonials are in scope (default: no)\n- As-of / footnote date convention\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@powerpoint** - Microsoft Powerpoint (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/pptx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local PowerPoint editing\n\nAfter downloading from @onedrive:\n- Run **`/pptx`** (bundled skill) for all `.pptx` create/edit work\n- User may also attach `@powerpoint` in the prompt to scope the session to Office\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Download both files. Build the **claim inventory**: every number, superlative, and stat in the deck with its slide location - this list, not the edit, is the audit.\n2. Match each claim to its source cell in the workbook. Claims with no source get status **UNVERIFIED** - they are never edited, only escalated.\n3. Update matched claims using the deck's own rounding conventions, with one integrity rule: rounding never moves in the flattering direction ('493' may become '490+', never '500+').\n4. Update every as-of/footnote date to the new data date; a fresh number over a stale footnote reads as sloppy or dishonest.\n5. Cross-slide consistency: the same metric must read identically everywhere it appears.\n6. Review table of every changed claim (old → new → source cell); on apply, save and upload as a new version, and hand the UNVERIFIED list to the approval owner.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Claim audit** - | Slide | Claim text | Old | New | Source cell | Status (updated / unchanged / UNVERIFIED) |\n\n**As-of dates** - footnotes updated, old → new.\n\n**Rounding notes** - anywhere the convention required judgment, shown explicitly.\n\n**UNVERIFIED queue** - claims with no source, for the approval owner (never silently kept or silently deleted).\n\nAfter apply - deck link + version #.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @powerpoint in the prompt when the session needs that app\n- Inventory every claim before editing any - coverage is the audit\n- Round conservatively; customer-facing numbers never flatter\n- Update footnote/as-of dates alongside the numbers\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't touch UNVERIFIED claims - escalate them\n- Don't edit customer logos, names, or testimonial quotes without explicit approval\n- Don't leave the same metric reading differently on two slides\n- Don't upload before the claim audit is approved\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @powerpoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **monthly**\n\nExample: `/loop 7d Run /m365-pptx-sales-deck-refresh`\n```\n\nAfter writing the file, confirm it exists. The user can then run /m365-pptx-sales-deck-refresh or describe the task from the skill description.", "skillContent": "---\nname: m365-pptx-sales-deck-refresh\ndescription: >\n  Update sales deck metrics from Excel. Requires integrations: @onedrive, @filesystem, @powerpoint. Uses search_tool + use_tool. Cadence: monthly. Triggers: \"m365 pptx sales deck refresh\", /m365-pptx-sales-deck-refresh.\nmetadata:\n  category: microsoft-office\n  type: combo\n  cadence: monthly\n  risk: medium\n  connectors: microsoft-onedrive,filesystem\n  integrations: @onedrive,@filesystem,@powerpoint\nallowed-tools: search_tool, use_tool\n---\n# /m365-pptx-sales-deck-refresh\n\nUpdate sales deck metrics from Excel.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nThe standard sales deck quotes numbers - customer count, growth, case-study stats - that go stale monthly and are customer-facing, so wrong is worse than stale. This audits every claim in the deck against the canonical metrics workbook, updates what's verifiable, and quarantines what isn't.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The deck's location and the canonical metrics workbook (which cells/ranges are the source of truth per claim)\n- The deck's rounding/format conventions ('500+', '~3x', '$12M ARR')\n- Who approves customer-facing claim changes, and whether logos/testimonials are in scope (default: no)\n- As-of / footnote date convention\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Download → local edit via docx/xlsx/pptx → re-upload flow.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@powerpoint** - Microsoft Powerpoint (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/pptx`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### Local PowerPoint editing\n\nAfter downloading from @onedrive:\n- Run **`/pptx`** (bundled skill) for all `.pptx` create/edit work\n- User may also attach `@powerpoint` in the prompt to scope the session to Office\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Download both files. Build the **claim inventory**: every number, superlative, and stat in the deck with its slide location - this list, not the edit, is the audit.\n2. Match each claim to its source cell in the workbook. Claims with no source get status **UNVERIFIED** - they are never edited, only escalated.\n3. Update matched claims using the deck's own rounding conventions, with one integrity rule: rounding never moves in the flattering direction ('493' may become '490+', never '500+').\n4. Update every as-of/footnote date to the new data date; a fresh number over a stale footnote reads as sloppy or dishonest.\n5. Cross-slide consistency: the same metric must read identically everywhere it appears.\n6. Review table of every changed claim (old → new → source cell); on apply, save and upload as a new version, and hand the UNVERIFIED list to the approval owner.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Claim audit** - | Slide | Claim text | Old | New | Source cell | Status (updated / unchanged / UNVERIFIED) |\n\n**As-of dates** - footnotes updated, old → new.\n\n**Rounding notes** - anywhere the convention required judgment, shown explicitly.\n\n**UNVERIFIED queue** - claims with no source, for the approval owner (never silently kept or silently deleted).\n\nAfter apply - deck link + version #.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @onedrive / @filesystem / @powerpoint in the prompt when the session needs that app\n- Inventory every claim before editing any - coverage is the audit\n- Round conservatively; customer-facing numbers never flatter\n- Update footnote/as-of dates alongside the numbers\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't touch UNVERIFIED claims - escalate them\n- Don't edit customer logos, names, or testimonial quotes without explicit approval\n- Don't leave the same metric reading differently on two slides\n- Don't upload before the claim audit is approved\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@onedrive, @filesystem, @powerpoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **monthly**\n\nExample: `/loop 7d Run /m365-pptx-sales-deck-refresh`\n"}]};

window.__XF_SKILL_PACKS__=window.__XF_SKILL_PACKS__||{};window.__XF_SKILL_PACKS__["microsoft-sharepoint-pack"]={"id": "microsoft-sharepoint-pack", "skills": [{"id": "sharepoint-contracts-index", "aiInstallPrompt": "Install the skill 'sharepoint-contracts-index' by writing the file below.\n\nUniversal path: ./skills/sharepoint-contracts-index/SKILL.md\nGrok path: ~/.grok/skills/sharepoint-contracts-index/SKILL.md\nCursor path: ~/.cursor/skills/sharepoint-contracts-index/SKILL.md\nClaude path: ~/.claude/skills/sharepoint-contracts-index/SKILL.md\nVS Code agent path: .github/skills/sharepoint-contracts-index/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: sharepoint-contracts-index\ndescription: >\n  Index contracts in SharePoint with metadata. Requires integrations: @sharepoint, @word. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint contracts index\", /sharepoint-contracts-index.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint,@word\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-contracts-index\n\nIndex contracts in SharePoint with metadata.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nContracts are scattered across a SharePoint library in inconsistent folders - some executed, some drafts named `final_v2` - and nobody can answer 'what renews in the next 90 days?'. This walks the library, reads each contract, extracts the metadata that matters (counterparty, dates, renewal notice), and builds a queryable index with a deadline watchlist.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Site › library › folders to scan, and what identifies a contract (subfolder, filename pattern, content type)\n- Index columns wanted: counterparty, effective date, end date, auto-renewal + notice period, value, internal owner, signed status\n- Where the index lives: a SharePoint list, an Excel workbook in the library, or a markdown report\n- How to treat drafts vs executed versions of the same agreement\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@word** - Microsoft Word (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/docx`\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n1. Check the library's existing metadata columns first - if someone already tagged counterparty/expiry, read those before re-deriving from documents.\n2. Enumerate files across the scoped folders (walking subfolders); note any folders the user's account can't open - they go in the coverage note, never silently skipped.\n3. Open each contract (Word/PDF) and extract: counterparty, effective date, term end, auto-renewal (yes/no + the notice window to stop it), stated value, and signature status. Mark each extraction's confidence; a low-confidence date is flagged, not asserted.\n4. De-duplicate versions of the same agreement (v1/v2/final/signed-scan): index the executed one, list the siblings under it.\n5. Compute the deadline watchlist from extracted dates: renewals and notice-by dates falling in the next 90 days, sorted soonest-first.\n6. If the destination is a SharePoint list: propose the column schema, then the rows, as the review table; if Excel/report, present the built artifact. Write only after apply.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Contracts index** - | Contract | Counterparty | Effective | Ends | Auto-renew (notice by!) | Value | Owner | Status (executed/draft) | File link | Confidence |\n\n**Deadline watchlist** - next 90 days: | Date | Contract | What happens if missed | - this table is the reason the index exists.\n\n**Needs human review** - low-confidence extractions, unsigned suspects, version tangles.\n\n**Coverage note** - folders scanned · access-denied folders · non-contract files skipped - so the index's completeness is honest.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint / @word in the prompt when the session needs that app\n- Read existing library metadata columns before re-extracting from documents\n- Index the executed version and subordinate the drafts under it\n- Turn notice periods into concrete notice-by dates in the watchlist\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't index a draft as if it were the executed agreement\n- Don't trust a filename date over the date inside the document\n- Don't skip access-denied folders silently - declare the coverage gap\n- Don't dress this up as legal review; it's an inventory\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint, @word) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /sharepoint-contracts-index or describe the task from the skill description.", "skillContent": "---\nname: sharepoint-contracts-index\ndescription: >\n  Index contracts in SharePoint with metadata. Requires integrations: @sharepoint, @word. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint contracts index\", /sharepoint-contracts-index.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint,@word\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-contracts-index\n\nIndex contracts in SharePoint with metadata.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nContracts are scattered across a SharePoint library in inconsistent folders - some executed, some drafts named `final_v2` - and nobody can answer 'what renews in the next 90 days?'. This walks the library, reads each contract, extracts the metadata that matters (counterparty, dates, renewal notice), and builds a queryable index with a deadline watchlist.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Site › library › folders to scan, and what identifies a contract (subfolder, filename pattern, content type)\n- Index columns wanted: counterparty, effective date, end date, auto-renewal + notice period, value, internal owner, signed status\n- Where the index lives: a SharePoint list, an Excel workbook in the library, or a markdown report\n- How to treat drafts vs executed versions of the same agreement\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@word** - Microsoft Word (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/docx`\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n1. Check the library's existing metadata columns first - if someone already tagged counterparty/expiry, read those before re-deriving from documents.\n2. Enumerate files across the scoped folders (walking subfolders); note any folders the user's account can't open - they go in the coverage note, never silently skipped.\n3. Open each contract (Word/PDF) and extract: counterparty, effective date, term end, auto-renewal (yes/no + the notice window to stop it), stated value, and signature status. Mark each extraction's confidence; a low-confidence date is flagged, not asserted.\n4. De-duplicate versions of the same agreement (v1/v2/final/signed-scan): index the executed one, list the siblings under it.\n5. Compute the deadline watchlist from extracted dates: renewals and notice-by dates falling in the next 90 days, sorted soonest-first.\n6. If the destination is a SharePoint list: propose the column schema, then the rows, as the review table; if Excel/report, present the built artifact. Write only after apply.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Contracts index** - | Contract | Counterparty | Effective | Ends | Auto-renew (notice by!) | Value | Owner | Status (executed/draft) | File link | Confidence |\n\n**Deadline watchlist** - next 90 days: | Date | Contract | What happens if missed | - this table is the reason the index exists.\n\n**Needs human review** - low-confidence extractions, unsigned suspects, version tangles.\n\n**Coverage note** - folders scanned · access-denied folders · non-contract files skipped - so the index's completeness is honest.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint / @word in the prompt when the session needs that app\n- Read existing library metadata columns before re-extracting from documents\n- Index the executed version and subordinate the drafts under it\n- Turn notice periods into concrete notice-by dates in the watchlist\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't index a draft as if it were the executed agreement\n- Don't trust a filename date over the date inside the document\n- Don't skip access-denied folders silently - declare the coverage gap\n- Don't dress this up as legal review; it's an inventory\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint, @word) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "sharepoint-doc-search", "aiInstallPrompt": "Install the skill 'sharepoint-doc-search' by writing the file below.\n\nUniversal path: ./skills/sharepoint-doc-search/SKILL.md\nGrok path: ~/.grok/skills/sharepoint-doc-search/SKILL.md\nCursor path: ~/.cursor/skills/sharepoint-doc-search/SKILL.md\nClaude path: ~/.claude/skills/sharepoint-doc-search/SKILL.md\nVS Code agent path: .github/skills/sharepoint-doc-search/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: sharepoint-doc-search\ndescription: >\n  Search documents across SharePoint sites. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint doc search\", /sharepoint-doc-search.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-doc-search\n\nSearch documents across SharePoint sites.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\n'It's on SharePoint somewhere' - a policy, a deck from last spring, the doc a departed colleague owned. The site search box gets one phrasing and gives up; this runs multiple query variants across the likely sites, tells document-library files apart from pages and list items, and confirms top hits by content, not title.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- What they remember: topic words, likely filename fragments, author, rough date, file type\n- Likely sites/libraries - or 'no idea', which widens the sweep\n- What kind of thing it is: a document, a site page, or a list item (people say 'document' for all three)\n- How many candidates they want back (default: top 10)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Build 3-5 query variants before searching: the exact phrase, synonyms/abbreviations (PTO/leave/vacation), author-scoped, filetype-scoped, date-bounded. One phrasing is how the search box already failed.\n2. Run the variants across the scoped sites (or the user's whole reachable estate); collect and de-duplicate hits.\n3. Label each hit by type - document-library file vs site page vs list item - because 'open' and 'edit' mean different things for each.\n4. Rank by match quality, recency, and relationship (user's own site, recently shared to them); for the top hits, peek at the content to confirm relevance instead of trusting the title.\n5. Deliver results with full location breadcrumbs (site › library › folder) and last-modified/author - the breadcrumb often jogs the user's memory even when the top hit is wrong.\n6. If nothing convincing surfaces, say the honest thing: SharePoint search is permission-trimmed, so the document may exist where this account can't see; suggest the likely owner to ask and the sites that couldn't be searched.\n7. This run is read-only: report findings and drafts in chat; do not modify, move, send, or mark anything without a separate confirmed `apply`.\n\n## Output format\n\n**Search results** - | # | Title | Type (doc/page/list item) | Site › Library › Folder | Modified (by) | Why it matches (content snippet) |\n\n**Query variants tried** - listed, so the user sees what's been covered and can add a phrasing.\n\n**If it's not here** - permission-trimming caveat, the most likely owner/site to ask, and the one alternate search term worth trying.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Run multiple phrasings - synonyms and abbreviations are where the wins are\n- Confirm top hits by opening/peeking content, not title-matching\n- Show full location breadcrumbs; they trigger the user's own recall\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't stop after one query variant\n- Don't claim the document doesn't exist - search is permission-trimmed and you only see this account's slice\n- Don't dump 50 undifferentiated hits; rank and cut\n- Don't confuse a site page or list item with a downloadable document\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /sharepoint-doc-search or describe the task from the skill description.", "skillContent": "---\nname: sharepoint-doc-search\ndescription: >\n  Search documents across SharePoint sites. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint doc search\", /sharepoint-doc-search.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-doc-search\n\nSearch documents across SharePoint sites.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\n'It's on SharePoint somewhere' - a policy, a deck from last spring, the doc a departed colleague owned. The site search box gets one phrasing and gives up; this runs multiple query variants across the likely sites, tells document-library files apart from pages and list items, and confirms top hits by content, not title.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- What they remember: topic words, likely filename fragments, author, rough date, file type\n- Likely sites/libraries - or 'no idea', which widens the sweep\n- What kind of thing it is: a document, a site page, or a list item (people say 'document' for all three)\n- How many candidates they want back (default: top 10)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Build 3-5 query variants before searching: the exact phrase, synonyms/abbreviations (PTO/leave/vacation), author-scoped, filetype-scoped, date-bounded. One phrasing is how the search box already failed.\n2. Run the variants across the scoped sites (or the user's whole reachable estate); collect and de-duplicate hits.\n3. Label each hit by type - document-library file vs site page vs list item - because 'open' and 'edit' mean different things for each.\n4. Rank by match quality, recency, and relationship (user's own site, recently shared to them); for the top hits, peek at the content to confirm relevance instead of trusting the title.\n5. Deliver results with full location breadcrumbs (site › library › folder) and last-modified/author - the breadcrumb often jogs the user's memory even when the top hit is wrong.\n6. If nothing convincing surfaces, say the honest thing: SharePoint search is permission-trimmed, so the document may exist where this account can't see; suggest the likely owner to ask and the sites that couldn't be searched.\n7. This run is read-only: report findings and drafts in chat; do not modify, move, send, or mark anything without a separate confirmed `apply`.\n\n## Output format\n\n**Search results** - | # | Title | Type (doc/page/list item) | Site › Library › Folder | Modified (by) | Why it matches (content snippet) |\n\n**Query variants tried** - listed, so the user sees what's been covered and can add a phrasing.\n\n**If it's not here** - permission-trimming caveat, the most likely owner/site to ask, and the one alternate search term worth trying.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Run multiple phrasings - synonyms and abbreviations are where the wins are\n- Confirm top hits by opening/peeking content, not title-matching\n- Show full location breadcrumbs; they trigger the user's own recall\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't stop after one query variant\n- Don't claim the document doesn't exist - search is permission-trimmed and you only see this account's slice\n- Don't dump 50 undifferentiated hits; rank and cut\n- Don't confuse a site page or list item with a downloadable document\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "sharepoint-hr-onboarding-pack", "aiInstallPrompt": "Install the skill 'sharepoint-hr-onboarding-pack' by writing the file below.\n\nUniversal path: ./skills/sharepoint-hr-onboarding-pack/SKILL.md\nGrok path: ~/.grok/skills/sharepoint-hr-onboarding-pack/SKILL.md\nCursor path: ~/.cursor/skills/sharepoint-hr-onboarding-pack/SKILL.md\nClaude path: ~/.claude/skills/sharepoint-hr-onboarding-pack/SKILL.md\nVS Code agent path: .github/skills/sharepoint-hr-onboarding-pack/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: sharepoint-hr-onboarding-pack\ndescription: >\n  Copy HR onboarding pack for new hire. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint hr onboarding pack\", /sharepoint-hr-onboarding-pack.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-hr-onboarding-pack\n\nCopy HR onboarding pack for new hire.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nA new hire signed. HR keeps a template pack - welcome letter, first-week checklist, policy acknowledgements, equipment form - that must be copied into a per-hire folder, personalized with name/role/dates, and access-checked, because onboarding folders routinely contain personal data sitting in over-shared libraries.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- New hire: name, role, start date, manager\n- Template pack location (site › library › folder)\n- Destination and folder-naming convention (e.g. `Onboarding/{Lastname, Firstname}`)\n- Which docs get personalized (name/role/date fields) vs copied as-is\n- Who should have access to the hire's folder (typically: HR + manager + the hire)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. List the template pack's contents and confirm it's the current set; flag templates that look stale (untouched in a year, or referencing old policies) for a quick human check rather than silently copying.\n2. Check the destination for an existing folder for this hire - duplicates fork the record.\n3. Build the copy plan doc-by-doc: plain copy vs personalize, and for personalized ones, exactly which fields get filled (name, role, start date, manager). Copies only - template originals are never edited.\n4. Check destination permissions: what does the new folder inherit? If the library is broadly readable and the pack contains personal data, flag it loudly before filing anything.\n5. On apply: create the folder, copy, personalize the copies, then verify the count (n templates = n copies) and that no placeholder survived in personalized docs.\n6. Return the organized links plus a short handoff draft to the manager (what's in the folder, what the hire completes by when).\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Pack plan** - | Template doc | Action (copy / personalize) | Fields to fill | → Destination |\n\n**Permission check** - who can see the destination folder, inherited from where; flagged if broader than HR + manager + hire.\n\nAfter apply - **Created**: folder link + per-doc links · verification (count match, zero placeholders left) · **Manager handoff draft** (blockquote).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Verify destination folder permissions before filing personal data\n- Copy, never move or edit, the template originals\n- Re-check personalized copies for surviving placeholders\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't put comp, ID, or other personal data into a broadly-readable library without flagging it\n- Don't overwrite an existing folder for the same hire - reconcile instead\n- Don't personalize the templates themselves by accident\n- Don't skip the staleness check on templates that clearly predate current policy\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /sharepoint-hr-onboarding-pack or describe the task from the skill description.", "skillContent": "---\nname: sharepoint-hr-onboarding-pack\ndescription: >\n  Copy HR onboarding pack for new hire. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint hr onboarding pack\", /sharepoint-hr-onboarding-pack.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-hr-onboarding-pack\n\nCopy HR onboarding pack for new hire.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nA new hire signed. HR keeps a template pack - welcome letter, first-week checklist, policy acknowledgements, equipment form - that must be copied into a per-hire folder, personalized with name/role/dates, and access-checked, because onboarding folders routinely contain personal data sitting in over-shared libraries.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- New hire: name, role, start date, manager\n- Template pack location (site › library › folder)\n- Destination and folder-naming convention (e.g. `Onboarding/{Lastname, Firstname}`)\n- Which docs get personalized (name/role/date fields) vs copied as-is\n- Who should have access to the hire's folder (typically: HR + manager + the hire)\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. List the template pack's contents and confirm it's the current set; flag templates that look stale (untouched in a year, or referencing old policies) for a quick human check rather than silently copying.\n2. Check the destination for an existing folder for this hire - duplicates fork the record.\n3. Build the copy plan doc-by-doc: plain copy vs personalize, and for personalized ones, exactly which fields get filled (name, role, start date, manager). Copies only - template originals are never edited.\n4. Check destination permissions: what does the new folder inherit? If the library is broadly readable and the pack contains personal data, flag it loudly before filing anything.\n5. On apply: create the folder, copy, personalize the copies, then verify the count (n templates = n copies) and that no placeholder survived in personalized docs.\n6. Return the organized links plus a short handoff draft to the manager (what's in the folder, what the hire completes by when).\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Pack plan** - | Template doc | Action (copy / personalize) | Fields to fill | → Destination |\n\n**Permission check** - who can see the destination folder, inherited from where; flagged if broader than HR + manager + hire.\n\nAfter apply - **Created**: folder link + per-doc links · verification (count match, zero placeholders left) · **Manager handoff draft** (blockquote).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Verify destination folder permissions before filing personal data\n- Copy, never move or edit, the template originals\n- Re-check personalized copies for surviving placeholders\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't put comp, ID, or other personal data into a broadly-readable library without flagging it\n- Don't overwrite an existing folder for the same hire - reconcile instead\n- Don't personalize the templates themselves by accident\n- Don't skip the staleness check on templates that clearly predate current policy\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "sharepoint-list-to-report", "aiInstallPrompt": "Install the skill 'sharepoint-list-to-report' by writing the file below.\n\nUniversal path: ./skills/sharepoint-list-to-report/SKILL.md\nGrok path: ~/.grok/skills/sharepoint-list-to-report/SKILL.md\nCursor path: ~/.cursor/skills/sharepoint-list-to-report/SKILL.md\nClaude path: ~/.claude/skills/sharepoint-list-to-report/SKILL.md\nVS Code agent path: .github/skills/sharepoint-list-to-report/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: sharepoint-list-to-report\ndescription: >\n  Convert SharePoint list to summary report. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint list to report\", /sharepoint-list-to-report.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-list-to-report\n\nConvert SharePoint list to summary report.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nA SharePoint list - tickets, asset register, intake requests - has quietly grown to 900 items and leadership wants a status picture: how many where, who's overloaded, what's overdue. This reads the schema properly, pages through *all* items, and computes a report where every number is auditable.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Site and list name\n- The report question: status distribution, overdue items, per-owner workload, aging, intake trend\n- Filters: active only, this quarter, one category\n- Audience and destination: chat summary, a document, or a page in the site\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Fetch the list schema first and respect column types: choice fields have fixed values, person fields return people (not strings), lookups point at other lists, calculated columns may not behave in filters. Misread types produce confidently wrong reports.\n2. Retrieve **all** items with pagination - a report silently built from the first page of a large list is the classic failure of this task. Record the retrieved count.\n3. Apply the requested filters and state them; then aggregate: counts by status/category/owner, ages from date fields, overdue = due date < today, created-per-week trend if timestamps support it.\n4. Log data-quality findings as their own section: blank owners, items untouched for 90+ days, statuses outside the expected choice set.\n5. Build the report at the audience's altitude - leadership gets headline numbers and exceptions; a working team gets the item-level tables with links.\n6. Offer (as a separate proposed step) saving the report as a page or document in the site.\n7. This run is read-only: report findings and drafts in chat; do not modify, move, send, or mark anything without a separate confirmed `apply`.\n\n## Output format\n\n**{List} report - {date}**\n**Method line first**: `{n} items retrieved (full list, paginated) · filters: {…}` - this is what makes every number below auditable.\n\n**Headline numbers** - total · by status · overdue count.\n\n**Breakdowns** - | Owner | Open | Overdue | Oldest item | and by-category equivalents.\n\n**Overdue / aging** - item-level table with links, sorted by days overdue.\n\n**Data quality** - blanks, stale items, out-of-range values.\n\n**Trend** - only if real timestamps support it; otherwise explicitly omitted.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Page through the entire list and print the retrieved count in the report\n- Read the schema first; treat person/lookup/calculated columns per their types\n- State filters inline so the numbers can be reproduced\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't report from a truncated first page of items\n- Don't render person-field objects as raw strings or IDs\n- Don't invent trends the timestamps can't support\n- Don't edit, complete, or touch any list item while reading\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /sharepoint-list-to-report or describe the task from the skill description.", "skillContent": "---\nname: sharepoint-list-to-report\ndescription: >\n  Convert SharePoint list to summary report. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint list to report\", /sharepoint-list-to-report.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-list-to-report\n\nConvert SharePoint list to summary report.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nA SharePoint list - tickets, asset register, intake requests - has quietly grown to 900 items and leadership wants a status picture: how many where, who's overloaded, what's overdue. This reads the schema properly, pages through *all* items, and computes a report where every number is auditable.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Site and list name\n- The report question: status distribution, overdue items, per-owner workload, aging, intake trend\n- Filters: active only, this quarter, one category\n- Audience and destination: chat summary, a document, or a page in the site\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Fetch the list schema first and respect column types: choice fields have fixed values, person fields return people (not strings), lookups point at other lists, calculated columns may not behave in filters. Misread types produce confidently wrong reports.\n2. Retrieve **all** items with pagination - a report silently built from the first page of a large list is the classic failure of this task. Record the retrieved count.\n3. Apply the requested filters and state them; then aggregate: counts by status/category/owner, ages from date fields, overdue = due date < today, created-per-week trend if timestamps support it.\n4. Log data-quality findings as their own section: blank owners, items untouched for 90+ days, statuses outside the expected choice set.\n5. Build the report at the audience's altitude - leadership gets headline numbers and exceptions; a working team gets the item-level tables with links.\n6. Offer (as a separate proposed step) saving the report as a page or document in the site.\n7. This run is read-only: report findings and drafts in chat; do not modify, move, send, or mark anything without a separate confirmed `apply`.\n\n## Output format\n\n**{List} report - {date}**\n**Method line first**: `{n} items retrieved (full list, paginated) · filters: {…}` - this is what makes every number below auditable.\n\n**Headline numbers** - total · by status · overdue count.\n\n**Breakdowns** - | Owner | Open | Overdue | Oldest item | and by-category equivalents.\n\n**Overdue / aging** - item-level table with links, sorted by days overdue.\n\n**Data quality** - blanks, stale items, out-of-range values.\n\n**Trend** - only if real timestamps support it; otherwise explicitly omitted.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Page through the entire list and print the retrieved count in the report\n- Read the schema first; treat person/lookup/calculated columns per their types\n- State filters inline so the numbers can be reproduced\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't report from a truncated first page of items\n- Don't render person-field objects as raw strings or IDs\n- Don't invent trends the timestamps can't support\n- Don't edit, complete, or touch any list item while reading\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "sharepoint-meeting-notes-archive", "aiInstallPrompt": "Install the skill 'sharepoint-meeting-notes-archive' by writing the file below.\n\nUniversal path: ./skills/sharepoint-meeting-notes-archive/SKILL.md\nGrok path: ~/.grok/skills/sharepoint-meeting-notes-archive/SKILL.md\nCursor path: ~/.cursor/skills/sharepoint-meeting-notes-archive/SKILL.md\nClaude path: ~/.claude/skills/sharepoint-meeting-notes-archive/SKILL.md\nVS Code agent path: .github/skills/sharepoint-meeting-notes-archive/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: sharepoint-meeting-notes-archive\ndescription: >\n  Archive meeting notes to SharePoint library. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint meeting notes archive\", /sharepoint-meeting-notes-archive.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-meeting-notes-archive\n\nArchive meeting notes to SharePoint library.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nMeeting notes live wherever they were typed - chat scrolls, personal files, someone's stickies - and the team's SharePoint library is supposed to be the record. This normalizes a set of notes into the standard structure, files it under the library's conventions, and fills the metadata columns that make the library searchable instead of a dumping ground.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The notes: pasted text, a file, or a pointer to a Teams meeting chat to pull\n- Target site › library and the folder convention (by meeting series? by month?)\n- Naming convention (e.g. `{series} - {YYYY-MM-DD}`) and stored format (docx / pdf / md)\n- The library's metadata columns (meeting date, series, owner) - filling these is part of the job, not optional\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Ingest the raw notes (pulling from Teams via the connector when that's the source) and normalize into the standard shape: title, date, attendees, agenda, discussion, decisions (verbatim), action items (owner + due), next meeting.\n2. Resolve the destination folder from the convention; create a folder only when the pattern clearly calls for it (new month, new series).\n3. Check for an existing archived note for the same meeting/date - if found, this is a conflict to surface, not a silent overwrite.\n4. Prepare the library metadata values (date, series, owner) alongside the file - a library with empty columns is just a folder with ambitions.\n5. Review: formatted note + destination + filename + metadata table. On apply, upload, set metadata, verify, and return the URL.\n6. Offer the follow-ups: backfill older notes on the same convention, and a draft line adding the notes link to the recurring meeting invite.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Archive plan**\n- **The note** - rendered in the normalized structure for review\n- **Filing** - | Destination folder | Filename | Format | Collision check |\n- **Metadata** - | Column | Value | - one row per library column being set\n\nAfter apply - **Archived**: URL · metadata confirmed set · the backfill/invite-link offers.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Fill the library's metadata columns - that's what makes the archive findable\n- Keep decisions and commitments verbatim while normalizing the structure\n- Surface same-meeting collisions instead of overwriting\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't dump into the library root when a folder convention exists\n- Don't reword what was decided while 'cleaning up' the notes\n- Don't overwrite an existing note for the same meeting\n- Don't skip metadata because the upload technically succeeded without it\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /sharepoint-meeting-notes-archive or describe the task from the skill description.", "skillContent": "---\nname: sharepoint-meeting-notes-archive\ndescription: >\n  Archive meeting notes to SharePoint library. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint meeting notes archive\", /sharepoint-meeting-notes-archive.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-meeting-notes-archive\n\nArchive meeting notes to SharePoint library.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nMeeting notes live wherever they were typed - chat scrolls, personal files, someone's stickies - and the team's SharePoint library is supposed to be the record. This normalizes a set of notes into the standard structure, files it under the library's conventions, and fills the metadata columns that make the library searchable instead of a dumping ground.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The notes: pasted text, a file, or a pointer to a Teams meeting chat to pull\n- Target site › library and the folder convention (by meeting series? by month?)\n- Naming convention (e.g. `{series} - {YYYY-MM-DD}`) and stored format (docx / pdf / md)\n- The library's metadata columns (meeting date, series, owner) - filling these is part of the job, not optional\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Ingest the raw notes (pulling from Teams via the connector when that's the source) and normalize into the standard shape: title, date, attendees, agenda, discussion, decisions (verbatim), action items (owner + due), next meeting.\n2. Resolve the destination folder from the convention; create a folder only when the pattern clearly calls for it (new month, new series).\n3. Check for an existing archived note for the same meeting/date - if found, this is a conflict to surface, not a silent overwrite.\n4. Prepare the library metadata values (date, series, owner) alongside the file - a library with empty columns is just a folder with ambitions.\n5. Review: formatted note + destination + filename + metadata table. On apply, upload, set metadata, verify, and return the URL.\n6. Offer the follow-ups: backfill older notes on the same convention, and a draft line adding the notes link to the recurring meeting invite.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Archive plan**\n- **The note** - rendered in the normalized structure for review\n- **Filing** - | Destination folder | Filename | Format | Collision check |\n- **Metadata** - | Column | Value | - one row per library column being set\n\nAfter apply - **Archived**: URL · metadata confirmed set · the backfill/invite-link offers.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Fill the library's metadata columns - that's what makes the archive findable\n- Keep decisions and commitments verbatim while normalizing the structure\n- Surface same-meeting collisions instead of overwriting\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't dump into the library root when a folder convention exists\n- Don't reword what was decided while 'cleaning up' the notes\n- Don't overwrite an existing note for the same meeting\n- Don't skip metadata because the upload technically succeeded without it\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "sharepoint-new-project-site", "aiInstallPrompt": "Install the skill 'sharepoint-new-project-site' by writing the file below.\n\nUniversal path: ./skills/sharepoint-new-project-site/SKILL.md\nGrok path: ~/.grok/skills/sharepoint-new-project-site/SKILL.md\nCursor path: ~/.cursor/skills/sharepoint-new-project-site/SKILL.md\nClaude path: ~/.claude/skills/sharepoint-new-project-site/SKILL.md\nVS Code agent path: .github/skills/sharepoint-new-project-site/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: sharepoint-new-project-site\ndescription: >\n  Scaffold SharePoint project folders and docs. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint new project site\", /sharepoint-new-project-site.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: medium\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-new-project-site\n\nScaffold SharePoint project folders and docs.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nProject kickoff: the team needs its SharePoint home - folder tree, starter docs (charter, decision log, RAID, notes template), sane permissions - matching how the team's other projects are laid out, not a hand-rolled snowflake. Usually this means scaffolding *within* an existing team site; creating a whole new site is often admin-gated and rarely necessary.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Project name/code and type (client / internal)\n- Host site - and if the user says 'new site', confirm they mean it: site creation is often restricted to admins, and a folder set in the existing team site usually serves\n- A reference project whose structure worked, to mirror\n- Starter docs wanted and where their templates live (copy real templates over inventing skeletons)\n- Any area needing restricted access (commercials, HR) - flagged early because it breaks permission inheritance\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Confirm the host site and check for name collisions with existing project folders.\n2. Propose the tree, mirroring the reference project or team convention - shallow beats deep (2-3 levels), and names stay short: SharePoint URL-length limits punish long nested names in ways that surface much later as un-openable files.\n3. Plan starter docs: copy from real templates where they exist (charter, RAID, decision log); generate a minimal skeleton only where no template exists, and say which is which.\n4. Handle permissions honestly: new folders inherit the site's access. Unique per-folder permissions are a standing maintenance cost - propose them only for the flagged restricted areas, and note who'd manage them.\n5. Review the full plan (tree + docs + permission notes); on apply, create folders, copy docs, verify everything landed.\n6. Return a clickable map of the created structure plus anything that failed or was skipped.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Scaffold plan**\n- **Tree** - indented folder structure as it will exist\n- **Starter docs** - | Doc | Source (template path / generated skeleton) | → Destination |\n- **Permissions** - inheritance statement + any proposed unique-permission area with its maintenance owner\n\nAfter apply - **Created**: the same tree with live links · verification (n planned = n created) · skips/failures called out.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Mirror a structure that already works for the team instead of inventing one\n- Keep the tree shallow and names short - URL length limits are real and late-breaking\n- Copy actual templates; mark generated skeletons as such\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't create a new SharePoint site when folders in an existing site serve - site sprawl has a long tail\n- Don't break permission inheritance casually; every unique-permission folder is a future audit finding\n- Don't overwrite an existing project's folders on a name collision\n- Don't scaffold before the plan is approved\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /sharepoint-new-project-site or describe the task from the skill description.", "skillContent": "---\nname: sharepoint-new-project-site\ndescription: >\n  Scaffold SharePoint project folders and docs. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sharepoint new project site\", /sharepoint-new-project-site.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: on-demand\n  risk: medium\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-new-project-site\n\nScaffold SharePoint project folders and docs.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nProject kickoff: the team needs its SharePoint home - folder tree, starter docs (charter, decision log, RAID, notes template), sane permissions - matching how the team's other projects are laid out, not a hand-rolled snowflake. Usually this means scaffolding *within* an existing team site; creating a whole new site is often admin-gated and rarely necessary.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Project name/code and type (client / internal)\n- Host site - and if the user says 'new site', confirm they mean it: site creation is often restricted to admins, and a folder set in the existing team site usually serves\n- A reference project whose structure worked, to mirror\n- Starter docs wanted and where their templates live (copy real templates over inventing skeletons)\n- Any area needing restricted access (commercials, HR) - flagged early because it breaks permission inheritance\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Confirm the host site and check for name collisions with existing project folders.\n2. Propose the tree, mirroring the reference project or team convention - shallow beats deep (2-3 levels), and names stay short: SharePoint URL-length limits punish long nested names in ways that surface much later as un-openable files.\n3. Plan starter docs: copy from real templates where they exist (charter, RAID, decision log); generate a minimal skeleton only where no template exists, and say which is which.\n4. Handle permissions honestly: new folders inherit the site's access. Unique per-folder permissions are a standing maintenance cost - propose them only for the flagged restricted areas, and note who'd manage them.\n5. Review the full plan (tree + docs + permission notes); on apply, create folders, copy docs, verify everything landed.\n6. Return a clickable map of the created structure plus anything that failed or was skipped.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Scaffold plan**\n- **Tree** - indented folder structure as it will exist\n- **Starter docs** - | Doc | Source (template path / generated skeleton) | → Destination |\n- **Permissions** - inheritance statement + any proposed unique-permission area with its maintenance owner\n\nAfter apply - **Created**: the same tree with live links · verification (n planned = n created) · skips/failures called out.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Mirror a structure that already works for the team instead of inventing one\n- Keep the tree shallow and names short - URL length limits are real and late-breaking\n- Copy actual templates; mark generated skeletons as such\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't create a new SharePoint site when folders in an existing site serve - site sprawl has a long tail\n- Don't break permission inheritance casually; every unique-permission folder is a future audit finding\n- Don't overwrite an existing project's folders on a name collision\n- Don't scaffold before the plan is approved\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "sharepoint-permission-review", "aiInstallPrompt": "Install the skill 'sharepoint-permission-review' by writing the file below.\n\nUniversal path: ./skills/sharepoint-permission-review/SKILL.md\nGrok path: ~/.grok/skills/sharepoint-permission-review/SKILL.md\nCursor path: ~/.cursor/skills/sharepoint-permission-review/SKILL.md\nClaude path: ~/.claude/skills/sharepoint-permission-review/SKILL.md\nVS Code agent path: .github/skills/sharepoint-permission-review/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: sharepoint-permission-review\ndescription: >\n  Audit SharePoint external sharing. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: monthly. Triggers: \"sharepoint permission review\", /sharepoint-permission-review.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: monthly\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-permission-review\n\nAudit SharePoint external sharing.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nMonthly sharing hygiene: somewhere in the team's SharePoint there's an anyone-with-the-link URL on a salary file, an external guest with edit rights on a contracts folder, a share created for a vendor who left in March. This audits what's visible to the user's account, ranks the risk, and proposes remediations - applied only after review, because revoking access breaks people's work mid-task.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Scope: which sites/libraries/folders to audit\n- What counts as sensitive here (HR, finance, contracts, customer data folders)\n- Org policy context: are anyone-links allowed at all? is external sharing normal for this team?\n- Who owns remediation: the user applies fixes, or this becomes a report for IT\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Enumerate sharing state across the scope as far as the connector can see: anyone-links, organization-wide links, direct external guests, and libraries/folders with broken inheritance (unique permissions).\n2. State the coverage limit up front: a full tenant permission report needs admin tooling - this audit covers what this account can inspect, and says so rather than implying completeness.\n3. Classify each finding by real risk: anyone-link on sensitive content > org-link on sensitive > external guest with **edit** > external guest with view > stale share (old, on inactive content). Edit-vs-view and link expiry are checked per item, not assumed.\n4. Recommend per finding: remove the link, downgrade edit→view, add an expiry, or convert to a direct named share - matched to why the share probably exists.\n5. Build the remediation review table; where removing access will visibly break someone's workflow, include a drafted heads-up message to the affected person.\n6. On apply: execute approved changes one by one, verify each, and log before/after states for the audit trail.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Sharing audit - {scope}, {date}**\n\n**Findings** - | Item | Location | Share type (anyone / org / guest) | Permission (view/edit) | Created / expiry if visible | Risk | Recommendation |\n\n**Top risks** - the 3-5 items to fix this week, in one glance.\n\n**Coverage note** - what this account could and couldn't inspect; what needs an admin-center report.\n\n**Remediation plan** - the review table for apply, with heads-up message drafts where a human loses access. After apply: before/after log per change.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Check edit-vs-view and expiry on every external share - the risk lives in those details\n- Pair every access removal with a drafted heads-up to whoever loses it\n- State the audit's coverage limits instead of implying tenant-wide completeness\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't revoke or downgrade anything before apply - broken access looks like an outage to the person affected\n- Don't claim full coverage the connector can't deliver\n- Don't wave off an anyone-link because 'nobody knows the URL' - that's the risk, not the mitigation\n- Don't leave stale external shares out of the report just because they're old and quiet\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **monthly**\n\nExample: `/loop 7d Run /sharepoint-permission-review`\n```\n\nAfter writing the file, confirm it exists. The user can then run /sharepoint-permission-review or describe the task from the skill description.", "skillContent": "---\nname: sharepoint-permission-review\ndescription: >\n  Audit SharePoint external sharing. Requires integrations: @sharepoint. Uses search_tool + use_tool. Cadence: monthly. Triggers: \"sharepoint permission review\", /sharepoint-permission-review.\nmetadata:\n  category: microsoft-sharepoint\n  type: connector\n  cadence: monthly\n  risk: low\n  connectors: microsoft-sharepoint\n  integrations: @sharepoint\nallowed-tools: search_tool, use_tool\n---\n# /sharepoint-permission-review\n\nAudit SharePoint external sharing.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nMonthly sharing hygiene: somewhere in the team's SharePoint there's an anyone-with-the-link URL on a salary file, an external guest with edit rights on a contracts folder, a share created for a vendor who left in March. This audits what's visible to the user's account, ranks the risk, and proposes remediations - applied only after review, because revoking access breaks people's work mid-task.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Scope: which sites/libraries/folders to audit\n- What counts as sensitive here (HR, finance, contracts, customer data folders)\n- Org policy context: are anyone-links allowed at all? is external sharing normal for this team?\n- Who owns remediation: the user applies fixes, or this becomes a report for IT\n- Read-only vs propose changes vs apply after confirmation\n- Which required @integration/workspace should be used - see **Connector access**\n\n## Tone & voice\n\n- **Tone:** Site/library paths explicit. Permission-aware.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Workflow\n\n1. Enumerate sharing state across the scope as far as the connector can see: anyone-links, organization-wide links, direct external guests, and libraries/folders with broken inheritance (unique permissions).\n2. State the coverage limit up front: a full tenant permission report needs admin tooling - this audit covers what this account can inspect, and says so rather than implying completeness.\n3. Classify each finding by real risk: anyone-link on sensitive content > org-link on sensitive > external guest with **edit** > external guest with view > stale share (old, on inactive content). Edit-vs-view and link expiry are checked per item, not assumed.\n4. Recommend per finding: remove the link, downgrade edit→view, add an expiry, or convert to a direct named share - matched to why the share probably exists.\n5. Build the remediation review table; where removing access will visibly break someone's workflow, include a drafted heads-up message to the affected person.\n6. On apply: execute approved changes one by one, verify each, and log before/after states for the audit trail.\n7. Present a review table of proposed changes before any write/send/create.\n8. Apply only after the user says `apply`, `apply all`, or `apply 1,3`.\n\n## Output format\n\n**Sharing audit - {scope}, {date}**\n\n**Findings** - | Item | Location | Share type (anyone / org / guest) | Permission (view/edit) | Created / expiry if visible | Risk | Recommendation |\n\n**Top risks** - the 3-5 items to fix this week, in one glance.\n\n**Coverage note** - what this account could and couldn't inspect; what needs an admin-center report.\n\n**Remediation plan** - the review table for apply, with heads-up message drafts where a human loses access. After apply: before/after log per change.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Tell the user to attach @sharepoint in the prompt when the session needs that app\n- Check edit-vs-view and expiry on every external share - the risk lives in those details\n- Pair every access removal with a drafted heads-up to whoever loses it\n- State the audit's coverage limits instead of implying tenant-wide completeness\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't revoke or downgrade anything before apply - broken access looks like an outage to the person affected\n- Don't claim full coverage the connector can't deliver\n- Don't wave off an anyone-link because 'nobody knows the URL' - that's the risk, not the mitigation\n- Don't leave stale external shares out of the report just because they're old and quiet\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't auto-send, publish, or delete - propose and wait for `apply`\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@sharepoint) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **monthly**\n\nExample: `/loop 7d Run /sharepoint-permission-review`\n"}]};

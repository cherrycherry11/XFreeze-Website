window.__XF_SKILL_PACKS__=window.__XF_SKILL_PACKS__||{};window.__XF_SKILL_PACKS__["notion-pack"]={"id": "notion-pack", "skills": [{"id": "notion-content-calendar", "aiInstallPrompt": "Install the skill 'notion-content-calendar' by writing the file below.\n\nUniversal path: ./skills/notion-content-calendar/SKILL.md\nGrok path: ~/.grok/skills/notion-content-calendar/SKILL.md\nCursor path: ~/.cursor/skills/notion-content-calendar/SKILL.md\nClaude path: ~/.claude/skills/notion-content-calendar/SKILL.md\nVS Code agent path: .github/skills/notion-content-calendar/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: notion-content-calendar\ndescription: >\n  Fill Notion marketing content calendar. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: monthly. Triggers: \"notion content calendar\", /notion-content-calendar.\nmetadata:\n  category: notion\n  type: connector\n  cadence: monthly\n  risk: medium\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-content-calendar\n\nFill Notion marketing content calendar.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this at monthly planning time when next month's content calendar in Notion is empty (or patchy) and the team needs it populated: blog posts, newsletters, social slots, launch-tied pieces - as properly-propertied database rows, balanced across channels and themes, with owners and draft-due dates that respect real lead times.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The calendar database - find it by name in the workspace and confirm; then **read its actual schema** (properties, select options, existing views) before proposing anything. Never assume a `Status` option called \"Drafting\" exists - fetch the real options\n- Planning period and cadence targets per channel (e.g. 2 blog posts/wk, 1 newsletter/wk, 3 social/wk) - infer from the last two months of rows if unstated and say so\n- Month's themes and anchors: product launches, events, seasonal moments the calendar must hit; which existing ideas/backlog rows should be promoted into slots vs net-new ideas\n- Ownership and lead-time rules: who writes what, and days-before-publish for draft/review dates (infer from past rows if visible)\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Working titles are specific and pitch-like (\"How we cut onboarding to 4 minutes\"), not category labels (\"Onboarding post\").\n- **Audience:** The content/marketing lead approving a month in one pass.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete rows, change the database schema (add/remove properties or select options), or publish anything without explicit confirmation - schema changes ripple through every view and automation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Locate the calendar database and pull its schema plus the last ~8 weeks of rows. Learn the house conventions: how titles are phrased, which Status a new idea starts in, how Channel/Theme selects are used, date property semantics (publish date vs draft due).\n2. Check the planning period for what already exists - never double-book a slot or recreate a row that's already there. Note gaps against the cadence targets.\n3. Draft the slot plan: distribute anchors first (launch posts pinned to launch dates, with teaser/announce/follow-up spacing), then fill remaining slots balancing channel cadence and theme mix. Avoid stacking two heavyweight pieces on the same day and respect the team's publish-day habits visible in past rows (e.g. newsletter always Tuesday).\n4. For each proposed row, prepare **every property the schema expects**, using only existing select options: specific working title, channel, theme/tag, owner (rotate fairly using past assignments unless told), publish date, draft-due date back-computed from lead time, status = the team's \"idea/planned\" state, and relations (e.g. link to the Campaign or Launch page) where the schema has them. Missing required info becomes an explicit question, not a guessed value.\n5. Present the review table (below). This is a row-creation job in an existing database - do NOT create a new database, new views, or new properties; if the schema genuinely can't express something, flag it as a recommendation instead.\n6. On `apply`, create approved rows, then report each with its Notion link. Offer a follow-up: one-line content brief inside each created page (goal, angle, CTA) as a second approval round.\n\n## Output format\n\n**Calendar state** - `Period: <month> · existing rows N · gaps vs cadence: blog −2, newsletter 0, social −5 · anchors: <launches/events>`\n\n**Proposed slots** (review table - wait for `apply`):\n\n| # | Publish date | Channel | Working title | Theme | Owner | Draft due | Status | Notes/relation |\n|---|---|---|---|---|---|---|---|---|\n\n**Balance check** - one line per dimension: channel mix, theme spread, per-owner load.\n\n**Open questions** - anything that blocked a property from being filled honestly.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the database schema and recent rows before proposing anything - match house conventions exactly\n- Fill every property the schema expects, from its real option list\n- Back-compute draft-due dates from publish dates and lead time\n- Balance owner load using visible past assignments\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent database properties, select options, or existing rows without a successful `use_tool` call\n- Don't create rows, edit rows, or touch the schema without `apply` - and never add new select options silently\n- Don't create a new database or new views when the job is filling the existing one\n- Don't double-book dates or duplicate rows that already exist for the period\n- Don't leave properties half-filled - a row with no owner or date is calendar debt\n- Don't publish or announce any content - this skill plans it\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **monthly**\n\nExample: `/loop 7d Run /notion-content-calendar`\n```\n\nAfter writing the file, confirm it exists. The user can then run /notion-content-calendar or describe the task from the skill description.", "skillContent": "---\nname: notion-content-calendar\ndescription: >\n  Fill Notion marketing content calendar. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: monthly. Triggers: \"notion content calendar\", /notion-content-calendar.\nmetadata:\n  category: notion\n  type: connector\n  cadence: monthly\n  risk: medium\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-content-calendar\n\nFill Notion marketing content calendar.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this at monthly planning time when next month's content calendar in Notion is empty (or patchy) and the team needs it populated: blog posts, newsletters, social slots, launch-tied pieces - as properly-propertied database rows, balanced across channels and themes, with owners and draft-due dates that respect real lead times.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The calendar database - find it by name in the workspace and confirm; then **read its actual schema** (properties, select options, existing views) before proposing anything. Never assume a `Status` option called \"Drafting\" exists - fetch the real options\n- Planning period and cadence targets per channel (e.g. 2 blog posts/wk, 1 newsletter/wk, 3 social/wk) - infer from the last two months of rows if unstated and say so\n- Month's themes and anchors: product launches, events, seasonal moments the calendar must hit; which existing ideas/backlog rows should be promoted into slots vs net-new ideas\n- Ownership and lead-time rules: who writes what, and days-before-publish for draft/review dates (infer from past rows if visible)\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Working titles are specific and pitch-like (\"How we cut onboarding to 4 minutes\"), not category labels (\"Onboarding post\").\n- **Audience:** The content/marketing lead approving a month in one pass.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete rows, change the database schema (add/remove properties or select options), or publish anything without explicit confirmation - schema changes ripple through every view and automation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Locate the calendar database and pull its schema plus the last ~8 weeks of rows. Learn the house conventions: how titles are phrased, which Status a new idea starts in, how Channel/Theme selects are used, date property semantics (publish date vs draft due).\n2. Check the planning period for what already exists - never double-book a slot or recreate a row that's already there. Note gaps against the cadence targets.\n3. Draft the slot plan: distribute anchors first (launch posts pinned to launch dates, with teaser/announce/follow-up spacing), then fill remaining slots balancing channel cadence and theme mix. Avoid stacking two heavyweight pieces on the same day and respect the team's publish-day habits visible in past rows (e.g. newsletter always Tuesday).\n4. For each proposed row, prepare **every property the schema expects**, using only existing select options: specific working title, channel, theme/tag, owner (rotate fairly using past assignments unless told), publish date, draft-due date back-computed from lead time, status = the team's \"idea/planned\" state, and relations (e.g. link to the Campaign or Launch page) where the schema has them. Missing required info becomes an explicit question, not a guessed value.\n5. Present the review table (below). This is a row-creation job in an existing database - do NOT create a new database, new views, or new properties; if the schema genuinely can't express something, flag it as a recommendation instead.\n6. On `apply`, create approved rows, then report each with its Notion link. Offer a follow-up: one-line content brief inside each created page (goal, angle, CTA) as a second approval round.\n\n## Output format\n\n**Calendar state** - `Period: <month> · existing rows N · gaps vs cadence: blog −2, newsletter 0, social −5 · anchors: <launches/events>`\n\n**Proposed slots** (review table - wait for `apply`):\n\n| # | Publish date | Channel | Working title | Theme | Owner | Draft due | Status | Notes/relation |\n|---|---|---|---|---|---|---|---|---|\n\n**Balance check** - one line per dimension: channel mix, theme spread, per-owner load.\n\n**Open questions** - anything that blocked a property from being filled honestly.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the database schema and recent rows before proposing anything - match house conventions exactly\n- Fill every property the schema expects, from its real option list\n- Back-compute draft-due dates from publish dates and lead time\n- Balance owner load using visible past assignments\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent database properties, select options, or existing rows without a successful `use_tool` call\n- Don't create rows, edit rows, or touch the schema without `apply` - and never add new select options silently\n- Don't create a new database or new views when the job is filling the existing one\n- Don't double-book dates or duplicate rows that already exist for the period\n- Don't leave properties half-filled - a row with no owner or date is calendar debt\n- Don't publish or announce any content - this skill plans it\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **monthly**\n\nExample: `/loop 7d Run /notion-content-calendar`\n"}, {"id": "notion-decision-log", "aiInstallPrompt": "Install the skill 'notion-decision-log' by writing the file below.\n\nUniversal path: ./skills/notion-decision-log/SKILL.md\nGrok path: ~/.grok/skills/notion-decision-log/SKILL.md\nCursor path: ~/.cursor/skills/notion-decision-log/SKILL.md\nClaude path: ~/.claude/skills/notion-decision-log/SKILL.md\nVS Code agent path: .github/skills/notion-decision-log/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: notion-decision-log\ndescription: >\n  Log decision to Notion Key Decisions. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"notion decision log\", /notion-decision-log.\nmetadata:\n  category: notion\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-decision-log\n\nLog decision to Notion Key Decisions.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this the moment a real decision lands - end of a meeting where the team picked option B, a Slack thread that concluded \"we're deferring the migration\", an architecture call. Six months later someone will ask \"why did we do this?\", and this entry is the answer. Capture while the context is fresh: the decision, the rejected alternatives, and *why* - the why is the part everyone forgets to write down.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The decision itself, stated as a committed choice (\"We will X\"), and where it came from (meeting, thread, doc) - link the source if one exists\n- **Alternatives considered and why they lost** - press for at least one rejected option; a decision log without alternatives is just an announcement\n- Who decided (the accountable person, not the whole attendee list), who was consulted, and the date\n- Reversibility and revisit trigger: one-way door or two-way door? What condition would reopen it (\"revisit if latency exceeds X\", \"review at Q4 planning\")\n- The Key Decisions database location - find by name, confirm, and read its real schema before writing\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Write for the reader six months out who has zero context - no meeting-room shorthand.\n- **Audience:** Future teammates (including ones not hired yet) auditing why things are the way they are.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show the full drafted entry; wait for `apply` before creating the page.\n- Never edit or overwrite an existing decision entry - decisions that change get a **new** entry marked \"supersedes [old]\", preserving the historical record. Never delete entries.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Locate the Key Decisions database (search by name; if none exists, propose creating a simple one - Title, Date, Status, Decider, Area - as a separate approval, and note that a plain page-per-decision under a \"Decisions\" page is fine for small teams; don't over-engineer).\n2. **Check for related prior decisions**: query the database for entries touching the same area. If this decision contradicts or replaces one, the new entry must say \"supersedes →\" with a relation/link, and you propose (not perform) a Status change on the old entry to \"Superseded\".\n3. Draft the entry in decision-record form:\n   - **Title**: verb-first and scannable (\"Adopt PostgreSQL for analytics store\", not \"Database discussion\")\n   - **Decision**: 1-2 sentences, committed tense\n   - **Context**: the problem/pressure that forced a choice, in plain language\n   - **Alternatives**: each rejected option with the one-line reason it lost\n   - **Consequences**: what this commits the team to, known trade-offs accepted\n   - **Reversibility & revisit trigger**: door type + the condition that reopens it\n   - Properties: date, decider, consulted, status = Decided, area/project relation if the schema has one\n4. Fill properties from the database's real schema and options only.\n5. Show the complete draft for review; on `apply`, create the page and report the link. Propose the superseded-status update on the old entry as its own apply item.\n\n## Output format\n\n**Duplicate/conflict check** - one line: related prior entries found (linked) or \"none found in <database>\".\n\n**Draft entry** (full text for review - wait for `apply`):\n\n```\nTitle: <verb-first decision>\nDate / Decider / Consulted / Status / Area\n---\nDecision: …\nContext: …\nAlternatives considered:\n  1. <option> - rejected because …\nConsequences: …\nReversibility: one-way / two-way · Revisit if: …\nSource: <meeting/thread link>\nSupersedes: <link or ->\n```\n\n**Proposed writes** - numbered: `1. Create entry` · `2. Mark <old entry> Superseded` (if applicable). Wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Search for prior related decisions before logging - superseding beats contradicting\n- Press for alternatives and the losing reasons; that's the log's future value\n- Name one accountable decider, not a committee list\n- Write titles a searcher will find (\"PostgreSQL\", not \"the database thing\")\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent alternatives, deciders, or context that wasn't provided or found via a successful `use_tool` call - a thin honest entry beats a padded fake one\n- Don't create the entry without `apply`\n- Don't edit or delete old decision entries - supersede with a new one\n- Don't log aspirations (\"we should probably…\") as decisions; if nothing was committed, say so and stop\n- Don't build an elaborate multi-relation database when the team just needs entries logged in the one they have\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /notion-decision-log or describe the task from the skill description.", "skillContent": "---\nname: notion-decision-log\ndescription: >\n  Log decision to Notion Key Decisions. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"notion decision log\", /notion-decision-log.\nmetadata:\n  category: notion\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-decision-log\n\nLog decision to Notion Key Decisions.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this the moment a real decision lands - end of a meeting where the team picked option B, a Slack thread that concluded \"we're deferring the migration\", an architecture call. Six months later someone will ask \"why did we do this?\", and this entry is the answer. Capture while the context is fresh: the decision, the rejected alternatives, and *why* - the why is the part everyone forgets to write down.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The decision itself, stated as a committed choice (\"We will X\"), and where it came from (meeting, thread, doc) - link the source if one exists\n- **Alternatives considered and why they lost** - press for at least one rejected option; a decision log without alternatives is just an announcement\n- Who decided (the accountable person, not the whole attendee list), who was consulted, and the date\n- Reversibility and revisit trigger: one-way door or two-way door? What condition would reopen it (\"revisit if latency exceeds X\", \"review at Q4 planning\")\n- The Key Decisions database location - find by name, confirm, and read its real schema before writing\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Write for the reader six months out who has zero context - no meeting-room shorthand.\n- **Audience:** Future teammates (including ones not hired yet) auditing why things are the way they are.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show the full drafted entry; wait for `apply` before creating the page.\n- Never edit or overwrite an existing decision entry - decisions that change get a **new** entry marked \"supersedes [old]\", preserving the historical record. Never delete entries.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Locate the Key Decisions database (search by name; if none exists, propose creating a simple one - Title, Date, Status, Decider, Area - as a separate approval, and note that a plain page-per-decision under a \"Decisions\" page is fine for small teams; don't over-engineer).\n2. **Check for related prior decisions**: query the database for entries touching the same area. If this decision contradicts or replaces one, the new entry must say \"supersedes →\" with a relation/link, and you propose (not perform) a Status change on the old entry to \"Superseded\".\n3. Draft the entry in decision-record form:\n   - **Title**: verb-first and scannable (\"Adopt PostgreSQL for analytics store\", not \"Database discussion\")\n   - **Decision**: 1-2 sentences, committed tense\n   - **Context**: the problem/pressure that forced a choice, in plain language\n   - **Alternatives**: each rejected option with the one-line reason it lost\n   - **Consequences**: what this commits the team to, known trade-offs accepted\n   - **Reversibility & revisit trigger**: door type + the condition that reopens it\n   - Properties: date, decider, consulted, status = Decided, area/project relation if the schema has one\n4. Fill properties from the database's real schema and options only.\n5. Show the complete draft for review; on `apply`, create the page and report the link. Propose the superseded-status update on the old entry as its own apply item.\n\n## Output format\n\n**Duplicate/conflict check** - one line: related prior entries found (linked) or \"none found in <database>\".\n\n**Draft entry** (full text for review - wait for `apply`):\n\n```\nTitle: <verb-first decision>\nDate / Decider / Consulted / Status / Area\n---\nDecision: …\nContext: …\nAlternatives considered:\n  1. <option> - rejected because …\nConsequences: …\nReversibility: one-way / two-way · Revisit if: …\nSource: <meeting/thread link>\nSupersedes: <link or ->\n```\n\n**Proposed writes** - numbered: `1. Create entry` · `2. Mark <old entry> Superseded` (if applicable). Wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Search for prior related decisions before logging - superseding beats contradicting\n- Press for alternatives and the losing reasons; that's the log's future value\n- Name one accountable decider, not a committee list\n- Write titles a searcher will find (\"PostgreSQL\", not \"the database thing\")\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent alternatives, deciders, or context that wasn't provided or found via a successful `use_tool` call - a thin honest entry beats a padded fake one\n- Don't create the entry without `apply`\n- Don't edit or delete old decision entries - supersede with a new one\n- Don't log aspirations (\"we should probably…\") as decisions; if nothing was committed, say so and stop\n- Don't build an elaborate multi-relation database when the team just needs entries logged in the one they have\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "notion-meeting-to-page", "aiInstallPrompt": "Install the skill 'notion-meeting-to-page' by writing the file below.\n\nUniversal path: ./skills/notion-meeting-to-page/SKILL.md\nGrok path: ~/.grok/skills/notion-meeting-to-page/SKILL.md\nCursor path: ~/.cursor/skills/notion-meeting-to-page/SKILL.md\nClaude path: ~/.claude/skills/notion-meeting-to-page/SKILL.md\nVS Code agent path: .github/skills/notion-meeting-to-page/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: notion-meeting-to-page\ndescription: >\n  Create Notion meeting notes from calendar. Requires integrations: @notion, @google-calendar. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"notion meeting to page\", /notion-meeting-to-page.\nmetadata:\n  category: notion\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,google-calendar\n  integrations: @notion,@google-calendar\nallowed-tools: search_tool, use_tool\n---\n# /notion-meeting-to-page\n\nCreate Notion meeting notes from calendar.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this right before a meeting (or at the start of the day for all of today's meetings): it reads the calendar event and pre-builds the Notion notes page - attendees, agenda from the event description, open action items carried from the previous instance of the same recurring meeting - so the note-taker starts from a structured page instead of a blank one.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which meeting(s): \"next meeting\", a named event, or \"all of today\" - resolve against the live calendar and confirm when titles are ambiguous\n- Where notes live: a Meeting Notes **database** (typical - has Date, Attendees, Meeting-type properties) vs plain sub-pages under a project page. Find it, confirm, and read the schema; match how previous meeting pages were titled and propertied\n- For recurring meetings: which past page is the predecessor (query by matching title/series and recent date) so open action items can carry forward\n- Note-taking conventions: the team's section layout if previous pages show one - mirror it rather than imposing a new template\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. The page is scaffolding for a human note-taker - clean headers, no filler prose.\n- **Audience:** The meeting owner/note-taker.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Never modify the calendar event itself; this skill reads calendar, writes Notion - and page creation waits for `apply`.\n\n## Workflow\n\n1. Fetch the target event(s) from the calendar: title, date/time, attendees, description/agenda text, attached doc links, video-call link, recurrence.\n2. **Check for an existing page first** - query the notes database for a page matching this event and date. If one exists, report it and stop (or offer to update it); duplicate meeting pages fragment notes.\n3. For recurring meetings, fetch the previous instance's page and extract: unresolved action items (unchecked to-dos), and \"parking lot\"/deferred topics - these carry forward into the new page.\n4. Draft the page:\n   - **Title** in the team's established pattern (e.g. `Product Sync - 2026-07-04`)\n   - **Properties** from the real schema: date, attendees (mapped to Notion people where emails match - list unmatched attendees as text rather than guessing), meeting type, project relation if the schema has one\n   - **Body scaffold**: Agenda (from the event description, itemized; \"no agenda in invite\" flagged honestly), Carried-over actions (as to-do blocks with owners), Notes (empty headers per agenda item), Decisions (empty), New action items (empty to-do block), Parking lot\n5. Show the draft (properties + body outline) and wait for `apply`.\n6. On apply, create the page, report the link. Offer optional follow-ups, each needing separate approval: link the page URL back into the calendar event description, or post the page link to a channel.\n\n## Output format\n\n**Event resolved** - `<title> · <date/time> · N attendees · recurring: yes/no · existing page: none / link`\n\n**Page draft** (review - wait for `apply`):\n- **Properties**: title · date · attendees (matched/unmatched) · type · relations\n- **Body outline**: the section scaffold, with the actual agenda items and the carried-over action items shown in full\n\n**Carried forward** - table: action item · owner · from which previous meeting (linked).\n\n**Proposed writes** - numbered: `1. Create page` (+ optional `2. Add page link to calendar event`). Wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Check for an existing page for this event before creating one\n- Carry unresolved action items forward from the previous recurring instance\n- Match the team's existing title pattern and page structure\n- Map attendees to Notion people only on solid email matches\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent agenda items, attendees, or previous action items without a successful `use_tool` call - an invite with no agenda gets an honest empty Agenda section\n- Don't create the page (or edit the calendar event) without `apply`\n- Don't create duplicate pages for the same meeting instance\n- Don't summarize or pre-fill \"notes\" for a meeting that hasn't happened\n- Don't create a new meeting-notes database when the team already has one - and if they truly have none, a simple database proposal is a separate approval, not a side effect\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion, @google-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /notion-meeting-to-page or describe the task from the skill description.", "skillContent": "---\nname: notion-meeting-to-page\ndescription: >\n  Create Notion meeting notes from calendar. Requires integrations: @notion, @google-calendar. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"notion meeting to page\", /notion-meeting-to-page.\nmetadata:\n  category: notion\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,google-calendar\n  integrations: @notion,@google-calendar\nallowed-tools: search_tool, use_tool\n---\n# /notion-meeting-to-page\n\nCreate Notion meeting notes from calendar.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this right before a meeting (or at the start of the day for all of today's meetings): it reads the calendar event and pre-builds the Notion notes page - attendees, agenda from the event description, open action items carried from the previous instance of the same recurring meeting - so the note-taker starts from a structured page instead of a blank one.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which meeting(s): \"next meeting\", a named event, or \"all of today\" - resolve against the live calendar and confirm when titles are ambiguous\n- Where notes live: a Meeting Notes **database** (typical - has Date, Attendees, Meeting-type properties) vs plain sub-pages under a project page. Find it, confirm, and read the schema; match how previous meeting pages were titled and propertied\n- For recurring meetings: which past page is the predecessor (query by matching title/series and recent date) so open action items can carry forward\n- Note-taking conventions: the team's section layout if previous pages show one - mirror it rather than imposing a new template\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. The page is scaffolding for a human note-taker - clean headers, no filler prose.\n- **Audience:** The meeting owner/note-taker.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Never modify the calendar event itself; this skill reads calendar, writes Notion - and page creation waits for `apply`.\n\n## Workflow\n\n1. Fetch the target event(s) from the calendar: title, date/time, attendees, description/agenda text, attached doc links, video-call link, recurrence.\n2. **Check for an existing page first** - query the notes database for a page matching this event and date. If one exists, report it and stop (or offer to update it); duplicate meeting pages fragment notes.\n3. For recurring meetings, fetch the previous instance's page and extract: unresolved action items (unchecked to-dos), and \"parking lot\"/deferred topics - these carry forward into the new page.\n4. Draft the page:\n   - **Title** in the team's established pattern (e.g. `Product Sync - 2026-07-04`)\n   - **Properties** from the real schema: date, attendees (mapped to Notion people where emails match - list unmatched attendees as text rather than guessing), meeting type, project relation if the schema has one\n   - **Body scaffold**: Agenda (from the event description, itemized; \"no agenda in invite\" flagged honestly), Carried-over actions (as to-do blocks with owners), Notes (empty headers per agenda item), Decisions (empty), New action items (empty to-do block), Parking lot\n5. Show the draft (properties + body outline) and wait for `apply`.\n6. On apply, create the page, report the link. Offer optional follow-ups, each needing separate approval: link the page URL back into the calendar event description, or post the page link to a channel.\n\n## Output format\n\n**Event resolved** - `<title> · <date/time> · N attendees · recurring: yes/no · existing page: none / link`\n\n**Page draft** (review - wait for `apply`):\n- **Properties**: title · date · attendees (matched/unmatched) · type · relations\n- **Body outline**: the section scaffold, with the actual agenda items and the carried-over action items shown in full\n\n**Carried forward** - table: action item · owner · from which previous meeting (linked).\n\n**Proposed writes** - numbered: `1. Create page` (+ optional `2. Add page link to calendar event`). Wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Check for an existing page for this event before creating one\n- Carry unresolved action items forward from the previous recurring instance\n- Match the team's existing title pattern and page structure\n- Map attendees to Notion people only on solid email matches\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent agenda items, attendees, or previous action items without a successful `use_tool` call - an invite with no agenda gets an honest empty Agenda section\n- Don't create the page (or edit the calendar event) without `apply`\n- Don't create duplicate pages for the same meeting instance\n- Don't summarize or pre-fill \"notes\" for a meeting that hasn't happened\n- Don't create a new meeting-notes database when the team already has one - and if they truly have none, a simple database proposal is a separate approval, not a side effect\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion, @google-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "notion-prd-writer", "aiInstallPrompt": "Install the skill 'notion-prd-writer' by writing the file below.\n\nUniversal path: ./skills/notion-prd-writer/SKILL.md\nGrok path: ~/.grok/skills/notion-prd-writer/SKILL.md\nCursor path: ~/.cursor/skills/notion-prd-writer/SKILL.md\nClaude path: ~/.claude/skills/notion-prd-writer/SKILL.md\nVS Code agent path: .github/skills/notion-prd-writer/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: notion-prd-writer\ndescription: >\n  Write PRD in Notion from brief. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"notion prd writer\", /notion-prd-writer.\nmetadata:\n  category: notion\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-prd-writer\n\nWrite PRD in Notion from brief.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a feature idea has enough shape to spec - a kickoff is coming, engineering is asking \"what exactly are we building?\", and the PM has a brief, a Slack thread, or meeting notes but no PRD. It turns that raw input into a structured PRD page in the team's Notion, in the team's own format, with every unknown surfaced as an open question instead of papered over.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The brief/source material: pasted text, or Notion pages to pull from (search and read them via the connector)\n- Where PRDs live: a PRD/Specs **database** (read its schema - Status, Owner, Team, Launch-date properties) vs pages under a project. Fetch one or two recent PRDs and **mirror their section structure** - a PRD in the house format gets read; a foreign template gets ignored\n- The essentials a PRD can't fake, chased explicitly if absent from the brief: the problem and for whom · success metrics · in/out of scope · hard constraints (platform, deadline, compliance) · dependencies on other teams\n- Review flow: who must sign off, and the target review date\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. PRD voice: declarative, testable statements (\"Users can export CSV up to 10k rows\"), no marketing fluff.\n- **Audience:** Engineers who will estimate from it and stakeholders who will approve it.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show the PRD outline (and on request the full draft) before creating the page; wait for `apply`.\n- Never overwrite an existing PRD page; if one exists for this feature, offer to update it as a clearly-diffed revision instead. Never delete pages.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Gather sources: the brief plus any linked Notion pages (research notes, meeting notes, prior related PRDs - search the workspace for the feature name to find them). Check no PRD already exists for this feature.\n2. Read 1-2 recent PRDs from the same database to learn the house structure and properties. Default structure if the team has none: Problem & context → Goals & success metrics → Non-goals → User stories/requirements → Design/UX (links) → Technical considerations & dependencies → Rollout & measurement → Risks → Open questions.\n3. **Sort the brief's content into claims vs gaps**: what the sources actually establish goes in; everything else becomes an explicit Open question. The classic failure is a plausible-sounding PRD full of invented specifics - never guess metrics targets, dates, or scope boundaries.\n4. Write requirements as numbered, testable statements with priority tags the team uses (P0/P1 or Must/Should). Each requirement traceable to the brief or a named assumption.\n5. Make **Non-goals** a real section - pull them from scope discussions in the sources; explicit non-goals are what stop scope creep at review.\n6. Show the outline (or full text) for review; refine on feedback.\n7. On `apply`: create the page in the PRD database with real properties (Status = Draft, owner, team, review date), body in Notion blocks (toggles for long sections, a table for requirements if that's the house style), and relations to the project/epic where the schema supports it. Report the link. Offer follow-ups as separate approvals: notify reviewers, or seed a linked task for each open question.\n\n## Output format\n\n**Source inventory** - what was read (linked) · existing PRD found: none / link.\n\n**PRD draft** (review before `apply`) - the house-format sections, with:\n- Requirements as a numbered priority-tagged list\n- Every unfounded specific replaced by `[OPEN: …]`\n\n**Coverage report** - one line per essential: `Problem ✅ · Metrics ⚠️ (no target given) · Scope ✅ · Constraints ❌ (not in brief)` - so the PM sees the gaps before reviewers do.\n\n**Open questions** - numbered, each with who most likely answers it.\n\n**Proposed writes** - `1. Create PRD page (Status: Draft)` + optional reviewer notifications. Wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Mirror the team's existing PRD structure and properties\n- Keep a hard line between what sources establish and what's assumed - assumptions get labeled\n- Write testable requirements, and give non-goals real estate\n- Set Status = Draft; the PM promotes it, not you\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent metrics targets, user research findings, dates, or stakeholder positions - no `use_tool` evidence or user statement, no claim\n- Don't create or overwrite pages without `apply`\n- Don't ship a PRD with zero open questions from a two-paragraph brief - that's a sign of invented content, not completeness\n- Don't create a new PRD database or a new template when the team has one\n- Don't bury requirements in prose paragraphs engineers can't estimate from\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /notion-prd-writer or describe the task from the skill description.", "skillContent": "---\nname: notion-prd-writer\ndescription: >\n  Write PRD in Notion from brief. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"notion prd writer\", /notion-prd-writer.\nmetadata:\n  category: notion\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-prd-writer\n\nWrite PRD in Notion from brief.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a feature idea has enough shape to spec - a kickoff is coming, engineering is asking \"what exactly are we building?\", and the PM has a brief, a Slack thread, or meeting notes but no PRD. It turns that raw input into a structured PRD page in the team's Notion, in the team's own format, with every unknown surfaced as an open question instead of papered over.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The brief/source material: pasted text, or Notion pages to pull from (search and read them via the connector)\n- Where PRDs live: a PRD/Specs **database** (read its schema - Status, Owner, Team, Launch-date properties) vs pages under a project. Fetch one or two recent PRDs and **mirror their section structure** - a PRD in the house format gets read; a foreign template gets ignored\n- The essentials a PRD can't fake, chased explicitly if absent from the brief: the problem and for whom · success metrics · in/out of scope · hard constraints (platform, deadline, compliance) · dependencies on other teams\n- Review flow: who must sign off, and the target review date\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. PRD voice: declarative, testable statements (\"Users can export CSV up to 10k rows\"), no marketing fluff.\n- **Audience:** Engineers who will estimate from it and stakeholders who will approve it.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show the PRD outline (and on request the full draft) before creating the page; wait for `apply`.\n- Never overwrite an existing PRD page; if one exists for this feature, offer to update it as a clearly-diffed revision instead. Never delete pages.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Gather sources: the brief plus any linked Notion pages (research notes, meeting notes, prior related PRDs - search the workspace for the feature name to find them). Check no PRD already exists for this feature.\n2. Read 1-2 recent PRDs from the same database to learn the house structure and properties. Default structure if the team has none: Problem & context → Goals & success metrics → Non-goals → User stories/requirements → Design/UX (links) → Technical considerations & dependencies → Rollout & measurement → Risks → Open questions.\n3. **Sort the brief's content into claims vs gaps**: what the sources actually establish goes in; everything else becomes an explicit Open question. The classic failure is a plausible-sounding PRD full of invented specifics - never guess metrics targets, dates, or scope boundaries.\n4. Write requirements as numbered, testable statements with priority tags the team uses (P0/P1 or Must/Should). Each requirement traceable to the brief or a named assumption.\n5. Make **Non-goals** a real section - pull them from scope discussions in the sources; explicit non-goals are what stop scope creep at review.\n6. Show the outline (or full text) for review; refine on feedback.\n7. On `apply`: create the page in the PRD database with real properties (Status = Draft, owner, team, review date), body in Notion blocks (toggles for long sections, a table for requirements if that's the house style), and relations to the project/epic where the schema supports it. Report the link. Offer follow-ups as separate approvals: notify reviewers, or seed a linked task for each open question.\n\n## Output format\n\n**Source inventory** - what was read (linked) · existing PRD found: none / link.\n\n**PRD draft** (review before `apply`) - the house-format sections, with:\n- Requirements as a numbered priority-tagged list\n- Every unfounded specific replaced by `[OPEN: …]`\n\n**Coverage report** - one line per essential: `Problem ✅ · Metrics ⚠️ (no target given) · Scope ✅ · Constraints ❌ (not in brief)` - so the PM sees the gaps before reviewers do.\n\n**Open questions** - numbered, each with who most likely answers it.\n\n**Proposed writes** - `1. Create PRD page (Status: Draft)` + optional reviewer notifications. Wait for `apply`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Mirror the team's existing PRD structure and properties\n- Keep a hard line between what sources establish and what's assumed - assumptions get labeled\n- Write testable requirements, and give non-goals real estate\n- Set Status = Draft; the PM promotes it, not you\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent metrics targets, user research findings, dates, or stakeholder positions - no `use_tool` evidence or user statement, no claim\n- Don't create or overwrite pages without `apply`\n- Don't ship a PRD with zero open questions from a two-paragraph brief - that's a sign of invented content, not completeness\n- Don't create a new PRD database or a new template when the team has one\n- Don't bury requirements in prose paragraphs engineers can't estimate from\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "notion-project-status", "aiInstallPrompt": "Install the skill 'notion-project-status' by writing the file below.\n\nUniversal path: ./skills/notion-project-status/SKILL.md\nGrok path: ~/.grok/skills/notion-project-status/SKILL.md\nCursor path: ~/.cursor/skills/notion-project-status/SKILL.md\nClaude path: ~/.claude/skills/notion-project-status/SKILL.md\nVS Code agent path: .github/skills/notion-project-status/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: notion-project-status\ndescription: >\n  Update Notion project status fields. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"notion project status\", /notion-project-status.\nmetadata:\n  category: notion\n  type: connector\n  cadence: weekly\n  risk: medium\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-project-status\n\nUpdate Notion project status fields.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this weekly (Friday wrap-up or before Monday review) when the Projects database in Notion has gone stale: statuses that nobody flipped, \"Last update\" fields weeks old, health selects contradicting what the linked-task rollups show. It reconciles each project row against the evidence inside the workspace and proposes corrected fields plus a short written update per project.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The Projects database - locate, confirm, and read its real schema: which properties encode status/health/dates/owner, what the select options actually are, and whether task relations/rollups exist\n- Scope: all active projects, one team's, or a named list; and which statuses are exempt (Paused, Icebox)\n- The evidence trail per project: linked task databases, recent edits on the project page body, sub-pages - plus anything the user tells you directly (\"Alpha slipped a week\")\n- Update conventions: does each project page keep an Updates section/log to append to, or do only properties change? Match what past updates look like\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Status text is dated, first-line-conclusive, and names the blocker if there is one.\n- **Audience:** Whoever scans the projects board Monday morning.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete rows or pages, never change the database schema, and never alter another person's written update text - append, don't rewrite history.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Query the Projects database for in-scope rows with all status-relevant properties and relations.\n2. Per project, **collect evidence**: task rollup numbers (done/total, overdue count), most recent edits to the project page and its sub-pages, dates mentioned in recent update entries, and the user's own verbal updates. Note evidence freshness - a rollup is current; a page comment may be three weeks old.\n3. **Compare recorded state vs evidence**:\n   - Status says In progress, all linked tasks done → propose Complete (or \"awaiting sign-off\" if that state exists)\n   - Health says On track, but overdue tasks accumulate or the end date passed → propose At risk/Off track with the numbers\n   - No edits anywhere in 14+ days → flag as silent; propose no health change on silence alone, just the flag (absence of edits is weak evidence)\n   - End date in the past with open work → propose date correction or explicit \"date needs owner review\"\n4. Draft each proposed change with its evidence line, and where conventions include written updates, draft the update entry: date, one-line state, what moved this week, blocker/ask. Append-style - never overwrite previous entries.\n5. Present the review table; changes based on strong evidence (rollups) marked ✓, weak-evidence proposals (staleness inference) marked ⚠ so the user can approve selectively.\n6. On `apply`, write approved property changes and append approved update entries; report each with links.\n\n## Output format\n\n**Board reality check** - `P projects scanned · M matching reality · D drifted · S silent 14d+ · C complete-but-open`\n\n**Proposed changes** (review table - wait for `apply`):\n\n| # | Project | Field | Current → Proposed | Evidence (with numbers) | Confidence |\n|---|---|---|---|---|---|\n\n**Draft update entries** - per project needing one, the full dated text to append.\n\n**Silent projects** - listed with owner, for the user to chase (no auto-changes proposed from silence).\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip` (⚠ items excluded from `apply all` unless the user says otherwise).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the schema first and use only its real select options\n- Attach evidence (rollup numbers, dates) to every proposed change\n- Append dated update entries; preserve the update history\n- Grade your own confidence so weak inferences aren't bulk-applied\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent task counts, dates, or page contents without a successful `use_tool` call\n- Don't change any field or append any text without `apply`\n- Don't downgrade a project's health purely because nobody edited Notion - flag silence instead; work may be happening elsewhere\n- Don't overwrite or edit previous update entries or another person's words\n- Don't add new select options or properties to make a proposal fit - flag the mismatch\n- Don't mark a project Complete when the rollup counts tasks that live outside the linked database (check the relation coverage first)\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /notion-project-status`\n```\n\nAfter writing the file, confirm it exists. The user can then run /notion-project-status or describe the task from the skill description.", "skillContent": "---\nname: notion-project-status\ndescription: >\n  Update Notion project status fields. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"notion project status\", /notion-project-status.\nmetadata:\n  category: notion\n  type: connector\n  cadence: weekly\n  risk: medium\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-project-status\n\nUpdate Notion project status fields.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this weekly (Friday wrap-up or before Monday review) when the Projects database in Notion has gone stale: statuses that nobody flipped, \"Last update\" fields weeks old, health selects contradicting what the linked-task rollups show. It reconciles each project row against the evidence inside the workspace and proposes corrected fields plus a short written update per project.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The Projects database - locate, confirm, and read its real schema: which properties encode status/health/dates/owner, what the select options actually are, and whether task relations/rollups exist\n- Scope: all active projects, one team's, or a named list; and which statuses are exempt (Paused, Icebox)\n- The evidence trail per project: linked task databases, recent edits on the project page body, sub-pages - plus anything the user tells you directly (\"Alpha slipped a week\")\n- Update conventions: does each project page keep an Updates section/log to append to, or do only properties change? Match what past updates look like\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Status text is dated, first-line-conclusive, and names the blocker if there is one.\n- **Audience:** Whoever scans the projects board Monday morning.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete rows or pages, never change the database schema, and never alter another person's written update text - append, don't rewrite history.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Query the Projects database for in-scope rows with all status-relevant properties and relations.\n2. Per project, **collect evidence**: task rollup numbers (done/total, overdue count), most recent edits to the project page and its sub-pages, dates mentioned in recent update entries, and the user's own verbal updates. Note evidence freshness - a rollup is current; a page comment may be three weeks old.\n3. **Compare recorded state vs evidence**:\n   - Status says In progress, all linked tasks done → propose Complete (or \"awaiting sign-off\" if that state exists)\n   - Health says On track, but overdue tasks accumulate or the end date passed → propose At risk/Off track with the numbers\n   - No edits anywhere in 14+ days → flag as silent; propose no health change on silence alone, just the flag (absence of edits is weak evidence)\n   - End date in the past with open work → propose date correction or explicit \"date needs owner review\"\n4. Draft each proposed change with its evidence line, and where conventions include written updates, draft the update entry: date, one-line state, what moved this week, blocker/ask. Append-style - never overwrite previous entries.\n5. Present the review table; changes based on strong evidence (rollups) marked ✓, weak-evidence proposals (staleness inference) marked ⚠ so the user can approve selectively.\n6. On `apply`, write approved property changes and append approved update entries; report each with links.\n\n## Output format\n\n**Board reality check** - `P projects scanned · M matching reality · D drifted · S silent 14d+ · C complete-but-open`\n\n**Proposed changes** (review table - wait for `apply`):\n\n| # | Project | Field | Current → Proposed | Evidence (with numbers) | Confidence |\n|---|---|---|---|---|---|\n\n**Draft update entries** - per project needing one, the full dated text to append.\n\n**Silent projects** - listed with owner, for the user to chase (no auto-changes proposed from silence).\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip` (⚠ items excluded from `apply all` unless the user says otherwise).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the schema first and use only its real select options\n- Attach evidence (rollup numbers, dates) to every proposed change\n- Append dated update entries; preserve the update history\n- Grade your own confidence so weak inferences aren't bulk-applied\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent task counts, dates, or page contents without a successful `use_tool` call\n- Don't change any field or append any text without `apply`\n- Don't downgrade a project's health purely because nobody edited Notion - flag silence instead; work may be happening elsewhere\n- Don't overwrite or edit previous update entries or another person's words\n- Don't add new select options or properties to make a proposal fit - flag the mismatch\n- Don't mark a project Complete when the rollup counts tasks that live outside the linked database (check the relation coverage first)\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /notion-project-status`\n"}, {"id": "notion-research-hub", "aiInstallPrompt": "Install the skill 'notion-research-hub' by writing the file below.\n\nUniversal path: ./skills/notion-research-hub/SKILL.md\nGrok path: ~/.grok/skills/notion-research-hub/SKILL.md\nCursor path: ~/.cursor/skills/notion-research-hub/SKILL.md\nClaude path: ~/.claude/skills/notion-research-hub/SKILL.md\nVS Code agent path: .github/skills/notion-research-hub/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: notion-research-hub\ndescription: >\n  Append research to Notion hub. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"notion research hub\", /notion-research-hub.\nmetadata:\n  category: notion\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-research-hub\n\nAppend research to Notion hub.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this at the end of a research session - competitor teardown, user-interview synthesis, market scan, technical investigation - when findings exist in the chat or in scattered notes and must land in the team's Notion research hub in a form someone will actually find and reuse later. The job is *filing well*: right location, house format, linked to related entries, sources attributed.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The findings to file: from this conversation, pasted notes, or existing Notion pages to consolidate\n- The hub's anatomy - inspect it before writing: is it a **database** (entries with Topic/Type/Date/Status properties - read the schema and options) or a **page hierarchy** (topic sections to append under)? Which existing entry, if any, does this research extend vs warrant a new entry?\n- Research metadata the hub format expects: topic/project, research type (interviews, competitive, desk research), date, researcher, confidence/status\n- Source attribution: where each finding came from (interview #, article, dataset) - findings without sources get labeled as unsourced rather than dressed up\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Findings first, evidence under them; a reader should get the \"so what\" in the first three lines.\n- **Audience:** A teammate landing on this entry from search, months later, without the context of today's session.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show the drafted entry and its destination; wait for `apply`.\n- Append and create - never overwrite or delete existing research content. When updating an existing entry, add a dated addendum section; prior findings stay intact even when contradicted (contradictions are findings too).\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Locate the research hub and inspect its structure (database schema + a couple of recent entries, or the page tree). Learn the house entry format - heading order, how sources are cited, tags in use.\n2. **Search the hub for related prior research** on this topic. Decide placement: extend an existing entry (dated addendum) when it's the same question; new entry when it's a new question. Never fork a second page on the same topic without linking the first.\n3. Synthesize the findings into the house shape - default if the hub has none:\n   - **TL;DR** - 2-3 sentences: what we learned and what it changes\n   - **Key findings** - numbered; each = one claim + its evidence + its source; confidence noted (strong/tentative)\n   - **Contradictions & surprises** - where this conflicts with prior hub entries (link them) or expectations\n   - **Implications / recommended next steps** - clearly separated from findings\n   - **Sources** - the raw list: links, interview refs, datasets, dates\n4. Prepare properties from the real schema (topic, type, date, researcher, status) and relations to the project/feature the research serves.\n5. Cross-link deliberately: mention-link related hub entries in the body, and propose backlink-worthy additions (\"add a link from the Pricing page's research section to this entry\") as separate apply items.\n6. Show the draft + destination; on `apply`, write it and report links.\n\n## Output format\n\n**Filing decision** - `Hub: <name> · placement: new entry / addendum to <entry> (linked) · related entries found: N`\n\n**Entry draft** (full text for review - wait for `apply`) - in the house format, with every finding carrying its source and confidence.\n\n**Cross-links proposed** - numbered: which existing pages get a link to this entry and why.\n\n**Unsourced items** - anything the user provided without attribution, listed so they can source or accept it as-is.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Search the hub for prior related entries before filing - extend, don't fork\n- Keep findings and implications visibly separate; attach source + confidence to each finding\n- Match the hub's existing entry format and tag vocabulary\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent findings, quotes, interview data, or statistics - file only what the user provided or a successful `use_tool` call returned, and label unsourced material as unsourced\n- Don't write to the hub without `apply`\n- Don't overwrite or \"clean up\" prior research entries - contradict them with a dated addendum instead\n- Don't create a new database when the hub already has a home for this (a page section is fine for a one-off)\n- Don't dump the raw session transcript as the entry - synthesize; the transcript can be linked or attached below the findings\n- Don't scatter new tags when existing ones cover the topic\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /notion-research-hub or describe the task from the skill description.", "skillContent": "---\nname: notion-research-hub\ndescription: >\n  Append research to Notion hub. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"notion research hub\", /notion-research-hub.\nmetadata:\n  category: notion\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-research-hub\n\nAppend research to Notion hub.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this at the end of a research session - competitor teardown, user-interview synthesis, market scan, technical investigation - when findings exist in the chat or in scattered notes and must land in the team's Notion research hub in a form someone will actually find and reuse later. The job is *filing well*: right location, house format, linked to related entries, sources attributed.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The findings to file: from this conversation, pasted notes, or existing Notion pages to consolidate\n- The hub's anatomy - inspect it before writing: is it a **database** (entries with Topic/Type/Date/Status properties - read the schema and options) or a **page hierarchy** (topic sections to append under)? Which existing entry, if any, does this research extend vs warrant a new entry?\n- Research metadata the hub format expects: topic/project, research type (interviews, competitive, desk research), date, researcher, confidence/status\n- Source attribution: where each finding came from (interview #, article, dataset) - findings without sources get labeled as unsourced rather than dressed up\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Findings first, evidence under them; a reader should get the \"so what\" in the first three lines.\n- **Audience:** A teammate landing on this entry from search, months later, without the context of today's session.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show the drafted entry and its destination; wait for `apply`.\n- Append and create - never overwrite or delete existing research content. When updating an existing entry, add a dated addendum section; prior findings stay intact even when contradicted (contradictions are findings too).\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Locate the research hub and inspect its structure (database schema + a couple of recent entries, or the page tree). Learn the house entry format - heading order, how sources are cited, tags in use.\n2. **Search the hub for related prior research** on this topic. Decide placement: extend an existing entry (dated addendum) when it's the same question; new entry when it's a new question. Never fork a second page on the same topic without linking the first.\n3. Synthesize the findings into the house shape - default if the hub has none:\n   - **TL;DR** - 2-3 sentences: what we learned and what it changes\n   - **Key findings** - numbered; each = one claim + its evidence + its source; confidence noted (strong/tentative)\n   - **Contradictions & surprises** - where this conflicts with prior hub entries (link them) or expectations\n   - **Implications / recommended next steps** - clearly separated from findings\n   - **Sources** - the raw list: links, interview refs, datasets, dates\n4. Prepare properties from the real schema (topic, type, date, researcher, status) and relations to the project/feature the research serves.\n5. Cross-link deliberately: mention-link related hub entries in the body, and propose backlink-worthy additions (\"add a link from the Pricing page's research section to this entry\") as separate apply items.\n6. Show the draft + destination; on `apply`, write it and report links.\n\n## Output format\n\n**Filing decision** - `Hub: <name> · placement: new entry / addendum to <entry> (linked) · related entries found: N`\n\n**Entry draft** (full text for review - wait for `apply`) - in the house format, with every finding carrying its source and confidence.\n\n**Cross-links proposed** - numbered: which existing pages get a link to this entry and why.\n\n**Unsourced items** - anything the user provided without attribution, listed so they can source or accept it as-is.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Search the hub for prior related entries before filing - extend, don't fork\n- Keep findings and implications visibly separate; attach source + confidence to each finding\n- Match the hub's existing entry format and tag vocabulary\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent findings, quotes, interview data, or statistics - file only what the user provided or a successful `use_tool` call returned, and label unsourced material as unsourced\n- Don't write to the hub without `apply`\n- Don't overwrite or \"clean up\" prior research entries - contradict them with a dated addendum instead\n- Don't create a new database when the hub already has a home for this (a page section is fine for a one-off)\n- Don't dump the raw session transcript as the entry - synthesize; the transcript can be linked or attached below the findings\n- Don't scatter new tags when existing ones cover the topic\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "notion-task-inbox", "aiInstallPrompt": "Install the skill 'notion-task-inbox' by writing the file below.\n\nUniversal path: ./skills/notion-task-inbox/SKILL.md\nGrok path: ~/.grok/skills/notion-task-inbox/SKILL.md\nCursor path: ~/.cursor/skills/notion-task-inbox/SKILL.md\nClaude path: ~/.claude/skills/notion-task-inbox/SKILL.md\nVS Code agent path: .github/skills/notion-task-inbox/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: notion-task-inbox\ndescription: >\n  Triage Notion task inbox database. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: daily. Triggers: \"notion task inbox\", /notion-task-inbox.\nmetadata:\n  category: notion\n  type: connector\n  cadence: daily\n  risk: medium\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-task-inbox\n\nTriage Notion task inbox database.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this daily (or whenever the inbox view crosses ~15 items) on a GTD-style Notion setup: quick-captured tasks with a title and nothing else pile up in the Inbox status, and each needs the classic triage - do/delegate/defer/drop - plus the properties (project, due, priority) that make it show up in the right views. Untriaged capture is where Notion task systems go to die.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The tasks database and what marks the inbox: `Status = Inbox`, an \"Inbox\" view, or empty-Project rows. Read the schema - statuses, priority options, project relation, assignee - before proposing anything\n- Triage rules the user runs on: 2-minute-rule habits, what \"someday/maybe\" maps to in their statuses, personal vs team database (delegation only makes sense in the latter)\n- Current commitments for scheduling honesty: what's already due this week (query it) so deferrals land on realistic dates\n- Drop policy: may the assistant propose dropping stale trivia (captured 30+ days ago, no context, no due date), and does dropping mean an \"Archived/Dropped\" status - never deletion\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Rewritten task titles start with a verb and would make sense to a stranger.\n- **Audience:** The database owner clearing their inbox in five minutes with your table.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete tasks - \"drop\" means moving to an archived/dropped status, reversibly. Never delegate (reassign) without the user approving that specific row.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Query the inbox set plus this week's already-committed tasks (for load context). Fetch the schema's real statuses, priorities, and project options.\n2. **Clarify each captured item**: rewrite vague titles into verb-first actionable ones (\"Dentist\" → \"Book dentist appointment for filling follow-up\"). If a title is so cryptic it can't be interpreted (\"the thing from Tuesday\"), put it in a \"needs your memory\" list - never guess a meaning.\n3. **Detect duplicates and near-duplicates** against inbox and active tasks; propose merging (keep the better-propertied row, archive the other).\n4. **Split multi-action captures**: \"Plan offsite (venue, budget, invites)\" becomes a parent task with subtasks or 3 tasks with a shared project - matching how the schema handles hierarchy.\n5. **Route each item**:\n   - *Do now* - genuinely under ~2 minutes: mark next-action/today status\n   - *Schedule* - has a real deadline or a natural slot; propose a due date consistent with existing load (don't stack 12 items on Monday)\n   - *Delegate* - (team databases) propose assignee with reason\n   - *Someday/maybe* - real but not now: the team's backlog/someday status\n   - *Drop* - stale, obsolete, or duplicated: archived status with a one-word reason\n6. Fill the hygiene properties on every routed item: project relation (infer from title keywords against real project names - mark low-confidence inferences), priority, and effort if the schema tracks it.\n7. Present the review table; apply approved rows; report every change with links. Note the after-state: \"inbox will be 0 / 3 items remain needing your input\".\n\n## Output format\n\n**Inbox state** - `N in inbox · oldest <x>d · this week already holds M due tasks`\n\n**Triage table** (review - wait for `apply`):\n\n| # | Task (rewritten title) | Route | Due | Project (confidence) | Priority | Notes (split/merge/why) |\n|---|---|---|---|---|---|---|\n\n**Needs your memory** - uninterpretable captures, listed verbatim for the user to decode.\n\n**Duplicates & splits** - the merge pairs and the parent→subtask breakdowns spelled out.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip`. Drops and delegations are excluded from `apply all` and approved by number.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Rewrite titles verb-first; a task you can't act on isn't triaged\n- Check existing weekly load before proposing due dates\n- Use only the schema's real statuses/options; flag gaps instead of inventing options\n- Mark low-confidence project inferences so they're easy to veto\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent task meanings, projects, or dates without evidence - cryptic captures go to \"needs your memory\"\n- Don't update, merge, archive, or reassign anything without `apply`\n- Don't delete tasks under any instruction short of the user explicitly confirming deletion of a named row\n- Don't bulk-defer everything to next week - that's snoozing, not triage\n- Don't let drops or delegations ride along in `apply all`\n- Don't create new statuses, tags, or databases to triage into\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **daily**\n\nExample: `/loop 7d Run /notion-task-inbox`\n```\n\nAfter writing the file, confirm it exists. The user can then run /notion-task-inbox or describe the task from the skill description.", "skillContent": "---\nname: notion-task-inbox\ndescription: >\n  Triage Notion task inbox database. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: daily. Triggers: \"notion task inbox\", /notion-task-inbox.\nmetadata:\n  category: notion\n  type: connector\n  cadence: daily\n  risk: medium\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-task-inbox\n\nTriage Notion task inbox database.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this daily (or whenever the inbox view crosses ~15 items) on a GTD-style Notion setup: quick-captured tasks with a title and nothing else pile up in the Inbox status, and each needs the classic triage - do/delegate/defer/drop - plus the properties (project, due, priority) that make it show up in the right views. Untriaged capture is where Notion task systems go to die.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The tasks database and what marks the inbox: `Status = Inbox`, an \"Inbox\" view, or empty-Project rows. Read the schema - statuses, priority options, project relation, assignee - before proposing anything\n- Triage rules the user runs on: 2-minute-rule habits, what \"someday/maybe\" maps to in their statuses, personal vs team database (delegation only makes sense in the latter)\n- Current commitments for scheduling honesty: what's already due this week (query it) so deferrals land on realistic dates\n- Drop policy: may the assistant propose dropping stale trivia (captured 30+ days ago, no context, no due date), and does dropping mean an \"Archived/Dropped\" status - never deletion\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Rewritten task titles start with a verb and would make sense to a stranger.\n- **Audience:** The database owner clearing their inbox in five minutes with your table.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete tasks - \"drop\" means moving to an archived/dropped status, reversibly. Never delegate (reassign) without the user approving that specific row.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Query the inbox set plus this week's already-committed tasks (for load context). Fetch the schema's real statuses, priorities, and project options.\n2. **Clarify each captured item**: rewrite vague titles into verb-first actionable ones (\"Dentist\" → \"Book dentist appointment for filling follow-up\"). If a title is so cryptic it can't be interpreted (\"the thing from Tuesday\"), put it in a \"needs your memory\" list - never guess a meaning.\n3. **Detect duplicates and near-duplicates** against inbox and active tasks; propose merging (keep the better-propertied row, archive the other).\n4. **Split multi-action captures**: \"Plan offsite (venue, budget, invites)\" becomes a parent task with subtasks or 3 tasks with a shared project - matching how the schema handles hierarchy.\n5. **Route each item**:\n   - *Do now* - genuinely under ~2 minutes: mark next-action/today status\n   - *Schedule* - has a real deadline or a natural slot; propose a due date consistent with existing load (don't stack 12 items on Monday)\n   - *Delegate* - (team databases) propose assignee with reason\n   - *Someday/maybe* - real but not now: the team's backlog/someday status\n   - *Drop* - stale, obsolete, or duplicated: archived status with a one-word reason\n6. Fill the hygiene properties on every routed item: project relation (infer from title keywords against real project names - mark low-confidence inferences), priority, and effort if the schema tracks it.\n7. Present the review table; apply approved rows; report every change with links. Note the after-state: \"inbox will be 0 / 3 items remain needing your input\".\n\n## Output format\n\n**Inbox state** - `N in inbox · oldest <x>d · this week already holds M due tasks`\n\n**Triage table** (review - wait for `apply`):\n\n| # | Task (rewritten title) | Route | Due | Project (confidence) | Priority | Notes (split/merge/why) |\n|---|---|---|---|---|---|---|\n\n**Needs your memory** - uninterpretable captures, listed verbatim for the user to decode.\n\n**Duplicates & splits** - the merge pairs and the parent→subtask breakdowns spelled out.\n\n**Apply instructions** - `apply all`, `apply 1,3`, or `skip`. Drops and delegations are excluded from `apply all` and approved by number.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Rewrite titles verb-first; a task you can't act on isn't triaged\n- Check existing weekly load before proposing due dates\n- Use only the schema's real statuses/options; flag gaps instead of inventing options\n- Mark low-confidence project inferences so they're easy to veto\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent task meanings, projects, or dates without evidence - cryptic captures go to \"needs your memory\"\n- Don't update, merge, archive, or reassign anything without `apply`\n- Don't delete tasks under any instruction short of the user explicitly confirming deletion of a named row\n- Don't bulk-defer everything to next week - that's snoozing, not triage\n- Don't let drops or delegations ride along in `apply all`\n- Don't create new statuses, tags, or databases to triage into\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **daily**\n\nExample: `/loop 7d Run /notion-task-inbox`\n"}, {"id": "notion-weekly-plan", "aiInstallPrompt": "Install the skill 'notion-weekly-plan' by writing the file below.\n\nUniversal path: ./skills/notion-weekly-plan/SKILL.md\nGrok path: ~/.grok/skills/notion-weekly-plan/SKILL.md\nCursor path: ~/.cursor/skills/notion-weekly-plan/SKILL.md\nClaude path: ~/.claude/skills/notion-weekly-plan/SKILL.md\nVS Code agent path: .github/skills/notion-weekly-plan/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: notion-weekly-plan\ndescription: >\n  Plan week in Notion from goals. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"notion weekly plan\", /notion-weekly-plan.\nmetadata:\n  category: notion\n  type: connector\n  cadence: weekly\n  risk: medium\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-weekly-plan\n\nPlan week in Notion from goals.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this Sunday evening or Monday morning: quarterly/monthly goals sit in one Notion page or database, tasks sit in another, and the week needs to be planned so the goals actually pull the tasks (not the other way round). It reviews last week's plan, carries over the unfinished, derives this week's priorities from the goals, and builds the weekly plan in whatever structure the user already keeps.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Where goals live (Goals database/page - read it) and where tasks live (tasks database - read schema), plus how weekly plans are kept: a Weekly Plans page per week, a filtered view, or \"This week\" statuses/dates on tasks. Match the existing pattern - don't introduce a new planning artifact\n- Last week's plan (find it) - carryover and win/loss review come from it\n- This week's fixed constraints: known meetings-heavy days, OOO, deadlines landing this week; and realistic capacity (how many meaningful tasks per day the user actually completes - infer from history if visible, and say so)\n- The user's prioritization lens: which 1-3 goals deserve this week's biggest blocks\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Plans in commitments, not wishes - few items, clearly goal-linked.\n- **Audience:** The user's Monday-morning self, who needs to know what matters without re-deriving it.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete tasks or previous weekly plans; carryover moves items forward, it doesn't erase the record of the missed week.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Read the goals source, the tasks database schema, and last week's plan.\n2. **Close last week honestly**: which planned items finished, which slipped, and the slip reasons where visible. Two-line retro: what the pattern says (e.g. \"deep-work items slip on meeting-heavy days\").\n3. **Carryover triage**: each unfinished item is either re-committed this week, consciously pushed to backlog, or flagged \"slipped twice+ - rescope or drop?\" (repeat offenders signal the task is mis-sized or not actually important).\n4. **Derive the week from goals**: for each focus goal, pick the 1-3 tasks that most advance it - pulling from the existing task backlog first (query it), proposing genuinely-new tasks only where a goal has no next action. Every planned item carries its goal link; work that serves no goal gets a visible \"maintenance\" bucket, kept small.\n5. **Reality-check capacity**: total commitments vs stated capacity and the week's fixed constraints. Over budget → propose what to cut, don't shrink silently. Assign items to days honoring the constraints (deep work away from meeting-heavy days).\n6. Build the plan in the house structure: create the Weekly Plan page from the same pattern as previous weeks, and/or set the tasks' week/status/date properties. All as proposals.\n7. Present the review table; on `apply`, write and report links. Offer to also set the \"Top 3 this week\" callout if their template has one.\n\n## Output format\n\n**Last week closed** - `planned N · done D · slipped S` + two-line retro.\n\n**Carryover decisions**:\n\n| Item | Verdict (recommit / backlog / rescope?) | Why |\n|---|---|---|\n\n**This week's plan** (review - wait for `apply`):\n\n| # | Day | Task | Goal it serves | Effort | New or existing |\n|---|---|---|---|---|---|\n\n**Capacity check** - one line: `X commitments vs capacity ~Y · buffer: <ok/over - cut list below>`\n\n**Proposed writes** - numbered: page creation, property updates per task. `apply all`, `apply 1,3`, or `skip`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Start from last week's plan - carryover before new commitments\n- Link every planned item to the goal it serves; cap the goal-less maintenance bucket\n- Pull from the existing backlog before inventing new tasks\n- Enforce the capacity budget out loud\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent goals, tasks, or last week's results without a successful `use_tool` call\n- Don't create pages or update tasks without `apply`\n- Don't plan 25 items into a 10-item week - an overstuffed plan is a skipped plan\n- Don't silently re-add a twice-slipped item; call it out for rescoping\n- Don't create a new planning database/template when the user already has a weekly pattern\n- Don't delete or rewrite past weekly plans\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /notion-weekly-plan`\n```\n\nAfter writing the file, confirm it exists. The user can then run /notion-weekly-plan or describe the task from the skill description.", "skillContent": "---\nname: notion-weekly-plan\ndescription: >\n  Plan week in Notion from goals. Requires integrations: @notion. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"notion weekly plan\", /notion-weekly-plan.\nmetadata:\n  category: notion\n  type: connector\n  cadence: weekly\n  risk: medium\n  connectors: notion\n  integrations: @notion\nallowed-tools: search_tool, use_tool\n---\n# /notion-weekly-plan\n\nPlan week in Notion from goals.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nRun this Sunday evening or Monday morning: quarterly/monthly goals sit in one Notion page or database, tasks sit in another, and the week needs to be planned so the goals actually pull the tasks (not the other way round). It reviews last week's plan, carries over the unfinished, derives this week's priorities from the goals, and builds the weekly plan in whatever structure the user already keeps.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Where goals live (Goals database/page - read it) and where tasks live (tasks database - read schema), plus how weekly plans are kept: a Weekly Plans page per week, a filtered view, or \"This week\" statuses/dates on tasks. Match the existing pattern - don't introduce a new planning artifact\n- Last week's plan (find it) - carryover and win/loss review come from it\n- This week's fixed constraints: known meetings-heavy days, OOO, deadlines landing this week; and realistic capacity (how many meaningful tasks per day the user actually completes - infer from history if visible, and say so)\n- The user's prioritization lens: which 1-3 goals deserve this week's biggest blocks\n\n## Tone & voice\n\n- **Tone:** Database-aware. Propose page/row updates before writing. Plans in commitments, not wishes - few items, clearly goal-linked.\n- **Audience:** The user's Monday-morning self, who needs to know what matters without re-deriving it.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete tasks or previous weekly plans; carryover moves items forward, it doesn't erase the record of the missed week.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n1. Read the goals source, the tasks database schema, and last week's plan.\n2. **Close last week honestly**: which planned items finished, which slipped, and the slip reasons where visible. Two-line retro: what the pattern says (e.g. \"deep-work items slip on meeting-heavy days\").\n3. **Carryover triage**: each unfinished item is either re-committed this week, consciously pushed to backlog, or flagged \"slipped twice+ - rescope or drop?\" (repeat offenders signal the task is mis-sized or not actually important).\n4. **Derive the week from goals**: for each focus goal, pick the 1-3 tasks that most advance it - pulling from the existing task backlog first (query it), proposing genuinely-new tasks only where a goal has no next action. Every planned item carries its goal link; work that serves no goal gets a visible \"maintenance\" bucket, kept small.\n5. **Reality-check capacity**: total commitments vs stated capacity and the week's fixed constraints. Over budget → propose what to cut, don't shrink silently. Assign items to days honoring the constraints (deep work away from meeting-heavy days).\n6. Build the plan in the house structure: create the Weekly Plan page from the same pattern as previous weeks, and/or set the tasks' week/status/date properties. All as proposals.\n7. Present the review table; on `apply`, write and report links. Offer to also set the \"Top 3 this week\" callout if their template has one.\n\n## Output format\n\n**Last week closed** - `planned N · done D · slipped S` + two-line retro.\n\n**Carryover decisions**:\n\n| Item | Verdict (recommit / backlog / rescope?) | Why |\n|---|---|---|\n\n**This week's plan** (review - wait for `apply`):\n\n| # | Day | Task | Goal it serves | Effort | New or existing |\n|---|---|---|---|---|---|\n\n**Capacity check** - one line: `X commitments vs capacity ~Y · buffer: <ok/over - cut list below>`\n\n**Proposed writes** - numbered: page creation, property updates per task. `apply all`, `apply 1,3`, or `skip`.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Start from last week's plan - carryover before new commitments\n- Link every planned item to the goal it serves; cap the goal-less maintenance bucket\n- Pull from the existing backlog before inventing new tasks\n- Enforce the capacity budget out loud\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't invent goals, tasks, or last week's results without a successful `use_tool` call\n- Don't create pages or update tasks without `apply`\n- Don't plan 25 items into a 10-item week - an overstuffed plan is a skipped plan\n- Don't silently re-add a twice-slipped item; call it out for rescoping\n- Don't create a new planning database/template when the user already has a weekly pattern\n- Don't delete or rewrite past weekly plans\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Output matches requested tone and channel\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] User can copy-paste the result immediately\n- [ ] Required integrations (@notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /notion-weekly-plan`\n"}]};

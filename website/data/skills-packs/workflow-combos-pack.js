window.__XF_SKILL_PACKS__=window.__XF_SKILL_PACKS__||{};window.__XF_SKILL_PACKS__["workflow-combos-pack"]={"id": "workflow-combos-pack", "skills": [{"id": "content-pipeline", "aiInstallPrompt": "Install the skill 'content-pipeline' by writing the file below.\n\nUniversal path: ./skills/content-pipeline/SKILL.md\nGrok path: ~/.grok/skills/content-pipeline/SKILL.md\nCursor path: ~/.cursor/skills/content-pipeline/SKILL.md\nClaude path: ~/.claude/skills/content-pipeline/SKILL.md\nVS Code agent path: .github/skills/content-pipeline/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: content-pipeline\ndescription: >\n  Brief → Canva design → Drive export → schedule post. Requires integrations: @notion, @canva, @google-drive, @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"content pipeline\", /content-pipeline.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,canva,google-drive,zapier\n  integrations: @notion,@canva,@google-drive,@zapier\nallowed-tools: search_tool, use_tool\n---\n# /content-pipeline\n\nBrief → Canva design → Drive export → schedule post.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a content brief already exists (or the user can dictate one) and they want it carried through production end-to-end: pull the brief from Notion, turn it into a Canva design, export the finished asset to Google Drive, and hand scheduling to a Zapier workflow. Not for drafting long-form articles from scratch - this skill moves an approved brief to a scheduled post with an approval checkpoint between every app.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which Notion database or page holds the brief (name, URL, or a search phrase like \"July campaign briefs\")\n- Target channel and format (e.g., Instagram square, LinkedIn link image, blog header) - this decides the Canva design dimensions\n- Drive destination folder for the exported asset\n- Name of the existing Zapier scheduling Zap - or confirm the user wants a proposed Zap configuration to build instead\n- Publish date/time and timezone\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase - never advance until the previous artifact is approved.\n- **Audience:** Infer from the brief's target channel.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@canva** - Canva\n  - MCP server key: `canva`\n  - Example tools: `canva__list_designs`, `canva__create_design`, `canva__export_design`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** after telling the user which connector is missing, offer the degraded path defined under each Workflow step (e.g., @canva missing → deliver the design brief as text). Only abandon the whole run if @notion is missing AND the user cannot paste the brief.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nAnnounce the 4-step chain up front, then run it with a checkpoint after every step. On a re-run, first check for artifacts from a previous run (existing Canva design, existing Drive export) and resume at the first incomplete step instead of duplicating.\n\n**Step 1 - Notion: pull the brief.**\n- Read: locate the brief page/database entry. Extract title, objective, audience, key message, mandatory copy (headline, body, CTA), visual direction, brand notes, deadline.\n- Transform: normalize into a one-screen **brief card**: Objective / Audience / Channel & dimensions / Headline / Body copy / CTA / Visual direction / Deadline.\n- **Checkpoint 1:** show the brief card and ask \"Design from this as-is, or edit first?\" Do not touch Canva until approved.\n- If @notion is missing or the brief isn't found: ask the user to paste the brief text, build the brief card from it, and continue.\n\n**Step 2 - Canva: create the design.**\n- Read: the approved brief card only - no silent rewording of copy between steps.\n- Do: create a design at the channel's dimensions; place headline, body, and CTA text; note which brand elements were applied and which need manual attention.\n- Write: a new Canva design (capture its title and link/ID).\n- **Checkpoint 2:** share the design link plus a summary of what was placed where. Ask the user to open it, adjust visuals, and reply \"approved\" or list changes.\n- If @canva is missing: deliver the **design brief as text** - exact dimensions, layout description, each text block verbatim, color/font guidance - so the user can build it manually in any editor. Resume at step 3 once they confirm the asset exists (ask for its location).\n\n**Step 3 - Google Drive: export the asset.**\n- Read: the approved Canva design; export in the channel's format (PNG for social, PDF for print).\n- Write: upload to the agreed Drive folder, named `YYYY-MM-DD_<channel>_<campaign>` (match the folder's existing convention if one is visible).\n- **Checkpoint 3:** show file name, folder path, and shareable link. Confirm this exact file is what gets scheduled.\n- If @google-drive is missing: provide the Canva export instructions and link inline; ask the user for the final asset URL before step 4.\n\n**Step 4 - Zapier: schedule the post.**\n- What the AI does: assembles the trigger payload - asset URL, caption text, channel, publish datetime - and, if a scheduling Zap exists, triggers it via the Zapier connector.\n- What the Zap does: the actual queueing/publishing to the channel happens inside Zapier, not in this chat.\n- If no scheduling Zap exists: propose a Zap configuration (trigger, action app, field-by-field mapping) for the user to create in Zapier - the AI cannot build Zaps in the Zapier editor itself.\n- **Checkpoint 4 (final):** show the exact payload as a review table. Trigger only after explicit `apply`.\n- If @zapier is missing: output the caption, asset link, and recommended posting time so the user can schedule natively in the channel.\n\n## Output format\n\nPer-step artifacts, each followed by its checkpoint question:\n\n1. **Brief card** - Objective / Audience / Channel & dimensions / Copy blocks / Visual direction / Deadline\n2. **Design summary** - Canva design title + link, what was placed, open items for manual polish\n3. **Export record** - file name, format, Drive folder path, link\n4. **Schedule payload** - review table: field → value (Zap name, channel, caption, asset URL, publish time)\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Brief | Notion | Brief card | approved |\n| 2 Design | Canva | Design + link | approved |\n| 3 Export | Drive | File in folder | done |\n| 4 Schedule | Zapier | Zap triggered / config proposed | awaiting apply |\n\nStatuses: done · approved · awaiting approval · fallback (manual) · skipped.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- On re-run, search Canva and Drive for artifacts from a previous run of the same brief and resume - don't create a duplicate design or export\n- Match Canva dimensions to the target channel *before* creating the design, not after\n- Carry copy verbatim from the approved brief card into the design - flag any character-limit trims explicitly\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't start step 2 until the step-1 brief card is approved - a wrong brief compounds through every later app\n- Don't schedule an asset the user hasn't seen exported; checkpoint 3 exists so the wrong file version never goes live\n- Don't re-trigger the scheduling Zap on a re-run without first checking whether a post is already queued for that datetime\n- Don't claim a Zap was built - the AI triggers existing Zaps or proposes configs; it cannot create Zaps in the Zapier editor\n- Don't invent Notion, Canva, or Drive contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every step ended with its checkpoint before the next app was touched\n- [ ] Chain summary table shows a status for all 4 steps, including any fallbacks used\n- [ ] Copy in the design matches the approved brief card verbatim\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@notion, @canva, @google-drive, @zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /content-pipeline or describe the task from the skill description.", "skillContent": "---\nname: content-pipeline\ndescription: >\n  Brief → Canva design → Drive export → schedule post. Requires integrations: @notion, @canva, @google-drive, @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"content pipeline\", /content-pipeline.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,canva,google-drive,zapier\n  integrations: @notion,@canva,@google-drive,@zapier\nallowed-tools: search_tool, use_tool\n---\n# /content-pipeline\n\nBrief → Canva design → Drive export → schedule post.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a content brief already exists (or the user can dictate one) and they want it carried through production end-to-end: pull the brief from Notion, turn it into a Canva design, export the finished asset to Google Drive, and hand scheduling to a Zapier workflow. Not for drafting long-form articles from scratch - this skill moves an approved brief to a scheduled post with an approval checkpoint between every app.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which Notion database or page holds the brief (name, URL, or a search phrase like \"July campaign briefs\")\n- Target channel and format (e.g., Instagram square, LinkedIn link image, blog header) - this decides the Canva design dimensions\n- Drive destination folder for the exported asset\n- Name of the existing Zapier scheduling Zap - or confirm the user wants a proposed Zap configuration to build instead\n- Publish date/time and timezone\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase - never advance until the previous artifact is approved.\n- **Audience:** Infer from the brief's target channel.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@canva** - Canva\n  - MCP server key: `canva`\n  - Example tools: `canva__list_designs`, `canva__create_design`, `canva__export_design`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** after telling the user which connector is missing, offer the degraded path defined under each Workflow step (e.g., @canva missing → deliver the design brief as text). Only abandon the whole run if @notion is missing AND the user cannot paste the brief.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nAnnounce the 4-step chain up front, then run it with a checkpoint after every step. On a re-run, first check for artifacts from a previous run (existing Canva design, existing Drive export) and resume at the first incomplete step instead of duplicating.\n\n**Step 1 - Notion: pull the brief.**\n- Read: locate the brief page/database entry. Extract title, objective, audience, key message, mandatory copy (headline, body, CTA), visual direction, brand notes, deadline.\n- Transform: normalize into a one-screen **brief card**: Objective / Audience / Channel & dimensions / Headline / Body copy / CTA / Visual direction / Deadline.\n- **Checkpoint 1:** show the brief card and ask \"Design from this as-is, or edit first?\" Do not touch Canva until approved.\n- If @notion is missing or the brief isn't found: ask the user to paste the brief text, build the brief card from it, and continue.\n\n**Step 2 - Canva: create the design.**\n- Read: the approved brief card only - no silent rewording of copy between steps.\n- Do: create a design at the channel's dimensions; place headline, body, and CTA text; note which brand elements were applied and which need manual attention.\n- Write: a new Canva design (capture its title and link/ID).\n- **Checkpoint 2:** share the design link plus a summary of what was placed where. Ask the user to open it, adjust visuals, and reply \"approved\" or list changes.\n- If @canva is missing: deliver the **design brief as text** - exact dimensions, layout description, each text block verbatim, color/font guidance - so the user can build it manually in any editor. Resume at step 3 once they confirm the asset exists (ask for its location).\n\n**Step 3 - Google Drive: export the asset.**\n- Read: the approved Canva design; export in the channel's format (PNG for social, PDF for print).\n- Write: upload to the agreed Drive folder, named `YYYY-MM-DD_<channel>_<campaign>` (match the folder's existing convention if one is visible).\n- **Checkpoint 3:** show file name, folder path, and shareable link. Confirm this exact file is what gets scheduled.\n- If @google-drive is missing: provide the Canva export instructions and link inline; ask the user for the final asset URL before step 4.\n\n**Step 4 - Zapier: schedule the post.**\n- What the AI does: assembles the trigger payload - asset URL, caption text, channel, publish datetime - and, if a scheduling Zap exists, triggers it via the Zapier connector.\n- What the Zap does: the actual queueing/publishing to the channel happens inside Zapier, not in this chat.\n- If no scheduling Zap exists: propose a Zap configuration (trigger, action app, field-by-field mapping) for the user to create in Zapier - the AI cannot build Zaps in the Zapier editor itself.\n- **Checkpoint 4 (final):** show the exact payload as a review table. Trigger only after explicit `apply`.\n- If @zapier is missing: output the caption, asset link, and recommended posting time so the user can schedule natively in the channel.\n\n## Output format\n\nPer-step artifacts, each followed by its checkpoint question:\n\n1. **Brief card** - Objective / Audience / Channel & dimensions / Copy blocks / Visual direction / Deadline\n2. **Design summary** - Canva design title + link, what was placed, open items for manual polish\n3. **Export record** - file name, format, Drive folder path, link\n4. **Schedule payload** - review table: field → value (Zap name, channel, caption, asset URL, publish time)\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Brief | Notion | Brief card | approved |\n| 2 Design | Canva | Design + link | approved |\n| 3 Export | Drive | File in folder | done |\n| 4 Schedule | Zapier | Zap triggered / config proposed | awaiting apply |\n\nStatuses: done · approved · awaiting approval · fallback (manual) · skipped.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- On re-run, search Canva and Drive for artifacts from a previous run of the same brief and resume - don't create a duplicate design or export\n- Match Canva dimensions to the target channel *before* creating the design, not after\n- Carry copy verbatim from the approved brief card into the design - flag any character-limit trims explicitly\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't start step 2 until the step-1 brief card is approved - a wrong brief compounds through every later app\n- Don't schedule an asset the user hasn't seen exported; checkpoint 3 exists so the wrong file version never goes live\n- Don't re-trigger the scheduling Zap on a re-run without first checking whether a post is already queued for that datetime\n- Don't claim a Zap was built - the AI triggers existing Zaps or proposes configs; it cannot create Zaps in the Zapier editor\n- Don't invent Notion, Canva, or Drive contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every step ended with its checkpoint before the next app was touched\n- [ ] Chain summary table shows a status for all 4 steps, including any fallbacks used\n- [ ] Copy in the design matches the approved brief card verbatim\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@notion, @canva, @google-drive, @zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "contract-review-loop", "aiInstallPrompt": "Install the skill 'contract-review-loop' by writing the file below.\n\nUniversal path: ./skills/contract-review-loop/SKILL.md\nGrok path: ~/.grok/skills/contract-review-loop/SKILL.md\nCursor path: ~/.cursor/skills/contract-review-loop/SKILL.md\nClaude path: ~/.claude/skills/contract-review-loop/SKILL.md\nVS Code agent path: .github/skills/contract-review-loop/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: contract-review-loop\ndescription: >\n  Find contract → summarize → log decision. Requires integrations: @google-drive, @word, @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"contract review loop\", /contract-review-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: google-drive,microsoft-word,notion\n  integrations: @google-drive,@word,@notion\nallowed-tools: search_tool, use_tool\n---\n# /contract-review-loop\n\nFind contract → summarize → log decision.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a contract (NDA, MSA, vendor agreement, lease, SOW) is sitting in Drive or attached as a Word file and the user needs a structured clause summary plus a permanent decision record: locate the document, extract and flag the clauses that matter, and log the review outcome to a Notion decision log. Not a substitute for legal counsel - this produces a review brief and an audit trail, not legal advice.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- How to find the contract: file name, Drive folder, counterparty name, or an attached .docx\n- Contract type and what the user cares about most (e.g., \"check the termination and liability clauses\" vs full review)\n- Which Notion database is the decision log (or whether to create a page in a specified location)\n- The decision context: who decides (user, legal, manager) and by when\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Precise clause references (section numbers), never paraphrase obligations loosely.\n- **Audience:** Business owner or ops lead reviewing before signature - plain English, flag jargon.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@word** - Microsoft Word (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/docx`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@word** - Microsoft Word (cloud)\n  - MCP server key: `microsoft-word`\n  - Example tools: `microsoft-word__read_document`, `microsoft-word__update_document`\n  - Local file edits: also run skill `/docx`\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** only ONE document source is needed - if @google-drive is missing but the user can attach the Word file (or vice versa), proceed with what's available. If @notion is missing at step 3, deliver the decision-log entry as copy-paste markdown instead.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nRun three steps with a checkpoint after each. Before step 3, check the Notion decision log for an existing entry for this contract - if one exists, offer to update it rather than create a duplicate.\n\n**Step 1 - Drive/Word: locate and read the contract.**\n- Read: search Drive by file name/counterparty (or read the attached .docx). If multiple versions match, list them with modified dates and ask which is current - reviewing a stale draft is the classic failure here.\n- Extract: full text, plus metadata (file name, folder, last modified, version marker in the filename if any).\n- **Checkpoint 1:** confirm the document identity - \"Reviewing *<file name>*, modified <date>, <n> pages, counterparty <X>. Correct version?\" Do not summarize an unconfirmed file.\n- Fallback: if neither Drive nor Word access works, ask the user to paste the contract text and note \"source: pasted text\" in the final log.\n\n**Step 2 - Summarize clauses (AI, no connector).**\n- Transform the text into a **clause review table** covering, at minimum: parties & term, payment terms, auto-renewal, termination & notice period, liability cap & indemnification, IP ownership, confidentiality, governing law, and anything unusual for the contract type.\n- For each clause: section reference, one-line plain-English summary, and a flag - 🟢 standard / 🟡 review / 🔴 unusual or one-sided - with a one-line reason. Quote exact language for every 🔴.\n- List **questions for the counterparty or legal** separately from the summary; mark all risk judgments as recommendations, not legal advice.\n- **Checkpoint 2:** present the clause table and ask for the user's decision: approve / negotiate (which clauses) / reject / escalate to legal.\n\n**Step 3 - Notion: log the decision.**\n- Write: create (or update) a decision-log entry containing: contract name + link to the Drive file, counterparty, review date, clause table, decision, rationale, follow-up owner and due date (e.g., \"renegotiate liability cap by Friday\").\n- **Checkpoint 3 (approval gate):** show the exact page content and target database before creating. Write only after explicit `apply`.\n- Fallback: if @notion is missing, output the full entry as markdown for the user to paste, and say exactly where it was meant to go.\n\n## Output format\n\n1. **Document confirmation** (step 1) - file name, version/date, counterparty, source\n2. **Clause review table** (step 2) - Section / Clause / Plain-English summary / Flag / Reason - followed by \"Questions for counterparty/legal\"\n3. **Decision-log entry preview** (step 3) - the exact Notion page content, awaiting `apply`\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Locate | Drive / Word | Confirmed contract file | done |\n| 2 Review | AI | Clause table + flags | approved |\n| 3 Log | Notion | Decision entry + follow-ups | awaiting apply / fallback |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Confirm the file version at checkpoint 1 before investing in the summary - \"final_v2\" and \"final_FINAL\" coexist in every Drive folder\n- Quote exact contract language for every red flag; paraphrase is not evidence\n- Check for an existing decision-log entry before creating one - re-runs should update, not duplicate\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't summarize a file the user hasn't confirmed is the current version - step 2 depends entirely on step 1 being right\n- Don't log a decision to Notion before the user has actually stated one at checkpoint 2 - no decision means the log entry says \"pending review\"\n- Don't present risk flags as legal conclusions; they are prompts for a lawyer or the counterparty\n- Don't invent file contents or Notion structures without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Document identity confirmed before any clause was summarized\n- [ ] Every 🔴 flag has a verbatim quote and section reference\n- [ ] Chain summary table shows a status for all 3 steps, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@google-drive, @word, @notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /contract-review-loop or describe the task from the skill description.", "skillContent": "---\nname: contract-review-loop\ndescription: >\n  Find contract → summarize → log decision. Requires integrations: @google-drive, @word, @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"contract review loop\", /contract-review-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: google-drive,microsoft-word,notion\n  integrations: @google-drive,@word,@notion\nallowed-tools: search_tool, use_tool\n---\n# /contract-review-loop\n\nFind contract → summarize → log decision.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a contract (NDA, MSA, vendor agreement, lease, SOW) is sitting in Drive or attached as a Word file and the user needs a structured clause summary plus a permanent decision record: locate the document, extract and flag the clauses that matter, and log the review outcome to a Notion decision log. Not a substitute for legal counsel - this produces a review brief and an audit trail, not legal advice.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- How to find the contract: file name, Drive folder, counterparty name, or an attached .docx\n- Contract type and what the user cares about most (e.g., \"check the termination and liability clauses\" vs full review)\n- Which Notion database is the decision log (or whether to create a page in a specified location)\n- The decision context: who decides (user, legal, manager) and by when\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Precise clause references (section numbers), never paraphrase obligations loosely.\n- **Audience:** Business owner or ops lead reviewing before signature - plain English, flag jargon.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@word** - Microsoft Word (attach in prompt or enable Office MCP)\n  - Local edits: run bundled skill `/docx`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@word** - Microsoft Word (cloud)\n  - MCP server key: `microsoft-word`\n  - Example tools: `microsoft-word__read_document`, `microsoft-word__update_document`\n  - Local file edits: also run skill `/docx`\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** only ONE document source is needed - if @google-drive is missing but the user can attach the Word file (or vice versa), proceed with what's available. If @notion is missing at step 3, deliver the decision-log entry as copy-paste markdown instead.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nRun three steps with a checkpoint after each. Before step 3, check the Notion decision log for an existing entry for this contract - if one exists, offer to update it rather than create a duplicate.\n\n**Step 1 - Drive/Word: locate and read the contract.**\n- Read: search Drive by file name/counterparty (or read the attached .docx). If multiple versions match, list them with modified dates and ask which is current - reviewing a stale draft is the classic failure here.\n- Extract: full text, plus metadata (file name, folder, last modified, version marker in the filename if any).\n- **Checkpoint 1:** confirm the document identity - \"Reviewing *<file name>*, modified <date>, <n> pages, counterparty <X>. Correct version?\" Do not summarize an unconfirmed file.\n- Fallback: if neither Drive nor Word access works, ask the user to paste the contract text and note \"source: pasted text\" in the final log.\n\n**Step 2 - Summarize clauses (AI, no connector).**\n- Transform the text into a **clause review table** covering, at minimum: parties & term, payment terms, auto-renewal, termination & notice period, liability cap & indemnification, IP ownership, confidentiality, governing law, and anything unusual for the contract type.\n- For each clause: section reference, one-line plain-English summary, and a flag - 🟢 standard / 🟡 review / 🔴 unusual or one-sided - with a one-line reason. Quote exact language for every 🔴.\n- List **questions for the counterparty or legal** separately from the summary; mark all risk judgments as recommendations, not legal advice.\n- **Checkpoint 2:** present the clause table and ask for the user's decision: approve / negotiate (which clauses) / reject / escalate to legal.\n\n**Step 3 - Notion: log the decision.**\n- Write: create (or update) a decision-log entry containing: contract name + link to the Drive file, counterparty, review date, clause table, decision, rationale, follow-up owner and due date (e.g., \"renegotiate liability cap by Friday\").\n- **Checkpoint 3 (approval gate):** show the exact page content and target database before creating. Write only after explicit `apply`.\n- Fallback: if @notion is missing, output the full entry as markdown for the user to paste, and say exactly where it was meant to go.\n\n## Output format\n\n1. **Document confirmation** (step 1) - file name, version/date, counterparty, source\n2. **Clause review table** (step 2) - Section / Clause / Plain-English summary / Flag / Reason - followed by \"Questions for counterparty/legal\"\n3. **Decision-log entry preview** (step 3) - the exact Notion page content, awaiting `apply`\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Locate | Drive / Word | Confirmed contract file | done |\n| 2 Review | AI | Clause table + flags | approved |\n| 3 Log | Notion | Decision entry + follow-ups | awaiting apply / fallback |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Confirm the file version at checkpoint 1 before investing in the summary - \"final_v2\" and \"final_FINAL\" coexist in every Drive folder\n- Quote exact contract language for every red flag; paraphrase is not evidence\n- Check for an existing decision-log entry before creating one - re-runs should update, not duplicate\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't summarize a file the user hasn't confirmed is the current version - step 2 depends entirely on step 1 being right\n- Don't log a decision to Notion before the user has actually stated one at checkpoint 2 - no decision means the log entry says \"pending review\"\n- Don't present risk flags as legal conclusions; they are prompts for a lawyer or the counterparty\n- Don't invent file contents or Notion structures without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Document identity confirmed before any clause was summarized\n- [ ] Every 🔴 flag has a verbatim quote and section reference\n- [ ] Chain summary table shows a status for all 3 steps, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@google-drive, @word, @notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "customer-onboarding", "aiInstallPrompt": "Install the skill 'customer-onboarding' by writing the file below.\n\nUniversal path: ./skills/customer-onboarding/SKILL.md\nGrok path: ~/.grok/skills/customer-onboarding/SKILL.md\nCursor path: ~/.cursor/skills/customer-onboarding/SKILL.md\nClaude path: ~/.claude/skills/customer-onboarding/SKILL.md\nVS Code agent path: .github/skills/customer-onboarding/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: customer-onboarding\ndescription: >\n  New customer welcome across HubSpot, mail, Notion, Teams. Requires integrations: @zapier, @gmail, @notion, @teams. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"customer onboarding\", /customer-onboarding.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: zapier,gmail,notion,microsoft-teams\n  integrations: @zapier,@gmail,@notion,@teams\nallowed-tools: search_tool, use_tool\n---\n# /customer-onboarding\n\nNew customer welcome across HubSpot, mail, Notion, Teams.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this the moment a deal closes and a new customer needs a consistent first week: mark them onboarding in the CRM (via the user's HubSpot Zap), send a personalized welcome email, spin up a success plan page in Notion, and introduce the account to the internal team in Teams. One customer per run - the value is that nothing in the first-touch sequence gets skipped or done twice.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Customer details: company name, primary contact name + email, plan/package purchased, start date, account owner (CSM)\n- The name of the HubSpot-update Zap and what payload it expects (deal ID or company name at minimum)\n- Welcome email specifics: sender identity, any onboarding call link to include, tone (formal vs friendly)\n- Which Notion database holds success plans, and which Teams channel gets new-customer intros\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Customer-facing text warm and specific; internal text brisk and factual.\n- **Audience:** The welcome email addresses the customer contact by name; the Teams intro addresses the delivery team.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@teams** - Microsoft Teams\n  - MCP server key: `microsoft-teams`\n  - Example tools: `microsoft-teams__list_channels`, `microsoft-teams__post_message`, `microsoft-teams__list_messages`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** each step degrades independently - @zapier missing → give the user the CRM field values to set by hand; @gmail missing → deliver the welcome email as copy-paste text; @notion missing → output the success plan as markdown; @teams missing → output the intro message text. Complete every step you can and mark fallbacks in the chain summary.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- The welcome email goes to a real customer: it is NEVER sent without explicit approval of the exact text and recipient. Prefer creating a draft over sending.\n\n## Workflow\n\nBefore step 1, run a duplicate check: search Gmail sent mail and the Notion success-plan database for this customer. If a welcome was already sent or a plan already exists, report what exists and resume at the first missing step - never re-onboard.\n\n**Step 1 - Zapier: update the CRM.**\n- What the AI does: builds the payload (company, contact, plan, start date, owner, lifecycle stage → \"onboarding\") and fires the user's existing HubSpot Zap after approval.\n- What the Zap does: the actual HubSpot record update happens inside Zapier. The AI cannot build Zaps - if none exists, propose a Zap config (trigger + HubSpot \"Update company/deal\" action + field mapping) for the user to create, and continue to step 2.\n- **Checkpoint 1:** show the payload table; fire only on `apply`. Report the Zapier response honestly (accepted ≠ record verified).\n\n**Step 2 - Gmail: welcome email.**\n- Transform: draft a welcome email using the customer's name, plan purchased, what happens next (kickoff call, resources), and the account owner's name/signature. No unexplained merge fields - every placeholder resolved or flagged.\n- Write: create a **draft** in Gmail addressed to the primary contact.\n- **Checkpoint 2:** show the full email (to/subject/body). On `apply`, save as draft (or send only if the user explicitly says send). Share the draft link/ID.\n- Fallback: no @gmail → output the email as copy-paste text with the recipient line.\n\n**Step 3 - Notion: success plan page.**\n- Write: create a page in the success-plan database titled \"<Company> - Success Plan\", with: account snapshot (plan, start date, owner, contact), goals (ask user for 1-3 if unknown), 30/60/90-day milestones, kickoff agenda, and a risks/notes section.\n- **Checkpoint 3:** show the page outline before creating; create on `apply`; return the page link.\n- Fallback: no @notion → deliver the same structure as markdown.\n\n**Step 4 - Teams: internal intro.**\n- Read: nothing new - reuse approved facts from steps 1-3 (never re-ask).\n- Write: post to the agreed channel: who the customer is, plan, start date, account owner, link to the Notion success plan, and the one thing the team should know (from user context).\n- **Checkpoint 4:** show the exact message text + target channel; post only on `apply`.\n- Fallback: no @teams → output the message text and name the intended channel.\n\n## Output format\n\n1. **CRM payload table** (step 1) - field → value, Zap name, awaiting `apply`\n2. **Welcome email preview** (step 2) - To / Subject / full body, awaiting `apply`\n3. **Success plan outline** (step 3) - page title + section structure, awaiting `apply`; then page link\n4. **Teams intro preview** (step 4) - channel + message text, awaiting `apply`\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 CRM update | Zapier → HubSpot | Payload fired / config proposed | done / fallback |\n| 2 Welcome | Gmail | Draft created (link) | done / fallback |\n| 3 Success plan | Notion | Page created (link) | done / fallback |\n| 4 Team intro | Teams | Message posted | done / fallback |\n\nStatuses: done · awaiting apply · fallback (manual) · skipped (already existed).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Run the duplicate check before step 1 - a second welcome email to the same customer is the most embarrassing failure this combo can produce\n- Keep customer name, plan, and start date identical across all four artifacts - one source of truth from the inputs\n- Link the Notion success plan in the Teams intro so the team lands in the right place\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't post the Teams intro (step 4) before the success plan (step 3) exists - the intro links to it\n- Don't send the welcome email without explicit approval of the exact text and recipient; default to draft\n- Don't claim the HubSpot record was updated when only the Zap trigger was accepted\n- Don't re-run all four steps on a partial re-run - resume at the first incomplete step\n- Don't invent CRM fields, email threads, or channel names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Duplicate check ran before any writes\n- [ ] Each customer-facing text was approved verbatim before send/create\n- [ ] Chain summary table shows all 4 steps with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier, @gmail, @notion, @teams) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /customer-onboarding or describe the task from the skill description.", "skillContent": "---\nname: customer-onboarding\ndescription: >\n  New customer welcome across HubSpot, mail, Notion, Teams. Requires integrations: @zapier, @gmail, @notion, @teams. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"customer onboarding\", /customer-onboarding.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: zapier,gmail,notion,microsoft-teams\n  integrations: @zapier,@gmail,@notion,@teams\nallowed-tools: search_tool, use_tool\n---\n# /customer-onboarding\n\nNew customer welcome across HubSpot, mail, Notion, Teams.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this the moment a deal closes and a new customer needs a consistent first week: mark them onboarding in the CRM (via the user's HubSpot Zap), send a personalized welcome email, spin up a success plan page in Notion, and introduce the account to the internal team in Teams. One customer per run - the value is that nothing in the first-touch sequence gets skipped or done twice.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Customer details: company name, primary contact name + email, plan/package purchased, start date, account owner (CSM)\n- The name of the HubSpot-update Zap and what payload it expects (deal ID or company name at minimum)\n- Welcome email specifics: sender identity, any onboarding call link to include, tone (formal vs friendly)\n- Which Notion database holds success plans, and which Teams channel gets new-customer intros\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Customer-facing text warm and specific; internal text brisk and factual.\n- **Audience:** The welcome email addresses the customer contact by name; the Teams intro addresses the delivery team.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@teams** - Microsoft Teams\n  - MCP server key: `microsoft-teams`\n  - Example tools: `microsoft-teams__list_channels`, `microsoft-teams__post_message`, `microsoft-teams__list_messages`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** each step degrades independently - @zapier missing → give the user the CRM field values to set by hand; @gmail missing → deliver the welcome email as copy-paste text; @notion missing → output the success plan as markdown; @teams missing → output the intro message text. Complete every step you can and mark fallbacks in the chain summary.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- The welcome email goes to a real customer: it is NEVER sent without explicit approval of the exact text and recipient. Prefer creating a draft over sending.\n\n## Workflow\n\nBefore step 1, run a duplicate check: search Gmail sent mail and the Notion success-plan database for this customer. If a welcome was already sent or a plan already exists, report what exists and resume at the first missing step - never re-onboard.\n\n**Step 1 - Zapier: update the CRM.**\n- What the AI does: builds the payload (company, contact, plan, start date, owner, lifecycle stage → \"onboarding\") and fires the user's existing HubSpot Zap after approval.\n- What the Zap does: the actual HubSpot record update happens inside Zapier. The AI cannot build Zaps - if none exists, propose a Zap config (trigger + HubSpot \"Update company/deal\" action + field mapping) for the user to create, and continue to step 2.\n- **Checkpoint 1:** show the payload table; fire only on `apply`. Report the Zapier response honestly (accepted ≠ record verified).\n\n**Step 2 - Gmail: welcome email.**\n- Transform: draft a welcome email using the customer's name, plan purchased, what happens next (kickoff call, resources), and the account owner's name/signature. No unexplained merge fields - every placeholder resolved or flagged.\n- Write: create a **draft** in Gmail addressed to the primary contact.\n- **Checkpoint 2:** show the full email (to/subject/body). On `apply`, save as draft (or send only if the user explicitly says send). Share the draft link/ID.\n- Fallback: no @gmail → output the email as copy-paste text with the recipient line.\n\n**Step 3 - Notion: success plan page.**\n- Write: create a page in the success-plan database titled \"<Company> - Success Plan\", with: account snapshot (plan, start date, owner, contact), goals (ask user for 1-3 if unknown), 30/60/90-day milestones, kickoff agenda, and a risks/notes section.\n- **Checkpoint 3:** show the page outline before creating; create on `apply`; return the page link.\n- Fallback: no @notion → deliver the same structure as markdown.\n\n**Step 4 - Teams: internal intro.**\n- Read: nothing new - reuse approved facts from steps 1-3 (never re-ask).\n- Write: post to the agreed channel: who the customer is, plan, start date, account owner, link to the Notion success plan, and the one thing the team should know (from user context).\n- **Checkpoint 4:** show the exact message text + target channel; post only on `apply`.\n- Fallback: no @teams → output the message text and name the intended channel.\n\n## Output format\n\n1. **CRM payload table** (step 1) - field → value, Zap name, awaiting `apply`\n2. **Welcome email preview** (step 2) - To / Subject / full body, awaiting `apply`\n3. **Success plan outline** (step 3) - page title + section structure, awaiting `apply`; then page link\n4. **Teams intro preview** (step 4) - channel + message text, awaiting `apply`\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 CRM update | Zapier → HubSpot | Payload fired / config proposed | done / fallback |\n| 2 Welcome | Gmail | Draft created (link) | done / fallback |\n| 3 Success plan | Notion | Page created (link) | done / fallback |\n| 4 Team intro | Teams | Message posted | done / fallback |\n\nStatuses: done · awaiting apply · fallback (manual) · skipped (already existed).\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Run the duplicate check before step 1 - a second welcome email to the same customer is the most embarrassing failure this combo can produce\n- Keep customer name, plan, and start date identical across all four artifacts - one source of truth from the inputs\n- Link the Notion success plan in the Teams intro so the team lands in the right place\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't post the Teams intro (step 4) before the success plan (step 3) exists - the intro links to it\n- Don't send the welcome email without explicit approval of the exact text and recipient; default to draft\n- Don't claim the HubSpot record was updated when only the Zap trigger was accepted\n- Don't re-run all four steps on a partial re-run - resume at the first incomplete step\n- Don't invent CRM fields, email threads, or channel names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Duplicate check ran before any writes\n- [ ] Each customer-facing text was approved verbatim before send/create\n- [ ] Chain summary table shows all 4 steps with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier, @gmail, @notion, @teams) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "design-to-dev", "aiInstallPrompt": "Install the skill 'design-to-dev' by writing the file below.\n\nUniversal path: ./skills/design-to-dev/SKILL.md\nGrok path: ~/.grok/skills/design-to-dev/SKILL.md\nCursor path: ~/.cursor/skills/design-to-dev/SKILL.md\nClaude path: ~/.claude/skills/design-to-dev/SKILL.md\nVS Code agent path: .github/skills/design-to-dev/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: design-to-dev\ndescription: >\n  Figma handoff → Linear tickets → GitHub → Slack. Requires integrations: @figma, @linear, @github, @slack. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"design to dev\", /design-to-dev.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: figma,linear,github,slack\n  integrations: @figma,@linear,@github,@slack\nallowed-tools: search_tool, use_tool\n---\n# /design-to-dev\n\nFigma handoff → Linear tickets → GitHub → Slack.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a Figma file (or specific frames) is ready for engineering: extract the implementable specs, break them into scoped Linear tickets, cross-link any related GitHub work, and announce the handoff in Slack. Best run once per feature/design milestone - it turns \"the designs are done\" into tickets an engineer can pick up without opening Figma first.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Figma file URL (and which frames/pages are in scope - \"everything\" is rarely true)\n- Linear team + project the tickets belong to, plus default priority and label (e.g., `design-handoff`)\n- Whether related GitHub work exists (repo name, or a branch/PR naming convention to search)\n- Which Slack channel gets the handoff post, and who the engineering point person is\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Tickets written for engineers: acceptance criteria, not vibes.\n- **Audience:** Design + engineering; the Slack post is skimmable by both.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@figma** - Figma\n  - MCP server key: `figma`\n  - Example tools: `figma__get_file`, `figma__get_components`\n\n- **@linear** - Linear\n  - MCP server key: `linear`\n  - Example tools: `linear__list_issues`, `linear__create_issue`, `linear__update_issue`\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n- **@slack** - Slack\n  - MCP server key: `slack`\n  - Example tools: `slack__list_channels`, `slack__get_channel_history`, `slack__post_message`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** @figma missing → ask the user to paste frame names + screenshots/spec notes and build the spec sheet from that; @linear missing → deliver tickets as copy-paste markdown blocks (title, description, acceptance criteria) for manual creation; @github missing → skip cross-linking and note it in the summary; @slack missing → output the handoff message as text.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nBefore step 2, search Linear for existing tickets referencing this Figma file/feature - a re-run must update or skip, never create duplicate tickets.\n\n**Step 1 - Figma: extract the spec.**\n- Read: the file's in-scope frames and components - frame names, component variants, text content, and any designer annotations/notes layers.\n- Transform: build a **handoff spec sheet**: one row per implementable unit (screen or component) with frame name + link, what it does, states/variants covered (hover, empty, error, mobile?), and open questions where the design is ambiguous (e.g., \"no error state for the upload field\").\n- **Checkpoint 1:** show the spec sheet and the open questions. Ask the user (or designer) to resolve ambiguities and confirm the ticket breakdown BEFORE anything is created - this is the moment scope gets decided.\n\n**Step 2 - Linear: create tickets.**\n- Transform: convert each approved spec row into a ticket: title (`[Feature] Build <component/screen>`), description with the Figma frame deep-link, acceptance criteria as a checklist (one per state/variant), and the agreed label + priority. Group under a parent issue/project if the team uses one.\n- **Checkpoint 2 (approval gate):** show the full ticket list as a review table (title, estimate-relevant notes, criteria count). Create only after `apply` - then report each new ticket's ID and URL.\n- Failure behavior: if some creations fail mid-batch, report exactly which tickets exist and which don't; never silently retry the whole batch (that's how duplicates happen).\n\n**Step 3 - GitHub: cross-link existing work.**\n- Read: search the repo for branches/PRs/issues already touching this feature (by feature name or convention).\n- Write: where matches exist, add the Linear ticket link to the PR (comment) and the PR link to the Linear ticket - propose the exact comments first.\n- **Checkpoint 3:** show the proposed link pairs (ticket ↔ PR). Apply on confirmation. If nothing matches, say so - an empty result is a valid result.\n\n**Step 4 - Slack: announce the handoff.**\n- Write: post to the agreed channel: feature name, Figma file link, list of created ticket IDs with links, engineering point person, and the open questions that survived checkpoint 1 (so unknowns are public, not buried).\n- **Checkpoint 4:** show the exact message + channel; post only on `apply`.\n\n## Output format\n\n1. **Handoff spec sheet** (step 1) - Frame / Purpose / States covered / Open questions\n2. **Ticket review table** (step 2) - proposed tickets awaiting `apply`; then created IDs + URLs\n3. **Cross-link report** (step 3) - ticket ↔ PR pairs applied, or \"no existing work found\"\n4. **Slack post preview** (step 4) - channel + message, awaiting `apply`\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Spec | Figma | Spec sheet, N units, M open questions | approved |\n| 2 Tickets | Linear | N tickets (IDs) | done |\n| 3 Links | GitHub | K ticket↔PR links | done / none found |\n| 4 Announce | Slack | Handoff post | done / awaiting apply |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Search Linear for existing tickets on this feature before creating any - re-runs update, never duplicate\n- Put the Figma frame deep-link in every ticket; \"see designs\" without a link wastes every engineer's first ten minutes\n- Surface missing states (error/empty/loading/mobile) as open questions at checkpoint 1, not as silently-invented acceptance criteria\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't create tickets (step 2) from a spec sheet the user hasn't approved - unresolved open questions become wrong acceptance criteria\n- Don't announce in Slack (step 4) before tickets exist; a handoff post with no ticket links just creates \"where are the tickets?\" replies\n- Don't retry a partially-failed ticket batch wholesale - reconcile what exists first\n- Don't invent Figma frames, ticket IDs, or PR numbers without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Spec sheet approved (with open questions resolved or explicitly carried) before any ticket was created\n- [ ] Every created ticket has a Figma deep-link and checklist acceptance criteria\n- [ ] Chain summary table shows all 4 steps with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@figma, @linear, @github, @slack) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /design-to-dev or describe the task from the skill description.", "skillContent": "---\nname: design-to-dev\ndescription: >\n  Figma handoff → Linear tickets → GitHub → Slack. Requires integrations: @figma, @linear, @github, @slack. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"design to dev\", /design-to-dev.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: figma,linear,github,slack\n  integrations: @figma,@linear,@github,@slack\nallowed-tools: search_tool, use_tool\n---\n# /design-to-dev\n\nFigma handoff → Linear tickets → GitHub → Slack.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a Figma file (or specific frames) is ready for engineering: extract the implementable specs, break them into scoped Linear tickets, cross-link any related GitHub work, and announce the handoff in Slack. Best run once per feature/design milestone - it turns \"the designs are done\" into tickets an engineer can pick up without opening Figma first.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Figma file URL (and which frames/pages are in scope - \"everything\" is rarely true)\n- Linear team + project the tickets belong to, plus default priority and label (e.g., `design-handoff`)\n- Whether related GitHub work exists (repo name, or a branch/PR naming convention to search)\n- Which Slack channel gets the handoff post, and who the engineering point person is\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Tickets written for engineers: acceptance criteria, not vibes.\n- **Audience:** Design + engineering; the Slack post is skimmable by both.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@figma** - Figma\n  - MCP server key: `figma`\n  - Example tools: `figma__get_file`, `figma__get_components`\n\n- **@linear** - Linear\n  - MCP server key: `linear`\n  - Example tools: `linear__list_issues`, `linear__create_issue`, `linear__update_issue`\n\n- **@github** - GitHub\n  - MCP server key: `github`\n  - Example tools: `github__list_pull_requests`, `github__list_issues`, `github__get_pull_request`\n\n- **@slack** - Slack\n  - MCP server key: `slack`\n  - Example tools: `slack__list_channels`, `slack__get_channel_history`, `slack__post_message`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** @figma missing → ask the user to paste frame names + screenshots/spec notes and build the spec sheet from that; @linear missing → deliver tickets as copy-paste markdown blocks (title, description, acceptance criteria) for manual creation; @github missing → skip cross-linking and note it in the summary; @slack missing → output the handoff message as text.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nBefore step 2, search Linear for existing tickets referencing this Figma file/feature - a re-run must update or skip, never create duplicate tickets.\n\n**Step 1 - Figma: extract the spec.**\n- Read: the file's in-scope frames and components - frame names, component variants, text content, and any designer annotations/notes layers.\n- Transform: build a **handoff spec sheet**: one row per implementable unit (screen or component) with frame name + link, what it does, states/variants covered (hover, empty, error, mobile?), and open questions where the design is ambiguous (e.g., \"no error state for the upload field\").\n- **Checkpoint 1:** show the spec sheet and the open questions. Ask the user (or designer) to resolve ambiguities and confirm the ticket breakdown BEFORE anything is created - this is the moment scope gets decided.\n\n**Step 2 - Linear: create tickets.**\n- Transform: convert each approved spec row into a ticket: title (`[Feature] Build <component/screen>`), description with the Figma frame deep-link, acceptance criteria as a checklist (one per state/variant), and the agreed label + priority. Group under a parent issue/project if the team uses one.\n- **Checkpoint 2 (approval gate):** show the full ticket list as a review table (title, estimate-relevant notes, criteria count). Create only after `apply` - then report each new ticket's ID and URL.\n- Failure behavior: if some creations fail mid-batch, report exactly which tickets exist and which don't; never silently retry the whole batch (that's how duplicates happen).\n\n**Step 3 - GitHub: cross-link existing work.**\n- Read: search the repo for branches/PRs/issues already touching this feature (by feature name or convention).\n- Write: where matches exist, add the Linear ticket link to the PR (comment) and the PR link to the Linear ticket - propose the exact comments first.\n- **Checkpoint 3:** show the proposed link pairs (ticket ↔ PR). Apply on confirmation. If nothing matches, say so - an empty result is a valid result.\n\n**Step 4 - Slack: announce the handoff.**\n- Write: post to the agreed channel: feature name, Figma file link, list of created ticket IDs with links, engineering point person, and the open questions that survived checkpoint 1 (so unknowns are public, not buried).\n- **Checkpoint 4:** show the exact message + channel; post only on `apply`.\n\n## Output format\n\n1. **Handoff spec sheet** (step 1) - Frame / Purpose / States covered / Open questions\n2. **Ticket review table** (step 2) - proposed tickets awaiting `apply`; then created IDs + URLs\n3. **Cross-link report** (step 3) - ticket ↔ PR pairs applied, or \"no existing work found\"\n4. **Slack post preview** (step 4) - channel + message, awaiting `apply`\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Spec | Figma | Spec sheet, N units, M open questions | approved |\n| 2 Tickets | Linear | N tickets (IDs) | done |\n| 3 Links | GitHub | K ticket↔PR links | done / none found |\n| 4 Announce | Slack | Handoff post | done / awaiting apply |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Search Linear for existing tickets on this feature before creating any - re-runs update, never duplicate\n- Put the Figma frame deep-link in every ticket; \"see designs\" without a link wastes every engineer's first ten minutes\n- Surface missing states (error/empty/loading/mobile) as open questions at checkpoint 1, not as silently-invented acceptance criteria\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't create tickets (step 2) from a spec sheet the user hasn't approved - unresolved open questions become wrong acceptance criteria\n- Don't announce in Slack (step 4) before tickets exist; a handoff post with no ticket links just creates \"where are the tickets?\" replies\n- Don't retry a partially-failed ticket batch wholesale - reconcile what exists first\n- Don't invent Figma frames, ticket IDs, or PR numbers without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Spec sheet approved (with open questions resolved or explicitly carried) before any ticket was created\n- [ ] Every created ticket has a Figma deep-link and checklist acceptance criteria\n- [ ] Chain summary table shows all 4 steps with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@figma, @linear, @github, @slack) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "finance-closed-loop", "aiInstallPrompt": "Install the skill 'finance-closed-loop' by writing the file below.\n\nUniversal path: ./skills/finance-closed-loop/SKILL.md\nGrok path: ~/.grok/skills/finance-closed-loop/SKILL.md\nCursor path: ~/.cursor/skills/finance-closed-loop/SKILL.md\nClaude path: ~/.claude/skills/finance-closed-loop/SKILL.md\nVS Code agent path: .github/skills/finance-closed-loop/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: finance-closed-loop\ndescription: >\n  Receipt email → expense sheet → tax deadline calendar. Requires integrations: @gmail, @google-drive, @google-calendar. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"finance closed loop\", /finance-closed-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: weekly\n  risk: medium\n  connectors: gmail,google-drive,google-calendar\n  integrations: @gmail,@google-drive,@google-calendar\nallowed-tools: search_tool, use_tool\n---\n# /finance-closed-loop\n\nReceipt email → expense sheet → tax deadline calendar.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as a weekly bookkeeping sweep for a freelancer or small business: harvest receipt and invoice emails from Gmail, append clean rows to the expense sheet in Drive, and make sure tax/filing deadlines sit on the calendar with prep time in front of them. It keeps the books current in one pass - it does not give tax advice or decide deductibility.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Date range to sweep (default: the last 7 days, or since the newest row already in the sheet)\n- The expense sheet in Drive (name or link) and its column layout - read the header row rather than assuming\n- Expense categories the user files under (read existing rows for the vocabulary; don't invent new categories)\n- Which jurisdiction's tax deadlines matter (e.g., quarterly estimated tax, VAT/GST filing) - from the user, never guessed\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Numbers exact - amount, currency, date, vendor - no rounding, no inferred totals.\n- **Audience:** The user's future self at tax time; every row must be traceable back to its email.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** @gmail is the source and has no fallback (stop if missing, unless the user forwards/pastes receipts). @google-drive missing → output the expense rows as a markdown/CSV block for manual paste. @google-calendar missing → output the deadline list with recommended reminder dates as text.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n**Step 1 - Gmail: harvest receipts.**\n- Read: search the date range for receipts/invoices (queries like vendor names the sheet already contains, plus terms such as \"receipt\", \"invoice\", \"payment confirmation\", \"order\"). Open each hit and extract: date, vendor, amount, currency, payment method, order/invoice number, and the email's subject + date for traceability.\n- Transform: normalize into candidate rows; suggest a category using ONLY the sheet's existing category vocabulary; mark uncertain fields with `?` rather than guessing amounts.\n- Exclude noise explicitly: marketing emails from vendors, shipping notifications without charges, and duplicates of receipts already swept.\n- **Checkpoint 1:** show the candidate table - Date / Vendor / Amount / Category(?) / Source email - plus a \"skipped as noise\" count. User corrects categories and removes false positives here.\n\n**Step 2 - Drive: update the expense sheet.**\n- Read FIRST: the sheet's existing rows for the same date range - dedupe on vendor + date + amount. Anything already present is marked \"already logged\" and dropped.\n- Write: append only the approved, deduped rows, matching the sheet's exact column order and date/currency formats.\n- **Checkpoint 2 (approval gate):** show exactly the rows to be appended and the running month total before/after. Apply only after `apply`, `apply all`, or `apply 1,3`. Report how many rows were written.\n- Failure behavior: if the write fails partway, report which rows landed; on the next run the dedupe pass makes re-running safe.\n\n**Step 3 - Calendar: tax deadlines.**\n- Read: existing calendar events for the user's stated deadlines (quarterly estimates, VAT/GST, annual filing).\n- Transform: for each deadline the user confirmed applies to them, plan two entries - a prep reminder ~2 weeks ahead (\"gather Q3 receipts - sheet link\") and the deadline itself. Diff against what already exists: propose ADD only for genuinely missing events.\n- **Checkpoint 3 (approval gate):** review table of proposed events (title, date, description). Create only after approval.\n- Note: deadline dates come from the user or their accountant; this skill does not assert official tax dates as fact - flag them as \"confirm with your accountant\" if the user was unsure.\n\n**Close the loop:** end with the run summary - receipts found, rows added, duplicates skipped, events created - so the user can audit the whole sweep in ten seconds.\n\n## Output format\n\n1. **Receipt candidates table** (step 1) - Date / Vendor / Amount / Currency / Category / Source email - plus skipped-as-noise count\n2. **Sheet append preview** (step 2) - rows to add, duplicates skipped, month total before → after; then \"N rows written\"\n3. **Calendar diff** (step 3) - proposed ADDs vs already-present deadlines, awaiting apply\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Harvest | Gmail | N receipts found, K noise skipped | done |\n| 2 Sheet | Drive | M rows appended, D duplicates skipped | done / awaiting apply |\n| 3 Deadlines | Calendar | E events created | done / none needed |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the sheet's header row and existing rows BEFORE appending - match its format and reuse its category vocabulary\n- Dedupe on vendor + date + amount against existing rows; weekly cadence means overlap is normal, so re-runs must be idempotent\n- Keep a source-email reference on every row so any amount can be verified at tax time\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't append rows (step 2) that weren't approved at checkpoint 1 - a wrong amount in the books is worse than a missing one\n- Don't guess amounts or currencies from truncated email previews; open the full message or mark the field `?`\n- Don't invent tax deadline dates or assert them as authoritative - the user/accountant confirms dates\n- Don't create calendar events that duplicate existing deadline reminders - diff first\n- Don't invent mail, sheet, or calendar contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every appended row traces to a specific source email\n- [ ] Dedupe pass ran against existing sheet rows before the append preview\n- [ ] Chain summary table shows all 3 steps with counts and real statuses\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@gmail, @google-drive, @google-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /finance-closed-loop`\n```\n\nAfter writing the file, confirm it exists. The user can then run /finance-closed-loop or describe the task from the skill description.", "skillContent": "---\nname: finance-closed-loop\ndescription: >\n  Receipt email → expense sheet → tax deadline calendar. Requires integrations: @gmail, @google-drive, @google-calendar. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"finance closed loop\", /finance-closed-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: weekly\n  risk: medium\n  connectors: gmail,google-drive,google-calendar\n  integrations: @gmail,@google-drive,@google-calendar\nallowed-tools: search_tool, use_tool\n---\n# /finance-closed-loop\n\nReceipt email → expense sheet → tax deadline calendar.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as a weekly bookkeeping sweep for a freelancer or small business: harvest receipt and invoice emails from Gmail, append clean rows to the expense sheet in Drive, and make sure tax/filing deadlines sit on the calendar with prep time in front of them. It keeps the books current in one pass - it does not give tax advice or decide deductibility.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Date range to sweep (default: the last 7 days, or since the newest row already in the sheet)\n- The expense sheet in Drive (name or link) and its column layout - read the header row rather than assuming\n- Expense categories the user files under (read existing rows for the vocabulary; don't invent new categories)\n- Which jurisdiction's tax deadlines matter (e.g., quarterly estimated tax, VAT/GST filing) - from the user, never guessed\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Numbers exact - amount, currency, date, vendor - no rounding, no inferred totals.\n- **Audience:** The user's future self at tax time; every row must be traceable back to its email.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** @gmail is the source and has no fallback (stop if missing, unless the user forwards/pastes receipts). @google-drive missing → output the expense rows as a markdown/CSV block for manual paste. @google-calendar missing → output the deadline list with recommended reminder dates as text.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n**Step 1 - Gmail: harvest receipts.**\n- Read: search the date range for receipts/invoices (queries like vendor names the sheet already contains, plus terms such as \"receipt\", \"invoice\", \"payment confirmation\", \"order\"). Open each hit and extract: date, vendor, amount, currency, payment method, order/invoice number, and the email's subject + date for traceability.\n- Transform: normalize into candidate rows; suggest a category using ONLY the sheet's existing category vocabulary; mark uncertain fields with `?` rather than guessing amounts.\n- Exclude noise explicitly: marketing emails from vendors, shipping notifications without charges, and duplicates of receipts already swept.\n- **Checkpoint 1:** show the candidate table - Date / Vendor / Amount / Category(?) / Source email - plus a \"skipped as noise\" count. User corrects categories and removes false positives here.\n\n**Step 2 - Drive: update the expense sheet.**\n- Read FIRST: the sheet's existing rows for the same date range - dedupe on vendor + date + amount. Anything already present is marked \"already logged\" and dropped.\n- Write: append only the approved, deduped rows, matching the sheet's exact column order and date/currency formats.\n- **Checkpoint 2 (approval gate):** show exactly the rows to be appended and the running month total before/after. Apply only after `apply`, `apply all`, or `apply 1,3`. Report how many rows were written.\n- Failure behavior: if the write fails partway, report which rows landed; on the next run the dedupe pass makes re-running safe.\n\n**Step 3 - Calendar: tax deadlines.**\n- Read: existing calendar events for the user's stated deadlines (quarterly estimates, VAT/GST, annual filing).\n- Transform: for each deadline the user confirmed applies to them, plan two entries - a prep reminder ~2 weeks ahead (\"gather Q3 receipts - sheet link\") and the deadline itself. Diff against what already exists: propose ADD only for genuinely missing events.\n- **Checkpoint 3 (approval gate):** review table of proposed events (title, date, description). Create only after approval.\n- Note: deadline dates come from the user or their accountant; this skill does not assert official tax dates as fact - flag them as \"confirm with your accountant\" if the user was unsure.\n\n**Close the loop:** end with the run summary - receipts found, rows added, duplicates skipped, events created - so the user can audit the whole sweep in ten seconds.\n\n## Output format\n\n1. **Receipt candidates table** (step 1) - Date / Vendor / Amount / Currency / Category / Source email - plus skipped-as-noise count\n2. **Sheet append preview** (step 2) - rows to add, duplicates skipped, month total before → after; then \"N rows written\"\n3. **Calendar diff** (step 3) - proposed ADDs vs already-present deadlines, awaiting apply\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Harvest | Gmail | N receipts found, K noise skipped | done |\n| 2 Sheet | Drive | M rows appended, D duplicates skipped | done / awaiting apply |\n| 3 Deadlines | Calendar | E events created | done / none needed |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the sheet's header row and existing rows BEFORE appending - match its format and reuse its category vocabulary\n- Dedupe on vendor + date + amount against existing rows; weekly cadence means overlap is normal, so re-runs must be idempotent\n- Keep a source-email reference on every row so any amount can be verified at tax time\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't append rows (step 2) that weren't approved at checkpoint 1 - a wrong amount in the books is worse than a missing one\n- Don't guess amounts or currencies from truncated email previews; open the full message or mark the field `?`\n- Don't invent tax deadline dates or assert them as authoritative - the user/accountant confirms dates\n- Don't create calendar events that duplicate existing deadline reminders - diff first\n- Don't invent mail, sheet, or calendar contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every appended row traces to a specific source email\n- [ ] Dedupe pass ran against existing sheet rows before the append preview\n- [ ] Chain summary table shows all 3 steps with counts and real statuses\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@gmail, @google-drive, @google-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /finance-closed-loop`\n"}, {"id": "hire-loop", "aiInstallPrompt": "Install the skill 'hire-loop' by writing the file below.\n\nUniversal path: ./skills/hire-loop/SKILL.md\nGrok path: ~/.grok/skills/hire-loop/SKILL.md\nCursor path: ~/.cursor/skills/hire-loop/SKILL.md\nClaude path: ~/.claude/skills/hire-loop/SKILL.md\nVS Code agent path: .github/skills/hire-loop/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: hire-loop\ndescription: >\n  JD → interview → offer pack across HR tools. Requires integrations: @notion, @outlook, @sharepoint, @teams. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"hire loop\", /hire-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,microsoft-mail,microsoft-sharepoint,microsoft-teams\n  integrations: @notion,@outlook,@sharepoint,@teams\nallowed-tools: search_tool, use_tool\n---\n# /hire-loop\n\nJD → interview → offer pack across HR tools.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this to run one hire end-to-end on a Microsoft + Notion stack: draft/publish the job description in Notion, coordinate interview scheduling by Outlook email, assemble the onboarding pack in SharePoint once the offer is accepted, and welcome the new hire in Teams on day one. The skill tracks which stage the hire is at and resumes there - run it repeatedly for the same role as the candidate progresses.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which stage this run is at: (1) JD, (2) interviews, (3) offer accepted → onboarding pack, (4) day-one welcome\n- Role details for stage 1: title, team, level, location/remote, salary band (or \"omit\"), must-have vs nice-to-have skills\n- For stage 2: candidate name + email, interviewer names, interview format and length\n- For stages 3-4: start date, manager, SharePoint site for HR docs, Teams channel for the welcome\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Candidate-facing text warm and unambiguous; internal artifacts factual.\n- **Audience:** Hiring manager coordinating without a dedicated recruiter.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@outlook** - Microsoft Outlook Mail\n  - MCP server key: `microsoft-mail`\n  - Example tools: `microsoft-mail__list_messages`, `microsoft-mail__create_draft`, `microsoft-mail__search_mail`\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n- **@teams** - Microsoft Teams\n  - MCP server key: `microsoft-teams`\n  - Example tools: `microsoft-teams__list_channels`, `microsoft-teams__post_message`, `microsoft-teams__list_messages`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** only the current stage's connector is truly required. @notion missing → deliver the JD as markdown; @outlook missing → deliver scheduling emails as copy-paste text; @sharepoint missing → deliver the onboarding pack contents inline as documents-to-create; @teams missing → deliver the welcome message as text with the intended channel named.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority - hiring decisions and compensation are the user's; this skill drafts and coordinates.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Emails to candidates are drafts by default; sending requires explicit approval of the exact text.\n\n## Workflow\n\nFirst, check Notion for an existing hiring page for this role. If found, report its stage and resume there - never restart a hire from stage 1 because the user re-ran the command.\n\n**Stage 1 - Notion: the JD.**\n- Read: any existing role notes/JD drafts in the hiring database.\n- Transform: draft the JD - role summary, responsibilities (5-7, outcome-phrased), must-have vs nice-to-have requirements kept separate, salary band if provided, process overview (stages + timeline). Flag any requirement that reads as potentially discriminatory (age proxies, unnecessary degree demands) as a recommendation to revise.\n- Write: create/update the role's hiring page with the JD plus a **stage tracker** (JD → Interviewing → Offer → Onboarding) this skill updates on every run.\n- **Checkpoint 1:** show the full JD before writing; apply on approval; return the page link.\n\n**Stage 2 - Outlook: interview scheduling.**\n- Read: recent mail with the candidate (search by candidate email) to see what's already been agreed - never propose times that contradict an existing thread.\n- Transform: draft the scheduling email - 2-3 concrete slots (from the user; this stack variant has no calendar connector, so slots come from the hiring manager), format, duration, participants, and what the candidate should prepare.\n- Write: create the email as an Outlook **draft**; also draft the interviewer-brief email (candidate context, focus areas per interviewer).\n- **Checkpoint 2:** show both drafts. On approval, save as drafts (send only if explicitly told). Update the Notion stage tracker to \"Interviewing\".\n\n**Stage 3 - SharePoint: onboarding pack (after offer accepted).**\n- Read: the SharePoint site's existing HR templates (search for onboarding/checklist docs) so the pack matches house style.\n- Transform: assemble the pack - welcome letter (name, role, start date, manager), 30/60/90 expectations drawn from the JD's responsibilities, first-week schedule, equipment/access checklist, key contacts.\n- Write: upload to the agreed SharePoint folder under `Onboarding/<Candidate name>/`.\n- **Checkpoint 3:** list each document and its one-line contents before uploading; upload on `apply`; return links. Update stage tracker to \"Onboarding\".\n\n**Stage 4 - Teams: day-one welcome.**\n- Transform: welcome post - name, role, one-line background (only what the candidate approved sharing), manager, and how the team can help in week one. No compensation or interview details ever appear here.\n- Write: post to the agreed channel.\n- **Checkpoint 4:** show exact message + channel; post only on `apply`. Mark the hire complete in the Notion tracker.\n\n## Output format\n\nPer stage (only the current stage's artifacts):\n\n1. **JD draft + hiring page link** - with flagged-requirement notes\n2. **Scheduling drafts** - candidate email + interviewer brief, awaiting approval\n3. **Onboarding pack manifest** - document list with SharePoint links\n4. **Welcome post preview** - channel + text, awaiting `apply`\n\nEvery run ends with the **hire chain summary table**:\n\n| Stage | App | Produced | Status |\n|---|---|---|---|\n| 1 JD | Notion | Hiring page + JD | done |\n| 2 Interviews | Outlook | Drafts saved | in progress |\n| 3 Offer pack | SharePoint | Pack uploaded | pending / done |\n| 4 Welcome | Teams | Post published | pending |\n\nStatuses: done · in progress · pending · fallback (manual) - the table shows the whole hire, not just this run.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Check the Notion stage tracker first and resume - the same command runs at every stage of one hire\n- Read the existing candidate email thread before drafting scheduling mail; contradicting agreed times looks careless to candidates\n- Keep candidate-private data (salary, interview feedback) out of Teams and out of the onboarding pack\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't build the onboarding pack (stage 3) before the user confirms the offer is accepted - a premature pack with a name on it leaks the decision\n- Don't post the Teams welcome before the start date is confirmed and the pack exists\n- Don't send candidate emails without explicit approval - drafts by default\n- Don't restart at stage 1 on re-run; check the tracker and resume\n- Don't invent mail threads, SharePoint templates, or channel names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Notion stage tracker checked at start and updated at end of the run\n- [ ] Candidate-facing text approved verbatim before any draft/send\n- [ ] Hire chain summary table reflects all 4 stages with real statuses\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@notion, @outlook, @sharepoint, @teams) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /hire-loop or describe the task from the skill description.", "skillContent": "---\nname: hire-loop\ndescription: >\n  JD → interview → offer pack across HR tools. Requires integrations: @notion, @outlook, @sharepoint, @teams. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"hire loop\", /hire-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,microsoft-mail,microsoft-sharepoint,microsoft-teams\n  integrations: @notion,@outlook,@sharepoint,@teams\nallowed-tools: search_tool, use_tool\n---\n# /hire-loop\n\nJD → interview → offer pack across HR tools.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this to run one hire end-to-end on a Microsoft + Notion stack: draft/publish the job description in Notion, coordinate interview scheduling by Outlook email, assemble the onboarding pack in SharePoint once the offer is accepted, and welcome the new hire in Teams on day one. The skill tracks which stage the hire is at and resumes there - run it repeatedly for the same role as the candidate progresses.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which stage this run is at: (1) JD, (2) interviews, (3) offer accepted → onboarding pack, (4) day-one welcome\n- Role details for stage 1: title, team, level, location/remote, salary band (or \"omit\"), must-have vs nice-to-have skills\n- For stage 2: candidate name + email, interviewer names, interview format and length\n- For stages 3-4: start date, manager, SharePoint site for HR docs, Teams channel for the welcome\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Candidate-facing text warm and unambiguous; internal artifacts factual.\n- **Audience:** Hiring manager coordinating without a dedicated recruiter.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@outlook** - Microsoft Outlook Mail\n  - MCP server key: `microsoft-mail`\n  - Example tools: `microsoft-mail__list_messages`, `microsoft-mail__create_draft`, `microsoft-mail__search_mail`\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n- **@teams** - Microsoft Teams\n  - MCP server key: `microsoft-teams`\n  - Example tools: `microsoft-teams__list_channels`, `microsoft-teams__post_message`, `microsoft-teams__list_messages`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** only the current stage's connector is truly required. @notion missing → deliver the JD as markdown; @outlook missing → deliver scheduling emails as copy-paste text; @sharepoint missing → deliver the onboarding pack contents inline as documents-to-create; @teams missing → deliver the welcome message as text with the intended channel named.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority - hiring decisions and compensation are the user's; this skill drafts and coordinates.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Emails to candidates are drafts by default; sending requires explicit approval of the exact text.\n\n## Workflow\n\nFirst, check Notion for an existing hiring page for this role. If found, report its stage and resume there - never restart a hire from stage 1 because the user re-ran the command.\n\n**Stage 1 - Notion: the JD.**\n- Read: any existing role notes/JD drafts in the hiring database.\n- Transform: draft the JD - role summary, responsibilities (5-7, outcome-phrased), must-have vs nice-to-have requirements kept separate, salary band if provided, process overview (stages + timeline). Flag any requirement that reads as potentially discriminatory (age proxies, unnecessary degree demands) as a recommendation to revise.\n- Write: create/update the role's hiring page with the JD plus a **stage tracker** (JD → Interviewing → Offer → Onboarding) this skill updates on every run.\n- **Checkpoint 1:** show the full JD before writing; apply on approval; return the page link.\n\n**Stage 2 - Outlook: interview scheduling.**\n- Read: recent mail with the candidate (search by candidate email) to see what's already been agreed - never propose times that contradict an existing thread.\n- Transform: draft the scheduling email - 2-3 concrete slots (from the user; this stack variant has no calendar connector, so slots come from the hiring manager), format, duration, participants, and what the candidate should prepare.\n- Write: create the email as an Outlook **draft**; also draft the interviewer-brief email (candidate context, focus areas per interviewer).\n- **Checkpoint 2:** show both drafts. On approval, save as drafts (send only if explicitly told). Update the Notion stage tracker to \"Interviewing\".\n\n**Stage 3 - SharePoint: onboarding pack (after offer accepted).**\n- Read: the SharePoint site's existing HR templates (search for onboarding/checklist docs) so the pack matches house style.\n- Transform: assemble the pack - welcome letter (name, role, start date, manager), 30/60/90 expectations drawn from the JD's responsibilities, first-week schedule, equipment/access checklist, key contacts.\n- Write: upload to the agreed SharePoint folder under `Onboarding/<Candidate name>/`.\n- **Checkpoint 3:** list each document and its one-line contents before uploading; upload on `apply`; return links. Update stage tracker to \"Onboarding\".\n\n**Stage 4 - Teams: day-one welcome.**\n- Transform: welcome post - name, role, one-line background (only what the candidate approved sharing), manager, and how the team can help in week one. No compensation or interview details ever appear here.\n- Write: post to the agreed channel.\n- **Checkpoint 4:** show exact message + channel; post only on `apply`. Mark the hire complete in the Notion tracker.\n\n## Output format\n\nPer stage (only the current stage's artifacts):\n\n1. **JD draft + hiring page link** - with flagged-requirement notes\n2. **Scheduling drafts** - candidate email + interviewer brief, awaiting approval\n3. **Onboarding pack manifest** - document list with SharePoint links\n4. **Welcome post preview** - channel + text, awaiting `apply`\n\nEvery run ends with the **hire chain summary table**:\n\n| Stage | App | Produced | Status |\n|---|---|---|---|\n| 1 JD | Notion | Hiring page + JD | done |\n| 2 Interviews | Outlook | Drafts saved | in progress |\n| 3 Offer pack | SharePoint | Pack uploaded | pending / done |\n| 4 Welcome | Teams | Post published | pending |\n\nStatuses: done · in progress · pending · fallback (manual) - the table shows the whole hire, not just this run.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Check the Notion stage tracker first and resume - the same command runs at every stage of one hire\n- Read the existing candidate email thread before drafting scheduling mail; contradicting agreed times looks careless to candidates\n- Keep candidate-private data (salary, interview feedback) out of Teams and out of the onboarding pack\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't build the onboarding pack (stage 3) before the user confirms the offer is accepted - a premature pack with a name on it leaks the decision\n- Don't post the Teams welcome before the start date is confirmed and the pack exists\n- Don't send candidate emails without explicit approval - drafts by default\n- Don't restart at stage 1 on re-run; check the tracker and resume\n- Don't invent mail threads, SharePoint templates, or channel names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Notion stage tracker checked at start and updated at end of the run\n- [ ] Candidate-facing text approved verbatim before any draft/send\n- [ ] Hire chain summary table reflects all 4 stages with real statuses\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@notion, @outlook, @sharepoint, @teams) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "inbox-to-calendar", "aiInstallPrompt": "Install the skill 'inbox-to-calendar' by writing the file below.\n\nUniversal path: ./skills/inbox-to-calendar/SKILL.md\nGrok path: ~/.grok/skills/inbox-to-calendar/SKILL.md\nCursor path: ~/.cursor/skills/inbox-to-calendar/SKILL.md\nClaude path: ~/.claude/skills/inbox-to-calendar/SKILL.md\nVS Code agent path: .github/skills/inbox-to-calendar/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: inbox-to-calendar\ndescription: >\n  Read mail, extract dates, propose calendar events (Gmail or Outlook). Requires integrations: @gmail, @google-calendar, @outlook, @outlook-calendar. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"inbox to calendar\", \"sync mail to calendar\", /inbox-to-calendar.\nmetadata:\n  category: combo\n  type: combo\n  cadence: weekly\n  risk: medium\n  connectors: gmail,google-calendar,microsoft-mail,microsoft-calendar\n  integrations: @gmail,@google-calendar,@outlook,@outlook-calendar\nallowed-tools: search_tool, use_tool\n---\n# /inbox-to-calendar\n\nRead mail, extract dates, propose calendar events (Gmail or Outlook).\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as a weekly sweep for commitments hiding in email: appointment confirmations, RSVPs, delivery windows, school/medical dates, deadlines stated in prose (\"get this back to me by the 15th\"), flight/hotel bookings. It reads the last week of mail, extracts anything time-bound, diffs against what's already on the calendar, and proposes ADD/UPDATE/SKIP - nothing is written without approval. Works with whichever stack is connected: Gmail + Google Calendar OR Outlook Mail + Outlook Calendar.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Date range to sweep (default: last 7 days of mail; look-ahead horizon for events: next 90 days)\n- Which mail/calendar pair to use if BOTH stacks are connected (Google, Microsoft, or both merged)\n- Folders/labels to include or skip (e.g., skip \"Newsletters\"; include \"Family\")\n- Default event settings: calendar to write to, reminder lead time, timezone for ambiguous times\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Dates and times exact and timezone-explicit; ambiguity is flagged, never resolved silently.\n- **Audience:** Someone triaging their week in two minutes.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@outlook** - Microsoft Outlook Mail\n  - MCP server key: `microsoft-mail`\n  - Example tools: `microsoft-mail__list_messages`, `microsoft-mail__create_draft`, `microsoft-mail__search_mail`\n\n- **@outlook-calendar** - Microsoft Outlook Calendar\n  - MCP server key: `microsoft-calendar`\n  - Example tools: `microsoft-calendar__list_events`, `microsoft-calendar__create_event`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Stack note for this combo:** only ONE mail + calendar pair is required, not all four connectors. If Gmail + Google Calendar work, run on Google; if Outlook Mail + Outlook Calendar work, run on Microsoft. If mail works but the matching calendar is missing, deliver the extracted date list as text (step 2 output) so the user can add events by hand.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n**Step 1 - Detect the stack.**\n- Probe which mail + calendar pair is live in this session (discover the exact tool names). Announce which stack the run will use. If both are available, ask once which to sweep (or both).\n\n**Step 2 - Mail: extract date items.**\n- Read: messages from the sweep window in the included folders/labels. For each hit, extract: what the commitment is, date, start/end time, location or meeting link, other people involved, and the source message (sender + subject + date).\n- Transform each into a **candidate event**: title (verb + noun - \"Dentist appointment\", not \"Re: Re: confirmation\"), start/end, location, and a description that cites the source email.\n- Flag ambiguity instead of guessing: \"next Friday\" with no date → compute from the email's sent date and mark *(interpreted)*; missing year → assume the next future occurrence and mark it; no timezone on a call with an out-of-area contact → flag it.\n- **Checkpoint 1:** show the candidate list with source citations and interpretation flags. The user strikes false positives here (e.g., a date in a newsletter that isn't their commitment).\n\n**Step 3 - Calendar: diff.**\n- Read: existing events across the look-ahead horizon.\n- Classify each approved candidate: **ADD** (nothing similar exists) / **UPDATE** (same event exists but time/location changed in a newer email - show old vs new) / **SKIP** (already on the calendar, matched by title similarity + date proximity).\n- **Checkpoint 2 (approval gate):** review table - # / Action / Title / Date & time (tz) / Source email / Note. Wait for `apply`, `apply all`, or `apply 1,3`.\n\n**Step 4 - Calendar: apply.**\n- Write only the approved rows. Report each result with the created/updated event's date and link if available.\n- Failure behavior: if a create fails, report which rows succeeded; the diff step makes re-running safe (already-created events classify as SKIP).\n\n## Output format\n\n1. **Stack announcement** (step 1) - which mail/calendar pair this run uses\n2. **Candidate events list** (step 2) - Title / When (tz) / Where / Source email / interpretation flags\n3. **Diff review table** (step 3) - # / ADD-UPDATE-SKIP / details / old→new for updates - awaiting apply\n4. **Apply report** (step 4) - per-row outcome\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Detect | - | Stack: Google / Microsoft | done |\n| 2 Extract | Gmail / Outlook | N candidates from M messages | approved |\n| 3 Diff | Calendar | A add, U update, S skip | approved |\n| 4 Apply | Calendar | A' created, U' updated | done / partial |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Cite the source email on every candidate so the user can verify a suspicious date in one click\n- Prefer the NEWEST email when the same event was rescheduled across a thread - that's an UPDATE, not a second ADD\n- Make every time timezone-explicit; \"3pm\" from a sender in another timezone is the classic silent error\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't write any event that wasn't in the approved review table - no \"while I was at it\" additions\n- Don't create duplicates on re-run: the diff against existing events is mandatory, not optional (weekly cadence guarantees overlap)\n- Don't resolve ambiguous dates silently - an *(interpreted)* flag lets the user catch a wrong guess before it books their afternoon\n- Don't treat every date in an email as a commitment; newsletters, promos, and other people's deadlines get struck at checkpoint 1\n- Don't invent mail or calendar contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every candidate cites its source email; every interpretation is flagged\n- [ ] Diff ran against existing calendar events before proposing ADDs\n- [ ] Chain summary table shows all 4 steps with counts\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@gmail, @google-calendar, @outlook, @outlook-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /inbox-to-calendar`\n```\n\nAfter writing the file, confirm it exists. The user can then run /inbox-to-calendar or describe the task from the skill description.", "skillContent": "---\nname: inbox-to-calendar\ndescription: >\n  Read mail, extract dates, propose calendar events (Gmail or Outlook). Requires integrations: @gmail, @google-calendar, @outlook, @outlook-calendar. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"inbox to calendar\", \"sync mail to calendar\", /inbox-to-calendar.\nmetadata:\n  category: combo\n  type: combo\n  cadence: weekly\n  risk: medium\n  connectors: gmail,google-calendar,microsoft-mail,microsoft-calendar\n  integrations: @gmail,@google-calendar,@outlook,@outlook-calendar\nallowed-tools: search_tool, use_tool\n---\n# /inbox-to-calendar\n\nRead mail, extract dates, propose calendar events (Gmail or Outlook).\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as a weekly sweep for commitments hiding in email: appointment confirmations, RSVPs, delivery windows, school/medical dates, deadlines stated in prose (\"get this back to me by the 15th\"), flight/hotel bookings. It reads the last week of mail, extracts anything time-bound, diffs against what's already on the calendar, and proposes ADD/UPDATE/SKIP - nothing is written without approval. Works with whichever stack is connected: Gmail + Google Calendar OR Outlook Mail + Outlook Calendar.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Date range to sweep (default: last 7 days of mail; look-ahead horizon for events: next 90 days)\n- Which mail/calendar pair to use if BOTH stacks are connected (Google, Microsoft, or both merged)\n- Folders/labels to include or skip (e.g., skip \"Newsletters\"; include \"Family\")\n- Default event settings: calendar to write to, reminder lead time, timezone for ambiguous times\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Dates and times exact and timezone-explicit; ambiguity is flagged, never resolved silently.\n- **Audience:** Someone triaging their week in two minutes.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@outlook** - Microsoft Outlook Mail\n  - MCP server key: `microsoft-mail`\n  - Example tools: `microsoft-mail__list_messages`, `microsoft-mail__create_draft`, `microsoft-mail__search_mail`\n\n- **@outlook-calendar** - Microsoft Outlook Calendar\n  - MCP server key: `microsoft-calendar`\n  - Example tools: `microsoft-calendar__list_events`, `microsoft-calendar__create_event`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Stack note for this combo:** only ONE mail + calendar pair is required, not all four connectors. If Gmail + Google Calendar work, run on Google; if Outlook Mail + Outlook Calendar work, run on Microsoft. If mail works but the matching calendar is missing, deliver the extracted date list as text (step 2 output) so the user can add events by hand.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n**Step 1 - Detect the stack.**\n- Probe which mail + calendar pair is live in this session (discover the exact tool names). Announce which stack the run will use. If both are available, ask once which to sweep (or both).\n\n**Step 2 - Mail: extract date items.**\n- Read: messages from the sweep window in the included folders/labels. For each hit, extract: what the commitment is, date, start/end time, location or meeting link, other people involved, and the source message (sender + subject + date).\n- Transform each into a **candidate event**: title (verb + noun - \"Dentist appointment\", not \"Re: Re: confirmation\"), start/end, location, and a description that cites the source email.\n- Flag ambiguity instead of guessing: \"next Friday\" with no date → compute from the email's sent date and mark *(interpreted)*; missing year → assume the next future occurrence and mark it; no timezone on a call with an out-of-area contact → flag it.\n- **Checkpoint 1:** show the candidate list with source citations and interpretation flags. The user strikes false positives here (e.g., a date in a newsletter that isn't their commitment).\n\n**Step 3 - Calendar: diff.**\n- Read: existing events across the look-ahead horizon.\n- Classify each approved candidate: **ADD** (nothing similar exists) / **UPDATE** (same event exists but time/location changed in a newer email - show old vs new) / **SKIP** (already on the calendar, matched by title similarity + date proximity).\n- **Checkpoint 2 (approval gate):** review table - # / Action / Title / Date & time (tz) / Source email / Note. Wait for `apply`, `apply all`, or `apply 1,3`.\n\n**Step 4 - Calendar: apply.**\n- Write only the approved rows. Report each result with the created/updated event's date and link if available.\n- Failure behavior: if a create fails, report which rows succeeded; the diff step makes re-running safe (already-created events classify as SKIP).\n\n## Output format\n\n1. **Stack announcement** (step 1) - which mail/calendar pair this run uses\n2. **Candidate events list** (step 2) - Title / When (tz) / Where / Source email / interpretation flags\n3. **Diff review table** (step 3) - # / ADD-UPDATE-SKIP / details / old→new for updates - awaiting apply\n4. **Apply report** (step 4) - per-row outcome\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Detect | - | Stack: Google / Microsoft | done |\n| 2 Extract | Gmail / Outlook | N candidates from M messages | approved |\n| 3 Diff | Calendar | A add, U update, S skip | approved |\n| 4 Apply | Calendar | A' created, U' updated | done / partial |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Cite the source email on every candidate so the user can verify a suspicious date in one click\n- Prefer the NEWEST email when the same event was rescheduled across a thread - that's an UPDATE, not a second ADD\n- Make every time timezone-explicit; \"3pm\" from a sender in another timezone is the classic silent error\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't write any event that wasn't in the approved review table - no \"while I was at it\" additions\n- Don't create duplicates on re-run: the diff against existing events is mandatory, not optional (weekly cadence guarantees overlap)\n- Don't resolve ambiguous dates silently - an *(interpreted)* flag lets the user catch a wrong guess before it books their afternoon\n- Don't treat every date in an email as a commitment; newsletters, promos, and other people's deadlines get struck at checkpoint 1\n- Don't invent mail or calendar contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every candidate cites its source email; every interpretation is flagged\n- [ ] Diff ran against existing calendar events before proposing ADDs\n- [ ] Chain summary table shows all 4 steps with counts\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@gmail, @google-calendar, @outlook, @outlook-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /inbox-to-calendar`\n"}, {"id": "incident-war-room", "aiInstallPrompt": "Install the skill 'incident-war-room' by writing the file below.\n\nUniversal path: ./skills/incident-war-room/SKILL.md\nGrok path: ~/.grok/skills/incident-war-room/SKILL.md\nCursor path: ~/.cursor/skills/incident-war-room/SKILL.md\nClaude path: ~/.claude/skills/incident-war-room/SKILL.md\nVS Code agent path: .github/skills/incident-war-room/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: incident-war-room\ndescription: >\n  Sentry + Slack + Linear + Calendar incident bundle. Requires integrations: @sentry, @slack, @linear, @google-calendar. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"incident war room\", /incident-war-room.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: high\n  connectors: sentry,slack,linear,google-calendar\n  integrations: @sentry,@slack,@linear,@google-calendar\nallowed-tools: search_tool, use_tool\n---\n# /incident-war-room\n\nSentry + Slack + Linear + Calendar incident bundle.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when production is on fire and coordination overhead is stealing minutes from the fix: triage the Sentry error spike into a severity picture, stand up (or reuse) a Slack incident channel with a status post, create the Linear tracking issues, and get a war-room/postmortem slot on the calendar. Speed matters here, so the skill batches its proposals - but writes still get approved, because a wrong severity call or a duplicate incident channel makes a bad hour worse.\n\n## Inputs to gather\n\nCheck context first; only ask if missing (during an incident, take defaults over questions):\n\n- What's known so far: symptom, when it started, affected service/customers (even one sentence)\n- Sentry project(s) to triage and the time window (default: last 60 minutes vs the prior baseline)\n- Incident channel convention (e.g., `#inc-YYYYMMDD-short-name`) and who is Incident Commander\n- Linear team for incident issues; severity scale the org uses (SEV1-3 or P0-P2)\n- Calendar attendees for the war room; whether to also book a postmortem slot now\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration, but terse - status-page voice. Facts with timestamps; hypotheses labeled as hypotheses. No blame, ever, in any artifact.\n- **Audience:** Responders under pressure; every artifact must be scannable in 10 seconds.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sentry** - Sentry\n  - MCP server key: `sentry`\n  - Example tools: `sentry__list_issues`, `sentry__get_issue_events`\n\n- **@slack** - Slack\n  - MCP server key: `slack`\n  - Example tools: `slack__list_channels`, `slack__get_channel_history`, `slack__post_message`\n\n- **@linear** - Linear\n  - MCP server key: `linear`\n  - Example tools: `linear__list_issues`, `linear__create_issue`, `linear__update_issue`\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo (an incident can't wait for OAuth):** @sentry missing → build the triage from what the user pastes (error text, dashboards) and mark severity \"unconfirmed - no error data\". @slack missing → output the status post as text for the user to paste. @linear missing → output the issues as copy-paste blocks. @google-calendar missing → output the war-room invite details as text. Skip and mark the step - never stall the whole response.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\nTo keep approval overhead low during an incident: run step 1 (read-only) immediately, then present steps 2-4 as ONE combined review table so the user can `apply all` in a single confirmation.\n\n**Step 1 - Sentry: triage (read-only, run first, no approval needed).**\n- Read: issues in the window, sorted by event count and users affected; for the top issues pull first-seen time, release/deploy tag, and a representative stack trace line.\n- Transform into a **triage snapshot**: top N issues (title, count, users affected, first seen, suspect release), whether this is a spike vs baseline, and a severity recommendation with one-line justification. Correlation to a deploy is a hypothesis - label it so.\n- **Checkpoint 1:** show the snapshot; user confirms/overrides severity and names the incident. This decision drives everything downstream.\n\n**Step 2 - Slack: incident channel + status post (proposed).**\n- Read FIRST: search existing channels for one matching this incident's convention/date - if the incident already has a channel, propose posting there instead of creating a duplicate.\n- Propose: channel name per convention, plus the pinned status post: severity, what's broken (user-visible symptom), impact, IC name, started-at (UTC + local), \"next update by <time>\", and links (Sentry issue, Linear issues once created).\n\n**Step 3 - Linear: tracking issues (proposed).**\n- Read FIRST: search for existing open incident issues referencing these Sentry issues - update, don't duplicate.\n- Propose: one parent incident issue (severity label, IC assigned, Sentry links) + child issues per confirmed distinct error (not one per Sentry duplicate). Each child: symptom, Sentry link, suspect release, priority mirroring severity.\n\n**Step 4 - Calendar: war room (proposed).**\n- Propose: a war-room event starting ASAP (video link, responders invited, agenda: status → hypotheses → owners) and optionally a 30-min postmortem placeholder 2-3 business days out, description linking the Slack channel and parent Linear issue.\n\n**Combined approval gate:** one review table covering steps 2-4 (item, app, exact content/name). On `apply all` (or selective `apply 1,3`), execute in order - channel first (its link feeds the Linear issues and calendar description), then issues, then event. Report each write's result; if one fails, continue the rest and mark the failure in the summary.\n\n**Stand-down note:** when the user says the incident is resolved, offer the closing sequence - final Slack status post, close/label Linear issues, confirm the postmortem slot.\n\n## Output format\n\n1. **Triage snapshot** (step 1) - table: Sentry issue / events / users / first seen / suspect release - plus severity recommendation\n2. **Combined proposal table** (steps 2-4) - # / App / Artifact / Exact content - awaiting apply\n3. **Execution report** - per-write outcome with links (channel, issue IDs, event)\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Triage | Sentry | Snapshot, severity SEV-n | done |\n| 2 Channel | Slack | #inc-... + status post | done / fallback |\n| 3 Issues | Linear | 1 parent + N children | done / fallback |\n| 4 War room | Calendar | Event + postmortem slot | done / skipped |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Search for an existing incident channel and existing Linear incident issues BEFORE proposing new ones - duplicate war rooms split the responders\n- Batch steps 2-4 into one approval so the user confirms once, not four times, mid-incident\n- Timestamp everything in UTC plus the team's local zone; include \"next update by\" in every status post\n- Ask at most ONE clarifying question if critical info is missing - during an incident, prefer stated defaults over questions\n\n## Don't\n\n- Don't create Linear issues or calendar events before severity is confirmed at checkpoint 1 - SEV1 artifacts for a SEV3 blip erode trust in the process\n- Don't state a deploy caused the errors - correlation from timestamps is a labeled hypothesis until a human confirms\n- Don't create a second incident channel because the name search was skipped\n- Don't page or @-mention people beyond those the user listed\n- Don't invent error counts, channel names, or issue IDs without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Severity confirmed by the user before any write was proposed as final\n- [ ] Duplicate checks ran (Slack channel, Linear issues) before the proposal table\n- [ ] Every artifact cross-links the others (channel ↔ issues ↔ event)\n- [ ] Chain summary table shows all 4 steps with real statuses, including fallbacks\n- [ ] Required integrations (@sentry, @slack, @linear, @google-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /incident-war-room or describe the task from the skill description.", "skillContent": "---\nname: incident-war-room\ndescription: >\n  Sentry + Slack + Linear + Calendar incident bundle. Requires integrations: @sentry, @slack, @linear, @google-calendar. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"incident war room\", /incident-war-room.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: high\n  connectors: sentry,slack,linear,google-calendar\n  integrations: @sentry,@slack,@linear,@google-calendar\nallowed-tools: search_tool, use_tool\n---\n# /incident-war-room\n\nSentry + Slack + Linear + Calendar incident bundle.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when production is on fire and coordination overhead is stealing minutes from the fix: triage the Sentry error spike into a severity picture, stand up (or reuse) a Slack incident channel with a status post, create the Linear tracking issues, and get a war-room/postmortem slot on the calendar. Speed matters here, so the skill batches its proposals - but writes still get approved, because a wrong severity call or a duplicate incident channel makes a bad hour worse.\n\n## Inputs to gather\n\nCheck context first; only ask if missing (during an incident, take defaults over questions):\n\n- What's known so far: symptom, when it started, affected service/customers (even one sentence)\n- Sentry project(s) to triage and the time window (default: last 60 minutes vs the prior baseline)\n- Incident channel convention (e.g., `#inc-YYYYMMDD-short-name`) and who is Incident Commander\n- Linear team for incident issues; severity scale the org uses (SEV1-3 or P0-P2)\n- Calendar attendees for the war room; whether to also book a postmortem slot now\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration, but terse - status-page voice. Facts with timestamps; hypotheses labeled as hypotheses. No blame, ever, in any artifact.\n- **Audience:** Responders under pressure; every artifact must be scannable in 10 seconds.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@sentry** - Sentry\n  - MCP server key: `sentry`\n  - Example tools: `sentry__list_issues`, `sentry__get_issue_events`\n\n- **@slack** - Slack\n  - MCP server key: `slack`\n  - Example tools: `slack__list_channels`, `slack__get_channel_history`, `slack__post_message`\n\n- **@linear** - Linear\n  - MCP server key: `linear`\n  - Example tools: `linear__list_issues`, `linear__create_issue`, `linear__update_issue`\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo (an incident can't wait for OAuth):** @sentry missing → build the triage from what the user pastes (error text, dashboards) and mark severity \"unconfirmed - no error data\". @slack missing → output the status post as text for the user to paste. @linear missing → output the issues as copy-paste blocks. @google-calendar missing → output the war-room invite details as text. Skip and mark the step - never stall the whole response.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\nTo keep approval overhead low during an incident: run step 1 (read-only) immediately, then present steps 2-4 as ONE combined review table so the user can `apply all` in a single confirmation.\n\n**Step 1 - Sentry: triage (read-only, run first, no approval needed).**\n- Read: issues in the window, sorted by event count and users affected; for the top issues pull first-seen time, release/deploy tag, and a representative stack trace line.\n- Transform into a **triage snapshot**: top N issues (title, count, users affected, first seen, suspect release), whether this is a spike vs baseline, and a severity recommendation with one-line justification. Correlation to a deploy is a hypothesis - label it so.\n- **Checkpoint 1:** show the snapshot; user confirms/overrides severity and names the incident. This decision drives everything downstream.\n\n**Step 2 - Slack: incident channel + status post (proposed).**\n- Read FIRST: search existing channels for one matching this incident's convention/date - if the incident already has a channel, propose posting there instead of creating a duplicate.\n- Propose: channel name per convention, plus the pinned status post: severity, what's broken (user-visible symptom), impact, IC name, started-at (UTC + local), \"next update by <time>\", and links (Sentry issue, Linear issues once created).\n\n**Step 3 - Linear: tracking issues (proposed).**\n- Read FIRST: search for existing open incident issues referencing these Sentry issues - update, don't duplicate.\n- Propose: one parent incident issue (severity label, IC assigned, Sentry links) + child issues per confirmed distinct error (not one per Sentry duplicate). Each child: symptom, Sentry link, suspect release, priority mirroring severity.\n\n**Step 4 - Calendar: war room (proposed).**\n- Propose: a war-room event starting ASAP (video link, responders invited, agenda: status → hypotheses → owners) and optionally a 30-min postmortem placeholder 2-3 business days out, description linking the Slack channel and parent Linear issue.\n\n**Combined approval gate:** one review table covering steps 2-4 (item, app, exact content/name). On `apply all` (or selective `apply 1,3`), execute in order - channel first (its link feeds the Linear issues and calendar description), then issues, then event. Report each write's result; if one fails, continue the rest and mark the failure in the summary.\n\n**Stand-down note:** when the user says the incident is resolved, offer the closing sequence - final Slack status post, close/label Linear issues, confirm the postmortem slot.\n\n## Output format\n\n1. **Triage snapshot** (step 1) - table: Sentry issue / events / users / first seen / suspect release - plus severity recommendation\n2. **Combined proposal table** (steps 2-4) - # / App / Artifact / Exact content - awaiting apply\n3. **Execution report** - per-write outcome with links (channel, issue IDs, event)\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Triage | Sentry | Snapshot, severity SEV-n | done |\n| 2 Channel | Slack | #inc-... + status post | done / fallback |\n| 3 Issues | Linear | 1 parent + N children | done / fallback |\n| 4 War room | Calendar | Event + postmortem slot | done / skipped |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Search for an existing incident channel and existing Linear incident issues BEFORE proposing new ones - duplicate war rooms split the responders\n- Batch steps 2-4 into one approval so the user confirms once, not four times, mid-incident\n- Timestamp everything in UTC plus the team's local zone; include \"next update by\" in every status post\n- Ask at most ONE clarifying question if critical info is missing - during an incident, prefer stated defaults over questions\n\n## Don't\n\n- Don't create Linear issues or calendar events before severity is confirmed at checkpoint 1 - SEV1 artifacts for a SEV3 blip erode trust in the process\n- Don't state a deploy caused the errors - correlation from timestamps is a labeled hypothesis until a human confirms\n- Don't create a second incident channel because the name search was skipped\n- Don't page or @-mention people beyond those the user listed\n- Don't invent error counts, channel names, or issue IDs without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Severity confirmed by the user before any write was proposed as final\n- [ ] Duplicate checks ran (Slack channel, Linear issues) before the proposal table\n- [ ] Every artifact cross-links the others (channel ↔ issues ↔ event)\n- [ ] Chain summary table shows all 4 steps with real statuses, including fallbacks\n- [ ] Required integrations (@sentry, @slack, @linear, @google-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "meeting-closed-loop", "aiInstallPrompt": "Install the skill 'meeting-closed-loop' by writing the file below.\n\nUniversal path: ./skills/meeting-closed-loop/SKILL.md\nGrok path: ~/.grok/skills/meeting-closed-loop/SKILL.md\nCursor path: ~/.cursor/skills/meeting-closed-loop/SKILL.md\nClaude path: ~/.claude/skills/meeting-closed-loop/SKILL.md\nVS Code agent path: .github/skills/meeting-closed-loop/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: meeting-closed-loop\ndescription: >\n  Before/during/after meeting: prep, notes, tasks, files. Requires integrations: @google-calendar, @gmail, @notion, @google-drive. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"meeting closed loop\", /meeting-closed-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: medium\n  connectors: google-calendar,gmail,notion,google-drive\n  integrations: @google-calendar,@gmail,@notion,@google-drive\nallowed-tools: search_tool, use_tool\n---\n# /meeting-closed-loop\n\nBefore/during/after meeting: prep, notes, tasks, files.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this around a specific meeting, in one of three modes the skill detects from timing and context:\n- **PRE** (before): build a prep brief from the calendar invite, related email threads, and relevant Drive files.\n- **DURING/RAW** (user has raw notes or a transcript): structure them into decisions, actions, and open questions.\n- **POST** (after): turn structured notes into a filed Notion page, a follow-up email draft, and clearly-owned action items.\nRun it before AND after the same meeting to close the loop - the POST run picks up the PRE brief so nothing discussed gets lost.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which meeting (title, attendee, or \"my 2pm\") - resolve against the calendar rather than trusting memory\n- Mode: PRE, DURING (paste notes/transcript), or POST - infer from timing, confirm if ambiguous\n- For POST: where notes live in Notion (meeting-notes database) and who receives the follow-up email\n- Any prior meetings in this series to pull context from\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Decisions verbatim; action items always have an owner and a date or an explicit \"TBD\".\n- **Audience:** PRE brief is for the user alone (candid); POST follow-up email is for all attendees (diplomatic).\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** @google-calendar missing → ask the user for meeting title/time/attendees and proceed. @gmail missing → skip thread context in PRE; in POST, deliver the follow-up email as copy-paste text. @google-drive missing → skip file context and note it in the brief. @notion missing → deliver the meeting page as markdown for manual paste.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n### PRE mode (before the meeting)\n\n**Step 1 - Calendar: anchor the meeting.** Read the event: exact title, time, attendees, agenda text in the description, attached docs. **Checkpoint:** confirm \"Prepping for *<title>* at <time> with <attendees> - right meeting?\"\n\n**Step 2 - Gmail: thread context.** Read recent threads with the external attendees (or on the meeting's topic): last thing each person said, open promises in either direction, unanswered questions.\n\n**Step 3 - Drive: file context.** Search files matching the meeting topic/attendee company; pull the 2-3 most recently modified with one-line relevance notes.\n\n**Step 4 - Assemble the prep brief** (no writes): Meeting facts / Who's who (one line per attendee) / Where things stand (from mail) / Relevant docs (links) / Open items from the last meeting in the series (from its Notion page, if one exists) / Suggested talking points (max 5, phrased as questions to raise). Deliver in chat - the PRE run is read-only.\n\n### DURING/RAW mode (notes or transcript pasted)\n\nStructure the raw text into: **Decisions** (verbatim where possible) / **Action items** (owner, task, due date - \"TBD\" if unstated, never invented) / **Open questions** / **Notable context**. **Checkpoint:** show the structured notes and ask the user to correct owners/dates before POST filing.\n\n### POST mode (after the meeting)\n\n**Step 1 - Notion: file the notes.** Check the meeting-notes database for an existing page for this meeting (re-run safety). Create/update a page: date, attendees, structured notes from DURING (or ask for raw notes now), link back to the PRE brief items so unresolved ones carry forward. **Checkpoint:** show page content before writing; write on `apply`; return the link.\n\n**Step 2 - Gmail: follow-up draft.** Draft the attendee follow-up: thanks + decisions + action items with owners/dates + open questions with proposed resolution paths. **Checkpoint:** show full text; on `apply` create a **draft** (send only if explicitly told).\n\n**Step 3 - Calendar: next steps.** If the meeting agreed on a follow-up session or dated commitments, propose calendar events (diff against existing ones first). Apply on approval.\n\n## Output format\n\n**PRE:** the **prep brief** - Meeting facts / Who's who / Where things stand / Docs / Carry-over items / Talking points.\n**DURING:** the **structured notes block** - Decisions / Actions (owner · task · due) / Open questions / Context.\n**POST:** per-step artifacts - Notion page preview → link; follow-up email draft; proposed events table.\n\nEvery run ends with the **chain summary table** (rows for the mode that ran):\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| Anchor | Calendar | Meeting confirmed | done |\n| Context | Gmail + Drive | Threads + files digested | done / partial |\n| Notes | Notion | Page created/updated (link) | done / fallback |\n| Follow-up | Gmail | Draft saved | awaiting apply |\n| Next steps | Calendar | N events proposed | awaiting apply |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Confirm which meeting at the start - prepping or filing against the wrong calendar event poisons every later step\n- In POST, check for an existing Notion page for this meeting before creating one; update it instead\n- Carry unresolved items from the PRE brief into the POST page so the loop actually closes\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't file notes to Notion before the user has corrected owners and due dates in the structured-notes checkpoint\n- Don't send the follow-up email - draft by default; sending needs explicit approval of the exact text\n- Don't invent due dates or owners for action items; \"TBD\" is honest, a guessed date is a false commitment\n- Don't create duplicate meeting pages or duplicate follow-up events on re-run - check first\n- Don't invent mail, calendar, file, or Notion contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Correct meeting confirmed before any context gathering\n- [ ] Every action item has owner + date or an explicit TBD\n- [ ] Chain summary table reflects the mode that actually ran, with fallbacks marked\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@google-calendar, @gmail, @notion, @google-drive) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /meeting-closed-loop or describe the task from the skill description.", "skillContent": "---\nname: meeting-closed-loop\ndescription: >\n  Before/during/after meeting: prep, notes, tasks, files. Requires integrations: @google-calendar, @gmail, @notion, @google-drive. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"meeting closed loop\", /meeting-closed-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: medium\n  connectors: google-calendar,gmail,notion,google-drive\n  integrations: @google-calendar,@gmail,@notion,@google-drive\nallowed-tools: search_tool, use_tool\n---\n# /meeting-closed-loop\n\nBefore/during/after meeting: prep, notes, tasks, files.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this around a specific meeting, in one of three modes the skill detects from timing and context:\n- **PRE** (before): build a prep brief from the calendar invite, related email threads, and relevant Drive files.\n- **DURING/RAW** (user has raw notes or a transcript): structure them into decisions, actions, and open questions.\n- **POST** (after): turn structured notes into a filed Notion page, a follow-up email draft, and clearly-owned action items.\nRun it before AND after the same meeting to close the loop - the POST run picks up the PRE brief so nothing discussed gets lost.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which meeting (title, attendee, or \"my 2pm\") - resolve against the calendar rather than trusting memory\n- Mode: PRE, DURING (paste notes/transcript), or POST - infer from timing, confirm if ambiguous\n- For POST: where notes live in Notion (meeting-notes database) and who receives the follow-up email\n- Any prior meetings in this series to pull context from\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Decisions verbatim; action items always have an owner and a date or an explicit \"TBD\".\n- **Audience:** PRE brief is for the user alone (candid); POST follow-up email is for all attendees (diplomatic).\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** @google-calendar missing → ask the user for meeting title/time/attendees and proceed. @gmail missing → skip thread context in PRE; in POST, deliver the follow-up email as copy-paste text. @google-drive missing → skip file context and note it in the brief. @notion missing → deliver the meeting page as markdown for manual paste.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n### PRE mode (before the meeting)\n\n**Step 1 - Calendar: anchor the meeting.** Read the event: exact title, time, attendees, agenda text in the description, attached docs. **Checkpoint:** confirm \"Prepping for *<title>* at <time> with <attendees> - right meeting?\"\n\n**Step 2 - Gmail: thread context.** Read recent threads with the external attendees (or on the meeting's topic): last thing each person said, open promises in either direction, unanswered questions.\n\n**Step 3 - Drive: file context.** Search files matching the meeting topic/attendee company; pull the 2-3 most recently modified with one-line relevance notes.\n\n**Step 4 - Assemble the prep brief** (no writes): Meeting facts / Who's who (one line per attendee) / Where things stand (from mail) / Relevant docs (links) / Open items from the last meeting in the series (from its Notion page, if one exists) / Suggested talking points (max 5, phrased as questions to raise). Deliver in chat - the PRE run is read-only.\n\n### DURING/RAW mode (notes or transcript pasted)\n\nStructure the raw text into: **Decisions** (verbatim where possible) / **Action items** (owner, task, due date - \"TBD\" if unstated, never invented) / **Open questions** / **Notable context**. **Checkpoint:** show the structured notes and ask the user to correct owners/dates before POST filing.\n\n### POST mode (after the meeting)\n\n**Step 1 - Notion: file the notes.** Check the meeting-notes database for an existing page for this meeting (re-run safety). Create/update a page: date, attendees, structured notes from DURING (or ask for raw notes now), link back to the PRE brief items so unresolved ones carry forward. **Checkpoint:** show page content before writing; write on `apply`; return the link.\n\n**Step 2 - Gmail: follow-up draft.** Draft the attendee follow-up: thanks + decisions + action items with owners/dates + open questions with proposed resolution paths. **Checkpoint:** show full text; on `apply` create a **draft** (send only if explicitly told).\n\n**Step 3 - Calendar: next steps.** If the meeting agreed on a follow-up session or dated commitments, propose calendar events (diff against existing ones first). Apply on approval.\n\n## Output format\n\n**PRE:** the **prep brief** - Meeting facts / Who's who / Where things stand / Docs / Carry-over items / Talking points.\n**DURING:** the **structured notes block** - Decisions / Actions (owner · task · due) / Open questions / Context.\n**POST:** per-step artifacts - Notion page preview → link; follow-up email draft; proposed events table.\n\nEvery run ends with the **chain summary table** (rows for the mode that ran):\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| Anchor | Calendar | Meeting confirmed | done |\n| Context | Gmail + Drive | Threads + files digested | done / partial |\n| Notes | Notion | Page created/updated (link) | done / fallback |\n| Follow-up | Gmail | Draft saved | awaiting apply |\n| Next steps | Calendar | N events proposed | awaiting apply |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Confirm which meeting at the start - prepping or filing against the wrong calendar event poisons every later step\n- In POST, check for an existing Notion page for this meeting before creating one; update it instead\n- Carry unresolved items from the PRE brief into the POST page so the loop actually closes\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't file notes to Notion before the user has corrected owners and due dates in the structured-notes checkpoint\n- Don't send the follow-up email - draft by default; sending needs explicit approval of the exact text\n- Don't invent due dates or owners for action items; \"TBD\" is honest, a guessed date is a false commitment\n- Don't create duplicate meeting pages or duplicate follow-up events on re-run - check first\n- Don't invent mail, calendar, file, or Notion contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Correct meeting confirmed before any context gathering\n- [ ] Every action item has owner + date or an explicit TBD\n- [ ] Chain summary table reflects the mode that actually ran, with fallbacks marked\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@google-calendar, @gmail, @notion, @google-drive) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "personal-ops-sunday", "aiInstallPrompt": "Install the skill 'personal-ops-sunday' by writing the file below.\n\nUniversal path: ./skills/personal-ops-sunday/SKILL.md\nGrok path: ~/.grok/skills/personal-ops-sunday/SKILL.md\nCursor path: ~/.cursor/skills/personal-ops-sunday/SKILL.md\nClaude path: ~/.claude/skills/personal-ops-sunday/SKILL.md\nVS Code agent path: .github/skills/personal-ops-sunday/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: personal-ops-sunday\ndescription: >\n  Weekly life admin across personal connectors. Requires integrations: @gmail, @google-calendar, @google-drive, @filesystem. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"personal ops sunday\", /personal-ops-sunday.\nmetadata:\n  category: combo\n  type: combo\n  cadence: weekly\n  risk: low\n  connectors: gmail,google-calendar,google-drive,filesystem\n  integrations: @gmail,@google-calendar,@google-drive,@filesystem\nallowed-tools: search_tool, use_tool\n---\n# /personal-ops-sunday\n\nWeekly life admin across personal connectors.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as a Sunday-evening sweep of personal admin so Monday starts clean: bills and renewals hiding in Gmail, the coming week's calendar (conflicts, gaps, things needing prep), emails that still owe someone a reply, and files dumped in Downloads/Drive during the week that need filing. The output is a prioritized Sunday checklist - mostly read-only, with a few optional proposed writes (reminders, file moves) that each need approval.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Sweep window (default: the past 7 days of mail/files; the next 7 days of calendar)\n- What counts as a \"bill\" for this user (utilities, subscriptions, rent, card payment due dates) and any senders to always include/ignore\n- Local folders to sweep via @filesystem (default: Downloads and Desktop) and where filed documents should go in Drive\n- Anything the user already knows is looming this week (trip, appointment, deadline) to check prep for\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Friendly but brisk - this is life admin, not a performance review; celebrate an empty section (\"No overdue replies 🎉\").\n- **Audience:** The user on a Sunday evening with 15 minutes and limited patience.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** this is a survey skill - run every lane whose connector works and mark the rest \"skipped (connector missing)\" in the checklist. A Sunday sweep with 3 of 4 lanes is still worth doing; never abandon the run because one connector is down.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nRun four read-only lanes, checkpoint each with a mini-summary, then assemble the checklist. Writes (reminders, file moves) are proposed at the end, never mid-sweep.\n\n**Lane 1 - Gmail: bills & money.**\n- Read: the past week for bills, payment confirmations, renewal notices, price-increase notices, and due dates (search by the user's known senders + terms like \"payment due\", \"renewal\", \"invoice\", \"expires\").\n- Transform: a money list - item, amount, due date, autopay or manual, source email. Separate \"action needed\" (manual payment due, card expiring, renewal you may want to cancel) from \"FYI\" (autopay went through fine).\n\n**Lane 2 - Gmail: replies owed.**\n- Read: threads where a real person asked the user something and the last message isn't the user's (exclude newsletters/notifications).\n- Transform: replies-owed list - who, what they need, how old. Offer to draft the 1-2 most overdue replies (drafts only, approval required).\n\n**Lane 3 - Calendar: the week ahead.**\n- Read: next 7 days of events.\n- Transform: a week map - conflicts/double-bookings, events missing location or video link, anything needing prep (gift, documents, booking), and the biggest free blocks for the user's own priorities.\n\n**Lane 4 - Drive + filesystem: file triage.**\n- Read: list new files this week in Downloads/Desktop (local) and Drive's recent uploads.\n- Transform: a filing list - file, guessed category (receipt → tax folder, photo, document), proposed destination. Flag anything that looks important-but-misplaced (e.g., an insurance PDF in Downloads).\n\n**Checkpoint after each lane:** two-line summary (\"Lane 1: 3 bills need action, 4 autopay FYIs\") so the user can redirect before the next lane runs.\n\n**Assemble - the Sunday checklist:** merge all lanes into one prioritized list: **Do today** (overdue or due before Wednesday) / **This week** / **FYI**. Each item names its source lane and links its evidence.\n\n**Optional writes (approval gate):** propose in one review table - payment-due calendar reminders (diffed against existing ones), file moves to Drive folders, reply drafts. Apply only the rows the user approves (`apply`, `apply all`, or `apply 1,3`). No sends, no deletions.\n\n## Output format\n\nPer-lane mini-summaries during the run, then:\n\n**☀️ Sunday Ops - <date>**\n1. **💸 Money** - action-needed table (item / amount / due / source) + autopay FYI line\n2. **✉️ Replies owed** - who / what / age\n3. **📅 Week ahead** - conflicts, missing details, prep needed, free blocks\n4. **🗂 File triage** - file / proposed destination\n\n**Checklist** - Do today / This week / FYI (each item sourced)\n**Proposed writes table** - reminders, moves, drafts - awaiting apply\n\nFinish with a **chain summary table**:\n\n| Lane | App | Found | Status |\n|---|---|---|---|\n| Money | Gmail | N action, M FYI | done |\n| Replies | Gmail | K owed | done |\n| Week ahead | Calendar | C conflicts, P prep items | done |\n| Files | Drive + local | F to file | done / skipped |\n| Writes | Calendar/Drive/Gmail | A applied of B proposed | done |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Distinguish action-needed from FYI in the money lane - autopay confirmations are noise dressed as urgency\n- Diff proposed reminders against existing calendar events; weekly cadence means last Sunday's reminders are probably still there\n- Keep the checklist under ~10 \"Do today/This week\" items - a 30-item Sunday list gets ignored, so overflow goes to FYI\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't pay, cancel, unsubscribe, or delete anything - this skill surfaces and proposes; the user acts\n- Don't move local files or create reminders before the user approves the specific rows in the writes table\n- Don't drag lane 2 replies into full email-writing mode mid-sweep - offer drafts at the end so the sweep finishes\n- Don't re-list items the user dismissed last week as FYI-forever (subscriptions they know about) if that preference was stated\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] All four lanes ran or are explicitly marked skipped with the reason\n- [ ] Every checklist item cites its source (email, event, file)\n- [ ] Proposed writes shown in one table and applied only after approval\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@gmail, @google-calendar, @google-drive, @filesystem) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /personal-ops-sunday`\n```\n\nAfter writing the file, confirm it exists. The user can then run /personal-ops-sunday or describe the task from the skill description.", "skillContent": "---\nname: personal-ops-sunday\ndescription: >\n  Weekly life admin across personal connectors. Requires integrations: @gmail, @google-calendar, @google-drive, @filesystem. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"personal ops sunday\", /personal-ops-sunday.\nmetadata:\n  category: combo\n  type: combo\n  cadence: weekly\n  risk: low\n  connectors: gmail,google-calendar,google-drive,filesystem\n  integrations: @gmail,@google-calendar,@google-drive,@filesystem\nallowed-tools: search_tool, use_tool\n---\n# /personal-ops-sunday\n\nWeekly life admin across personal connectors.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as a Sunday-evening sweep of personal admin so Monday starts clean: bills and renewals hiding in Gmail, the coming week's calendar (conflicts, gaps, things needing prep), emails that still owe someone a reply, and files dumped in Downloads/Drive during the week that need filing. The output is a prioritized Sunday checklist - mostly read-only, with a few optional proposed writes (reminders, file moves) that each need approval.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Sweep window (default: the past 7 days of mail/files; the next 7 days of calendar)\n- What counts as a \"bill\" for this user (utilities, subscriptions, rent, card payment due dates) and any senders to always include/ignore\n- Local folders to sweep via @filesystem (default: Downloads and Desktop) and where filed documents should go in Drive\n- Anything the user already knows is looming this week (trip, appointment, deadline) to check prep for\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Friendly but brisk - this is life admin, not a performance review; celebrate an empty section (\"No overdue replies 🎉\").\n- **Audience:** The user on a Sunday evening with 15 minutes and limited patience.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** this is a survey skill - run every lane whose connector works and mark the rest \"skipped (connector missing)\" in the checklist. A Sunday sweep with 3 of 4 lanes is still worth doing; never abandon the run because one connector is down.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nRun four read-only lanes, checkpoint each with a mini-summary, then assemble the checklist. Writes (reminders, file moves) are proposed at the end, never mid-sweep.\n\n**Lane 1 - Gmail: bills & money.**\n- Read: the past week for bills, payment confirmations, renewal notices, price-increase notices, and due dates (search by the user's known senders + terms like \"payment due\", \"renewal\", \"invoice\", \"expires\").\n- Transform: a money list - item, amount, due date, autopay or manual, source email. Separate \"action needed\" (manual payment due, card expiring, renewal you may want to cancel) from \"FYI\" (autopay went through fine).\n\n**Lane 2 - Gmail: replies owed.**\n- Read: threads where a real person asked the user something and the last message isn't the user's (exclude newsletters/notifications).\n- Transform: replies-owed list - who, what they need, how old. Offer to draft the 1-2 most overdue replies (drafts only, approval required).\n\n**Lane 3 - Calendar: the week ahead.**\n- Read: next 7 days of events.\n- Transform: a week map - conflicts/double-bookings, events missing location or video link, anything needing prep (gift, documents, booking), and the biggest free blocks for the user's own priorities.\n\n**Lane 4 - Drive + filesystem: file triage.**\n- Read: list new files this week in Downloads/Desktop (local) and Drive's recent uploads.\n- Transform: a filing list - file, guessed category (receipt → tax folder, photo, document), proposed destination. Flag anything that looks important-but-misplaced (e.g., an insurance PDF in Downloads).\n\n**Checkpoint after each lane:** two-line summary (\"Lane 1: 3 bills need action, 4 autopay FYIs\") so the user can redirect before the next lane runs.\n\n**Assemble - the Sunday checklist:** merge all lanes into one prioritized list: **Do today** (overdue or due before Wednesday) / **This week** / **FYI**. Each item names its source lane and links its evidence.\n\n**Optional writes (approval gate):** propose in one review table - payment-due calendar reminders (diffed against existing ones), file moves to Drive folders, reply drafts. Apply only the rows the user approves (`apply`, `apply all`, or `apply 1,3`). No sends, no deletions.\n\n## Output format\n\nPer-lane mini-summaries during the run, then:\n\n**☀️ Sunday Ops - <date>**\n1. **💸 Money** - action-needed table (item / amount / due / source) + autopay FYI line\n2. **✉️ Replies owed** - who / what / age\n3. **📅 Week ahead** - conflicts, missing details, prep needed, free blocks\n4. **🗂 File triage** - file / proposed destination\n\n**Checklist** - Do today / This week / FYI (each item sourced)\n**Proposed writes table** - reminders, moves, drafts - awaiting apply\n\nFinish with a **chain summary table**:\n\n| Lane | App | Found | Status |\n|---|---|---|---|\n| Money | Gmail | N action, M FYI | done |\n| Replies | Gmail | K owed | done |\n| Week ahead | Calendar | C conflicts, P prep items | done |\n| Files | Drive + local | F to file | done / skipped |\n| Writes | Calendar/Drive/Gmail | A applied of B proposed | done |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Distinguish action-needed from FYI in the money lane - autopay confirmations are noise dressed as urgency\n- Diff proposed reminders against existing calendar events; weekly cadence means last Sunday's reminders are probably still there\n- Keep the checklist under ~10 \"Do today/This week\" items - a 30-item Sunday list gets ignored, so overflow goes to FYI\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't pay, cancel, unsubscribe, or delete anything - this skill surfaces and proposes; the user acts\n- Don't move local files or create reminders before the user approves the specific rows in the writes table\n- Don't drag lane 2 replies into full email-writing mode mid-sweep - offer drafts at the end so the sweep finishes\n- Don't re-list items the user dismissed last week as FYI-forever (subscriptions they know about) if that preference was stated\n- Don't invent mail, calendar, or file contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] All four lanes ran or are explicitly marked skipped with the reason\n- [ ] Every checklist item cites its source (email, event, file)\n- [ ] Proposed writes shown in one table and applied only after approval\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@gmail, @google-calendar, @google-drive, @filesystem) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /personal-ops-sunday`\n"}, {"id": "project-kickoff-pack", "aiInstallPrompt": "Install the skill 'project-kickoff-pack' by writing the file below.\n\nUniversal path: ./skills/project-kickoff-pack/SKILL.md\nGrok path: ~/.grok/skills/project-kickoff-pack/SKILL.md\nCursor path: ~/.cursor/skills/project-kickoff-pack/SKILL.md\nClaude path: ~/.claude/skills/project-kickoff-pack/SKILL.md\nVS Code agent path: .github/skills/project-kickoff-pack/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: project-kickoff-pack\ndescription: >\n  Scaffold new project across Notion, SharePoint, Teams, Calendar. Requires integrations: @notion, @sharepoint, @teams, @outlook-calendar. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"project kickoff pack\", /project-kickoff-pack.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,microsoft-sharepoint,microsoft-teams,microsoft-calendar\n  integrations: @notion,@sharepoint,@teams,@outlook-calendar\nallowed-tools: search_tool, use_tool\n---\n# /project-kickoff-pack\n\nScaffold new project across Notion, SharePoint, Teams, Calendar.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a project is approved and needs its workspace stood up in one pass instead of four separate chores: a project hub page in Notion (charter, milestones, decision log), a document folder structure in SharePoint, a kickoff announcement in Teams, and the kickoff meeting on the calendar. Everything cross-linked, so day one starts with one link instead of \"where do we put things?\".\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Project name, one-line objective, sponsor, project lead, and team members\n- Target dates: kickoff date, key milestone dates (even rough), end date if known\n- Where things live: Notion projects database, SharePoint site, Teams channel (existing channel or announce in a team-wide one)\n- Kickoff meeting details: length (default 45 min), required vs optional attendees\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Scaffolding, not strategy - the charter records what the user states; gaps become \"TBD at kickoff\" agenda items, not invented content.\n- **Audience:** The project team seeing this workspace for the first time.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n- **@teams** - Microsoft Teams\n  - MCP server key: `microsoft-teams`\n  - Example tools: `microsoft-teams__list_channels`, `microsoft-teams__post_message`, `microsoft-teams__list_messages`\n\n- **@outlook-calendar** - Microsoft Outlook Calendar\n  - MCP server key: `microsoft-calendar`\n  - Example tools: `microsoft-calendar__list_events`, `microsoft-calendar__create_event`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** each artifact degrades to copy-paste form - @notion missing → hub page as markdown; @sharepoint missing → folder structure as a text tree to create manually; @teams missing → announcement as text; @outlook-calendar missing → invite details (title, attendees, agenda) as text. Build what you can and mark fallbacks in the chain summary.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n**Step 0 - Duplicate check.** Search the Notion projects database and Teams for artifacts already bearing this project name. If found, report them and offer to fill gaps rather than scaffold from scratch - a re-run must never produce \"Project X (2)\".\n\n**Step 1 - Notion: the project hub.**\n- Transform the gathered inputs into a hub page: **Charter** (objective, sponsor, lead, team, success criteria - unstated ones become \"TBD at kickoff\") / **Milestones table** (name, target date, owner) / **Decision log** (empty, dated-entry format) / **Links** section (placeholders for SharePoint + Teams filled in later steps) / **Risks** (empty scaffold).\n- **Checkpoint 1:** show the full page outline with content before creating. Create on `apply`; capture the page URL - every later artifact links back to it.\n\n**Step 2 - SharePoint: the document home.**\n- Read: the site's existing project folders to mirror local convention (naming, subfolder pattern) instead of imposing one.\n- Write: create `Projects/<Project name>/` with subfolders - default `01 Charter & plans`, `02 Working docs`, `03 Meetings`, `04 Deliverables`, `05 Archive` (adjust to house style).\n- **Checkpoint 2:** show the proposed folder tree; create on `apply`; return the folder link and paste it into the Notion hub's Links section.\n\n**Step 3 - Outlook Calendar: the kickoff.**\n- Read: attendee availability if the session's calendar tools expose it; otherwise take the user's chosen slot.\n- Write: kickoff event - title `<Project> kickoff`, agenda in the body (intros → charter walkthrough → milestones → TBD items → next steps), Notion hub + SharePoint links in the description, required/optional attendees as given.\n- **Checkpoint 3:** show the event details table; create on `apply`. Sending invites notifies real people - confirm attendee list explicitly.\n\n**Step 4 - Teams: the announcement.**\n- Write: post to the agreed channel - project name, one-line objective, lead, kickoff date/time, and the Notion hub link as the single front door. Keep it under ~6 lines.\n- **Checkpoint 4:** show exact text + channel; post on `apply`.\n- Close the loop: update the Notion hub's Links section with the Teams channel link, so all four artifacts reference each other.\n\n## Output format\n\n1. **Hub page preview** (step 1) - full outline, then page URL\n2. **Folder tree** (step 2) - proposed structure, then SharePoint link\n3. **Kickoff event table** (step 3) - title / when / attendees / agenda - awaiting apply\n4. **Announcement preview** (step 4) - channel + text - awaiting apply\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 0 Dup check | Notion + Teams | Existing artifacts: none / listed | done |\n| 1 Hub | Notion | Project page (URL) | done |\n| 2 Docs | SharePoint | Folder tree (link) | done / fallback |\n| 3 Kickoff | Calendar | Event, N attendees | done / awaiting apply |\n| 4 Announce | Teams | Channel post | done / awaiting apply |\n\nPlus the **one link** to share: the Notion hub, now containing all the others.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Run the step-0 duplicate check - half-scaffolded projects from earlier attempts are common; fill gaps, don't fork\n- Create the Notion hub first: its URL feeds every other artifact\n- Mirror the SharePoint site's existing folder conventions rather than imposing a new scheme\n- Mark unknown charter fields \"TBD at kickoff\" and mirror them into the kickoff agenda\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't announce in Teams (step 4) before the hub and kickoff exist - an announcement with no link and no date is noise\n- Don't send calendar invites without the attendee list explicitly confirmed; invites notify people immediately\n- Don't invent success criteria, milestones, or risks the user never stated - scaffold, don't fabricate strategy\n- Don't leave the cross-links half-done: hub → SharePoint/Teams/event AND back\n- Don't invent site structures, channel names, or page contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Duplicate check ran before any creation\n- [ ] All four artifacts cross-link (hub is the front door)\n- [ ] Chain summary table shows steps 0-4 with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@notion, @sharepoint, @teams, @outlook-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /project-kickoff-pack or describe the task from the skill description.", "skillContent": "---\nname: project-kickoff-pack\ndescription: >\n  Scaffold new project across Notion, SharePoint, Teams, Calendar. Requires integrations: @notion, @sharepoint, @teams, @outlook-calendar. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"project kickoff pack\", /project-kickoff-pack.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,microsoft-sharepoint,microsoft-teams,microsoft-calendar\n  integrations: @notion,@sharepoint,@teams,@outlook-calendar\nallowed-tools: search_tool, use_tool\n---\n# /project-kickoff-pack\n\nScaffold new project across Notion, SharePoint, Teams, Calendar.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a project is approved and needs its workspace stood up in one pass instead of four separate chores: a project hub page in Notion (charter, milestones, decision log), a document folder structure in SharePoint, a kickoff announcement in Teams, and the kickoff meeting on the calendar. Everything cross-linked, so day one starts with one link instead of \"where do we put things?\".\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Project name, one-line objective, sponsor, project lead, and team members\n- Target dates: kickoff date, key milestone dates (even rough), end date if known\n- Where things live: Notion projects database, SharePoint site, Teams channel (existing channel or announce in a team-wide one)\n- Kickoff meeting details: length (default 45 min), required vs optional attendees\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Scaffolding, not strategy - the charter records what the user states; gaps become \"TBD at kickoff\" agenda items, not invented content.\n- **Audience:** The project team seeing this workspace for the first time.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@sharepoint** - Microsoft SharePoint\n  - MCP server key: `microsoft-sharepoint`\n  - Example tools: `microsoft-sharepoint__search`, `microsoft-sharepoint__upload_file`, `microsoft-sharepoint__list_sites`\n\n- **@teams** - Microsoft Teams\n  - MCP server key: `microsoft-teams`\n  - Example tools: `microsoft-teams__list_channels`, `microsoft-teams__post_message`, `microsoft-teams__list_messages`\n\n- **@outlook-calendar** - Microsoft Outlook Calendar\n  - MCP server key: `microsoft-calendar`\n  - Example tools: `microsoft-calendar__list_events`, `microsoft-calendar__create_event`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** each artifact degrades to copy-paste form - @notion missing → hub page as markdown; @sharepoint missing → folder structure as a text tree to create manually; @teams missing → announcement as text; @outlook-calendar missing → invite details (title, attendees, agenda) as text. Build what you can and mark fallbacks in the chain summary.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n**Step 0 - Duplicate check.** Search the Notion projects database and Teams for artifacts already bearing this project name. If found, report them and offer to fill gaps rather than scaffold from scratch - a re-run must never produce \"Project X (2)\".\n\n**Step 1 - Notion: the project hub.**\n- Transform the gathered inputs into a hub page: **Charter** (objective, sponsor, lead, team, success criteria - unstated ones become \"TBD at kickoff\") / **Milestones table** (name, target date, owner) / **Decision log** (empty, dated-entry format) / **Links** section (placeholders for SharePoint + Teams filled in later steps) / **Risks** (empty scaffold).\n- **Checkpoint 1:** show the full page outline with content before creating. Create on `apply`; capture the page URL - every later artifact links back to it.\n\n**Step 2 - SharePoint: the document home.**\n- Read: the site's existing project folders to mirror local convention (naming, subfolder pattern) instead of imposing one.\n- Write: create `Projects/<Project name>/` with subfolders - default `01 Charter & plans`, `02 Working docs`, `03 Meetings`, `04 Deliverables`, `05 Archive` (adjust to house style).\n- **Checkpoint 2:** show the proposed folder tree; create on `apply`; return the folder link and paste it into the Notion hub's Links section.\n\n**Step 3 - Outlook Calendar: the kickoff.**\n- Read: attendee availability if the session's calendar tools expose it; otherwise take the user's chosen slot.\n- Write: kickoff event - title `<Project> kickoff`, agenda in the body (intros → charter walkthrough → milestones → TBD items → next steps), Notion hub + SharePoint links in the description, required/optional attendees as given.\n- **Checkpoint 3:** show the event details table; create on `apply`. Sending invites notifies real people - confirm attendee list explicitly.\n\n**Step 4 - Teams: the announcement.**\n- Write: post to the agreed channel - project name, one-line objective, lead, kickoff date/time, and the Notion hub link as the single front door. Keep it under ~6 lines.\n- **Checkpoint 4:** show exact text + channel; post on `apply`.\n- Close the loop: update the Notion hub's Links section with the Teams channel link, so all four artifacts reference each other.\n\n## Output format\n\n1. **Hub page preview** (step 1) - full outline, then page URL\n2. **Folder tree** (step 2) - proposed structure, then SharePoint link\n3. **Kickoff event table** (step 3) - title / when / attendees / agenda - awaiting apply\n4. **Announcement preview** (step 4) - channel + text - awaiting apply\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 0 Dup check | Notion + Teams | Existing artifacts: none / listed | done |\n| 1 Hub | Notion | Project page (URL) | done |\n| 2 Docs | SharePoint | Folder tree (link) | done / fallback |\n| 3 Kickoff | Calendar | Event, N attendees | done / awaiting apply |\n| 4 Announce | Teams | Channel post | done / awaiting apply |\n\nPlus the **one link** to share: the Notion hub, now containing all the others.\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Run the step-0 duplicate check - half-scaffolded projects from earlier attempts are common; fill gaps, don't fork\n- Create the Notion hub first: its URL feeds every other artifact\n- Mirror the SharePoint site's existing folder conventions rather than imposing a new scheme\n- Mark unknown charter fields \"TBD at kickoff\" and mirror them into the kickoff agenda\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't announce in Teams (step 4) before the hub and kickoff exist - an announcement with no link and no date is noise\n- Don't send calendar invites without the attendee list explicitly confirmed; invites notify people immediately\n- Don't invent success criteria, milestones, or risks the user never stated - scaffold, don't fabricate strategy\n- Don't leave the cross-links half-done: hub → SharePoint/Teams/event AND back\n- Don't invent site structures, channel names, or page contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Duplicate check ran before any creation\n- [ ] All four artifacts cross-link (hub is the front door)\n- [ ] Chain summary table shows steps 0-4 with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@notion, @sharepoint, @teams, @outlook-calendar) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "qbr-automation", "aiInstallPrompt": "Install the skill 'qbr-automation' by writing the file below.\n\nUniversal path: ./skills/qbr-automation/SKILL.md\nGrok path: ~/.grok/skills/qbr-automation/SKILL.md\nCursor path: ~/.cursor/skills/qbr-automation/SKILL.md\nClaude path: ~/.claude/skills/qbr-automation/SKILL.md\nVS Code agent path: .github/skills/qbr-automation/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: qbr-automation\ndescription: >\n  Quarterly metrics from Excel/Sheets into slide deck. Requires integrations: @google-drive, @onedrive, @filesystem. Uses search_tool + use_tool. Cadence: quarterly. Triggers: \"qbr automation\", /qbr-automation.\nmetadata:\n  category: combo\n  type: combo\n  cadence: quarterly\n  risk: medium\n  connectors: google-drive,microsoft-onedrive,filesystem\n  integrations: @google-drive,@onedrive,@filesystem\nallowed-tools: search_tool, use_tool\n---\n# /qbr-automation\n\nQuarterly metrics from Excel/Sheets into slide deck.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this at quarter end when the numbers live in spreadsheets (Google Sheets in Drive, Excel in OneDrive, or local files) and a QBR deck needs building: pull the quarter's metrics, compute quarter-over-quarter and vs-target deltas, turn them into a slide-by-slide narrative, and produce the deck content plus a distributable summary. The user (or their deck tool) applies the slides - this skill guarantees the numbers in them are traceable to the source sheet.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which quarter (e.g., Q2 FY26) and the comparison baseline (prior quarter, same quarter last year, or plan/target)\n- Where the metrics live: sheet/workbook names or links in Drive/OneDrive, or local file paths - and which tabs/ranges are the source of truth\n- The metric set the QBR covers (e.g., revenue, pipeline, churn, NPS, headcount) and any known definitions/caveats\n- Deck destination: update an existing template deck (link it) or produce slide-by-slide content for the user to paste\n- Audience: internal leadership vs customer-facing QBR - this changes what is shown and how candid the commentary is\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Executive register - every number carries its comparison (Δ QoQ, vs target), every claim traces to a cell.\n- **Audience:** Leadership or customer stakeholders reviewing in 20 minutes.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Source-flexibility note for this combo:** only the connector holding the metrics is truly required - Drive OR OneDrive OR local files. If none is available, ask the user to paste the metric table and mark every downstream number \"source: pasted, unverified\". If the deck destination's storage is missing, deliver the full slide content inline (fallback at step 3).\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n**Step 1 - Drive/OneDrive/local: pull the metrics.**\n- Read: locate the named workbook(s); read the specified tabs/ranges. If multiple files match (e.g., \"Q2 metrics v3\" and \"Q2 metrics FINAL\"), list them with modified dates and ask which is authoritative.\n- Extract into a **metrics base table**: metric / this quarter / baseline / Δ absolute / Δ % / vs target / source (file + tab + cell range). Flag gaps: blank cells, #REF! errors, obviously stale tabs (last quarter's dates), or metrics requested but absent from the sheet.\n- **Checkpoint 1 (the numbers gate):** show the base table + flags. The user validates the numbers HERE - nothing narrative gets built on unvalidated figures. Wrong-in-the-sheet beats wrong-in-the-deck-in-front-of-the-board.\n\n**Step 2 - Build the QBR narrative (AI, no connector).**\n- Transform the validated table into slide-by-slide content:\n  1. **Executive summary** - the quarter in 3 bullets + one headline number\n  2. **Scorecard** - the metrics table, color logic stated (green ≥ target, amber within 10%, red below)\n  3. **Wins** - top 2-3 improvements with drivers *as stated by the user or the sheet's notes* - never invented causes\n  4. **Misses & risks** - metrics below target with the user's stated context; label unexplained misses \"driver TBD - ask owner\"\n  5. **Next-quarter priorities** - from the user; the skill scaffolds, doesn't set strategy\n- Each slide: title that states the takeaway (\"Churn improved for the 2nd straight quarter\", not \"Churn\"), body bullets, and a speaker note.\n- **Checkpoint 2:** show the full slide-by-slide content; user edits commentary and confirms.\n\n**Step 3 - Write the deck artifacts.**\n- Write: save the deck content to the agreed destination - e.g., a formatted doc/markdown file in Drive or OneDrive next to the source workbook, named `QBR_<quarter>_<team>` - for pasting into the deck template. (If the session's tools can update the deck file directly, propose those edits slide-by-slide first.)\n- Also produce the **one-paragraph distribution summary** (for email/chat) with the headline numbers.\n- **Checkpoint 3 (approval gate):** review table of files to be written/updated. Apply on `apply`. Report links/paths.\n- Fallback: no writable destination → deliver deck content + summary inline, clearly sectioned for copy-paste.\n\n## Output format\n\n1. **Metrics base table** (step 1) - metric / actual / baseline / Δ / vs target / source cell - plus data-quality flags\n2. **Slide-by-slide content** (step 2) - 5 slides, each: takeaway title / bullets / speaker note\n3. **Write proposal + result** (step 3) - files, destinations, links; distribution summary paragraph\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Metrics | Drive / OneDrive / local | Base table, N metrics, K flags | validated |\n| 2 Narrative | AI | 5 slides + notes | approved |\n| 3 Deck out | Drive / OneDrive | QBR_<q> file + summary | done / inline fallback |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Confirm which file version is authoritative before extracting - quarter-end folders are full of near-duplicates\n- Keep a source reference (file/tab/range) on every number so any figure can be defended live in the meeting\n- Recompute deltas from raw values; don't trust pre-computed Δ columns without noting you used them\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't build narrative (step 2) on numbers the user hasn't validated at checkpoint 1 - commentary on wrong numbers is compound damage\n- Don't invent drivers for wins or misses; unexplained variance is labeled \"driver TBD\", which is a legitimate QBR line\n- Don't mix quarters or currencies silently - if tabs disagree on units or periods, stop and flag it\n- Don't overwrite an existing deck file without showing what changes; propose slide-by-slide\n- Don't invent file contents or figures without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every number in the slides traces to the validated base table\n- [ ] Numbers gate (checkpoint 1) passed before any narrative was written\n- [ ] Chain summary table shows all 3 steps with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@google-drive, @onedrive, @filesystem) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **quarterly**\n\nExample: `/loop 7d Run /qbr-automation`\n```\n\nAfter writing the file, confirm it exists. The user can then run /qbr-automation or describe the task from the skill description.", "skillContent": "---\nname: qbr-automation\ndescription: >\n  Quarterly metrics from Excel/Sheets into slide deck. Requires integrations: @google-drive, @onedrive, @filesystem. Uses search_tool + use_tool. Cadence: quarterly. Triggers: \"qbr automation\", /qbr-automation.\nmetadata:\n  category: combo\n  type: combo\n  cadence: quarterly\n  risk: medium\n  connectors: google-drive,microsoft-onedrive,filesystem\n  integrations: @google-drive,@onedrive,@filesystem\nallowed-tools: search_tool, use_tool\n---\n# /qbr-automation\n\nQuarterly metrics from Excel/Sheets into slide deck.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this at quarter end when the numbers live in spreadsheets (Google Sheets in Drive, Excel in OneDrive, or local files) and a QBR deck needs building: pull the quarter's metrics, compute quarter-over-quarter and vs-target deltas, turn them into a slide-by-slide narrative, and produce the deck content plus a distributable summary. The user (or their deck tool) applies the slides - this skill guarantees the numbers in them are traceable to the source sheet.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which quarter (e.g., Q2 FY26) and the comparison baseline (prior quarter, same quarter last year, or plan/target)\n- Where the metrics live: sheet/workbook names or links in Drive/OneDrive, or local file paths - and which tabs/ranges are the source of truth\n- The metric set the QBR covers (e.g., revenue, pipeline, churn, NPS, headcount) and any known definitions/caveats\n- Deck destination: update an existing template deck (link it) or produce slide-by-slide content for the user to paste\n- Audience: internal leadership vs customer-facing QBR - this changes what is shown and how candid the commentary is\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Executive register - every number carries its comparison (Δ QoQ, vs target), every claim traces to a cell.\n- **Audience:** Leadership or customer stakeholders reviewing in 20 minutes.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@onedrive** - Microsoft OneDrive\n  - MCP server key: `microsoft-onedrive`\n  - Example tools: `microsoft-onedrive__list_files`, `microsoft-onedrive__download_file`, `microsoft-onedrive__upload_file`\n\n- **@filesystem** - Local filesystem\n  - MCP server key: `filesystem`\n  - Example tools: `filesystem__read_file`, `filesystem__write_file`, `filesystem__list_directory`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Source-flexibility note for this combo:** only the connector holding the metrics is truly required - Drive OR OneDrive OR local files. If none is available, ask the user to paste the metric table and mark every downstream number \"source: pasted, unverified\". If the deck destination's storage is missing, deliver the full slide content inline (fallback at step 3).\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n**Step 1 - Drive/OneDrive/local: pull the metrics.**\n- Read: locate the named workbook(s); read the specified tabs/ranges. If multiple files match (e.g., \"Q2 metrics v3\" and \"Q2 metrics FINAL\"), list them with modified dates and ask which is authoritative.\n- Extract into a **metrics base table**: metric / this quarter / baseline / Δ absolute / Δ % / vs target / source (file + tab + cell range). Flag gaps: blank cells, #REF! errors, obviously stale tabs (last quarter's dates), or metrics requested but absent from the sheet.\n- **Checkpoint 1 (the numbers gate):** show the base table + flags. The user validates the numbers HERE - nothing narrative gets built on unvalidated figures. Wrong-in-the-sheet beats wrong-in-the-deck-in-front-of-the-board.\n\n**Step 2 - Build the QBR narrative (AI, no connector).**\n- Transform the validated table into slide-by-slide content:\n  1. **Executive summary** - the quarter in 3 bullets + one headline number\n  2. **Scorecard** - the metrics table, color logic stated (green ≥ target, amber within 10%, red below)\n  3. **Wins** - top 2-3 improvements with drivers *as stated by the user or the sheet's notes* - never invented causes\n  4. **Misses & risks** - metrics below target with the user's stated context; label unexplained misses \"driver TBD - ask owner\"\n  5. **Next-quarter priorities** - from the user; the skill scaffolds, doesn't set strategy\n- Each slide: title that states the takeaway (\"Churn improved for the 2nd straight quarter\", not \"Churn\"), body bullets, and a speaker note.\n- **Checkpoint 2:** show the full slide-by-slide content; user edits commentary and confirms.\n\n**Step 3 - Write the deck artifacts.**\n- Write: save the deck content to the agreed destination - e.g., a formatted doc/markdown file in Drive or OneDrive next to the source workbook, named `QBR_<quarter>_<team>` - for pasting into the deck template. (If the session's tools can update the deck file directly, propose those edits slide-by-slide first.)\n- Also produce the **one-paragraph distribution summary** (for email/chat) with the headline numbers.\n- **Checkpoint 3 (approval gate):** review table of files to be written/updated. Apply on `apply`. Report links/paths.\n- Fallback: no writable destination → deliver deck content + summary inline, clearly sectioned for copy-paste.\n\n## Output format\n\n1. **Metrics base table** (step 1) - metric / actual / baseline / Δ / vs target / source cell - plus data-quality flags\n2. **Slide-by-slide content** (step 2) - 5 slides, each: takeaway title / bullets / speaker note\n3. **Write proposal + result** (step 3) - files, destinations, links; distribution summary paragraph\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Metrics | Drive / OneDrive / local | Base table, N metrics, K flags | validated |\n| 2 Narrative | AI | 5 slides + notes | approved |\n| 3 Deck out | Drive / OneDrive | QBR_<q> file + summary | done / inline fallback |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Confirm which file version is authoritative before extracting - quarter-end folders are full of near-duplicates\n- Keep a source reference (file/tab/range) on every number so any figure can be defended live in the meeting\n- Recompute deltas from raw values; don't trust pre-computed Δ columns without noting you used them\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't build narrative (step 2) on numbers the user hasn't validated at checkpoint 1 - commentary on wrong numbers is compound damage\n- Don't invent drivers for wins or misses; unexplained variance is labeled \"driver TBD\", which is a legitimate QBR line\n- Don't mix quarters or currencies silently - if tabs disagree on units or periods, stop and flag it\n- Don't overwrite an existing deck file without showing what changes; propose slide-by-slide\n- Don't invent file contents or figures without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every number in the slides traces to the validated base table\n- [ ] Numbers gate (checkpoint 1) passed before any narrative was written\n- [ ] Chain summary table shows all 3 steps with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@google-drive, @onedrive, @filesystem) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **quarterly**\n\nExample: `/loop 7d Run /qbr-automation`\n"}, {"id": "research-to-doc", "aiInstallPrompt": "Install the skill 'research-to-doc' by writing the file below.\n\nUniversal path: ./skills/research-to-doc/SKILL.md\nGrok path: ~/.grok/skills/research-to-doc/SKILL.md\nCursor path: ~/.cursor/skills/research-to-doc/SKILL.md\nClaude path: ~/.claude/skills/research-to-doc/SKILL.md\nVS Code agent path: .github/skills/research-to-doc/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: research-to-doc\ndescription: >\n  Web research → Notion → Google Doc. Requires integrations: @notion, @google-drive. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"research to doc\", /research-to-doc.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,google-drive\n  integrations: @notion,@google-drive\nallowed-tools: search_tool, use_tool\n---\n# /research-to-doc\n\nWeb research → Notion → Google Doc.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a research question needs to end as two distinct artifacts: a **raw research hub in Notion** (all sources, notes, contradictions - the working memory) and a **polished summary document in Drive** (the shareable deliverable). Typical questions: competitor landscape, tool evaluation, market sizing, \"should we do X\" briefings. The two outputs serve different readers - the hub is for whoever continues the research; the doc is for whoever decides.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The research question, phrased as a question - plus 2-3 sub-questions that would settle it\n- Depth and recency: quick scan vs thorough; do sources need to be from the last N months?\n- Which Notion database/page acts as the research hub (or where to create one)\n- Drive folder for the final doc, its audience (self / team / exec), and target length (default: 1-2 pages)\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Findings cite sources; confidence stated per claim (confirmed by 2+ sources / single source / contested).\n- **Audience:** Hub - future researchers; Doc - the decision-maker named in inputs.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** the research itself (step 1) uses web search and needs no connector. @notion missing → deliver the hub content as structured markdown for manual paste, and continue to the doc. @google-drive missing → deliver the summary doc inline. Never let a missing storage connector waste completed research.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nBefore starting, search the Notion hub for existing research on this question. If found, show it and ask: extend it, refresh stale findings, or start clean? Never silently create a parallel hub for the same question.\n\n**Step 1 - Research (web, no storage connector).**\n- Work the sub-questions one at a time. For each source captured, record: claim, source name + URL, publication date, and independence (vendor's own claim vs third-party).\n- Actively look for disagreement - one contradicting source is worth more than a fifth confirmation. Track dead ends too (\"no public pricing found\") so the next researcher doesn't repeat them.\n- **Checkpoint 1:** present the **findings digest** - per sub-question: what's established (with confidence level), what's contested, what's unknown. Ask: dig deeper on anything, or proceed to filing?\n\n**Step 2 - Notion: file the research hub.**\n- Transform: structure the full material - question, date, per-sub-question findings with ALL sources (including the ones that didn't make the summary), contradictions section, dead ends, and \"open threads\" for future research.\n- Write: create the hub page (or append a dated section to the existing one).\n- **Checkpoint 2 (approval gate):** show the page structure + content preview; write on `apply`; return the link. The hub deliberately keeps everything - pruning happens in the doc, not here.\n\n**Step 3 - Drive: the summary doc.**\n- Transform: distill the approved findings for the named audience - **answer first** (the question + the one-paragraph answer), then key findings (each with confidence + footnote-style source), then \"what we couldn't determine\", then recommendation *clearly labeled as recommendation*, then a link back to the Notion hub for the full trail.\n- Write: create the doc in the agreed Drive folder, named `<Topic> - research summary - <YYYY-MM-DD>`.\n- **Checkpoint 3 (approval gate):** show the full doc text; create on `apply`; return the link.\n\n## Output format\n\n1. **Findings digest** (step 1) - per sub-question: established / contested / unknown, with source count and confidence\n2. **Hub page preview → link** (step 2) - the full structured research record\n3. **Summary doc preview → link** (step 3) - answer-first, 1-2 pages, hub-linked\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Research | Web | N sources across K sub-questions | approved |\n| 2 Hub | Notion | Hub page (link) | done / fallback |\n| 3 Doc | Drive | Summary doc (link) | done / fallback |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Check the hub for prior research on this question before starting - extend, don't duplicate\n- Keep the two artifacts different: hub = complete and messy-tolerant; doc = pruned and answer-first. A doc that's just the hub re-pasted failed\n- Date-stamp findings and prefer recent sources when the topic moves fast; note each source's publication date\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't write the summary doc before the findings digest is approved - the user's \"dig deeper\" at checkpoint 1 changes the conclusion\n- Don't present single-source or vendor-self-reported claims at the same confidence as independently confirmed ones\n- Don't drop contradictions to make the doc cleaner - contested points go in the doc as contested\n- Don't fabricate URLs, publication dates, or statistics - an unknown is recorded as unknown\n- Don't invent Notion or Drive contents without a successful `use_tool` call\n- Don't skip the hub because the doc \"is enough\" - without the source trail the doc can't be defended later\n\n## Quality check (before responding)\n\n- [ ] Every claim in the doc traces to a source in the hub with a stated confidence level\n- [ ] Hub and doc are meaningfully different artifacts (complete record vs distilled answer)\n- [ ] Chain summary table shows all 3 steps with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@notion, @google-drive) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /research-to-doc or describe the task from the skill description.", "skillContent": "---\nname: research-to-doc\ndescription: >\n  Web research → Notion → Google Doc. Requires integrations: @notion, @google-drive. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"research to doc\", /research-to-doc.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: notion,google-drive\n  integrations: @notion,@google-drive\nallowed-tools: search_tool, use_tool\n---\n# /research-to-doc\n\nWeb research → Notion → Google Doc.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a research question needs to end as two distinct artifacts: a **raw research hub in Notion** (all sources, notes, contradictions - the working memory) and a **polished summary document in Drive** (the shareable deliverable). Typical questions: competitor landscape, tool evaluation, market sizing, \"should we do X\" briefings. The two outputs serve different readers - the hub is for whoever continues the research; the doc is for whoever decides.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The research question, phrased as a question - plus 2-3 sub-questions that would settle it\n- Depth and recency: quick scan vs thorough; do sources need to be from the last N months?\n- Which Notion database/page acts as the research hub (or where to create one)\n- Drive folder for the final doc, its audience (self / team / exec), and target length (default: 1-2 pages)\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Findings cite sources; confidence stated per claim (confirmed by 2+ sources / single source / contested).\n- **Audience:** Hub - future researchers; Doc - the decision-maker named in inputs.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** the research itself (step 1) uses web search and needs no connector. @notion missing → deliver the hub content as structured markdown for manual paste, and continue to the doc. @google-drive missing → deliver the summary doc inline. Never let a missing storage connector waste completed research.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nBefore starting, search the Notion hub for existing research on this question. If found, show it and ask: extend it, refresh stale findings, or start clean? Never silently create a parallel hub for the same question.\n\n**Step 1 - Research (web, no storage connector).**\n- Work the sub-questions one at a time. For each source captured, record: claim, source name + URL, publication date, and independence (vendor's own claim vs third-party).\n- Actively look for disagreement - one contradicting source is worth more than a fifth confirmation. Track dead ends too (\"no public pricing found\") so the next researcher doesn't repeat them.\n- **Checkpoint 1:** present the **findings digest** - per sub-question: what's established (with confidence level), what's contested, what's unknown. Ask: dig deeper on anything, or proceed to filing?\n\n**Step 2 - Notion: file the research hub.**\n- Transform: structure the full material - question, date, per-sub-question findings with ALL sources (including the ones that didn't make the summary), contradictions section, dead ends, and \"open threads\" for future research.\n- Write: create the hub page (or append a dated section to the existing one).\n- **Checkpoint 2 (approval gate):** show the page structure + content preview; write on `apply`; return the link. The hub deliberately keeps everything - pruning happens in the doc, not here.\n\n**Step 3 - Drive: the summary doc.**\n- Transform: distill the approved findings for the named audience - **answer first** (the question + the one-paragraph answer), then key findings (each with confidence + footnote-style source), then \"what we couldn't determine\", then recommendation *clearly labeled as recommendation*, then a link back to the Notion hub for the full trail.\n- Write: create the doc in the agreed Drive folder, named `<Topic> - research summary - <YYYY-MM-DD>`.\n- **Checkpoint 3 (approval gate):** show the full doc text; create on `apply`; return the link.\n\n## Output format\n\n1. **Findings digest** (step 1) - per sub-question: established / contested / unknown, with source count and confidence\n2. **Hub page preview → link** (step 2) - the full structured research record\n3. **Summary doc preview → link** (step 3) - answer-first, 1-2 pages, hub-linked\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Research | Web | N sources across K sub-questions | approved |\n| 2 Hub | Notion | Hub page (link) | done / fallback |\n| 3 Doc | Drive | Summary doc (link) | done / fallback |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Check the hub for prior research on this question before starting - extend, don't duplicate\n- Keep the two artifacts different: hub = complete and messy-tolerant; doc = pruned and answer-first. A doc that's just the hub re-pasted failed\n- Date-stamp findings and prefer recent sources when the topic moves fast; note each source's publication date\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't write the summary doc before the findings digest is approved - the user's \"dig deeper\" at checkpoint 1 changes the conclusion\n- Don't present single-source or vendor-self-reported claims at the same confidence as independently confirmed ones\n- Don't drop contradictions to make the doc cleaner - contested points go in the doc as contested\n- Don't fabricate URLs, publication dates, or statistics - an unknown is recorded as unknown\n- Don't invent Notion or Drive contents without a successful `use_tool` call\n- Don't skip the hub because the doc \"is enough\" - without the source trail the doc can't be defended later\n\n## Quality check (before responding)\n\n- [ ] Every claim in the doc traces to a source in the hub with a stated confidence level\n- [ ] Hub and doc are meaningfully different artifacts (complete record vs distilled answer)\n- [ ] Chain summary table shows all 3 steps with real statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@notion, @google-drive) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "sales-closed-loop", "aiInstallPrompt": "Install the skill 'sales-closed-loop' by writing the file below.\n\nUniversal path: ./skills/sales-closed-loop/SKILL.md\nGrok path: ~/.grok/skills/sales-closed-loop/SKILL.md\nCursor path: ~/.cursor/skills/sales-closed-loop/SKILL.md\nClaude path: ~/.claude/skills/sales-closed-loop/SKILL.md\nVS Code agent path: .github/skills/sales-closed-loop/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: sales-closed-loop\ndescription: >\n  Lead email → meeting → CRM note → follow-up. Requires integrations: @gmail, @google-calendar, @zapier, @slack. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sales closed loop\", /sales-closed-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: medium\n  connectors: gmail,google-calendar,zapier,slack\n  integrations: @gmail,@google-calendar,@zapier,@slack\nallowed-tools: search_tool, use_tool\n---\n# /sales-closed-loop\n\nLead email → meeting → CRM note → follow-up.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this to move ONE lead through the next stage without anything falling through: draft the outreach or reply in Gmail grounded in the actual thread history, get the meeting on the calendar, push the interaction into the CRM via the user's Zapier workflow, and set the follow-up (calendar reminder + optional Slack nudge to the deal channel). Run it each time the lead advances - the skill reads the thread first, so it always knows where the conversation actually stands.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The lead: name, email, company - and whether this is cold outreach or an active thread\n- What the user is selling into this conversation and the ONE next step the email should drive (meeting? proposal review?)\n- Meeting parameters: length, format, 2-3 available slots (or permission to read the calendar for free slots)\n- The CRM-logging Zap's name and expected payload fields; which Slack channel tracks deals\n- Follow-up rule: how many days of silence before the nudge (default 3 business days)\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Outreach text in the user's voice - mirror their previous sent emails in the thread, not generic sales-speak.\n- **Audience:** Email - the lead (external, high stakes); Slack - the internal deal channel (candid, brief).\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n- **@slack** - Slack\n  - MCP server key: `slack`\n  - Example tools: `slack__list_channels`, `slack__get_channel_history`, `slack__post_message`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** @gmail missing → deliver the outreach as copy-paste text. @google-calendar missing → put proposed times in the email and skip the hold. @zapier missing → output the CRM note as a formatted block to paste into the CRM, plus a proposed Zap config to automate next time. @slack missing → output the deal-channel update as text. Each step degrades independently; finish the chain.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n**Step 0 - Gmail: read the thread state (read-only).**\n- Search for existing threads with this lead. Establish: last message, from whom, when, and any commitments either side made (\"I'll send pricing Friday\"). Cold outreach with no thread → note it and check sent mail for previous attempts (a third cold email needs a different angle than a first).\n- **Checkpoint 0:** one-paragraph thread state - \"Last contact <date>: they asked X; you owe them Y.\" User confirms before anything is drafted. This is what prevents the classic disaster: a \"just checking in\" email to someone who already replied.\n\n**Step 1 - Gmail: draft the outreach/reply.**\n- Transform: draft in the user's voice (mirroring their earlier messages in the thread): reference the real last exchange, deliver anything owed, and drive the ONE next step with concrete meeting slots.\n- Write: save as a Gmail **draft** on the existing thread (or new thread for cold outreach).\n- **Checkpoint 1 (approval gate):** show To/Subject/full body. `apply` = save draft; sending requires the user to explicitly say send.\n\n**Step 2 - Calendar: hold the meeting slot.**\n- Read: check the proposed slots against the user's calendar for conflicts.\n- Write: once the lead accepts a time (or immediately, as a tentative hold, if the user prefers), create the event with the lead invited, agenda in the description, and video link.\n- **Checkpoint 2:** show the event details table; create on `apply`. Inviting the lead notifies them - call that out explicitly.\n\n**Step 3 - Zapier: log to CRM.**\n- What the AI does: builds the payload - contact identifier, interaction summary (2-3 sentences from the thread state), stage signal, next step + date - and fires the user's CRM-logging Zap after approval. What the Zap does: the actual CRM write happens inside Zapier.\n- The AI cannot build Zaps: if no logging Zap exists, propose the config (trigger + CRM action + field mapping) and deliver the note as paste-ready text this time.\n- **Checkpoint 3:** payload review table; fire on `apply`; report the Zapier response honestly (trigger accepted ≠ record confirmed).\n\n**Step 4 - Follow-up insurance.**\n- Write (proposed together): a calendar reminder N business days out - \"Follow up with <lead> if no reply (thread link)\" - and an optional Slack post to the deal channel: lead, stage, what was sent, next step + date.\n- **Checkpoint 4:** show both; apply the ones approved. If a follow-up reminder for this lead already exists from a previous run, move it rather than stacking a second one.\n\n## Output format\n\n1. **Thread state** (step 0) - where the conversation stands, cited to the last messages\n2. **Email draft** (step 1) - To / Subject / body - awaiting apply\n3. **Event table** (step 2) - when / attendees / agenda - awaiting apply\n4. **CRM payload table** (step 3) - field → value, Zap name - awaiting apply\n5. **Follow-up pair** (step 4) - reminder + Slack update - awaiting apply\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 0 Thread state | Gmail | Confirmed summary | done |\n| 1 Outreach | Gmail | Draft saved | done / awaiting apply |\n| 2 Meeting | Calendar | Event / hold | done / pending lead |\n| 3 CRM log | Zapier | Zap fired / note text | done / fallback |\n| 4 Follow-up | Calendar + Slack | Reminder + channel update | done |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the real thread before drafting - every claim in the email (\"as you mentioned\") must exist in the actual correspondence\n- Check for an existing follow-up reminder for this lead before creating one; move it, don't stack duplicates\n- Keep one consistent next-step date across email, CRM payload, reminder, and Slack post\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't draft (step 1) before the thread state is confirmed at checkpoint 0 - emailing someone who already answered is how deals die\n- Don't send email or calendar invites without explicit approval - both notify the lead immediately; drafts and holds are the default\n- Don't fire the CRM Zap twice for the same interaction on a re-run - ask whether this touchpoint was already logged\n- Don't put internal candor (deal odds, \"they're price-shopping us\") in anything the lead can see; that tone belongs only in Slack\n- Don't invent thread contents, Zap names, or channel history without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Thread state confirmed before any drafting\n- [ ] External-facing writes (email, invite) individually approved; internal ones batched\n- [ ] Next-step date identical across all four artifacts\n- [ ] Chain summary table shows steps 0-4 with real statuses, including fallbacks\n- [ ] Required integrations (@gmail, @google-calendar, @zapier, @slack) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /sales-closed-loop or describe the task from the skill description.", "skillContent": "---\nname: sales-closed-loop\ndescription: >\n  Lead email → meeting → CRM note → follow-up. Requires integrations: @gmail, @google-calendar, @zapier, @slack. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"sales closed loop\", /sales-closed-loop.\nmetadata:\n  category: combo\n  type: combo\n  cadence: on-demand\n  risk: medium\n  connectors: gmail,google-calendar,zapier,slack\n  integrations: @gmail,@google-calendar,@zapier,@slack\nallowed-tools: search_tool, use_tool\n---\n# /sales-closed-loop\n\nLead email → meeting → CRM note → follow-up.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this to move ONE lead through the next stage without anything falling through: draft the outreach or reply in Gmail grounded in the actual thread history, get the meeting on the calendar, push the interaction into the CRM via the user's Zapier workflow, and set the follow-up (calendar reminder + optional Slack nudge to the deal channel). Run it each time the lead advances - the skill reads the thread first, so it always knows where the conversation actually stands.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The lead: name, email, company - and whether this is cold outreach or an active thread\n- What the user is selling into this conversation and the ONE next step the email should drive (meeting? proposal review?)\n- Meeting parameters: length, format, 2-3 available slots (or permission to read the calendar for free slots)\n- The CRM-logging Zap's name and expected payload fields; which Slack channel tracks deals\n- Follow-up rule: how many days of silence before the nudge (default 3 business days)\n\n## Tone & voice\n\n- **Tone:** Multi-step orchestration. Checkpoint after each phase. Outreach text in the user's voice - mirror their previous sent emails in the thread, not generic sales-speak.\n- **Audience:** Email - the lead (external, high stakes); Slack - the internal deal channel (candid, brief).\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n- **@slack** - Slack\n  - MCP server key: `slack`\n  - Example tools: `slack__list_channels`, `slack__get_channel_history`, `slack__post_message`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** @gmail missing → deliver the outreach as copy-paste text. @google-calendar missing → put proposed times in the email and skip the hold. @zapier missing → output the CRM note as a formatted block to paste into the CRM, plus a proposed Zap config to automate next time. @slack missing → output the deal-channel update as text. Each step degrades independently; finish the chain.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n\n## Workflow\n\n**Step 0 - Gmail: read the thread state (read-only).**\n- Search for existing threads with this lead. Establish: last message, from whom, when, and any commitments either side made (\"I'll send pricing Friday\"). Cold outreach with no thread → note it and check sent mail for previous attempts (a third cold email needs a different angle than a first).\n- **Checkpoint 0:** one-paragraph thread state - \"Last contact <date>: they asked X; you owe them Y.\" User confirms before anything is drafted. This is what prevents the classic disaster: a \"just checking in\" email to someone who already replied.\n\n**Step 1 - Gmail: draft the outreach/reply.**\n- Transform: draft in the user's voice (mirroring their earlier messages in the thread): reference the real last exchange, deliver anything owed, and drive the ONE next step with concrete meeting slots.\n- Write: save as a Gmail **draft** on the existing thread (or new thread for cold outreach).\n- **Checkpoint 1 (approval gate):** show To/Subject/full body. `apply` = save draft; sending requires the user to explicitly say send.\n\n**Step 2 - Calendar: hold the meeting slot.**\n- Read: check the proposed slots against the user's calendar for conflicts.\n- Write: once the lead accepts a time (or immediately, as a tentative hold, if the user prefers), create the event with the lead invited, agenda in the description, and video link.\n- **Checkpoint 2:** show the event details table; create on `apply`. Inviting the lead notifies them - call that out explicitly.\n\n**Step 3 - Zapier: log to CRM.**\n- What the AI does: builds the payload - contact identifier, interaction summary (2-3 sentences from the thread state), stage signal, next step + date - and fires the user's CRM-logging Zap after approval. What the Zap does: the actual CRM write happens inside Zapier.\n- The AI cannot build Zaps: if no logging Zap exists, propose the config (trigger + CRM action + field mapping) and deliver the note as paste-ready text this time.\n- **Checkpoint 3:** payload review table; fire on `apply`; report the Zapier response honestly (trigger accepted ≠ record confirmed).\n\n**Step 4 - Follow-up insurance.**\n- Write (proposed together): a calendar reminder N business days out - \"Follow up with <lead> if no reply (thread link)\" - and an optional Slack post to the deal channel: lead, stage, what was sent, next step + date.\n- **Checkpoint 4:** show both; apply the ones approved. If a follow-up reminder for this lead already exists from a previous run, move it rather than stacking a second one.\n\n## Output format\n\n1. **Thread state** (step 0) - where the conversation stands, cited to the last messages\n2. **Email draft** (step 1) - To / Subject / body - awaiting apply\n3. **Event table** (step 2) - when / attendees / agenda - awaiting apply\n4. **CRM payload table** (step 3) - field → value, Zap name - awaiting apply\n5. **Follow-up pair** (step 4) - reminder + Slack update - awaiting apply\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 0 Thread state | Gmail | Confirmed summary | done |\n| 1 Outreach | Gmail | Draft saved | done / awaiting apply |\n| 2 Meeting | Calendar | Event / hold | done / pending lead |\n| 3 CRM log | Zapier | Zap fired / note text | done / fallback |\n| 4 Follow-up | Calendar + Slack | Reminder + channel update | done |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Read the real thread before drafting - every claim in the email (\"as you mentioned\") must exist in the actual correspondence\n- Check for an existing follow-up reminder for this lead before creating one; move it, don't stack duplicates\n- Keep one consistent next-step date across email, CRM payload, reminder, and Slack post\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't draft (step 1) before the thread state is confirmed at checkpoint 0 - emailing someone who already answered is how deals die\n- Don't send email or calendar invites without explicit approval - both notify the lead immediately; drafts and holds are the default\n- Don't fire the CRM Zap twice for the same interaction on a re-run - ask whether this touchpoint was already logged\n- Don't put internal candor (deal odds, \"they're price-shopping us\") in anything the lead can see; that tone belongs only in Slack\n- Don't invent thread contents, Zap names, or channel history without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Thread state confirmed before any drafting\n- [ ] External-facing writes (email, invite) individually approved; internal ones batched\n- [ ] Next-step date identical across all four artifacts\n- [ ] Chain summary table shows steps 0-4 with real statuses, including fallbacks\n- [ ] Required integrations (@gmail, @google-calendar, @zapier, @slack) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "weekly-ceo-brief", "aiInstallPrompt": "Install the skill 'weekly-ceo-brief' by writing the file below.\n\nUniversal path: ./skills/weekly-ceo-brief/SKILL.md\nGrok path: ~/.grok/skills/weekly-ceo-brief/SKILL.md\nCursor path: ~/.cursor/skills/weekly-ceo-brief/SKILL.md\nClaude path: ~/.claude/skills/weekly-ceo-brief/SKILL.md\nVS Code agent path: .github/skills/weekly-ceo-brief/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: weekly-ceo-brief\ndescription: >\n  Monday brief from mail, calendar, Drive, Notion. Requires integrations: @gmail, @google-calendar, @google-drive, @notion. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"weekly ceo brief\", /weekly-ceo-brief.\nmetadata:\n  category: combo\n  type: combo\n  cadence: weekly\n  risk: low\n  connectors: gmail,google-calendar,google-drive,notion\n  integrations: @gmail,@google-calendar,@google-drive,@notion\nallowed-tools: search_tool, use_tool\n---\n# /weekly-ceo-brief\n\nMonday brief from mail, calendar, Drive, Notion.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this Monday morning (or Sunday night) to compress four surfaces into one page a founder/CEO reads in five minutes: what moved in email over the weekend that needs a decision, the week's calendar shape, documents that changed in Drive, and goal/project status from Notion. It is a synthesis, not four separate reports - the value is in the cross-references (\"board deck due Thursday per Notion, but no prep block on your calendar\").\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Lookback window (default: since Friday 5pm) and the week ahead (default: Mon-Fri)\n- VIP senders whose email always surfaces (investors, board, key customers, direct reports)\n- Which Notion databases hold goals/OKRs and project status\n- Drive folders that matter (board materials, finance, product specs) vs noise to skip\n- Delivery: in chat only (default), or also saved to a Notion \"briefs\" page after approval\n\n## Tone & voice\n\n- **Tone:** Executive compression. Every line earns its place; lead with what changed and what needs the CEO specifically. Flags over prose: 🔴 needs decision · 🟡 watch · 🟢 FYI.\n- **Audience:** A CEO between meetings - one page, no scrolling regret.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** this is a read-only synthesis - build the brief from whichever of the four sources respond and mark the missing lane in the brief itself (\"Notion unavailable - goal status not covered this week\"). A three-source brief on time beats a four-source brief that never ships.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nFour read-only collection passes, then a synthesis pass that does the actual thinking. The run is read-only except the optional final save.\n\n**Pass 1 - Gmail: signals.**\n- Read: since the lookback cutoff - VIP-sender threads, anything with a direct question to the user, and threads where the user is the bottleneck (last message waits on them). Extract: who, one-line ask, age, and a suggested disposition (reply today / delegate / can wait).\n\n**Pass 2 - Calendar: the week's shape.**\n- Read: Mon-Fri events. Extract: total meeting hours vs open hours, the 2-3 highest-stakes meetings (external, board, all-hands), conflicts or missing details, and the largest uninterrupted focus blocks.\n\n**Pass 3 - Drive: document movement.**\n- Read: files modified in the window inside the watched folders. Extract: file, who edited, and why it matters in one line (e.g., \"Board deck v4 - CFO edited the forecast tab\"). Skip mechanical changes.\n\n**Pass 4 - Notion: goals & projects.**\n- Read: the goals/OKR and project-status databases. Extract: items due this week, anything that changed status (esp. → at-risk/blocked), and stale items (no update in 14+ days) worth a nudge.\n\n**Synthesis - the brief (this is the product).**\n- Cross-reference the four lanes; every connection is worth ten raw items: deadlines in Notion with no matching calendar time, VIP emails about meetings on this week's calendar, at-risk projects whose owner the CEO meets this week anyway.\n- Rank ruthlessly: max 3 items in \"Needs you\", max 5 in \"Watch\". Everything else is FYI or cut.\n- **Checkpoint:** deliver the brief in chat. If the user wants it archived, propose the Notion save (page preview → `apply`) - the only write this skill makes.\n\n## Output format\n\n**📋 Monday Brief - <date>**\n\n1. **Needs you (🔴 max 3)** - decision/reply, who's waiting, one-line context + source link\n2. **This week's shape** - meeting load, top-stakes meetings, biggest focus block, conflicts\n3. **Watch (🟡 max 5)** - cross-referenced risks (\"X due Thu, no prep time scheduled\")\n4. **Moved since Friday (🟢)** - docs edited, projects changed, notable-but-fine email\n5. **One suggestion** - the single highest-leverage calendar/attention change this week\n\nThen the **chain summary table**:\n\n| Pass | App | Scanned | Surfaced | Status |\n|---|---|---|---|---|\n| 1 Mail | Gmail | N threads | K items | done |\n| 2 Week | Calendar | E events | conflicts: C | done |\n| 3 Docs | Drive | F files | D notable | done / skipped |\n| 4 Goals | Notion | P projects | R at-risk | done / skipped |\n| Save | Notion | - | brief archived | applied / not requested |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Spend the effort on cross-references between lanes - that synthesis is why this beats reading four apps\n- Enforce the caps (3 red, 5 yellow); a brief that flags everything flags nothing\n- Link every item to its source (thread, event, file, Notion page) so acting takes one click\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't pad the brief to look thorough - a quiet week honestly reported as quiet builds trust in the loud weeks\n- Don't mark something 🔴 unless it genuinely needs the CEO this week; inflation kills the signal by week three\n- Don't repeat items the user already dismissed in last week's brief unless something changed - check the archived brief if one exists\n- Don't reply to emails, move meetings, or edit Notion - this skill reads and reports; the only write is the approved brief archive\n- Don't invent mail, calendar, file, or Notion contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Brief fits one page; caps respected (≤3 red, ≤5 yellow)\n- [ ] At least one cross-lane connection surfaced (or noted that none exist this week)\n- [ ] Every item links to its source; skipped lanes marked in the brief and the table\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@gmail, @google-calendar, @google-drive, @notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /weekly-ceo-brief`\n```\n\nAfter writing the file, confirm it exists. The user can then run /weekly-ceo-brief or describe the task from the skill description.", "skillContent": "---\nname: weekly-ceo-brief\ndescription: >\n  Monday brief from mail, calendar, Drive, Notion. Requires integrations: @gmail, @google-calendar, @google-drive, @notion. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"weekly ceo brief\", /weekly-ceo-brief.\nmetadata:\n  category: combo\n  type: combo\n  cadence: weekly\n  risk: low\n  connectors: gmail,google-calendar,google-drive,notion\n  integrations: @gmail,@google-calendar,@google-drive,@notion\nallowed-tools: search_tool, use_tool\n---\n# /weekly-ceo-brief\n\nMonday brief from mail, calendar, Drive, Notion.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this Monday morning (or Sunday night) to compress four surfaces into one page a founder/CEO reads in five minutes: what moved in email over the weekend that needs a decision, the week's calendar shape, documents that changed in Drive, and goal/project status from Notion. It is a synthesis, not four separate reports - the value is in the cross-references (\"board deck due Thursday per Notion, but no prep block on your calendar\").\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Lookback window (default: since Friday 5pm) and the week ahead (default: Mon-Fri)\n- VIP senders whose email always surfaces (investors, board, key customers, direct reports)\n- Which Notion databases hold goals/OKRs and project status\n- Drive folders that matter (board materials, finance, product specs) vs noise to skip\n- Delivery: in chat only (default), or also saved to a Notion \"briefs\" page after approval\n\n## Tone & voice\n\n- **Tone:** Executive compression. Every line earns its place; lead with what changed and what needs the CEO specifically. Flags over prose: 🔴 needs decision · 🟡 watch · 🟢 FYI.\n- **Audience:** A CEO between meetings - one page, no scrolling regret.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@gmail** - Gmail\n  - MCP server key: `gmail`\n  - Example tools: `gmail__search_threads`, `gmail__get_thread`, `gmail__create_draft`\n  Setup: enable this app in your AI tool's connector/integration settings and sign in when prompted.\n\n- **@google-calendar** - Google Calendar\n  - MCP server key: `google-calendar`\n  - Example tools: `google-calendar__list_events`, `google-calendar__create_event`, `google-calendar__suggest_time`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks for this combo:** this is a read-only synthesis - build the brief from whichever of the four sources respond and mark the missing lane in the brief itself (\"Notion unavailable - goal status not covered this week\"). A three-source brief on time beats a four-source brief that never ships.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\nFour read-only collection passes, then a synthesis pass that does the actual thinking. The run is read-only except the optional final save.\n\n**Pass 1 - Gmail: signals.**\n- Read: since the lookback cutoff - VIP-sender threads, anything with a direct question to the user, and threads where the user is the bottleneck (last message waits on them). Extract: who, one-line ask, age, and a suggested disposition (reply today / delegate / can wait).\n\n**Pass 2 - Calendar: the week's shape.**\n- Read: Mon-Fri events. Extract: total meeting hours vs open hours, the 2-3 highest-stakes meetings (external, board, all-hands), conflicts or missing details, and the largest uninterrupted focus blocks.\n\n**Pass 3 - Drive: document movement.**\n- Read: files modified in the window inside the watched folders. Extract: file, who edited, and why it matters in one line (e.g., \"Board deck v4 - CFO edited the forecast tab\"). Skip mechanical changes.\n\n**Pass 4 - Notion: goals & projects.**\n- Read: the goals/OKR and project-status databases. Extract: items due this week, anything that changed status (esp. → at-risk/blocked), and stale items (no update in 14+ days) worth a nudge.\n\n**Synthesis - the brief (this is the product).**\n- Cross-reference the four lanes; every connection is worth ten raw items: deadlines in Notion with no matching calendar time, VIP emails about meetings on this week's calendar, at-risk projects whose owner the CEO meets this week anyway.\n- Rank ruthlessly: max 3 items in \"Needs you\", max 5 in \"Watch\". Everything else is FYI or cut.\n- **Checkpoint:** deliver the brief in chat. If the user wants it archived, propose the Notion save (page preview → `apply`) - the only write this skill makes.\n\n## Output format\n\n**📋 Monday Brief - <date>**\n\n1. **Needs you (🔴 max 3)** - decision/reply, who's waiting, one-line context + source link\n2. **This week's shape** - meeting load, top-stakes meetings, biggest focus block, conflicts\n3. **Watch (🟡 max 5)** - cross-referenced risks (\"X due Thu, no prep time scheduled\")\n4. **Moved since Friday (🟢)** - docs edited, projects changed, notable-but-fine email\n5. **One suggestion** - the single highest-leverage calendar/attention change this week\n\nThen the **chain summary table**:\n\n| Pass | App | Scanned | Surfaced | Status |\n|---|---|---|---|---|\n| 1 Mail | Gmail | N threads | K items | done |\n| 2 Week | Calendar | E events | conflicts: C | done |\n| 3 Docs | Drive | F files | D notable | done / skipped |\n| 4 Goals | Notion | P projects | R at-risk | done / skipped |\n| Save | Notion | - | brief archived | applied / not requested |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Spend the effort on cross-references between lanes - that synthesis is why this beats reading four apps\n- Enforce the caps (3 red, 5 yellow); a brief that flags everything flags nothing\n- Link every item to its source (thread, event, file, Notion page) so acting takes one click\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't pad the brief to look thorough - a quiet week honestly reported as quiet builds trust in the loud weeks\n- Don't mark something 🔴 unless it genuinely needs the CEO this week; inflation kills the signal by week three\n- Don't repeat items the user already dismissed in last week's brief unless something changed - check the archived brief if one exists\n- Don't reply to emails, move meetings, or edit Notion - this skill reads and reports; the only write is the approved brief archive\n- Don't invent mail, calendar, file, or Notion contents without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Brief fits one page; caps respected (≤3 red, ≤5 yellow)\n- [ ] At least one cross-lane connection surfaced (or noted that none exist this week)\n- [ ] Every item links to its source; skipped lanes marked in the brief and the table\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@gmail, @google-calendar, @google-drive, @notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /weekly-ceo-brief`\n"}]};

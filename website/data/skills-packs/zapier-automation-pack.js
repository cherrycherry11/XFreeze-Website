window.__XF_SKILL_PACKS__=window.__XF_SKILL_PACKS__||{};window.__XF_SKILL_PACKS__["zapier-automation-pack"]={"id": "zapier-automation-pack", "skills": [{"id": "zap-airtable-crm-update", "aiInstallPrompt": "Install the skill 'zap-airtable-crm-update' by writing the file below.\n\nUniversal path: ./skills/zap-airtable-crm-update/SKILL.md\nGrok path: ~/.grok/skills/zap-airtable-crm-update/SKILL.md\nCursor path: ~/.cursor/skills/zap-airtable-crm-update/SKILL.md\nClaude path: ~/.claude/skills/zap-airtable-crm-update/SKILL.md\nVS Code agent path: .github/skills/zap-airtable-crm-update/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-airtable-crm-update\ndescription: >\n  Update Airtable CRM from meeting notes via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap airtable crm update\", /zap-airtable-crm-update.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-airtable-crm-update\n\nUpdate Airtable CRM from meeting notes via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this after a customer or prospect meeting when the user has raw notes (pasted text, a transcript, or bullet points) and wants their Airtable CRM record updated without opening Airtable: extract the structured fields from the notes, then fire the user's Airtable-update Zap with a clean payload. Not for bulk imports or creating a new CRM base - this is one meeting → one or a few record updates.\n\n## What the AI does vs what the Zap does\n\n- **The AI** parses the notes, maps them to CRM fields, builds the trigger payload, and (after approval) fires an *existing* Zap through the Zapier connector.\n- **The Zap** performs the actual Airtable write (find record → update fields / create row) inside Zapier.\n- The AI **cannot build or edit Zaps** in the Zapier editor. If the user has no Airtable-update Zap yet, the AI proposes a configuration (trigger type, Airtable base/table, field mapping) for the user to create, then this skill can fire it on future runs.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The meeting notes (pasted text, transcript, or file contents)\n- Which company/contact/deal the notes belong to - the Zap needs a lookup key (record name, email, or record ID)\n- The name of the Airtable-update Zap and what payload fields it expects (ask once, then remember for the session)\n- Which fields to update vs append (e.g., overwrite \"Stage\", append to \"Notes log\")\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Name the Zap and confirm the trigger payload before firing. Terse, field-oriented.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier stays unavailable, still deliver value - output the parsed CRM field table so the user can paste values into Airtable by hand, plus a proposed Zap configuration to automate this next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n**Step 1 - Parse the meeting notes (AI, no connector).**\n- Extract: contact/company name, deal stage signal (e.g., \"sending contract Friday\" → Stage: Contract sent), next step + owner + due date, budget/pricing mentions, objections/risks, and a 2-3 sentence meeting summary for the notes log.\n- Mark anything inferred rather than stated as *(inferred)* - deal-stage changes are recommendations, not facts.\n- **Checkpoint:** show the extracted fields as a table (Field → Current guess → Source line from the notes). Ask the user to correct before anything is sent.\n\n**Step 2 - Locate the Zap (Zapier connector, read-only).**\n- List the user's Zaps (discover the exact tool name in your session) and find the Airtable-update Zap by name. If multiple candidates, show them and ask which one.\n- If none exists: skip to the fallback - propose a Zap config (trigger: webhook/AI trigger; action: Airtable \"Find record\" by lookup key + \"Update record\"; field mapping table) and stop.\n\n**Step 3 - Fire the Zap (Zapier connector, write).**\n- Build the exact trigger payload matching the Zap's expected fields (lookup key + field values from the approved table).\n- **Approval gate:** show the payload as a review table - Zap name, lookup key, each field → new value, overwrite vs append. Fire only after explicit `apply`.\n- After firing: report the Zapier response (accepted / errored). Note honestly that the Airtable write happens asynchronously inside Zapier - if the connector returns a run status or record link, show it; otherwise tell the user to spot-check the record.\n\n**Step 4 - Confirm.**\n- If a second Zapier call can verify the run (e.g., a run-history tool exists in the session), check it. Otherwise ask the user to confirm the record looks right, and offer to re-fire with corrections if not.\n\n## Output format\n\n1. **Extracted fields table** (step 1) - Field / Value / Source line / stated-vs-inferred\n2. **Zap match** (step 2) - Zap name + what payload it expects (or the proposed Zap config if none exists)\n3. **Trigger payload review table** (step 3) - awaiting `apply`\n4. **Run result** - Zapier response + record link if available\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Parse notes | AI | Field table | approved |\n| 2 Find Zap | Zapier | Zap identified | done |\n| 3 Fire Zap | Zapier | Payload sent | done / awaiting apply |\n| 4 Confirm | Zapier/user | Record verified | done / manual check |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Always include a lookup key in the payload - a Zap that can't find the record either fails silently or creates a duplicate row\n- Distinguish overwrite fields (Stage, Next step) from append fields (Notes log) in the review table\n- Quote the source line from the notes for every extracted value so the user can verify fast\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire the Zap before the user approves both the extracted fields AND the payload - a wrong stage update pollutes pipeline reporting\n- Don't re-fire on a re-run of the same meeting notes without warning - the Zap may append the meeting summary twice\n- Don't claim the Airtable record was updated when only the trigger was accepted; the write happens inside Zapier\n- Don't invent meeting content, Zap names, or run results without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Extracted fields shown with source lines and inferred values flagged\n- [ ] Payload review table shown and `apply` received before firing\n- [ ] Chain summary table reflects actual statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-airtable-crm-update or describe the task from the skill description.", "skillContent": "---\nname: zap-airtable-crm-update\ndescription: >\n  Update Airtable CRM from meeting notes via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap airtable crm update\", /zap-airtable-crm-update.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-airtable-crm-update\n\nUpdate Airtable CRM from meeting notes via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this after a customer or prospect meeting when the user has raw notes (pasted text, a transcript, or bullet points) and wants their Airtable CRM record updated without opening Airtable: extract the structured fields from the notes, then fire the user's Airtable-update Zap with a clean payload. Not for bulk imports or creating a new CRM base - this is one meeting → one or a few record updates.\n\n## What the AI does vs what the Zap does\n\n- **The AI** parses the notes, maps them to CRM fields, builds the trigger payload, and (after approval) fires an *existing* Zap through the Zapier connector.\n- **The Zap** performs the actual Airtable write (find record → update fields / create row) inside Zapier.\n- The AI **cannot build or edit Zaps** in the Zapier editor. If the user has no Airtable-update Zap yet, the AI proposes a configuration (trigger type, Airtable base/table, field mapping) for the user to create, then this skill can fire it on future runs.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The meeting notes (pasted text, transcript, or file contents)\n- Which company/contact/deal the notes belong to - the Zap needs a lookup key (record name, email, or record ID)\n- The name of the Airtable-update Zap and what payload fields it expects (ask once, then remember for the session)\n- Which fields to update vs append (e.g., overwrite \"Stage\", append to \"Notes log\")\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Name the Zap and confirm the trigger payload before firing. Terse, field-oriented.\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier stays unavailable, still deliver value - output the parsed CRM field table so the user can paste values into Airtable by hand, plus a proposed Zap configuration to automate this next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n**Step 1 - Parse the meeting notes (AI, no connector).**\n- Extract: contact/company name, deal stage signal (e.g., \"sending contract Friday\" → Stage: Contract sent), next step + owner + due date, budget/pricing mentions, objections/risks, and a 2-3 sentence meeting summary for the notes log.\n- Mark anything inferred rather than stated as *(inferred)* - deal-stage changes are recommendations, not facts.\n- **Checkpoint:** show the extracted fields as a table (Field → Current guess → Source line from the notes). Ask the user to correct before anything is sent.\n\n**Step 2 - Locate the Zap (Zapier connector, read-only).**\n- List the user's Zaps (discover the exact tool name in your session) and find the Airtable-update Zap by name. If multiple candidates, show them and ask which one.\n- If none exists: skip to the fallback - propose a Zap config (trigger: webhook/AI trigger; action: Airtable \"Find record\" by lookup key + \"Update record\"; field mapping table) and stop.\n\n**Step 3 - Fire the Zap (Zapier connector, write).**\n- Build the exact trigger payload matching the Zap's expected fields (lookup key + field values from the approved table).\n- **Approval gate:** show the payload as a review table - Zap name, lookup key, each field → new value, overwrite vs append. Fire only after explicit `apply`.\n- After firing: report the Zapier response (accepted / errored). Note honestly that the Airtable write happens asynchronously inside Zapier - if the connector returns a run status or record link, show it; otherwise tell the user to spot-check the record.\n\n**Step 4 - Confirm.**\n- If a second Zapier call can verify the run (e.g., a run-history tool exists in the session), check it. Otherwise ask the user to confirm the record looks right, and offer to re-fire with corrections if not.\n\n## Output format\n\n1. **Extracted fields table** (step 1) - Field / Value / Source line / stated-vs-inferred\n2. **Zap match** (step 2) - Zap name + what payload it expects (or the proposed Zap config if none exists)\n3. **Trigger payload review table** (step 3) - awaiting `apply`\n4. **Run result** - Zapier response + record link if available\n\nFinish with a **chain summary table**:\n\n| Step | App | Produced | Status |\n|---|---|---|---|\n| 1 Parse notes | AI | Field table | approved |\n| 2 Find Zap | Zapier | Zap identified | done |\n| 3 Fire Zap | Zapier | Payload sent | done / awaiting apply |\n| 4 Confirm | Zapier/user | Record verified | done / manual check |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Always include a lookup key in the payload - a Zap that can't find the record either fails silently or creates a duplicate row\n- Distinguish overwrite fields (Stage, Next step) from append fields (Notes log) in the review table\n- Quote the source line from the notes for every extracted value so the user can verify fast\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire the Zap before the user approves both the extracted fields AND the payload - a wrong stage update pollutes pipeline reporting\n- Don't re-fire on a re-run of the same meeting notes without warning - the Zap may append the meeting summary twice\n- Don't claim the Airtable record was updated when only the trigger was accepted; the write happens inside Zapier\n- Don't invent meeting content, Zap names, or run results without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Extracted fields shown with source lines and inferred values flagged\n- [ ] Payload review table shown and `apply` received before firing\n- [ ] Chain summary table reflects actual statuses, including fallbacks\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "zap-custom-workflow", "aiInstallPrompt": "Install the skill 'zap-custom-workflow' by writing the file below.\n\nUniversal path: ./skills/zap-custom-workflow/SKILL.md\nGrok path: ~/.grok/skills/zap-custom-workflow/SKILL.md\nCursor path: ~/.cursor/skills/zap-custom-workflow/SKILL.md\nClaude path: ~/.claude/skills/zap-custom-workflow/SKILL.md\nVS Code agent path: .github/skills/zap-custom-workflow/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-custom-workflow\ndescription: >\n  Run user's existing Zapier workflow on command. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap custom workflow\", /zap-custom-workflow.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-custom-workflow\n\nRun user's existing Zapier workflow on command.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as the generic \"run my Zap\" command when no purpose-specific skill fits: the user has an existing Zapier workflow (any trigger, any destination apps) and wants to fire it from chat with a specific payload - \"run my invoice Zap for client X\", \"trigger the weekly-report workflow now\". The value over clicking in Zapier: the AI helps assemble a correct, complete payload and keeps a record of what was sent.\n\n## What the AI does vs what the Zap does\n\n- **The AI** finds the right Zap by name, asks for/assembles the payload fields the Zap expects, shows the payload for approval, fires the trigger, and reports the response.\n- **The Zap** does everything downstream - whatever apps and steps the user built into it run inside Zapier, invisible to this chat.\n- The AI **cannot create, edit, or debug Zaps** in the Zapier editor. If the requested workflow doesn't exist, it can propose a Zap design (trigger + steps + field mapping) for the user to build - and can then run it on future invocations.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which Zap to run - a name or close description (\"the one that files invoices\")\n- The payload values for this run - after step 1 reveals which fields the Zap expects\n- Whether the Zap has side effects the user should be reminded of (sends email? posts publicly? charges anything?) - ask once and remember for the session\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Name the Zap. Confirm the trigger payload field-by-field before firing. Precise about what is known (trigger accepted) vs assumed (downstream steps ran).\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** with no @zapier access there is nothing to fire - but still help: capture the intended Zap + payload as a ready-to-use block, and offer a Zap design if the workflow doesn't exist yet.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- A custom Zap's downstream side effects are invisible from chat - treat every unfamiliar Zap as if it sends/publishes until the user says otherwise.\n\n## Workflow\n\n**Step 1 - Identify the Zap.**\n- List available Zaps via the connector (discover the exact tool name in your session) and match the user's description. Multiple candidates → show them (name + trigger type if visible) and ask which. No match → offer a proposed Zap design (trigger, steps, field mapping) the user can build in Zapier, then stop.\n- Ask once: \"What does this Zap do downstream, and does it send/post/charge anything?\" - record the answer; it decides how strict the approval gate is.\n\n**Step 2 - Assemble the payload.**\n- Determine what fields the Zap's trigger expects (from the connector's metadata if exposed; otherwise from the user's description of the Zap).\n- Fill each field from the user's request; list every field with its value and mark anything defaulted or left empty. Empty required fields are a question, not a guess.\n\n**Step 3 - Approval gate and fire.**\n- Show the review table: Zap name, each field → value, known side effects. Fire only after explicit `apply`.\n- Report the connector's response verbatim in meaning: accepted / queued / error. Be explicit: \"Zapier accepted the trigger\" - the downstream steps run inside Zapier and are not confirmed from here unless a run-status tool exists in the session (if one does, check it and report).\n\n**Step 4 - Record.**\n- Close with the run record: timestamp, Zap, payload summary, response - so a re-run or a debugging session has something to reference. If the Zap seemed to fail (error response), suggest the user check Zap History in Zapier and offer to re-fire with corrected fields.\n\n## Output format\n\n1. **Zap identification** (step 1) - matched Zap name (or candidate list / proposed design)\n2. **Payload review table** (step 2-3) - field / value / source (user-provided, defaulted, empty) + side-effect note - awaiting `apply`\n3. **Trigger result** (step 3) - accepted / error, plus run-status if verifiable\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Identify | AI + Zapier | Zap matched | done |\n| 2 Payload | AI | N fields assembled | approved |\n| 3 Fire | Zapier | Trigger response | accepted / error |\n| 4 Downstream | Zap (inside Zapier) | User's workflow steps | not verifiable from chat / verified |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Ask what the Zap does downstream before first firing it - an innocuous name can hide a public post\n- Show every payload field, including defaults and blanks, in the review table\n- Keep the run record so repeated invocations are comparable\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire a Zap the user hasn't matched by name - \"probably that one\" is not identification\n- Don't claim downstream steps completed; only the trigger response is observable from chat\n- Don't guess values for empty required fields - a Zap fired with wrong data often can't be un-fired\n- Don't re-fire after an ambiguous response without checking whether the first run went through (duplicate side effects)\n- Don't invent Zap names, fields, or run results without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Zap identified by name and side effects asked about before firing\n- [ ] Payload review table shown and `apply` received before the trigger call\n- [ ] Result reported as trigger-accepted vs workflow-completed, honestly distinguished\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-custom-workflow or describe the task from the skill description.", "skillContent": "---\nname: zap-custom-workflow\ndescription: >\n  Run user's existing Zapier workflow on command. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap custom workflow\", /zap-custom-workflow.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-custom-workflow\n\nRun user's existing Zapier workflow on command.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as the generic \"run my Zap\" command when no purpose-specific skill fits: the user has an existing Zapier workflow (any trigger, any destination apps) and wants to fire it from chat with a specific payload - \"run my invoice Zap for client X\", \"trigger the weekly-report workflow now\". The value over clicking in Zapier: the AI helps assemble a correct, complete payload and keeps a record of what was sent.\n\n## What the AI does vs what the Zap does\n\n- **The AI** finds the right Zap by name, asks for/assembles the payload fields the Zap expects, shows the payload for approval, fires the trigger, and reports the response.\n- **The Zap** does everything downstream - whatever apps and steps the user built into it run inside Zapier, invisible to this chat.\n- The AI **cannot create, edit, or debug Zaps** in the Zapier editor. If the requested workflow doesn't exist, it can propose a Zap design (trigger + steps + field mapping) for the user to build - and can then run it on future invocations.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- Which Zap to run - a name or close description (\"the one that files invoices\")\n- The payload values for this run - after step 1 reveals which fields the Zap expects\n- Whether the Zap has side effects the user should be reminded of (sends email? posts publicly? charges anything?) - ask once and remember for the session\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Name the Zap. Confirm the trigger payload field-by-field before firing. Precise about what is known (trigger accepted) vs assumed (downstream steps ran).\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** with no @zapier access there is nothing to fire - but still help: capture the intended Zap + payload as a ready-to-use block, and offer a Zap design if the workflow doesn't exist yet.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- A custom Zap's downstream side effects are invisible from chat - treat every unfamiliar Zap as if it sends/publishes until the user says otherwise.\n\n## Workflow\n\n**Step 1 - Identify the Zap.**\n- List available Zaps via the connector (discover the exact tool name in your session) and match the user's description. Multiple candidates → show them (name + trigger type if visible) and ask which. No match → offer a proposed Zap design (trigger, steps, field mapping) the user can build in Zapier, then stop.\n- Ask once: \"What does this Zap do downstream, and does it send/post/charge anything?\" - record the answer; it decides how strict the approval gate is.\n\n**Step 2 - Assemble the payload.**\n- Determine what fields the Zap's trigger expects (from the connector's metadata if exposed; otherwise from the user's description of the Zap).\n- Fill each field from the user's request; list every field with its value and mark anything defaulted or left empty. Empty required fields are a question, not a guess.\n\n**Step 3 - Approval gate and fire.**\n- Show the review table: Zap name, each field → value, known side effects. Fire only after explicit `apply`.\n- Report the connector's response verbatim in meaning: accepted / queued / error. Be explicit: \"Zapier accepted the trigger\" - the downstream steps run inside Zapier and are not confirmed from here unless a run-status tool exists in the session (if one does, check it and report).\n\n**Step 4 - Record.**\n- Close with the run record: timestamp, Zap, payload summary, response - so a re-run or a debugging session has something to reference. If the Zap seemed to fail (error response), suggest the user check Zap History in Zapier and offer to re-fire with corrected fields.\n\n## Output format\n\n1. **Zap identification** (step 1) - matched Zap name (or candidate list / proposed design)\n2. **Payload review table** (step 2-3) - field / value / source (user-provided, defaulted, empty) + side-effect note - awaiting `apply`\n3. **Trigger result** (step 3) - accepted / error, plus run-status if verifiable\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Identify | AI + Zapier | Zap matched | done |\n| 2 Payload | AI | N fields assembled | approved |\n| 3 Fire | Zapier | Trigger response | accepted / error |\n| 4 Downstream | Zap (inside Zapier) | User's workflow steps | not verifiable from chat / verified |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Ask what the Zap does downstream before first firing it - an innocuous name can hide a public post\n- Show every payload field, including defaults and blanks, in the review table\n- Keep the run record so repeated invocations are comparable\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire a Zap the user hasn't matched by name - \"probably that one\" is not identification\n- Don't claim downstream steps completed; only the trigger response is observable from chat\n- Don't guess values for empty required fields - a Zap fired with wrong data often can't be un-fired\n- Don't re-fire after an ambiguous response without checking whether the first run went through (duplicate side effects)\n- Don't invent Zap names, fields, or run results without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Zap identified by name and side effects asked about before firing\n- [ ] Payload review table shown and `apply` received before the trigger call\n- [ ] Result reported as trigger-accepted vs workflow-completed, honestly distinguished\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "zap-dropbox-filer", "aiInstallPrompt": "Install the skill 'zap-dropbox-filer' by writing the file below.\n\nUniversal path: ./skills/zap-dropbox-filer/SKILL.md\nGrok path: ~/.grok/skills/zap-dropbox-filer/SKILL.md\nCursor path: ~/.cursor/skills/zap-dropbox-filer/SKILL.md\nClaude path: ~/.claude/skills/zap-dropbox-filer/SKILL.md\nVS Code agent path: .github/skills/zap-dropbox-filer/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-dropbox-filer\ndescription: >\n  Sort Dropbox uploads via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap dropbox filer\", /zap-dropbox-filer.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-dropbox-filer\n\nSort Dropbox uploads via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a Dropbox intake folder (typically an uploads/inbox folder that clients, scanners, or phone backups dump into) has accumulated unsorted files and the user wants them classified and moved into their folder structure via their Zapier organize workflow. The AI does the thinking (what is each file, where does it belong); the Zap does the moving.\n\n## What the AI does vs what the Zap does\n\n- **The AI** classifies each file by name/type/context into the user's folder taxonomy, produces the file → destination plan, and fires the organize Zap with that mapping after approval.\n- **The Zap** performs the actual Dropbox moves/renames inside Zapier.\n- The AI **cannot build Zaps**. If no organize Zap exists, it proposes one (e.g., trigger: \"New file in folder\" or webhook with file path + destination; action: Dropbox \"Move file\") for the user to create - and meanwhile delivers the sorting plan as a manual checklist.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The intake folder path (e.g., `/Uploads`) and the file list to sort - from the user, or via the Zapier connector if it exposes a Dropbox find/list action in this session\n- The destination taxonomy: the user's top-level folders and what belongs in each (e.g., `/Receipts/<year>`, `/Clients/<name>`, `/Media`)\n- Naming convention, if files should be renamed while filing (e.g., `YYYY-MM-DD_vendor_amount`)\n- The organize Zap's name and what payload it expects (file path + destination path per file, or batch)\n- What to do with unclassifiable files (leave in place + flag, or move to `/To sort manually`)\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Inventory-style precision - exact paths, no \"etc.\"\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier is unavailable, still produce the full sorting plan (file → destination table) as a manual checklist the user can execute in Dropbox, plus a proposed organize-Zap config to automate next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Moves are reversible in principle but painful at volume - the full move plan is ALWAYS approved before the Zap fires; this skill never proposes deletions.\n\n## Workflow\n\n**Step 1 - Get the file inventory.**\n- Obtain the intake folder's file list: via a Dropbox list/find action if the Zapier connector exposes one in this session, otherwise ask the user to paste the file list. Record name, extension, size/date if available.\n\n**Step 2 - Classify (AI, no writes).**\n- Sort each file into the user's taxonomy using filename signals (vendor names, dates, \"invoice\"/\"IMG_\"/\".sketch\"), extension, and anything the user said about expected content.\n- Assign a confidence per file: **high** (clear match), **low** (plausible guess - marked with the reason), **unclassifiable** (goes to the agreed catch-all, never force-fitted).\n- Apply the renaming convention where requested, showing old → new.\n- **Checkpoint:** show the full **move plan table** - # / file / → destination / new name / confidence. The user corrects low-confidence rows here; nothing has moved yet.\n\n**Step 3 - Fire the organize Zap.**\n- Build the payload the Zap expects (per-file path + destination, or the batch format the user's Zap takes).\n- **Approval gate:** confirm the final plan (with the user's corrections applied) - fire only after `apply`, `apply all`, or `apply 1,3` (partial approval fires only those rows if the Zap supports per-file triggering).\n- Report the trigger response honestly: accepted ≠ moved. The moves happen inside Zapier.\n\n**Step 4 - Verify.**\n- If the session has a way to re-list the folders (via the connector), spot-check that the intake folder emptied and destinations gained files; otherwise ask the user to glance at Dropbox and offer to re-fire corrections for any file that landed wrong.\n\n## Output format\n\n1. **Inventory** (step 1) - N files found in `<intake path>`\n2. **Move plan table** (step 2) - # / File / Destination / New name / Confidence - awaiting corrections + `apply`\n3. **Trigger result** (step 3) - Zap name, rows fired, Zapier response\n4. **Verification note** (step 4) - spot-check result or manual-check request\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Inventory | Zapier / user | N files listed | done |\n| 2 Classify | AI | Move plan, H high / L low / U unsorted | approved |\n| 3 Move | Zap (inside Zapier) | Trigger fired for M files | accepted / fallback |\n| 4 Verify | AI / user | Folders spot-checked | done / manual |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Give every file an explicit confidence level - the low-confidence rows are exactly where the user's 30 seconds of review pay off\n- Route unclassifiable files to the agreed catch-all folder rather than guessing a destination\n- Show old → new names when renaming; a bad rename convention applied to 80 files is a mess\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire the organize Zap on an unapproved plan - bulk moves to wrong folders take longer to undo than the sorting saved\n- Don't re-fire the whole plan on a re-run; re-inventory first, since already-moved files will be gone from intake\n- Don't propose deleting anything - this skill files, it never destroys\n- Don't claim files were moved when only the trigger was accepted\n- Don't invent file names or folder structures without a successful `use_tool` call (or the user's pasted list)\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every file in the inventory appears exactly once in the move plan\n- [ ] Plan approved (with corrections) before the Zap fired\n- [ ] Run summary distinguishes trigger-accepted from verified-moved\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-dropbox-filer or describe the task from the skill description.", "skillContent": "---\nname: zap-dropbox-filer\ndescription: >\n  Sort Dropbox uploads via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap dropbox filer\", /zap-dropbox-filer.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-dropbox-filer\n\nSort Dropbox uploads via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a Dropbox intake folder (typically an uploads/inbox folder that clients, scanners, or phone backups dump into) has accumulated unsorted files and the user wants them classified and moved into their folder structure via their Zapier organize workflow. The AI does the thinking (what is each file, where does it belong); the Zap does the moving.\n\n## What the AI does vs what the Zap does\n\n- **The AI** classifies each file by name/type/context into the user's folder taxonomy, produces the file → destination plan, and fires the organize Zap with that mapping after approval.\n- **The Zap** performs the actual Dropbox moves/renames inside Zapier.\n- The AI **cannot build Zaps**. If no organize Zap exists, it proposes one (e.g., trigger: \"New file in folder\" or webhook with file path + destination; action: Dropbox \"Move file\") for the user to create - and meanwhile delivers the sorting plan as a manual checklist.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The intake folder path (e.g., `/Uploads`) and the file list to sort - from the user, or via the Zapier connector if it exposes a Dropbox find/list action in this session\n- The destination taxonomy: the user's top-level folders and what belongs in each (e.g., `/Receipts/<year>`, `/Clients/<name>`, `/Media`)\n- Naming convention, if files should be renamed while filing (e.g., `YYYY-MM-DD_vendor_amount`)\n- The organize Zap's name and what payload it expects (file path + destination path per file, or batch)\n- What to do with unclassifiable files (leave in place + flag, or move to `/To sort manually`)\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Inventory-style precision - exact paths, no \"etc.\"\n- **Audience:** Infer from user context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier is unavailable, still produce the full sorting plan (file → destination table) as a manual checklist the user can execute in Dropbox, plus a proposed organize-Zap config to automate next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Moves are reversible in principle but painful at volume - the full move plan is ALWAYS approved before the Zap fires; this skill never proposes deletions.\n\n## Workflow\n\n**Step 1 - Get the file inventory.**\n- Obtain the intake folder's file list: via a Dropbox list/find action if the Zapier connector exposes one in this session, otherwise ask the user to paste the file list. Record name, extension, size/date if available.\n\n**Step 2 - Classify (AI, no writes).**\n- Sort each file into the user's taxonomy using filename signals (vendor names, dates, \"invoice\"/\"IMG_\"/\".sketch\"), extension, and anything the user said about expected content.\n- Assign a confidence per file: **high** (clear match), **low** (plausible guess - marked with the reason), **unclassifiable** (goes to the agreed catch-all, never force-fitted).\n- Apply the renaming convention where requested, showing old → new.\n- **Checkpoint:** show the full **move plan table** - # / file / → destination / new name / confidence. The user corrects low-confidence rows here; nothing has moved yet.\n\n**Step 3 - Fire the organize Zap.**\n- Build the payload the Zap expects (per-file path + destination, or the batch format the user's Zap takes).\n- **Approval gate:** confirm the final plan (with the user's corrections applied) - fire only after `apply`, `apply all`, or `apply 1,3` (partial approval fires only those rows if the Zap supports per-file triggering).\n- Report the trigger response honestly: accepted ≠ moved. The moves happen inside Zapier.\n\n**Step 4 - Verify.**\n- If the session has a way to re-list the folders (via the connector), spot-check that the intake folder emptied and destinations gained files; otherwise ask the user to glance at Dropbox and offer to re-fire corrections for any file that landed wrong.\n\n## Output format\n\n1. **Inventory** (step 1) - N files found in `<intake path>`\n2. **Move plan table** (step 2) - # / File / Destination / New name / Confidence - awaiting corrections + `apply`\n3. **Trigger result** (step 3) - Zap name, rows fired, Zapier response\n4. **Verification note** (step 4) - spot-check result or manual-check request\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Inventory | Zapier / user | N files listed | done |\n| 2 Classify | AI | Move plan, H high / L low / U unsorted | approved |\n| 3 Move | Zap (inside Zapier) | Trigger fired for M files | accepted / fallback |\n| 4 Verify | AI / user | Folders spot-checked | done / manual |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Give every file an explicit confidence level - the low-confidence rows are exactly where the user's 30 seconds of review pay off\n- Route unclassifiable files to the agreed catch-all folder rather than guessing a destination\n- Show old → new names when renaming; a bad rename convention applied to 80 files is a mess\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire the organize Zap on an unapproved plan - bulk moves to wrong folders take longer to undo than the sorting saved\n- Don't re-fire the whole plan on a re-run; re-inventory first, since already-moved files will be gone from intake\n- Don't propose deleting anything - this skill files, it never destroys\n- Don't claim files were moved when only the trigger was accepted\n- Don't invent file names or folder structures without a successful `use_tool` call (or the user's pasted list)\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every file in the inventory appears exactly once in the move plan\n- [ ] Plan approved (with corrections) before the Zap fired\n- [ ] Run summary distinguishes trigger-accepted from verified-moved\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "zap-google-forms-to-sheet", "aiInstallPrompt": "Install the skill 'zap-google-forms-to-sheet' by writing the file below.\n\nUniversal path: ./skills/zap-google-forms-to-sheet/SKILL.md\nGrok path: ~/.grok/skills/zap-google-forms-to-sheet/SKILL.md\nCursor path: ~/.cursor/skills/zap-google-forms-to-sheet/SKILL.md\nClaude path: ~/.claude/skills/zap-google-forms-to-sheet/SKILL.md\nVS Code agent path: .github/skills/zap-google-forms-to-sheet/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-google-forms-to-sheet\ndescription: >\n  Process new Google Forms responses. Requires integrations: @zapier, @google-drive. Uses search_tool + use_tool. Cadence: daily. Triggers: \"zap google forms to sheet\", /zap-google-forms-to-sheet.\nmetadata:\n  category: zapier\n  type: combo\n  cadence: daily\n  risk: low\n  connectors: zapier,google-drive\n  integrations: @zapier,@google-drive\nallowed-tools: search_tool, use_tool\n---\n# /zap-google-forms-to-sheet\n\nProcess new Google Forms responses.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as a daily check on a Google Form that feeds a response sheet (signups, support intake, survey, event RSVPs): read the new rows since the last run, summarize what came in, flag responses that need action (complaints, high-value leads, \"contact me\" requests), and route the actionable ones via the user's Zapier workflow. The Form→Sheet write itself is Google's native link (or the user's existing Zap) - this skill is the daily *processing* layer on top.\n\n## What the AI does vs what the Zap does\n\n- **The AI** reads the response sheet from Drive, digests the new rows, classifies which need routing, and fires the routing Zap (e.g., \"notify on hot lead\", \"create support case\") with an approved payload per flagged response.\n- **The Zap** performs the downstream routing inside Zapier (notification, CRM entry, task creation - whatever the user built).\n- The AI **cannot build Zaps**. If no routing Zap exists, it proposes the configuration (trigger + action + field mapping from sheet columns) and meanwhile delivers the flagged list so the user can act manually.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The response sheet (name/link in Drive) and which form it belongs to\n- What \"new\" means: since the last run's timestamp/row marker (preferred), or the last N days on first run\n- The flag rules: which answers make a response actionable (e.g., rating ≤ 2, budget over X, \"yes\" to contact, keyword in free text)\n- The routing Zap's name and expected payload per response type\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Digest-style: counts and patterns first, then the individual flags.\n- **Audience:** Whoever owns the form's inbox - they should know in 30 seconds whether today needs them.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks:** @google-drive missing → the sheet can't be read; ask the user to paste/export the new rows and continue from step 2. @zapier missing → skip routing and deliver the flagged responses as an action checklist, plus a proposed routing-Zap config for next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Form responses are personal data: quote only what's needed for the digest, and never route a response anywhere the user didn't specify.\n\n## Workflow\n\n**Step 1 - Drive: read the new responses.**\n- Read the response sheet; identify new rows since the last processed marker (timestamp column is the anchor - Google Forms writes one automatically). On first run, take the agreed window and note \"first run: no marker yet\".\n- Sanity-check the data: rows with missing timestamps, duplicate submissions (same email within minutes), test entries (\"asdf\") - count them and set them aside.\n\n**Step 2 - Digest and classify (AI, no writes).**\n- Digest: N new responses; per key question, the distribution or notable answers; any trend vs what the user said is normal (\"3 complaints today vs usually 0\").\n- Apply the flag rules to each response: **route** (matches a rule → which rule), **watch** (borderline - say why), **no action**.\n- **Checkpoint:** show the digest plus the flagged-responses table (row # / respondent / trigger rule / key answer excerpt). The user confirms or adjusts flags before anything is routed.\n\n**Step 3 - Zapier: route the flagged responses.**\n- Build one payload per approved flag, mapped from the sheet's columns to the Zap's expected fields.\n- **Approval gate:** review table - response / Zap / payload summary. Fire only after `apply` (or `apply 1,3` for a subset). Report each trigger response; accepted ≠ downstream action completed (that runs inside Zapier).\n- Failure behavior: if a trigger errors, keep that response in the flagged list for the next run rather than dropping it silently.\n\n**Step 4 - Advance the marker.**\n- State the new \"processed through\" marker (last row timestamp) so tomorrow's run starts exactly where this one ended - this is what makes the daily cadence duplicate-proof. If the user keeps the marker in the sheet or a note, propose that single-cell update for approval.\n\n## Output format\n\n1. **New-responses digest** (steps 1-2) - count, per-question highlights, anomalies, set-aside junk/test entries\n2. **Flagged table** (step 2) - Row / Respondent / Rule matched / Excerpt - awaiting flag confirmation\n3. **Routing review table** (step 3) - Response / Zap / Payload - awaiting `apply`; then per-trigger results\n4. **Marker line** (step 4) - \"Processed through <timestamp> (row N)\"\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Read | Drive | N new rows (J junk aside) | done |\n| 2 Digest | AI | Digest + F flags | approved |\n| 3 Route | Zap (inside Zapier) | R triggers fired | accepted / fallback |\n| 4 Marker | AI | Processed-through timestamp | done |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Anchor \"new\" on the timestamp marker, not on memory - daily runs must never re-route yesterday's responses\n- State the rule that triggered each flag so the user can tune rules over time\n- Keep failed routings in the flagged list for the next run instead of dropping them\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't route responses (step 3) whose flags the user hasn't confirmed - a \"complaint\" flag on sarcasm routes a happy customer to escalation\n- Don't process without establishing the marker first; overlapping windows create duplicate CRM entries and double notifications\n- Don't paste full response texts into the digest when an excerpt identifies the issue - form data is personal data\n- Don't treat trigger-accepted as routed-and-handled; the downstream steps run inside Zapier\n- Don't invent sheet contents or Zap names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] \"New\" window anchored on the previous marker; new marker stated at the end\n- [ ] Every flag names its triggering rule; flags confirmed before routing\n- [ ] Run summary distinguishes trigger-accepted from downstream-completed\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier, @google-drive) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **daily**\n\nExample: `/loop 7d Run /zap-google-forms-to-sheet`\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-google-forms-to-sheet or describe the task from the skill description.", "skillContent": "---\nname: zap-google-forms-to-sheet\ndescription: >\n  Process new Google Forms responses. Requires integrations: @zapier, @google-drive. Uses search_tool + use_tool. Cadence: daily. Triggers: \"zap google forms to sheet\", /zap-google-forms-to-sheet.\nmetadata:\n  category: zapier\n  type: combo\n  cadence: daily\n  risk: low\n  connectors: zapier,google-drive\n  integrations: @zapier,@google-drive\nallowed-tools: search_tool, use_tool\n---\n# /zap-google-forms-to-sheet\n\nProcess new Google Forms responses.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this as a daily check on a Google Form that feeds a response sheet (signups, support intake, survey, event RSVPs): read the new rows since the last run, summarize what came in, flag responses that need action (complaints, high-value leads, \"contact me\" requests), and route the actionable ones via the user's Zapier workflow. The Form→Sheet write itself is Google's native link (or the user's existing Zap) - this skill is the daily *processing* layer on top.\n\n## What the AI does vs what the Zap does\n\n- **The AI** reads the response sheet from Drive, digests the new rows, classifies which need routing, and fires the routing Zap (e.g., \"notify on hot lead\", \"create support case\") with an approved payload per flagged response.\n- **The Zap** performs the downstream routing inside Zapier (notification, CRM entry, task creation - whatever the user built).\n- The AI **cannot build Zaps**. If no routing Zap exists, it proposes the configuration (trigger + action + field mapping from sheet columns) and meanwhile delivers the flagged list so the user can act manually.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The response sheet (name/link in Drive) and which form it belongs to\n- What \"new\" means: since the last run's timestamp/row marker (preferred), or the last N days on first run\n- The flag rules: which answers make a response actionable (e.g., rating ≤ 2, budget over X, \"yes\" to contact, keyword in free text)\n- The routing Zap's name and expected payload per response type\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Digest-style: counts and patterns first, then the individual flags.\n- **Audience:** Whoever owns the form's inbox - they should know in 30 seconds whether today needs them.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n- **@google-drive** - Google Drive\n  - MCP server key: `google-drive`\n  - Example tools: `google-drive__search_files`, `google-drive__read_file`, `google-drive__create_file`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks:** @google-drive missing → the sheet can't be read; ask the user to paste/export the new rows and continue from step 2. @zapier missing → skip routing and deliver the flagged responses as an action checklist, plus a proposed routing-Zap config for next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Form responses are personal data: quote only what's needed for the digest, and never route a response anywhere the user didn't specify.\n\n## Workflow\n\n**Step 1 - Drive: read the new responses.**\n- Read the response sheet; identify new rows since the last processed marker (timestamp column is the anchor - Google Forms writes one automatically). On first run, take the agreed window and note \"first run: no marker yet\".\n- Sanity-check the data: rows with missing timestamps, duplicate submissions (same email within minutes), test entries (\"asdf\") - count them and set them aside.\n\n**Step 2 - Digest and classify (AI, no writes).**\n- Digest: N new responses; per key question, the distribution or notable answers; any trend vs what the user said is normal (\"3 complaints today vs usually 0\").\n- Apply the flag rules to each response: **route** (matches a rule → which rule), **watch** (borderline - say why), **no action**.\n- **Checkpoint:** show the digest plus the flagged-responses table (row # / respondent / trigger rule / key answer excerpt). The user confirms or adjusts flags before anything is routed.\n\n**Step 3 - Zapier: route the flagged responses.**\n- Build one payload per approved flag, mapped from the sheet's columns to the Zap's expected fields.\n- **Approval gate:** review table - response / Zap / payload summary. Fire only after `apply` (or `apply 1,3` for a subset). Report each trigger response; accepted ≠ downstream action completed (that runs inside Zapier).\n- Failure behavior: if a trigger errors, keep that response in the flagged list for the next run rather than dropping it silently.\n\n**Step 4 - Advance the marker.**\n- State the new \"processed through\" marker (last row timestamp) so tomorrow's run starts exactly where this one ended - this is what makes the daily cadence duplicate-proof. If the user keeps the marker in the sheet or a note, propose that single-cell update for approval.\n\n## Output format\n\n1. **New-responses digest** (steps 1-2) - count, per-question highlights, anomalies, set-aside junk/test entries\n2. **Flagged table** (step 2) - Row / Respondent / Rule matched / Excerpt - awaiting flag confirmation\n3. **Routing review table** (step 3) - Response / Zap / Payload - awaiting `apply`; then per-trigger results\n4. **Marker line** (step 4) - \"Processed through <timestamp> (row N)\"\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Read | Drive | N new rows (J junk aside) | done |\n| 2 Digest | AI | Digest + F flags | approved |\n| 3 Route | Zap (inside Zapier) | R triggers fired | accepted / fallback |\n| 4 Marker | AI | Processed-through timestamp | done |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Anchor \"new\" on the timestamp marker, not on memory - daily runs must never re-route yesterday's responses\n- State the rule that triggered each flag so the user can tune rules over time\n- Keep failed routings in the flagged list for the next run instead of dropping them\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't route responses (step 3) whose flags the user hasn't confirmed - a \"complaint\" flag on sarcasm routes a happy customer to escalation\n- Don't process without establishing the marker first; overlapping windows create duplicate CRM entries and double notifications\n- Don't paste full response texts into the digest when an excerpt identifies the issue - form data is personal data\n- Don't treat trigger-accepted as routed-and-handled; the downstream steps run inside Zapier\n- Don't invent sheet contents or Zap names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] \"New\" window anchored on the previous marker; new marker stated at the end\n- [ ] Every flag names its triggering rule; flags confirmed before routing\n- [ ] Run summary distinguishes trigger-accepted from downstream-completed\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier, @google-drive) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **daily**\n\nExample: `/loop 7d Run /zap-google-forms-to-sheet`\n"}, {"id": "zap-hubspot-deal-note", "aiInstallPrompt": "Install the skill 'zap-hubspot-deal-note' by writing the file below.\n\nUniversal path: ./skills/zap-hubspot-deal-note/SKILL.md\nGrok path: ~/.grok/skills/zap-hubspot-deal-note/SKILL.md\nCursor path: ~/.cursor/skills/zap-hubspot-deal-note/SKILL.md\nClaude path: ~/.claude/skills/zap-hubspot-deal-note/SKILL.md\nVS Code agent path: .github/skills/zap-hubspot-deal-note/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-hubspot-deal-note\ndescription: >\n  Log call notes to HubSpot deal via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap hubspot deal note\", /zap-hubspot-deal-note.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-hubspot-deal-note\n\nLog call notes to HubSpot deal via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this straight after a sales call, while it's fresh: turn rambling call notes or a transcript into a structured HubSpot deal note (summary, stakeholders, objections, next step) and log it to the right deal via the user's Zapier workflow - without opening HubSpot. One call → one note on one deal.\n\n## What the AI does vs what the Zap does\n\n- **The AI** structures the raw notes into HubSpot-ready fields, identifies the deal to attach to, and fires the user's note-logging Zap with the approved payload.\n- **The Zap** creates the note/activity on the deal record inside HubSpot (typically \"Find deal\" → \"Create note\" / \"Create engagement\").\n- The AI **cannot build Zaps or write to HubSpot directly** through this skill. No logging Zap yet → it proposes the config (webhook trigger; HubSpot find-deal by name/ID; create-note with the field mapping below) and delivers the formatted note for manual paste this time.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The raw material: call notes, transcript, or voice-memo text\n- Which deal: deal name or ID as it appears in HubSpot (the Zap's find step needs an exact-enough key)\n- Call metadata: date/time, who was on the call (their side and yours), call type (discovery, demo, negotiation)\n- Any deal-stage implication the user wants recorded (\"this moves us to contract sent\") - stage changes are stated by the user, not inferred silently\n- The logging Zap's name and expected payload fields\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. The note itself: scannable by a colleague who wasn't on the call - structure over prose.\n- **Audience:** The future reader of the deal timeline (a teammate or the user in 3 months).\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier stays unavailable, deliver the fully formatted note (step 1 output) for manual paste into HubSpot, plus a proposed logging-Zap config so next time is one command.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Deal notes are read by the whole revenue team: facts from the call stay separate from the rep's speculation, and the note never contains anything that would embarrass if the prospect saw it in a subpoena.\n\n## Workflow\n\n**Step 1 - Structure the note (AI, no connector).**\n- Transform the raw notes into the deal-note format:\n  - **Call:** date, type, attendees (both sides, with roles)\n  - **Summary:** 3-5 bullets of what actually happened\n  - **Signals:** budget/authority/need/timeline points that surfaced - quoted or closely paraphrased, marked \"stated\" vs \"implied\"\n  - **Objections & responses:** each objection and how it was handled\n  - **Next step:** the ONE agreed action, owner, and date (if none was agreed, write \"No next step agreed - flag\", which is itself important deal intel)\n- **Checkpoint:** show the structured note; the user corrects attributions and fills gaps. Nothing fires yet.\n\n**Step 2 - Identify Zap and deal.**\n- Find the note-logging Zap via the connector (list Zaps, match by name; multiple → ask). Confirm the deal key the Zap's find-step will use - a fuzzy deal name that matches two deals puts the note on the wrong one, and misfiled notes are effectively lost.\n- No Zap → deliver the proposed Zap config + the note for manual paste; stop.\n\n**Step 3 - Fire and confirm.**\n- Build the payload: deal key + note body + call date + any stage-change field the user explicitly requested.\n- **Approval gate:** review table (Zap, deal key, payload preview, stage change yes/no). Fire only on `apply`.\n- Report the trigger response honestly - accepted ≠ note visible on the deal. If a run-status tool exists in the session, check it; otherwise ask the user to confirm the note appears on the deal timeline, and offer to re-fire with a corrected deal key if it landed wrong.\n\n## Output format\n\n1. **Structured deal note** (step 1) - Call / Summary / Signals / Objections / Next step - awaiting corrections\n2. **Zap + deal match** (step 2) - Zap name, deal key to be used (or proposed Zap config)\n3. **Payload review table** (step 3) - awaiting `apply`; then the trigger result\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Note | AI | Structured note | approved |\n| 2 Match | Zapier | Zap + deal key | done |\n| 3 Log | Zap (inside Zapier) | Note trigger fired | accepted / fallback |\n| 4 Verify | User / Zapier | Note on deal timeline | confirmed / pending |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Confirm the deal key precisely before firing - \"Acme deal\" matching \"Acme renewal\" AND \"Acme expansion\" is the classic misfile\n- Mark BANT-type signals \"stated\" vs \"implied\"; the team will make forecasts off this note\n- Record \"no next step agreed\" explicitly when true - it's a red flag worth surfacing, not a blank to hide\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire before the user approves both the note text and the deal key\n- Don't change the deal stage unless the user explicitly asked - a note-logging run that silently moves stage corrupts the pipeline\n- Don't log the same call twice on a re-run; ask whether this call was already logged if context suggests it\n- Don't put speculation in as fact - \"sounded hesitant about budget (my read)\" not \"has no budget\"\n- Don't invent Zap names, deal names, or run results without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Note approved and deal key confirmed before firing\n- [ ] Facts vs rep-speculation clearly separated in the note\n- [ ] Run summary distinguishes trigger-accepted from note-verified\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-hubspot-deal-note or describe the task from the skill description.", "skillContent": "---\nname: zap-hubspot-deal-note\ndescription: >\n  Log call notes to HubSpot deal via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap hubspot deal note\", /zap-hubspot-deal-note.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-hubspot-deal-note\n\nLog call notes to HubSpot deal via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this straight after a sales call, while it's fresh: turn rambling call notes or a transcript into a structured HubSpot deal note (summary, stakeholders, objections, next step) and log it to the right deal via the user's Zapier workflow - without opening HubSpot. One call → one note on one deal.\n\n## What the AI does vs what the Zap does\n\n- **The AI** structures the raw notes into HubSpot-ready fields, identifies the deal to attach to, and fires the user's note-logging Zap with the approved payload.\n- **The Zap** creates the note/activity on the deal record inside HubSpot (typically \"Find deal\" → \"Create note\" / \"Create engagement\").\n- The AI **cannot build Zaps or write to HubSpot directly** through this skill. No logging Zap yet → it proposes the config (webhook trigger; HubSpot find-deal by name/ID; create-note with the field mapping below) and delivers the formatted note for manual paste this time.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The raw material: call notes, transcript, or voice-memo text\n- Which deal: deal name or ID as it appears in HubSpot (the Zap's find step needs an exact-enough key)\n- Call metadata: date/time, who was on the call (their side and yours), call type (discovery, demo, negotiation)\n- Any deal-stage implication the user wants recorded (\"this moves us to contract sent\") - stage changes are stated by the user, not inferred silently\n- The logging Zap's name and expected payload fields\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. The note itself: scannable by a colleague who wasn't on the call - structure over prose.\n- **Audience:** The future reader of the deal timeline (a teammate or the user in 3 months).\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier stays unavailable, deliver the fully formatted note (step 1 output) for manual paste into HubSpot, plus a proposed logging-Zap config so next time is one command.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- Deal notes are read by the whole revenue team: facts from the call stay separate from the rep's speculation, and the note never contains anything that would embarrass if the prospect saw it in a subpoena.\n\n## Workflow\n\n**Step 1 - Structure the note (AI, no connector).**\n- Transform the raw notes into the deal-note format:\n  - **Call:** date, type, attendees (both sides, with roles)\n  - **Summary:** 3-5 bullets of what actually happened\n  - **Signals:** budget/authority/need/timeline points that surfaced - quoted or closely paraphrased, marked \"stated\" vs \"implied\"\n  - **Objections & responses:** each objection and how it was handled\n  - **Next step:** the ONE agreed action, owner, and date (if none was agreed, write \"No next step agreed - flag\", which is itself important deal intel)\n- **Checkpoint:** show the structured note; the user corrects attributions and fills gaps. Nothing fires yet.\n\n**Step 2 - Identify Zap and deal.**\n- Find the note-logging Zap via the connector (list Zaps, match by name; multiple → ask). Confirm the deal key the Zap's find-step will use - a fuzzy deal name that matches two deals puts the note on the wrong one, and misfiled notes are effectively lost.\n- No Zap → deliver the proposed Zap config + the note for manual paste; stop.\n\n**Step 3 - Fire and confirm.**\n- Build the payload: deal key + note body + call date + any stage-change field the user explicitly requested.\n- **Approval gate:** review table (Zap, deal key, payload preview, stage change yes/no). Fire only on `apply`.\n- Report the trigger response honestly - accepted ≠ note visible on the deal. If a run-status tool exists in the session, check it; otherwise ask the user to confirm the note appears on the deal timeline, and offer to re-fire with a corrected deal key if it landed wrong.\n\n## Output format\n\n1. **Structured deal note** (step 1) - Call / Summary / Signals / Objections / Next step - awaiting corrections\n2. **Zap + deal match** (step 2) - Zap name, deal key to be used (or proposed Zap config)\n3. **Payload review table** (step 3) - awaiting `apply`; then the trigger result\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Note | AI | Structured note | approved |\n| 2 Match | Zapier | Zap + deal key | done |\n| 3 Log | Zap (inside Zapier) | Note trigger fired | accepted / fallback |\n| 4 Verify | User / Zapier | Note on deal timeline | confirmed / pending |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Confirm the deal key precisely before firing - \"Acme deal\" matching \"Acme renewal\" AND \"Acme expansion\" is the classic misfile\n- Mark BANT-type signals \"stated\" vs \"implied\"; the team will make forecasts off this note\n- Record \"no next step agreed\" explicitly when true - it's a red flag worth surfacing, not a blank to hide\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire before the user approves both the note text and the deal key\n- Don't change the deal stage unless the user explicitly asked - a note-logging run that silently moves stage corrupts the pipeline\n- Don't log the same call twice on a re-run; ask whether this call was already logged if context suggests it\n- Don't put speculation in as fact - \"sounded hesitant about budget (my read)\" not \"has no budget\"\n- Don't invent Zap names, deal names, or run results without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Note approved and deal key confirmed before firing\n- [ ] Facts vs rep-speculation clearly separated in the note\n- [ ] Run summary distinguishes trigger-accepted from note-verified\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "zap-jira-create-ticket", "aiInstallPrompt": "Install the skill 'zap-jira-create-ticket' by writing the file below.\n\nUniversal path: ./skills/zap-jira-create-ticket/SKILL.md\nGrok path: ~/.grok/skills/zap-jira-create-ticket/SKILL.md\nCursor path: ~/.cursor/skills/zap-jira-create-ticket/SKILL.md\nClaude path: ~/.claude/skills/zap-jira-create-ticket/SKILL.md\nVS Code agent path: .github/skills/zap-jira-create-ticket/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-jira-create-ticket\ndescription: >\n  Create Jira ticket from email/Slack via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap jira create ticket\", /zap-jira-create-ticket.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-jira-create-ticket\n\nCreate Jira ticket from email/Slack via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a bug report or request arrives as prose - a customer email, a Slack thread, a support message pasted into chat - and it needs to become a well-formed Jira ticket via the user's Zapier workflow. The AI's job is the translation: turning \"the export thing is broken again for the London team\" into a ticket an engineer can act on without asking three follow-up questions.\n\n## What the AI does vs what the Zap does\n\n- **The AI** extracts the issue from the source text, writes the ticket fields (summary, description with repro steps, type, priority), and fires the user's Jira-create Zap with the approved payload.\n- **The Zap** creates the actual Jira issue inside Zapier (project, issue type, fields as mapped by the user when they built it).\n- The AI **cannot build Zaps**. No Jira-create Zap yet → it proposes the config (webhook trigger; Jira \"Create issue\" action; field mapping) and delivers the ticket content as a copy-paste block for manual creation this time.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The source material: the email text, Slack thread, or description of the problem (pasted - this skill's stack has no mail/Slack connector; the Zapier workflow owns those hookups)\n- Target Jira project key and issue type (Bug / Task / Story) if the Zap doesn't hard-code them\n- Priority signal: who is affected, how many, is there a workaround - the AI recommends a priority from this, the user confirms\n- Reporter attribution: file it as the user, or name the original requester in the description\n- The Jira-create Zap's name and expected payload fields\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Ticket text in engineering register: reproducible facts, expected vs actual, no adjectives doing load-bearing work.\n- **Audience:** The engineer who picks the ticket up cold.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier is unavailable, deliver the complete ticket (summary, description, type, priority) as a copy-paste block for manual creation in Jira, plus a proposed Jira-create Zap config for next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n**Step 1 - Extract the issue (AI, no connector).**\n- From the source text, build the ticket draft:\n  - **Summary:** one line, symptom + scope - \"CSV export fails with timeout for workspaces >10k rows\", never \"Export broken\"\n  - **Description:** what happened / expected vs actual / steps to reproduce (as far as the source reveals them) / who reported it and when / environment details if mentioned (browser, plan, region)\n  - **Gaps:** anything an engineer will immediately ask, listed as open questions IN the description (\"repro rate unknown - reporter saw it twice\") rather than invented\n  - **Type + recommended priority:** with the one-line justification (\"affects one customer, workaround exists → Medium\")\n- Quote the source verbatim for the key error message if there is one - paraphrased error text is famously useless.\n- **Checkpoint:** show the ticket draft. The user corrects scope/priority before anything is created.\n\n**Step 2 - Identify the Zap.**\n- Find the Jira-create Zap via the connector; confirm which fields it expects (project key and type may be hard-coded in the Zap or expected in the payload). Multiple candidate Zaps (per project/team) → show and ask.\n- Duplicate guard: ask the user whether this issue might already be filed (same symptom reported earlier); if in doubt, note \"possible duplicate of <what the user recalls>\" in the description rather than skipping filing.\n\n**Step 3 - Fire and report.**\n- **Approval gate:** review table - Zap name, project, type, summary, priority, description preview. Fire only on `apply`.\n- Report the trigger response honestly: accepted ≠ issue created. If the connector or a run-status tool returns the new issue key/URL, report it; otherwise tell the user where to look (\"check the project's newest issues - expected summary: ...\") and offer to re-fire if it didn't land.\n\n## Output format\n\n1. **Ticket draft** (step 1) - Summary / Description (with open questions) / Type / Priority + justification - awaiting corrections\n2. **Zap match** (step 2) - Zap name + expected fields (or proposed config)\n3. **Payload review table** (step 3) - awaiting `apply`; then trigger result + issue key/URL if available\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Draft | AI | Ticket content | approved |\n| 2 Match | Zapier | Zap identified | done |\n| 3 Create | Zap (inside Zapier) | Trigger fired | accepted / fallback |\n| 4 Verify | Zapier / user | Issue key confirmed | confirmed / pending |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Quote error messages verbatim and put unknown details in as open questions - an honest incomplete ticket beats a confidently wrong one\n- Justify the priority recommendation in one line so the user can veto it fast\n- Ask about possible duplicates before filing; a \"possible duplicate\" note beats a silent second ticket\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire before the user approves the draft - a mis-prioritized or mis-scoped ticket wastes an engineer's triage pass\n- Don't invent repro steps the source doesn't support; write \"repro steps unknown beyond the above\"\n- Don't re-fire on an ambiguous response without checking - duplicate tickets fragment the discussion\n- Don't inflate priority to \"get attention\"; recommend from the affected-users/workaround facts\n- Don't invent Zap names, project keys, or issue keys without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Summary states symptom + scope; error text quoted verbatim where available\n- [ ] Draft approved before the Zap fired; unknowns listed as open questions, not filled in\n- [ ] Run summary distinguishes trigger-accepted from issue-confirmed\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-jira-create-ticket or describe the task from the skill description.", "skillContent": "---\nname: zap-jira-create-ticket\ndescription: >\n  Create Jira ticket from email/Slack via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap jira create ticket\", /zap-jira-create-ticket.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-jira-create-ticket\n\nCreate Jira ticket from email/Slack via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when a bug report or request arrives as prose - a customer email, a Slack thread, a support message pasted into chat - and it needs to become a well-formed Jira ticket via the user's Zapier workflow. The AI's job is the translation: turning \"the export thing is broken again for the London team\" into a ticket an engineer can act on without asking three follow-up questions.\n\n## What the AI does vs what the Zap does\n\n- **The AI** extracts the issue from the source text, writes the ticket fields (summary, description with repro steps, type, priority), and fires the user's Jira-create Zap with the approved payload.\n- **The Zap** creates the actual Jira issue inside Zapier (project, issue type, fields as mapped by the user when they built it).\n- The AI **cannot build Zaps**. No Jira-create Zap yet → it proposes the config (webhook trigger; Jira \"Create issue\" action; field mapping) and delivers the ticket content as a copy-paste block for manual creation this time.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The source material: the email text, Slack thread, or description of the problem (pasted - this skill's stack has no mail/Slack connector; the Zapier workflow owns those hookups)\n- Target Jira project key and issue type (Bug / Task / Story) if the Zap doesn't hard-code them\n- Priority signal: who is affected, how many, is there a workaround - the AI recommends a priority from this, the user confirms\n- Reporter attribution: file it as the user, or name the original requester in the description\n- The Jira-create Zap's name and expected payload fields\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Ticket text in engineering register: reproducible facts, expected vs actual, no adjectives doing load-bearing work.\n- **Audience:** The engineer who picks the ticket up cold.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier is unavailable, deliver the complete ticket (summary, description, type, priority) as a copy-paste block for manual creation in Jira, plus a proposed Jira-create Zap config for next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n**Step 1 - Extract the issue (AI, no connector).**\n- From the source text, build the ticket draft:\n  - **Summary:** one line, symptom + scope - \"CSV export fails with timeout for workspaces >10k rows\", never \"Export broken\"\n  - **Description:** what happened / expected vs actual / steps to reproduce (as far as the source reveals them) / who reported it and when / environment details if mentioned (browser, plan, region)\n  - **Gaps:** anything an engineer will immediately ask, listed as open questions IN the description (\"repro rate unknown - reporter saw it twice\") rather than invented\n  - **Type + recommended priority:** with the one-line justification (\"affects one customer, workaround exists → Medium\")\n- Quote the source verbatim for the key error message if there is one - paraphrased error text is famously useless.\n- **Checkpoint:** show the ticket draft. The user corrects scope/priority before anything is created.\n\n**Step 2 - Identify the Zap.**\n- Find the Jira-create Zap via the connector; confirm which fields it expects (project key and type may be hard-coded in the Zap or expected in the payload). Multiple candidate Zaps (per project/team) → show and ask.\n- Duplicate guard: ask the user whether this issue might already be filed (same symptom reported earlier); if in doubt, note \"possible duplicate of <what the user recalls>\" in the description rather than skipping filing.\n\n**Step 3 - Fire and report.**\n- **Approval gate:** review table - Zap name, project, type, summary, priority, description preview. Fire only on `apply`.\n- Report the trigger response honestly: accepted ≠ issue created. If the connector or a run-status tool returns the new issue key/URL, report it; otherwise tell the user where to look (\"check the project's newest issues - expected summary: ...\") and offer to re-fire if it didn't land.\n\n## Output format\n\n1. **Ticket draft** (step 1) - Summary / Description (with open questions) / Type / Priority + justification - awaiting corrections\n2. **Zap match** (step 2) - Zap name + expected fields (or proposed config)\n3. **Payload review table** (step 3) - awaiting `apply`; then trigger result + issue key/URL if available\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Draft | AI | Ticket content | approved |\n| 2 Match | Zapier | Zap identified | done |\n| 3 Create | Zap (inside Zapier) | Trigger fired | accepted / fallback |\n| 4 Verify | Zapier / user | Issue key confirmed | confirmed / pending |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Quote error messages verbatim and put unknown details in as open questions - an honest incomplete ticket beats a confidently wrong one\n- Justify the priority recommendation in one line so the user can veto it fast\n- Ask about possible duplicates before filing; a \"possible duplicate\" note beats a silent second ticket\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire before the user approves the draft - a mis-prioritized or mis-scoped ticket wastes an engineer's triage pass\n- Don't invent repro steps the source doesn't support; write \"repro steps unknown beyond the above\"\n- Don't re-fire on an ambiguous response without checking - duplicate tickets fragment the discussion\n- Don't inflate priority to \"get attention\"; recommend from the affected-users/workaround facts\n- Don't invent Zap names, project keys, or issue keys without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Summary states symptom + scope; error text quoted verbatim where available\n- [ ] Draft approved before the Zap fired; unknowns listed as open questions, not filled in\n- [ ] Run summary distinguishes trigger-accepted from issue-confirmed\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "zap-stripe-revenue-digest", "aiInstallPrompt": "Install the skill 'zap-stripe-revenue-digest' by writing the file below.\n\nUniversal path: ./skills/zap-stripe-revenue-digest/SKILL.md\nGrok path: ~/.grok/skills/zap-stripe-revenue-digest/SKILL.md\nCursor path: ~/.cursor/skills/zap-stripe-revenue-digest/SKILL.md\nClaude path: ~/.claude/skills/zap-stripe-revenue-digest/SKILL.md\nVS Code agent path: .github/skills/zap-stripe-revenue-digest/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-stripe-revenue-digest\ndescription: >\n  Weekly Stripe revenue digest via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"zap stripe revenue digest\", /zap-stripe-revenue-digest.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: weekly\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-stripe-revenue-digest\n\nWeekly Stripe revenue digest via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this once a week to turn raw Stripe activity into a founder-readable digest: what came in, what churned or refunded, what failed to charge, and how the week compares to the last one. Stripe data reaches the chat through the user's Zapier connection - either Stripe actions the Zapier connector exposes in this session, or a digest-data Zap the user built that returns/forwards the week's numbers.\n\n## What the AI does vs what the Zap does\n\n- **The AI** requests the week's data through the Zapier connector, computes the aggregates and week-over-week deltas, writes the digest, and flags items needing action (failed payments, disputes).\n- **The Zapier side** is the pipe to Stripe: session-exposed Stripe search/list actions, or the user's existing Zap that pulls charges/subscriptions for a period.\n- The AI **cannot build Zaps** and has no direct Stripe access. If the session's Zapier connector exposes no Stripe data path, it proposes the Zap/action setup for the user to create and stops - it never estimates revenue.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The week to digest (default: last completed Mon-Sun) and the comparison week (default: the one before)\n- How Stripe data arrives: which Zapier-exposed Stripe actions exist in this session, or which Zap returns the data - discover first, then confirm\n- Currency handling if multi-currency (report per currency; never silently convert)\n- What counts as \"needs action\" for this business: failed payments above X, any dispute, refunds above Y\n- Where the digest goes: chat only (default), or also fire a delivery Zap (email/Slack) after approval\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Numbers exact with currency symbols; deltas always shown next to absolutes; no celebration or doom language - the numbers speak.\n- **Audience:** Founder/finance owner scanning in 60 seconds.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Hard rule for this skill:** no data, no digest. If Stripe numbers can't be fetched through the connector, deliver the proposed Zap setup instead - never a digest built on estimates or memory. Money reports are only worth anything when they're real.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority - revenue interpretation is reporting, not financial advice.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- This skill READS payment data only; it never proposes refunds, cancellations, or charges.\n\n## Workflow\n\n**Step 1 - Discover the data path.**\n- Search the session's Zapier connector for Stripe-related actions (find/list charges, subscriptions, invoices) or the user's named digest Zap. State exactly which path this run uses. None found → deliver the proposed setup (Zap or connector actions to enable) and stop.\n\n**Step 2 - Pull the week.**\n- Fetch for the digest week and the comparison week: successful charges, refunds, failed payments, disputes/chargebacks if exposed, and new/canceled subscriptions if the business is recurring.\n- State coverage honestly: which of these the connector actually returned. A digest missing disputes says \"disputes: not available via current connection\" rather than \"disputes: 0\".\n\n**Step 3 - Compute the digest (AI, deterministic math).**\n- Aggregate per currency: gross volume, refunds, net; charge count; average charge. Week-over-week deltas in absolute and %.\n- Recurring view (if data available): new subscriptions, cancellations, net change.\n- **Action items:** each failed payment (customer, amount, failure reason if present), each dispute, each large refund per the user's thresholds.\n- Note anomalies factually: \"single $4,000 charge accounts for 60% of the WoW increase\".\n\n**Step 4 - Deliver (and optionally distribute).**\n- Present the digest in chat. If the user wants it emailed/posted, build the delivery-Zap payload, show it, and fire only after `apply`.\n\n## Output format\n\n**📊 Stripe week - <Mon date> to <Sun date>** (per currency if multiple)\n\n1. **Headline** - net revenue, WoW delta (absolute + %)\n2. **Money table** - Gross / Refunds / Net / Charges / Avg charge - this week vs last, with deltas\n3. **Recurring** - new / canceled / net subscriptions (or \"not available via current connection\")\n4. **⚠️ Needs action** - failed payments, disputes, large refunds - each with customer, amount, reason\n5. **Notes** - anomalies and data-coverage caveats\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Data path | Zapier | Actions/Zap identified | done / setup proposed |\n| 2 Pull | Zapier → Stripe | 2 weeks of data, coverage noted | done / partial |\n| 3 Digest | AI | Tables + action items | done |\n| 4 Deliver | Zap (optional) | Email/Slack sent | applied / chat only |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- State the data coverage explicitly - the difference between \"0 disputes\" and \"disputes not fetched\" matters enormously in a money report\n- Show WoW deltas next to every absolute; a number without comparison is decoration\n- Report per currency; never merge currencies with an invented exchange rate\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fabricate, extrapolate, or \"approximately\" any revenue figure - no data path means no digest, full stop\n- Don't compare a partial week to a full week without labeling it prominently\n- Don't propose refunds or subscription changes - read-only on payment data\n- Don't fire the delivery Zap before the user approves the digest content it will carry\n- Don't invent Stripe records or Zap names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every figure came from fetched data; coverage gaps declared in the digest itself\n- [ ] WoW deltas present for all headline numbers; currencies kept separate\n- [ ] Run summary shows the data path actually used\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /zap-stripe-revenue-digest`\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-stripe-revenue-digest or describe the task from the skill description.", "skillContent": "---\nname: zap-stripe-revenue-digest\ndescription: >\n  Weekly Stripe revenue digest via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: weekly. Triggers: \"zap stripe revenue digest\", /zap-stripe-revenue-digest.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: weekly\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-stripe-revenue-digest\n\nWeekly Stripe revenue digest via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this once a week to turn raw Stripe activity into a founder-readable digest: what came in, what churned or refunded, what failed to charge, and how the week compares to the last one. Stripe data reaches the chat through the user's Zapier connection - either Stripe actions the Zapier connector exposes in this session, or a digest-data Zap the user built that returns/forwards the week's numbers.\n\n## What the AI does vs what the Zap does\n\n- **The AI** requests the week's data through the Zapier connector, computes the aggregates and week-over-week deltas, writes the digest, and flags items needing action (failed payments, disputes).\n- **The Zapier side** is the pipe to Stripe: session-exposed Stripe search/list actions, or the user's existing Zap that pulls charges/subscriptions for a period.\n- The AI **cannot build Zaps** and has no direct Stripe access. If the session's Zapier connector exposes no Stripe data path, it proposes the Zap/action setup for the user to create and stops - it never estimates revenue.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The week to digest (default: last completed Mon-Sun) and the comparison week (default: the one before)\n- How Stripe data arrives: which Zapier-exposed Stripe actions exist in this session, or which Zap returns the data - discover first, then confirm\n- Currency handling if multi-currency (report per currency; never silently convert)\n- What counts as \"needs action\" for this business: failed payments above X, any dispute, refunds above Y\n- Where the digest goes: chat only (default), or also fire a delivery Zap (email/Slack) after approval\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Numbers exact with currency symbols; deltas always shown next to absolutes; no celebration or doom language - the numbers speak.\n- **Audience:** Founder/finance owner scanning in 60 seconds.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Hard rule for this skill:** no data, no digest. If Stripe numbers can't be fetched through the connector, deliver the proposed Zap setup instead - never a digest built on estimates or memory. Money reports are only worth anything when they're real.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority - revenue interpretation is reporting, not financial advice.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- This skill READS payment data only; it never proposes refunds, cancellations, or charges.\n\n## Workflow\n\n**Step 1 - Discover the data path.**\n- Search the session's Zapier connector for Stripe-related actions (find/list charges, subscriptions, invoices) or the user's named digest Zap. State exactly which path this run uses. None found → deliver the proposed setup (Zap or connector actions to enable) and stop.\n\n**Step 2 - Pull the week.**\n- Fetch for the digest week and the comparison week: successful charges, refunds, failed payments, disputes/chargebacks if exposed, and new/canceled subscriptions if the business is recurring.\n- State coverage honestly: which of these the connector actually returned. A digest missing disputes says \"disputes: not available via current connection\" rather than \"disputes: 0\".\n\n**Step 3 - Compute the digest (AI, deterministic math).**\n- Aggregate per currency: gross volume, refunds, net; charge count; average charge. Week-over-week deltas in absolute and %.\n- Recurring view (if data available): new subscriptions, cancellations, net change.\n- **Action items:** each failed payment (customer, amount, failure reason if present), each dispute, each large refund per the user's thresholds.\n- Note anomalies factually: \"single $4,000 charge accounts for 60% of the WoW increase\".\n\n**Step 4 - Deliver (and optionally distribute).**\n- Present the digest in chat. If the user wants it emailed/posted, build the delivery-Zap payload, show it, and fire only after `apply`.\n\n## Output format\n\n**📊 Stripe week - <Mon date> to <Sun date>** (per currency if multiple)\n\n1. **Headline** - net revenue, WoW delta (absolute + %)\n2. **Money table** - Gross / Refunds / Net / Charges / Avg charge - this week vs last, with deltas\n3. **Recurring** - new / canceled / net subscriptions (or \"not available via current connection\")\n4. **⚠️ Needs action** - failed payments, disputes, large refunds - each with customer, amount, reason\n5. **Notes** - anomalies and data-coverage caveats\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Data path | Zapier | Actions/Zap identified | done / setup proposed |\n| 2 Pull | Zapier → Stripe | 2 weeks of data, coverage noted | done / partial |\n| 3 Digest | AI | Tables + action items | done |\n| 4 Deliver | Zap (optional) | Email/Slack sent | applied / chat only |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- State the data coverage explicitly - the difference between \"0 disputes\" and \"disputes not fetched\" matters enormously in a money report\n- Show WoW deltas next to every absolute; a number without comparison is decoration\n- Report per currency; never merge currencies with an invented exchange rate\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fabricate, extrapolate, or \"approximately\" any revenue figure - no data path means no digest, full stop\n- Don't compare a partial week to a full week without labeling it prominently\n- Don't propose refunds or subscription changes - read-only on payment data\n- Don't fire the delivery Zap before the user approves the digest content it will carry\n- Don't invent Stripe records or Zap names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every figure came from fetched data; coverage gaps declared in the digest itself\n- [ ] WoW deltas present for all headline numbers; currencies kept separate\n- [ ] Run summary shows the data path actually used\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n\n## Schedule\n\nRecommended cadence: **weekly**\n\nExample: `/loop 7d Run /zap-stripe-revenue-digest`\n"}, {"id": "zap-todoist-from-mail", "aiInstallPrompt": "Install the skill 'zap-todoist-from-mail' by writing the file below.\n\nUniversal path: ./skills/zap-todoist-from-mail/SKILL.md\nGrok path: ~/.grok/skills/zap-todoist-from-mail/SKILL.md\nCursor path: ~/.cursor/skills/zap-todoist-from-mail/SKILL.md\nClaude path: ~/.claude/skills/zap-todoist-from-mail/SKILL.md\nVS Code agent path: .github/skills/zap-todoist-from-mail/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-todoist-from-mail\ndescription: >\n  Create Todoist tasks from mail via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap todoist from mail\", /zap-todoist-from-mail.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-todoist-from-mail\n\nCreate Todoist tasks from mail via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when emails contain commitments that need to live in Todoist instead of dying in the inbox: the user pastes one or several emails (or forwards their text into chat), the AI extracts the actual to-dos, and the user's Zapier workflow creates them as properly-filed Todoist tasks. Best for the \"I have five emails asking me for things\" moment - extraction is the skill; task creation is the Zap.\n\n## What the AI does vs what the Zap does\n\n- **The AI** reads the pasted email text, extracts each genuine action item, phrases it as a Todoist-style task (verb-first, with due date and project/label suggestion), and fires the user's task-creation Zap per approved task.\n- **The Zap** creates the tasks inside Todoist (project, labels, priority as the user mapped them when building it).\n- The AI **cannot build Zaps** and - in this skill's stack - has no mail connector: email content arrives by paste/forward. No task-creation Zap yet → it proposes the config (webhook or email trigger → Todoist \"Create task\" with content/due/project fields) and delivers the task list for manual entry this time.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The email(s): pasted text, including sender and date lines when possible (they anchor due-date interpretation)\n- The user's Todoist layout: project names and label conventions to suggest against (ask once, remember for the session)\n- Due-date policy for vague asks: \"by end of week\" → which day? \"ASAP\" → today or no date + priority flag?\n- The task-creation Zap's name and expected payload fields (content, due string, project, priority)\n- Whether reply-type obligations (\"let me know your thoughts\") count as tasks or get skipped\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Task phrasing: imperative, self-contained - readable in Todoist next week without the email (\"Send Q3 invoice to Dana at Acme\", not \"Reply to Dana\").\n- **Audience:** The user's future self, in Todoist, out of context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier is unavailable, the extraction still has full value - deliver the task table in Todoist quick-add-friendly syntax (task text + due string + #project) for manual entry, plus a proposed task-creation Zap config for next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n**Step 1 - Extract tasks (AI, no connector).**\n- Read each pasted email. Extract only genuine action items FOR THE USER: direct asks, commitments the user made (\"I'll get you the deck Friday\"), and hard deadlines. Skip FYIs, marketing, and other people's action items (list those separately as \"waiting-on\" if useful).\n- Phrase each as a self-contained task: verb-first, names the person/thing, carries the due date interpreted per the agreed policy - with the interpretation marked (\"Friday\" → 2026-07-10 *(interpreted)*).\n- Suggest a project and labels from the user's stated layout; mark suggestions as suggestions.\n- **Checkpoint:** show the task table - # / Task / Due / Project / Source email - plus the skipped-as-not-a-task list. The user edits phrasing, dates, and projects here, and can rescue anything wrongly skipped.\n\n**Step 2 - Identify the Zap.**\n- Find the task-creation Zap via the connector; confirm the payload shape (one task per trigger, or batch). No Zap → deliver the quick-add-syntax list + proposed Zap config; stop.\n\n**Step 3 - Fire per approved task.**\n- **Approval gate:** final review table; fire on `apply`, `apply all`, or `apply 1,3` (fire only those rows).\n- Fire one trigger per task (or the batch format the Zap expects). Report per-task results: accepted / errored. Accepted ≠ visible in Todoist - the creation happens inside Zapier; tell the user which project(s) to glance at.\n- Failure behavior: errored tasks stay listed as \"not created\" with their quick-add syntax so nothing silently vanishes.\n\n## Output format\n\n1. **Task table** (step 1) - # / Task / Due (interpretations marked) / Project / Source - plus \"skipped\" and \"waiting-on\" lists\n2. **Zap match** (step 2) - Zap name + payload shape (or proposed config + quick-add list)\n3. **Creation report** (step 3) - per-task: fired / errored (with quick-add fallback text)\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Extract | AI | N tasks from M emails, K skipped | approved |\n| 2 Match | Zapier | Zap identified | done |\n| 3 Create | Zap (inside Zapier) | T triggers fired, E errors | accepted / fallback |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Make each task self-contained - in Todoist there is no email underneath it to explain \"reply to him\"\n- Mark every interpreted date so the user catches a wrong \"Friday\" before it's a missed deadline\n- Show the skipped list; the user rescuing one wrongly-skipped item beats ten false tasks\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't create tasks for FYI emails or other people's obligations - inbox-shaped noise in Todoist defeats the whole point\n- Don't fire before the task table is approved; bulk-created wrong tasks take longer to clean up than the email triage saved\n- Don't re-run the same pasted emails into duplicate tasks - ask whether a batch was already processed if it looks familiar\n- Don't guess projects silently; a task filed to the wrong project is effectively hidden\n- Don't invent email content or Zap names without a successful `use_tool` call (email text must be provided by the user in this stack)\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every task is verb-first, self-contained, and traceable to its source email\n- [ ] Interpreted dates flagged; skipped items listed for rescue\n- [ ] Run summary distinguishes triggers-fired from tasks-verified-in-Todoist\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-todoist-from-mail or describe the task from the skill description.", "skillContent": "---\nname: zap-todoist-from-mail\ndescription: >\n  Create Todoist tasks from mail via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap todoist from mail\", /zap-todoist-from-mail.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: low\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-todoist-from-mail\n\nCreate Todoist tasks from mail via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this when emails contain commitments that need to live in Todoist instead of dying in the inbox: the user pastes one or several emails (or forwards their text into chat), the AI extracts the actual to-dos, and the user's Zapier workflow creates them as properly-filed Todoist tasks. Best for the \"I have five emails asking me for things\" moment - extraction is the skill; task creation is the Zap.\n\n## What the AI does vs what the Zap does\n\n- **The AI** reads the pasted email text, extracts each genuine action item, phrases it as a Todoist-style task (verb-first, with due date and project/label suggestion), and fires the user's task-creation Zap per approved task.\n- **The Zap** creates the tasks inside Todoist (project, labels, priority as the user mapped them when building it).\n- The AI **cannot build Zaps** and - in this skill's stack - has no mail connector: email content arrives by paste/forward. No task-creation Zap yet → it proposes the config (webhook or email trigger → Todoist \"Create task\" with content/due/project fields) and delivers the task list for manual entry this time.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The email(s): pasted text, including sender and date lines when possible (they anchor due-date interpretation)\n- The user's Todoist layout: project names and label conventions to suggest against (ask once, remember for the session)\n- Due-date policy for vague asks: \"by end of week\" → which day? \"ASAP\" → today or no date + priority flag?\n- The task-creation Zap's name and expected payload fields (content, due string, project, priority)\n- Whether reply-type obligations (\"let me know your thoughts\") count as tasks or get skipped\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Task phrasing: imperative, self-contained - readable in Todoist next week without the email (\"Send Q3 invoice to Dana at Acme\", not \"Reply to Dana\").\n- **Audience:** The user's future self, in Todoist, out of context.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier is unavailable, the extraction still has full value - deliver the task table in Todoist quick-add-friendly syntax (task text + due string + #project) for manual entry, plus a proposed task-creation Zap config for next time.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n\n## Workflow\n\n**Step 1 - Extract tasks (AI, no connector).**\n- Read each pasted email. Extract only genuine action items FOR THE USER: direct asks, commitments the user made (\"I'll get you the deck Friday\"), and hard deadlines. Skip FYIs, marketing, and other people's action items (list those separately as \"waiting-on\" if useful).\n- Phrase each as a self-contained task: verb-first, names the person/thing, carries the due date interpreted per the agreed policy - with the interpretation marked (\"Friday\" → 2026-07-10 *(interpreted)*).\n- Suggest a project and labels from the user's stated layout; mark suggestions as suggestions.\n- **Checkpoint:** show the task table - # / Task / Due / Project / Source email - plus the skipped-as-not-a-task list. The user edits phrasing, dates, and projects here, and can rescue anything wrongly skipped.\n\n**Step 2 - Identify the Zap.**\n- Find the task-creation Zap via the connector; confirm the payload shape (one task per trigger, or batch). No Zap → deliver the quick-add-syntax list + proposed Zap config; stop.\n\n**Step 3 - Fire per approved task.**\n- **Approval gate:** final review table; fire on `apply`, `apply all`, or `apply 1,3` (fire only those rows).\n- Fire one trigger per task (or the batch format the Zap expects). Report per-task results: accepted / errored. Accepted ≠ visible in Todoist - the creation happens inside Zapier; tell the user which project(s) to glance at.\n- Failure behavior: errored tasks stay listed as \"not created\" with their quick-add syntax so nothing silently vanishes.\n\n## Output format\n\n1. **Task table** (step 1) - # / Task / Due (interpretations marked) / Project / Source - plus \"skipped\" and \"waiting-on\" lists\n2. **Zap match** (step 2) - Zap name + payload shape (or proposed config + quick-add list)\n3. **Creation report** (step 3) - per-task: fired / errored (with quick-add fallback text)\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Extract | AI | N tasks from M emails, K skipped | approved |\n| 2 Match | Zapier | Zap identified | done |\n| 3 Create | Zap (inside Zapier) | T triggers fired, E errors | accepted / fallback |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Make each task self-contained - in Todoist there is no email underneath it to explain \"reply to him\"\n- Mark every interpreted date so the user catches a wrong \"Friday\" before it's a missed deadline\n- Show the skipped list; the user rescuing one wrongly-skipped item beats ten false tasks\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't create tasks for FYI emails or other people's obligations - inbox-shaped noise in Todoist defeats the whole point\n- Don't fire before the task table is approved; bulk-created wrong tasks take longer to clean up than the email triage saved\n- Don't re-run the same pasted emails into duplicate tasks - ask whether a batch was already processed if it looks familiar\n- Don't guess projects silently; a task filed to the wrong project is effectively hidden\n- Don't invent email content or Zap names without a successful `use_tool` call (email text must be provided by the user in this stack)\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every task is verb-first, self-contained, and traceable to its source email\n- [ ] Interpreted dates flagged; skipped items listed for rescue\n- [ ] Run summary distinguishes triggers-fired from tasks-verified-in-Todoist\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "zap-twitter-schedule", "aiInstallPrompt": "Install the skill 'zap-twitter-schedule' by writing the file below.\n\nUniversal path: ./skills/zap-twitter-schedule/SKILL.md\nGrok path: ~/.grok/skills/zap-twitter-schedule/SKILL.md\nCursor path: ~/.cursor/skills/zap-twitter-schedule/SKILL.md\nClaude path: ~/.claude/skills/zap-twitter-schedule/SKILL.md\nVS Code agent path: .github/skills/zap-twitter-schedule/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-twitter-schedule\ndescription: >\n  Schedule X posts via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap twitter schedule\", /zap-twitter-schedule.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: medium\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-twitter-schedule\n\nSchedule X posts via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this to draft and queue X (Twitter) posts - singles or a thread - through the user's Zapier scheduling workflow: the AI writes/polishes the posts in the user's voice, the user approves every character, and the Zap queues them for their publish times. This is a PUBLISHING skill: nothing fires without explicit approval of the exact final text, because a queued post becomes a public post.\n\n## What the AI does vs what the Zap does\n\n- **The AI** drafts the posts, enforces length limits, sequences the thread, assembles per-post payloads (text + publish time + media reference), and fires the scheduling Zap after approval.\n- **The Zap** does the queueing/publishing to X inside Zapier at the scheduled times.\n- The AI **cannot build Zaps** and does not post to X directly. No scheduling Zap yet → it proposes the config (e.g., trigger with text + timestamp fields → the user's scheduler or X action) and delivers the approved posts + times as text for manual queueing this time.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The content source: topic + key points to draft from, or existing text to adapt\n- Format: single posts or a thread; how many; with/without media (and where media files live - the Zap needs a reference it can use)\n- Publish schedule: dates/times AND timezone - explicit, never assumed\n- Voice reference: 2-3 example posts from the user's account, or their stated style (emoji? hashtags? lowercase?)\n- The scheduling Zap's name and expected payload fields\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Draft register: the user's account voice, not generic social-media-manager voice.\n- **Audience:** The user's public followers - every draft is written as if it will be screenshotted.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier is unavailable, the drafting is still the hard part - deliver the approved posts with their target times as a copy-paste schedule for manual queueing (in X's own scheduler or the user's tool), plus a proposed scheduling-Zap config for next time.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n- Publishing is the highest-stakes action in this pack: the approved text must be character-identical to what fires - any edit after approval resets the approval.\n\n## Workflow\n\n**Step 1 - Draft (AI, no connector).**\n- Write the posts in the user's voice, from their key points. Enforce the character limit per post - show the count next to each. For threads: hook first, one idea per post, numbered (1/n), a closer with the call-to-action.\n- Fact-check discipline: any claim, number, or quote in a draft comes from the user's input - never invented for punchiness. Flag anything that reads risky (claims about competitors, health/finance-adjacent statements) for the user's judgment.\n- **Checkpoint 1:** show every draft with character counts. Iterate until the user approves the EXACT text. No paraphrase survives approval - what's approved is what queues.\n\n**Step 2 - Schedule plan.**\n- Map each approved post to its publish datetime (explicit timezone). For threads: confirm whether the Zap posts the whole thread at once or needs staggered times.\n- Duplicate guard: ask whether any of this content is already queued from a previous run or another tool - double-posting the same text looks broken to followers.\n- **Checkpoint 2:** the schedule table - # / post preview (first line) / chars / publish datetime + tz.\n\n**Step 3 - Fire the scheduling Zap.**\n- Find the Zap via the connector; build one payload per post (text, timestamp, media reference) in exactly the field shape it expects.\n- **Approval gate:** final review table. Fire only on `apply`, `apply all`, or `apply 1,3` (fires only those posts).\n- Report per-post trigger responses. Accepted ≠ queued-and-verified: the queue lives in Zapier/X - tell the user where to see the pending queue, and offer to re-fire any post that didn't land.\n- Failure behavior: a failed trigger keeps that post in \"not queued\" with its full text so it can be re-fired or queued manually - it never silently disappears.\n\n## Output format\n\n1. **Drafts** (step 1) - each post: full text + character count (threads numbered) - awaiting exact-text approval\n2. **Schedule table** (step 2) - # / preview / chars / publish time (tz) - awaiting confirmation\n3. **Queue report** (step 3) - per-post: fired / errored, with where to verify the queue\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Draft | AI | N posts / thread of N | text approved |\n| 2 Plan | AI | Schedule, tz-explicit | confirmed |\n| 3 Queue | Zap (inside Zapier) | T triggers fired, E errors | accepted / fallback |\n| 4 Verify | User | Queue checked in Zapier/X | pending |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Show character counts on every draft and re-show them after every edit\n- Make the timezone explicit in every scheduled time - a 9am post that fires at 2am reads as a bot glitch\n- Ask about already-queued content before firing; duplicate public posts can't be quietly fixed\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire with text that differs by even one character from what was approved - edits reset approval\n- Don't invent statistics, quotes, or claims to make a post punchier; everything factual comes from the user\n- Don't re-fire the whole batch after a partial failure - only the posts that errored\n- Don't schedule without an explicit timezone confirmation\n- Don't claim posts are queued when only the trigger was accepted; point the user at the actual queue\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every post within the character limit, count displayed\n- [ ] Exact final text approved before any trigger fired\n- [ ] Schedule timezone-explicit; duplicate check asked\n- [ ] Run summary distinguishes trigger-accepted from queue-verified\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-twitter-schedule or describe the task from the skill description.", "skillContent": "---\nname: zap-twitter-schedule\ndescription: >\n  Schedule X posts via Zapier. Requires integrations: @zapier. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap twitter schedule\", /zap-twitter-schedule.\nmetadata:\n  category: zapier\n  type: connector\n  cadence: on-demand\n  risk: medium\n  connectors: zapier\n  integrations: @zapier\nallowed-tools: search_tool, use_tool\n---\n# /zap-twitter-schedule\n\nSchedule X posts via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this to draft and queue X (Twitter) posts - singles or a thread - through the user's Zapier scheduling workflow: the AI writes/polishes the posts in the user's voice, the user approves every character, and the Zap queues them for their publish times. This is a PUBLISHING skill: nothing fires without explicit approval of the exact final text, because a queued post becomes a public post.\n\n## What the AI does vs what the Zap does\n\n- **The AI** drafts the posts, enforces length limits, sequences the thread, assembles per-post payloads (text + publish time + media reference), and fires the scheduling Zap after approval.\n- **The Zap** does the queueing/publishing to X inside Zapier at the scheduled times.\n- The AI **cannot build Zaps** and does not post to X directly. No scheduling Zap yet → it proposes the config (e.g., trigger with text + timestamp fields → the user's scheduler or X action) and delivers the approved posts + times as text for manual queueing this time.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The content source: topic + key points to draft from, or existing text to adapt\n- Format: single posts or a thread; how many; with/without media (and where media files live - the Zap needs a reference it can use)\n- Publish schedule: dates/times AND timezone - explicit, never assumed\n- Voice reference: 2-3 example posts from the user's account, or their stated style (emoji? hashtags? lowercase?)\n- The scheduling Zap's name and expected payload fields\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Draft register: the user's account voice, not generic social-media-manager voice.\n- **Audience:** The user's public followers - every draft is written as if it will be screenshotted.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Fallback:** if @zapier is unavailable, the drafting is still the hard part - deliver the approved posts with their target times as a copy-paste schedule for manual queueing (in X's own scheduler or the user's tool), plus a proposed scheduling-Zap config for next time.\n\n## Safety\n\n- **Propose before write**: show a review table; wait for user to say `apply`, `apply all`, or `apply 1,3`.\n- Never delete, send, or publish without explicit confirmation.\n- Summarize every action taken at the end of the run so the user can review it.\n- Publishing is the highest-stakes action in this pack: the approved text must be character-identical to what fires - any edit after approval resets the approval.\n\n## Workflow\n\n**Step 1 - Draft (AI, no connector).**\n- Write the posts in the user's voice, from their key points. Enforce the character limit per post - show the count next to each. For threads: hook first, one idea per post, numbered (1/n), a closer with the call-to-action.\n- Fact-check discipline: any claim, number, or quote in a draft comes from the user's input - never invented for punchiness. Flag anything that reads risky (claims about competitors, health/finance-adjacent statements) for the user's judgment.\n- **Checkpoint 1:** show every draft with character counts. Iterate until the user approves the EXACT text. No paraphrase survives approval - what's approved is what queues.\n\n**Step 2 - Schedule plan.**\n- Map each approved post to its publish datetime (explicit timezone). For threads: confirm whether the Zap posts the whole thread at once or needs staggered times.\n- Duplicate guard: ask whether any of this content is already queued from a previous run or another tool - double-posting the same text looks broken to followers.\n- **Checkpoint 2:** the schedule table - # / post preview (first line) / chars / publish datetime + tz.\n\n**Step 3 - Fire the scheduling Zap.**\n- Find the Zap via the connector; build one payload per post (text, timestamp, media reference) in exactly the field shape it expects.\n- **Approval gate:** final review table. Fire only on `apply`, `apply all`, or `apply 1,3` (fires only those posts).\n- Report per-post trigger responses. Accepted ≠ queued-and-verified: the queue lives in Zapier/X - tell the user where to see the pending queue, and offer to re-fire any post that didn't land.\n- Failure behavior: a failed trigger keeps that post in \"not queued\" with its full text so it can be re-fired or queued manually - it never silently disappears.\n\n## Output format\n\n1. **Drafts** (step 1) - each post: full text + character count (threads numbered) - awaiting exact-text approval\n2. **Schedule table** (step 2) - # / preview / chars / publish time (tz) - awaiting confirmation\n3. **Queue report** (step 3) - per-post: fired / errored, with where to verify the queue\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Draft | AI | N posts / thread of N | text approved |\n| 2 Plan | AI | Schedule, tz-explicit | confirmed |\n| 3 Queue | Zap (inside Zapier) | T triggers fired, E errors | accepted / fallback |\n| 4 Verify | User | Queue checked in Zapier/X | pending |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Show character counts on every draft and re-show them after every edit\n- Make the timezone explicit in every scheduled time - a 9am post that fires at 2am reads as a bot glitch\n- Ask about already-queued content before firing; duplicate public posts can't be quietly fixed\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't fire with text that differs by even one character from what was approved - edits reset approval\n- Don't invent statistics, quotes, or claims to make a post punchier; everything factual comes from the user\n- Don't re-fire the whole batch after a partial failure - only the posts that errored\n- Don't schedule without an explicit timezone confirmation\n- Don't claim posts are queued when only the trigger was accepted; point the user at the actual queue\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Every post within the character limit, count displayed\n- [ ] Exact final text approved before any trigger fired\n- [ ] Schedule timezone-explicit; duplicate check asked\n- [ ] Run summary distinguishes trigger-accepted from queue-verified\n- [ ] Required integrations (@zapier) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}, {"id": "zap-zoom-meeting-recap", "aiInstallPrompt": "Install the skill 'zap-zoom-meeting-recap' by writing the file below.\n\nUniversal path: ./skills/zap-zoom-meeting-recap/SKILL.md\nGrok path: ~/.grok/skills/zap-zoom-meeting-recap/SKILL.md\nCursor path: ~/.cursor/skills/zap-zoom-meeting-recap/SKILL.md\nClaude path: ~/.claude/skills/zap-zoom-meeting-recap/SKILL.md\nVS Code agent path: .github/skills/zap-zoom-meeting-recap/SKILL.md\n\nCreate the folder if needed, then write SKILL.md with this exact content:\n\n```markdown\n---\nname: zap-zoom-meeting-recap\ndescription: >\n  Zoom meeting recap to Notion via Zapier. Requires integrations: @zapier, @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap zoom meeting recap\", /zap-zoom-meeting-recap.\nmetadata:\n  category: zapier\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: zapier,notion\n  integrations: @zapier,@notion\nallowed-tools: search_tool, use_tool\n---\n# /zap-zoom-meeting-recap\n\nZoom meeting recap to Notion via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this after a Zoom meeting to turn its raw record - a transcript, Zoom's AI summary, or the user's notes - into a structured recap page in the team's Notion meeting database. Zoom material reaches the chat via paste or via the user's Zapier hookup to Zoom (if the session's Zapier connector exposes Zoom actions); the recap lands in Notion either directly through the Notion connector or through the user's recap Zap.\n\n## What the AI does vs what the Zap does\n\n- **The AI** digests the transcript/summary into the recap structure (decisions, actions, discussion), checks Notion for an existing page for this meeting, and writes the page via the Notion connector - or fires the user's recap Zap with the structured payload.\n- **The Zap** (when used) creates/updates the Notion page inside Zapier; some users' Zaps also fetch the Zoom recording/transcript - that fetching happens in Zapier, not in chat.\n- The AI **cannot build Zaps**. If neither a Notion write path nor a recap Zap exists, it delivers the recap as markdown for manual paste and proposes the Zap config (Zoom \"recording completed\" trigger → format → Notion \"create page\") to automate future meetings end-to-end.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The meeting record: pasted transcript / Zoom AI summary / user notes - plus meeting title, date, and attendees if not in the text\n- Which Notion database holds meeting recaps, and its required properties (date, attendees, project relation, tags)\n- The write path: direct via @notion (preferred when available) or the recap Zap's name and payload shape\n- Sensitivity check: was anything discussed that should NOT go into a team-visible page (comp, personnel, legal)? Excluded topics are listed by name in the recap as \"discussed off-record\"\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Recap register: neutral minutes - decisions verbatim, discussion summarized without editorializing about who \"won\".\n- **Audience:** Teammates who missed the meeting and the team's future self searching Notion.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks:** only ONE write path is needed - @notion direct OR a recap Zap via @zapier. Both missing → deliver the recap as paste-ready markdown plus the proposed automation config. The meeting record itself can always arrive by paste, so the digest step never blocks.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- A recap page is team-visible: the sensitivity check in the inputs is mandatory, and attributed statements (\"Sam said…\") are kept only when accurate and necessary.\n\n## Workflow\n\n**Step 1 - Get the meeting record.**\n- Take the pasted transcript/summary/notes. If the session's Zapier connector exposes Zoom actions (find recording/transcript), offer to fetch instead - discover the exact tools first. Confirm meeting title, date, attendees.\n\n**Step 2 - Digest into the recap structure (AI).**\n- **TL;DR** - 2-3 sentences: what the meeting was for and what changed\n- **Decisions** - each as close to verbatim as the record allows, with who ratified it\n- **Action items** - owner / task / due date (\"TBD\" when unstated - never invented)\n- **Discussion highlights** - the substance of the main threads, 1-2 bullets each\n- **Open questions / parked items** - plus anything excluded per the sensitivity check, named as \"discussed off-record\"\n- Transcript-quality warning: mark garbled attributions (\"speaker unclear\") rather than guessing who said what.\n- **Checkpoint:** show the full recap; the user corrects owners, dates, and attributions before anything is written. This is the accuracy gate - after filing, teammates treat it as the record.\n\n**Step 3 - File to Notion.**\n- Dedupe first: query the meeting database for an existing page with this title/date - offer to update rather than create a second recap of the same meeting.\n- Direct path (@notion): create/update the page with the database's required properties filled.\n- Zap path (@zapier): assemble the payload in the recap Zap's expected shape; fire after approval; note honestly that page creation happens inside Zapier.\n- **Approval gate:** show target database + properties + content preview; write/fire only on `apply`. Return the page link (direct path) or where to find it (Zap path).\n\n**Step 4 - Offer the follow-through.**\n- Offer once, don't push: distribute action items (if a task Zap or tool exists in the session) or produce a short recap message for the team channel. Each is its own proposed write.\n\n## Output format\n\n1. **Meeting header** (step 1) - title, date, attendees, record source (pasted / fetched)\n2. **Recap** (step 2) - TL;DR / Decisions / Action items (owner·task·due) / Discussion / Open & off-record - awaiting corrections\n3. **Filing proposal + result** (step 3) - database, properties, path (direct vs Zap) - then page link or location\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Record | User / Zapier→Zoom | Transcript/summary obtained | done |\n| 2 Digest | AI | Structured recap | approved |\n| 3 File | Notion / Zap | Page created/updated (link) | done / fallback |\n| 4 Follow-through | optional | Tasks / channel message | offered / applied |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Query for an existing recap of this meeting before creating - one meeting, one page\n- Run the sensitivity check before filing; a comp discussion in a team-visible recap is unrecoverable\n- Mark unclear attributions as unclear; a confidently wrong \"Sam agreed\" causes real friction\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't file the recap before the user corrects owners/dates/attributions at the checkpoint - post-filing, it becomes the official record\n- Don't invent due dates or owners for action items; \"TBD\" is accurate\n- Don't include verbatim quotes of sensitive moments the user flagged for exclusion\n- Don't create a duplicate page on re-run; update the existing one\n- Don't invent transcript content, database schemas, or Zap names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Recap approved (owners, dates, attributions) before filing\n- [ ] Dedupe query ran; sensitivity exclusions honored and noted\n- [ ] Run summary shows which write path was used (direct Notion vs Zap) and its real status\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier, @notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n```\n\nAfter writing the file, confirm it exists. The user can then run /zap-zoom-meeting-recap or describe the task from the skill description.", "skillContent": "---\nname: zap-zoom-meeting-recap\ndescription: >\n  Zoom meeting recap to Notion via Zapier. Requires integrations: @zapier, @notion. Uses search_tool + use_tool. Cadence: on-demand. Triggers: \"zap zoom meeting recap\", /zap-zoom-meeting-recap.\nmetadata:\n  category: zapier\n  type: combo\n  cadence: on-demand\n  risk: low\n  connectors: zapier,notion\n  integrations: @zapier,@notion\nallowed-tools: search_tool, use_tool\n---\n# /zap-zoom-meeting-recap\n\nZoom meeting recap to Notion via Zapier.\n\n## Works with\n\nUse this as a portable `SKILL.md` in Grok, Claude, Cursor, ChatGPT, Codex, VS Code agent skills, Gemini, or any AI workspace that supports reusable markdown instructions. If a tool does not support slash commands, paste the task and ask the AI to follow this skill.\n\n## When to use\n\nUse this after a Zoom meeting to turn its raw record - a transcript, Zoom's AI summary, or the user's notes - into a structured recap page in the team's Notion meeting database. Zoom material reaches the chat via paste or via the user's Zapier hookup to Zoom (if the session's Zapier connector exposes Zoom actions); the recap lands in Notion either directly through the Notion connector or through the user's recap Zap.\n\n## What the AI does vs what the Zap does\n\n- **The AI** digests the transcript/summary into the recap structure (decisions, actions, discussion), checks Notion for an existing page for this meeting, and writes the page via the Notion connector - or fires the user's recap Zap with the structured payload.\n- **The Zap** (when used) creates/updates the Notion page inside Zapier; some users' Zaps also fetch the Zoom recording/transcript - that fetching happens in Zapier, not in chat.\n- The AI **cannot build Zaps**. If neither a Notion write path nor a recap Zap exists, it delivers the recap as markdown for manual paste and proposes the Zap config (Zoom \"recording completed\" trigger → format → Notion \"create page\") to automate future meetings end-to-end.\n\n## Inputs to gather\n\nCheck context first; only ask if missing:\n\n- The meeting record: pasted transcript / Zoom AI summary / user notes - plus meeting title, date, and attendees if not in the text\n- Which Notion database holds meeting recaps, and its required properties (date, attendees, project relation, tags)\n- The write path: direct via @notion (preferred when available) or the recap Zap's name and payload shape\n- Sensitivity check: was anything discussed that should NOT go into a team-visible page (comp, personnel, legal)? Excluded topics are listed by name in the recap as \"discussed off-record\"\n- Read-only vs propose changes vs apply after confirmation\n\n## Tone & voice\n\n- **Tone:** Name the zap/workflow. Confirm trigger payload. Recap register: neutral minutes - decisions verbatim, discussion summarized without editorializing about who \"won\".\n- **Audience:** Teammates who missed the meeting and the team's future self searching Notion.\n\n## Connector access (required)\n\nThis skill **cannot run from text alone** - it must call live integrations.\nBefore any workflow step:\n\n1. Check your AI tool's connector settings and confirm each integration below is enabled and authenticated (Grok: run `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: enable the app in settings · other tools: ask your AI what app connections it has).\n2. Use `search_tool` to discover tools for each connector (search by app name).\n3. Call integrations with `use_tool` and fully-qualified names (`server__tool_name`).\n4. Connector tool names vary by AI tool. Discover the available tools first, then call the exact tool name exposed in the current session.\n\n### Required integrations\n\n- **@zapier** - Zapier\n  - MCP server key: `zapier`\n  - Example tools: `zapier__trigger_zap`, `zapier__list_zaps`\n\n- **@notion** - Notion\n  - MCP server key: `notion`\n  - Example tools: `notion__query_database`, `notion__create_page`, `notion__update_page`\n\n### If a connector is missing\n\nStop immediately. Tell the user:\n\n```\nThis skill needs <@mention> enabled.\n1. Open your AI tool's connector/integration settings (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings)\n2. Enable and authenticate <app>\n3. Refresh your tool list, then run this skill again\n```\n\nDo **not** pretend you accessed Gmail/Canva/Drive without a successful `use_tool` call.\n\n**Per-step fallbacks:** only ONE write path is needed - @notion direct OR a recap Zap via @zapier. Both missing → deliver the recap as paste-ready markdown plus the proposed automation config. The meeting record itself can always arrive by paste, so the digest step never blocks.\n\n## Safety & approval\n\n- Separate facts, assumptions, and recommendations.\n- Do not give legal, medical, tax, investment, hiring, or security advice as a final authority.\n- For sends, publishing, deletes, refunds, account changes, or other irreversible actions, show a review table and wait for explicit approval.\n- A recap page is team-visible: the sensitivity check in the inputs is mandatory, and attributed statements (\"Sam said…\") are kept only when accurate and necessary.\n\n## Workflow\n\n**Step 1 - Get the meeting record.**\n- Take the pasted transcript/summary/notes. If the session's Zapier connector exposes Zoom actions (find recording/transcript), offer to fetch instead - discover the exact tools first. Confirm meeting title, date, attendees.\n\n**Step 2 - Digest into the recap structure (AI).**\n- **TL;DR** - 2-3 sentences: what the meeting was for and what changed\n- **Decisions** - each as close to verbatim as the record allows, with who ratified it\n- **Action items** - owner / task / due date (\"TBD\" when unstated - never invented)\n- **Discussion highlights** - the substance of the main threads, 1-2 bullets each\n- **Open questions / parked items** - plus anything excluded per the sensitivity check, named as \"discussed off-record\"\n- Transcript-quality warning: mark garbled attributions (\"speaker unclear\") rather than guessing who said what.\n- **Checkpoint:** show the full recap; the user corrects owners, dates, and attributions before anything is written. This is the accuracy gate - after filing, teammates treat it as the record.\n\n**Step 3 - File to Notion.**\n- Dedupe first: query the meeting database for an existing page with this title/date - offer to update rather than create a second recap of the same meeting.\n- Direct path (@notion): create/update the page with the database's required properties filled.\n- Zap path (@zapier): assemble the payload in the recap Zap's expected shape; fire after approval; note honestly that page creation happens inside Zapier.\n- **Approval gate:** show target database + properties + content preview; write/fire only on `apply`. Return the page link (direct path) or where to find it (Zap path).\n\n**Step 4 - Offer the follow-through.**\n- Offer once, don't push: distribute action items (if a task Zap or tool exists in the session) or produce a short recap message for the team channel. Each is its own proposed write.\n\n## Output format\n\n1. **Meeting header** (step 1) - title, date, attendees, record source (pasted / fetched)\n2. **Recap** (step 2) - TL;DR / Decisions / Action items (owner·task·due) / Discussion / Open & off-record - awaiting corrections\n3. **Filing proposal + result** (step 3) - database, properties, path (direct vs Zap) - then page link or location\n\nFinish with a **run summary table**:\n\n| Step | Actor | Produced | Status |\n|---|---|---|---|\n| 1 Record | User / Zapier→Zoom | Transcript/summary obtained | done |\n| 2 Digest | AI | Structured recap | approved |\n| 3 File | Notion / Zap | Page created/updated (link) | done / fallback |\n| 4 Follow-through | optional | Tasks / channel message | offered / applied |\n\n## Do\n\n- Check your AI tool's connector settings first (Grok: `/mcps` · Claude: Settings → Connectors · Cursor: Settings → MCP · ChatGPT: app settings); confirm each required @mention is enabled and authenticated\n- Use `search_tool` then `use_tool` with fully-qualified names (`server__tool`)\n- Query for an existing recap of this meeting before creating - one meeting, one page\n- Run the sensitivity check before filing; a comp discussion in a team-visible recap is unrecoverable\n- Mark unclear attributions as unclear; a confidently wrong \"Sam agreed\" causes real friction\n- Ask at most ONE clarifying question if critical info is missing\n\n## Don't\n\n- Don't file the recap before the user corrects owners/dates/attributions at the checkpoint - post-filing, it becomes the official record\n- Don't invent due dates or owners for action items; \"TBD\" is accurate\n- Don't include verbatim quotes of sensitive moments the user flagged for exclusion\n- Don't create a duplicate page on re-run; update the existing one\n- Don't invent transcript content, database schemas, or Zap names without a successful `use_tool` call\n- Don't substitute generic web search when the skill requires a live connector\n\n## Quality check (before responding)\n\n- [ ] Recap approved (owners, dates, attributions) before filing\n- [ ] Dedupe query ran; sensitivity exclusions honored and noted\n- [ ] Run summary shows which write path was used (direct Notion vs Zap) and its real status\n- [ ] No placeholder brackets left unfilled (use sensible defaults or ask)\n- [ ] Required integrations (@zapier, @notion) verified in your AI tool's connector settings before `use_tool` calls\n- [ ] Connector actions proposed, not auto-applied (unless read-only)\n"}]};
